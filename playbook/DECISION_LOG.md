# DECISION_LOG — VNP Global

Format: `D-## | Decision | Rationale | Status`. Append-only; never delete, mark superseded instead. Future sessions: add your judgment calls here.

## Strategic Decisions (2026-07-31, with owner)

| ID | Decision | Rationale | Status |
|---|---|---|---|
| D-01 | Remove ALL fabricated claims ("60+ buyers", any invented stats) | Company is new, zero customers; fake claims are existential risk with export buyers who verify | LOCKED |
| D-02 | Positioning: premium processor with hard trust proof (not price-led exporter) | Owner choice; margin strategy; pulp/IQF buyers value consistency over cheapest price | LOCKED |
| D-03 | Trust strategy for zero-customer company: facility + process + certifications-in-progress + documentation samples + founder accessibility; NO social proof section until real | Only honest option; buyers vetting new suppliers weigh factory & docs over testimonials | LOCKED |
| D-04 | Lean on Kavita/Piyush Farms manufacturing reality: "manufactured at export-oriented facility, Karad" with facility content | The plant is real and operating; borrows legitimate operational credibility without claiming VNP history | LOCKED |
| D-05 | Certifications page shows named certs with status badges + placeholder numbers; never claim "certified" until numbers confirmed | Certs (APEDA, FSSAI, IEC, GST, ISO, HALAL, KOSHER) in progress; site not yet public so placeholders safe pre-launch | LOCKED |
| D-06 | Market priority: Gulf/Middle East #1, SEA #2; EU/NA/Africa listed not prioritized | Owner's 18-month sales focus | LOCKED |
| D-07 | Buyer priority: importers/distributors + B2B food processors; no B2C | Owner confirmed B2B priority | LOCKED |
| D-08 | English-only content now; i18n-ready architecture for Arabic (RTL) later; remove/hide the current empty language dropdown until Arabic ships | Half-built language switcher signals unfinished site — worse than none | LOCKED |
| D-09 | Leads: form → email + WhatsApp CTA; structured CRM-ready fields; no gating on downloads | Owner choice; gating kills trust for unknown supplier | LOCKED |
| D-10 | Stack assumption: Next.js-style + headless CMS (Payload signals on current site); playbook stays portable | Owner confirmed modern JS + CMS-ready already present | LOCKED |
| D-11 | Media: plan real Karad facility shoot; AI-generated interim imagery allowed with honesty limits (no fake team/facility-as-real/certs) | Budget-phased approach | LOCKED |
| D-12 | Logo + 6-color logo palette locked; extended UI palette derived in DESIGN_SYSTEM (deep green ink, warm neutrals) | Logo colors alone are too saturated for premium UI surfaces | LOCKED |
| D-13 | Visual relation to Piyush Farms: shared green family, distinct expression — VNP darker/more editorial (export B2B), Piyush fresher/consumer | Same group, different audiences; avoid looking like the same site | LOCKED |
| D-14 | IQF honesty: describe frozen products as "IQF-processed" (true of product) but never claim in-house IQF line; capability page says "IQF processing through qualified partner lines; in-house cold storage" | Owner: no in-house IQF yet, can source outside; cold storage is in-house | LOCKED |
| D-15 | "Insights"/blog: architecture reserved (route + CMS collection), content deferred to Phase 4 | New company should nail core trust pages before thin blog content (thin blog hurts credibility) | LOCKED |
| D-16 | 5-step order workflow section kept but rewritten as "How We Work" with documentation/incoterms transparency | Buyers vetting new suppliers want process predictability | LOCKED |

## Post-Audit Decisions (2026-07-31, after full site audit)

| ID | Decision | Rationale | Status |
|---|---|---|---|
| D-17 | Staging domain star-agrevolution.vercel.app must be protected/noindexed; all SEO identity (canonical/sitemap/robots/JSON-LD/og) moves to vnpglobal.in | Audit found entire SEO identity pointing at live staging duplicate — site can't rank under own domain | LOCKED |
| D-18 | Fake locale trees (/ar /fr /ru — English content, no translations) removed from sitemap + 410/301 until real translations exist; extends D-08 | RTL-rendered English insults the #1 target market (Gulf); duplicate-content risk | LOCKED |
| D-19 | /company consolidates into /about (+compliance content → /certifications); /manufacturing renames to /facility; /export becomes /markets/gulf-middle-east; 301s per MASTER_PLAN §5.2 | Three-entity confusion + footer-orphaned trust pages; nav label "Global Markets" 404s as URL | LOCKED |
| D-20 | All contact channels (phone/WhatsApp/email) served from ONE CMS global; components may never hardcode numbers | Audit found 10 dead wa.me/910000000000 links — class of bug prevented structurally | LOCKED |
| D-21 | Current RFQ form engineering retained and extended (not rewritten); CAPTCHA-vs-honeypot decided at implementation and logged | Audit verified validation/rate-limit/?product= deep links are solid | LOCKED |

## Expensive-to-Change-Later Decisions (flagged per Phase 5 brief)

| ID | Item | Why expensive later | Chosen direction |
|---|---|---|---|
| E-01 | URL structure | Changing URLs after indexing/outreach loses SEO + breaks printed materials | `/products/{category}/{product}` , `/certifications`, `/facility`, `/markets/{region}` (see SEO_PLAYBOOK §3) |
| E-02 | i18n routing scheme | Retrofitting locale prefixes breaks every URL | Path-prefix strategy reserved: `/{locale}/...`, default `en` unprefixed now, hreflang plan documented |
| E-03 | CMS content model | Re-modeling products later = migration pain | Product schema defined in MASTER_PLAN §7.3 (specs, packaging, docs as structured fields, not rich text) |
| E-04 | Design tokens | Hard-coded colors/spacing spread everywhere | All styling through tokens in DESIGN_SYSTEM §2 from day one |
| E-05 | Form → lead pipeline | Swapping form backends breaks tracking continuity | Fields + payload schema fixed now (COMPONENT_LIBRARY §Forms); email backend swappable behind one endpoint |

## Open Placeholders (must be resolved before public launch)

| Token | Needed value | Owner action |
|---|---|---|
| `{{PLANT_CAPACITY_MT_YEAR}}` | Processing capacity (MT/year or per-day) | Get from Kavita |
| `{{COLD_STORAGE_CAPACITY}}` | Cold storage capacity (MT / pallets) | Get from Kavita |
| `{{CERT_NUMBER_APEDA}}` `{{CERT_NUMBER_FSSAI}}` `{{CERT_NUMBER_IEC}}` `{{CERT_NUMBER_GST}}` `{{CERT_NUMBER_ISO}}` `{{CERT_NUMBER_HALAL}}` `{{CERT_NUMBER_KOSHER}}` `{{CERT_NUMBER_CIN}}` | Registration/license numbers + issue dates. APEDA/IEC/GST/CIN confirmed registered 2026-08-09 (D-61), numbers still pending — the other 4 are still genuinely in-certification | Provide as each cert lands |
| `{{ISO_STANDARD}}` | Which ISO exactly (22000? 9001?) | Confirm |
| ~~`{{FOUNDER_NAME}}` / `{{FOUNDER_BIO}}`~~ | ~~Founder identity + 3-sentence bio + photo~~ | **RESOLVED 2026-08-09** — 3 founders confirmed (Virendra Patil, Piyush Chavan, Nilesh Kalbhor), real story live on About + Company pages, see D-61 |
| `{{MOQ_PER_PRODUCT}}` | MOQ per SKU (e.g. 1×20ft FCL) | Sales decision |
| `{{PACKAGING_SPECS}}` | Pack sizes per SKU (e.g. mango pulp: 3.1kg A10 tins ×6, aseptic 215kg drums) | Get from Kavita — VERIFY, do not assume |
| `{{PORTS_OF_LOADING}}` | JNPT/Nhava Sheva? Mundra? | Confirm |
| `{{PAYMENT_TERMS}}` | e.g. LC at sight / advance % | Sales decision |
| `{{SHELF_LIFE_PER_PRODUCT}}` | Per SKU shelf life + storage temp | Get from Kavita QA |

## Session Additions

*(Future Claude sessions append below this line: `D-## | date | decision | rationale`)*

D-22 | 2026-07-31 | Codebase state at Phase-0 execution time differs materially from this playbook's original audit — the repo has an independent GSD-managed roadmap (`.planning/`) already through Phase 9 of a "v2.0 Premium Redesign" (i18n/RTL, CMS content model, motion polish), stopped mid-Phase-10 "Hardening." Several Phase-0 findings (T-002 WhatsApp single-source, T-005 real per-page translation gating + fallback notices, T-007 footer/redirect) were already fixed by that separate effort. Treated each T-0XX card as a hypothesis to verify against live code rather than assuming the original audit still held; re-scoped cards where reality diverged instead of re-doing already-correct work or blindly forcing the card's original literal scope. | Prevents duplicate/wasted work and prevents overwriting already-correct fixes with the playbook's stale assumptions. | LOCKED

D-23 | 2026-07-31 | Product catalog mismatch flagged, not fixed, in Phase 0: the live CMS catalog is rice/spices/lentils/sesame (6 SKUs), not PROJECT_MEMORY's frozen-veg/pulp/value-added 8-SKU list. This looks like GSD's own "realistic-shaped placeholder" catalog from its Phase 3 build (real specs weren't available yet), not a deliberate business pivot. | Rebuilding the real catalog requires the CMS content model work already scoped as T-103 (Phase 1, Opus) and real spec data that doesn't exist yet (capacity/packaging/MOQ are all still Open Placeholders) — out of "surgical, no-redesign" Phase-0 scope, and rice-swapped-for-peas placeholder data would just be more of the same problem. | How to apply: T-103/T-105 (Phase 1) must confirm with the owner whether this placeholder catalog gets replaced with the real 8 SKUs, and should not silently ship rice/spices as the launch catalog without an explicit decision. Flagging as a new Open Placeholder-equivalent, not yet in the formal Open Placeholders table (add there if Phase 1 confirms it's still open at that point).

D-24 | 2026-07-31 | T-004 (/admin lockdown) marked DONE for Phase 0 without adding new middleware, because Vercel SSO Deployment Protection currently gates the entire site (including /admin) pre-launch, on top of Payload's default per-account login lockout (5 attempts / 10 min). No IP-allowlist/basic-auth middleware was added. | Adding dedicated /admin middleware now would be effort spent hardening a surface that's already double-gated pre-launch, with no owner-supplied static IP to allowlist. | How to apply: T-306 (pre-launch gate) must add IP-allowlist or basic-auth middleware on /admin BEFORE Vercel SSO protection is turned off for public launch — this is a real gap, just not a Phase-0 one. Carry this forward into T-306's checklist.

D-25 | 2026-07-31 | T-003's production domain env var (NEXT_PUBLIC_SITE_URL) and DNS cutover to vnpglobal.in were NOT set/executed in this session — only the `.env.example` documentation comment was corrected. | Setting live Vercel project env vars and DNS is an infra/deploy action affecting shared production state; out of scope for an unattended code-fix session per this playbook's own P-00 preamble and general judgment-call discipline. | How to apply: owner or a session with deploy access must set the real Vercel env var and execute DNS cutover per the existing HANDOFF.md deploy notes; until then this stays BLOCKED(owner) even though the code-side mechanism is correct.

D-26 | 2026-08-01 | T-003 and T-005, both previously marked DONE on mechanism only, verified against live production and found still broken: robots.txt/sitemap still pointed at the staging alias (D-25's blocker — session now had deploy access, so executed it: `vercel env add NEXT_PUBLIC_SITE_URL` = `https://vnpglobal.in`, redeployed); and `/products`/`/insights` sitemap entries unconditionally listed all 4 locales including ar/fr/ru with zero real translation for those two routes (the INTERIOR_SLUGS pages were already correctly gated via getTranslatedLocales — only these two code-only listing routes bypassed that gate by design). Also added: the two known persistent Vercel staging aliases now 301-redirect to vnpglobal.in in middleware, since there's no CLI access to toggle Vercel's dashboard deployment-protection directly — guarantees no indexable duplicate content regardless of dashboard settings, scoped to those two hostnames only (not `*.vercel.app` broadly) so ephemeral preview-deployment URLs used for pre-merge verification still work. | "DONE (mechanism)" without a live check left both silently unfixed in production for a full day; this playbook's own D-22 philosophy (verify against live code, don't trust prior status claims) applies to its own DECISION_LOG entries just as much as to the original audit. | Both now genuinely closed. Commit b760b6b.

D-27 | 2026-08-01 | D-23's flagged catalog mismatch is resolved: a prior session (before this playbook's Phase 0 ran) already replaced the placeholder rice/spices/lentils/sesame catalog with PROJECT_MEMORY's real 8-SKU frozen-veg/pulp/value-added list (frozen green peas, sweet corn, mixed vegetables, baby corn, mango/guava/strawberry pulp, ginger-garlic paste), plus added the factory address (Kavita Facility Management Pvt Ltd, Tasawade MIDC) to SiteSettings and honest Piyush Farms partnership copy to /manufacturing. Real photos exist for mango/guava/strawberry/mixed-veg; peas/corn/baby-corn/ginger-garlic show the honest "no photo yet" empty state rather than a mismatched stock image. | Records that this was done, and by what means (direct Payload REST API calls against production, not a CMS content-model migration) — T-103's structured spec-table/packaging/container-loading schema (MASTER_PLAN §7.3) is still NOT built; current products only have name/shortDescription/loose specifications array, not the 4-block physico-chemical/organoleptic/microbiological/contaminants model this playbook specifies. | T-103 must build the real schema and migrate this existing basic data into it, not treat the catalog as "already done" — only the SKU list/naming is resolved, the structured-data depth is not.

D-28 | 2026-08-01 | T-101 (design tokens) implemented via VALUE remapping, not token renaming: existing CSS custom property names (--color-primary-900/700/500/100, --color-accent-800/600/100, --color-neutral-900/600/300/100, --font-sans, --font-display) kept their names but got new hex values/font families matching DESIGN_SYSTEM §2-3 (forest-green/gold palette, Fraunces display + Archivo body replacing Geist + IBM Plex Sans Latin; IBM Plex Sans Arabic untouched). New tokens --color-cream-50 and --color-lime-500 added (lime-500 unused so far — accent-only per spec, no existing component reached for it). Also: Button's `default` variant given its own explicit gold styling (bg-accent-600) decoupled from the shared shadcn `--primary` token (kept green — badges/inputs/selection-color still correctly use it), matching DESIGN_SYSTEM §2.3's "gold is the primary CTA, green is secondary" rule; display-tier font-weight tokens moved 300→500 (Fraunces' display weight per §3, not a substitute-thin-serif look) and HeroBlock.tsx's `font-light` class changed to `font-medium` to match. | Every component already consumes this exact token vocabulary (`bg-primary-700`, `text-accent-600`, `font-display`, etc.) across ~40 files — remapping values instead of renaming tokens delivers the same visual transformation instantly with zero component-file rename-sweep risk. Deliberately did NOT also migrate the full spacing/radius scale (playbook's is px-based and near-identical to the existing rem-based one) or rename tokens to the playbook's literal `--green-950` etc names — lower value-to-risk ratio than the color/font swap, deferred rather than bundled in. | Visually verified via Playwright screenshots (homepage hero/scroll/footer, /products, /ar RTL) before shipping given how drastic a change this is; e2e regression suite run before deploy. If future work wants the playbook's literal token names (e.g. for new code written against DESIGN_SYSTEM.md directly), add them as aliases pointing at the same values rather than renaming the existing ones out from under ~40 files.

D-29 | 2026-08-01 | T-102's mega-menu built one column per real CMS category (`getProductsByCategory`) instead of COMPONENT_LIBRARY C-02's literal "two columns by processing method" spec (IQF Frozen Vegetables / Aseptic Fruit Pulps / Value-Added as hardcoded groupings). Buyer-type links ("For Importers & Distributors" / "For Food Processors") and the catalog-PDF-download link were left out of the mega-menu entirely; header nav was NOT extended with "Facility & Quality" or "Resources" items; mobile drawer's product entry stays a flat link, not a nested accordion tree. | Hardcoding a 3-group processing-method taxonomy in the mega-menu component would silently drift from the real CMS category list the moment a category is renamed or a new one is added — deriving columns from `getProductsByCategory` is the correct data model for a catalog that's explicitly supposed to grow via CMS rows, not code (MASTER_PLAN §7.4). The buyer-type links, PDF download, and the two missing nav items all point at pages/pipelines that don't exist yet (T-107 Facility page, T-202 Resources hub + PDF pipeline, both Phase 2) — linking to them now would mean dead links or empty stub pages, worse than deferring. Mobile tree: /products already surfaces the full categorized catalog on tap; a nested mobile accordion is a real but separable UI build, lower value than shipping the desktop mega-menu now. | T-102 is functionally done for what's buildable today. Revisit the mega-menu's buyer-type links + PDF link once T-202 lands (the two menu items become trivial additions at that point, not new scope); add "Facility & Quality"/"Resources" nav items once T-107/T-202 ship; mobile nested tree can be picked up independently whenever — none of these three block Phase 1's remaining cards.

D-30 | 2026-08-01 | T-103's MASTER_PLAN §7.3 schema shipped ADDITIVELY, not as a rip-and-replace of Products' existing flat `specifications`/`packaging` fields: the new `specs.{physicoChemical,organoleptic,microbiological,contaminants}` arrays, `packagingOptions[]`, `containerLoading`, `shelfLife`/`storageTemp`/`moqRange`, `faq[]`, `downloads[]`, `seo` overrides, and `imageGallery[].role` were all added alongside the old fields, which stay in place untouched. Certifications got `status`/`number`/`validFrom`/`validTo`/`scope`/`targetDate` added the same way (all optional, no default on `status`). Two new collections (FacilityFacts, Resources) and a SiteSettings `sla.responseTime` group were also added. No product-page rendering changes shipped in this pass — the product detail template still reads only the pre-existing flat fields; consuming the new structured fields is T-105's job. | Per D-27, real content already exists in production against the OLD flat shape for the real 8-SKU frozen-veg/pulp catalog (rebuilt via direct Payload REST calls, not through this schema). There is no reliable automated way to recategorize an existing flat `{label, value}` spec row into one of the four new typed buckets — that requires a human judgment call per row, which is an editor/content task (T-110), not something to fake with a migration script. A destructive rename/replace risked silently losing or mis-bucketing that real data. Rendering was deliberately left out of this pass because every new field is empty on every real product right now (T-110 hasn't populated it yet) — building the full template UI against zero real content would mean shipping unverifiable/placeholder-driven layout, which is exactly what D-01's zero-fabricated-claims discipline argues against; T-105 (product template, Phase 1) is already scoped to do this once content exists to render against. | Schema is live in prod (migration `20260801_145559_t103_cms_content_model`, commit d6cb334) and available for editors/T-110 data entry immediately. T-105 must consume the NEW structured fields (not the old flat ones) when it builds the product template, and should treat "still empty" as the CAT-04 0-row-resilience case already established elsewhere (omit the section, don't render a placeholder). Once content is migrated into the new shape by an editor, the old `specifications`/`packaging` fields become dead and can be removed in a follow-up cleanup task — not yet, while they're still the only populated source for the live catalog.

D-31 | 2026-08-01 | T-104 rebuilt the homepage from 7 blocks to CONTENT_PLAYBOOK.md §4's full 9-section order: Hero, Proof strip (trustBar repurposed with real facility facts — multi-checkpoint QC, cold storage, traceability, Karad location — instead of generic "regions served" tags), Product categories (NEW featureGrid, 3 cards using the exact live /products category intro copy verbatim), De-risk tiles (existing featureGrid's Quality/Reliability/Compliance/Global-Reach copy replaced with the playbook's exact wording: Open Specifications / Sample Program / Inspection Welcome / 24-Hour Response), Facility band (mediaGallery, unchanged), Cert board (certStrip, unchanged — still honest "coming soon"), Process timeline (exportProcess, unchanged), Markets (NEW exportMap instance), CTA band (unchanged). ExportMap gained one additive field, `intro` (textarea, mirrors CertStrip's existing pattern) for Gulf-first capability copy. Content was applied directly to the live production 'home' Page doc via a one-off script (`scripts/update-homepage-t104.ts`, Payload local API, same precedent as D-27) since this is a content edit not a schema change, then folded into `seed-content.ts` so fresh local installs match — accepting that the new category-card copy references the real catalog (Frozen Vegetables/Fruit Pulp & Purees/Value-Added & Specialty) while the local seed's placeholder Products/Categories still say Grains/Spices/Pulses/Oilseeds (pre-existing D-27 gap, not fixed by this pass). | The prior homepage was missing 2 of the playbook's 9 sections entirely (Product categories, Markets) and had copy drift on 2 more (Proof strip content, De-risk tile wording) — this closes that gap using only real, already-published facts (D-01), reusing existing components (trustBar/featureGrid/exportMap) rather than building new ones. Markets specifically: an earlier session had already REMOVED exportMap from the homepage over exactly this risk — a 16-country highlighted map + shipment-count stat tiles reads as an export-history claim for a company with zero real customer history (see the removed test's own comment, now replaced). This reintroduction is NOT a reversal of that judgment — it deliberately avoids the original problem: Gulf-only 6-country highlight (not 16), zero stat tiles (component already renders gracefully with an empty stats array), and copy framed as "built to serve" / "available on request" (capability), never "we export to" (history). | Verified via Playwright locally (all 9 headings present, correct DOM order, no console errors) and via full e2e regression (`homepage.spec.ts` rewritten for the new sequence, `catalog-index.spec.ts`/`certifications.spec.ts` unaffected, all pass). Local Lighthouse (mobile, `next start`, no CDN) scored 71 performance / 100 a11y+SEO+best-practices — treated as non-representative of the real Vercel-edge production environment (no CDN caching, competing local-machine load, mobile CPU throttle) rather than a blocking signal; production Lighthouse is the actual QA_CHECKLIST/SEO_PLAYBOOK gate and must be checked post-deploy before this card is called fully closed.

D-32 | 2026-08-01 | Production Lighthouse (mobile) on the new homepage measured performance at 42/100 with a 29.3s LCP — traced to `next.config.ts`'s blanket `images.unoptimized: true` (a Phase-2-era stopgap for a remote-SVG optimizer failure) shipping the 2.3MB AI-generated hero PNG and the 2.1MB header/footer logo PNG as raw, unresized originals on every page load. Fixed in two commits: (1) removed the global flag, added a per-instance `unoptimized={isSvgUrl(...)}` guard (`src/lib/is-svg-url.ts`) on every CMS-media `<Image>` (8 call sites) so SVG sources still skip the optimizer while raster media gets resized/converted — LCP dropped to 8.8s; (2) `BrandMark`'s logo (a plain `<img>`, deliberately bypassing next/image for its unknown per-upload aspect ratio) converted to `next/image` using Payload/sharp's auto-populated `width`/`height` metadata (plumbed through `getSiteBrand` → `GlobalHeader`/`GlobalFooter`/`MobileNavPanel`), falling back to the old plain `<img>` if that metadata is ever missing — logo dropped 2093KB → 72KB, LCP to 4.8s. Final production score: performance 51, accessibility 96, best-practices 100, SEO 100. | 90 is the QA_CHECKLIST/SEO_PLAYBOOK acceptance gate; not reached. Remaining gap is `mainthread-work-breakdown` (3.8s of JS execution/hydration) plus a ~640ms first-visit-only redirect from next-intl's cookie-based locale negotiation (no cookie set yet on a cold Lighthouse run) — both are diffuse architectural/bundle-size work, not a single fixable bug, and chasing them further under this session's ASAP-ship priority (already delivered a 6x LCP improvement, the actual user-facing harm) has a much worse effort-to-value ratio than the two image fixes did. | Not closing the loop on 90 — flagging as a dedicated follow-up (bundle-size/hydration audit: likely candidates are the motion/reveal animation library, GTM/Plausible script loading strategy, and next-intl's locale-negotiation strategy) rather than silently calling this "done" against the stated acceptance criterion. Commits: b6505d0 (image optimizer), 0644c3b (logo).

D-33 | 2026-08-02 | Investigated the three e2e specs flagged as "known flaky" across earlier sessions (`nav-links.spec.ts`, two distinct failures inside `contact-error-state.spec.ts`) instead of continuing to treat them as environment noise — all three turned out to be real, fixable bugs, not flakiness. (1) `nav-links.spec.ts`'s Footer-nav test does 9 sequential full-page navigations in one test, legitimately exceeding the default 30s budget — bumped to 90s via `test.setTimeout`. (2) ContactForm's required-field labels (Name/Company/Country/Message) compute an accessible name that includes the visually-hidden "*" `RequiredMark`'s text despite `aria-hidden="true"` on it — confirmed via direct DOM inspection (`textContent` "Company *" vs. intended accessible name "Company"), a real Chromium accname-computation quirk that affects actual screen-reader users, not just Playwright's `getByLabel`. Fixed with an explicit `aria-label` on each affected `<Input>`/`<Textarea>` (NOT the `<label>` element — a first attempt putting it on `<FormLabel>` created a duplicate accessible-name target that collided with the input's own computed name, caught by rerunning immediately after). (3) The Turnstile-script-load-failure test asserted the wrong banner text — copy-pasted from the adjacent server-failure test's "something went wrong sending your message" instead of `ContactForm`'s actual `captchaErrorBanner` copy ("couldn't load the security check..."); two genuinely different error states sharing one regex by mistake. | "Known flaky, safe to ignore" had been the running assumption for these three specs across multiple sessions; actually reading the failure output instead of pattern-matching against that assumption found three unrelated real bugs, one of which (the label a11y issue) affects real users with screen readers on the live contact form, not just test tooling. Matches this playbook's own D-22 "verify against reality" discipline applied to test failures, not just code claims. | All three fixed, full contact-form regression suite (26 tests across `contact.spec.ts`/`contact-rfq-mode.spec.ts`/`contact-error-state.spec.ts`) passes clean, verified live on production. Commit f483db6. Noted but NOT fixed in this pass: a console warning on interior pages (`/about`, `/manufacturing`, etc.) that their compact-hero stock-photo fallback isn't marked `priority`/`loading="eager"` despite being the LCP element — same family of issue as D-32 but scoped to pages not measured in this session's Lighthouse pass; flagged for the same future bundle/perf follow-up.

D-34 | 2026-08-02 | T-105 migrated product URLs from flat `/products/{slug}` to nested `/products/{category}/{slug}` (MASTER_PLAN §5.2, LOCKED — E-01) and built out the product detail template to consume T-103's structured schema (specs by category, packaging options, container loading, shelf life/storage/MOQ, applications, downloads, FAQ + FAQPage JSON-LD) — the first real consumer of that schema (D-30 shipped it unconsumed). Old flat URLs now 308-redirect via a live category lookup (not a hardcoded slug map), so the redirect target self-corrects if a product ever changes category. Explicitly did NOT implement MASTER_PLAN §5.2's other listed renames (`/manufacturing`→`/facility`, `/company`→301→`/about`, `/export`→`/markets/gulf-middle-east`) — those move CONTENT that belongs to T-107/T-108/a not-yet-built Markets page, not T-105's product-template scope; renaming the URL ahead of the destination page existing would be premature. Also explicitly did NOT implement §5.2's `/ar /fr /ru` → redirect-to-`/` line — that instruction is from the playbook's superseded English-only assumption (D-08/D-18), already overridden by D-22's finding that a real, working 4-locale i18n architecture predates this playbook and is a hard CLAUDE.md requirement; implementing it would have deleted the entire working multi-language site. | The full §5.2 table is written as one LOCKED block, but literally executing all of it in one pass would mean either building three not-yet-scoped destination pages under time pressure or deleting the site's core i18n requirement — both wrong. Splitting URL migration from content migration and keeping only the parts that are genuinely this task's scope (products) is the same "re-scope pragmatically, log the deviation" discipline used throughout this playbook (D-22, D-27, D-29, D-30). | Two real bugs found and fixed during implementation, not just scope decisions: Next.js requires sibling dynamic routes at the same path depth to share one segment name — the old flat route's folder had to be renamed `[slug]`→`[category]` (holding a product slug despite the name) to coexist with the new `[category]/[slug]` detail route, a real Next.js App Router constraint, not a style choice. And the redirect target was originally missing its locale prefix, sending `/ar/products/{slug}` to unprefixed English instead of `/ar/products/{category}/{slug}` — caught by rerunning the existing `product-detail.spec.ts` RTL test after the URL change, fixed via a new `localePath()` helper (`src/lib/seo/alternates.ts`) used by both redirect sites. Verified: new/old URLs, locale-prefixed redirects, structured sections (populated + empty-fallback states, both visually screenshotted), RTL layout, full 257-test e2e suite (zero regressions — the only failures were two confirmed-pre-existing timing flakes in unrelated specs, `company.spec.ts`/`language-switcher.spec.ts`, verified via direct content check that the "missing" text is actually present in the HTML). Commit b233447.

D-35 | 2026-08-02 | T-106 populated the Certifications collection with the 7 real in-progress certifications PROJECT_MEMORY.md §3 lists (APEDA, FSSAI, IEC, GST, ISO 22000, HALAL, KOSHER) — all status "in-certification", no numbers, since none are confirmed yet. This is real business data (PROJECT_MEMORY explicitly names these + explicitly green-lights placeholder display pre-launch), not fabrication under D-01. Changed `Certifications.logo` from required to optional: the schema previously forced either blocking on real certification-body logos this company doesn't have rights to display pre-certification, or displaying them anyway — the second option overclaims regardless of the "in certification" text next to it, the same visual-trust problem D-01 exists to prevent. `CertCard` now renders a text-only status pill + generic shield icon when no logo is set, and structurally guards against the QA rule this task's acceptance criterion names directly ("no cert rendered as held w/o number"): a cert only ever shows "Registered" when `status==="registered"` AND a real `number` exists — a mis-set status field literally cannot render as held. | Same judgment as D-32/D-33/D-34: read what's actually true (PROJECT_MEMORY's own words) rather than defaulting to "no real data exists, ship nothing" OR blindly forcing the literal PROMPT_LIBRARY P-05 spec (name real cert-issuing companies for ISO/Halal/Kosher that haven't actually been chosen yet — issuingBody uses generic descriptors for those three, real named authorities for APEDA/FSSAI/IEC/GST since those are unambiguous regardless of registration status). | Found and fixed a real bug this surfaced: `DocumentCardBlock` (Company Profile / Sample COA cards) reuses `CertCard` verbatim per its own existing doc comment — once `CertCard` gained a status pill, it leaked "In Certification" onto document cards that aren't certifications at all, caught via the /certifications page screenshot. Fixed by gating the pill behind an explicit `status` prop check (`DocumentCardBlock` never passes one). Also fixed the homepage `CertStrip` strip-variant's logo-less state, which previously rendered an empty link box — now shows the cert name as text. Migration `20260802_030931_t106_cert_logo_optional` is non-destructive (`DROP NOT NULL` only). `certifications.spec.ts`/`homepage.spec.ts` rewritten for the new "real certs, not coming-soon" reality; full regression clean except the same 2 confirmed dev-mode-only cold-Turbopack-compile timeouts already characterized in T-105's verification (not a functional bug — production uses `next build`, no runtime compile cost, and the exact same flow was independently verified fast and correct on production after T-105's deploy). Commit 087d25e.

D-36 | 2026-08-02 | T-107 added D-14's exact LOCKED capability wording to `/manufacturing` ("Our frozen products are IQF-processed... IQF freezing runs through qualified partner lines; cold storage is in-house at the facility") plus a traceability paragraph (lot-code chain, intake through dispatch) and an audit-openness statement (buyer-nominated third-party inspection, in-person or video). Also fixed `seed-content.ts`'s manufacturing page, found stale mid-task: it still said "our own processing floor", an earlier draft that predates the honest-partnership reframing (D-27) already live on production — fresh local installs were silently drifting from reality on this page too, same class of gap as D-27 flagged for the product catalog. Deliberately did NOT execute MASTER_PLAN §5.2's `/manufacturing`→`/facility` rename, despite T-105 explicitly deferring it to this task. | The rename touches nav labels (`GlobalHeader`/`MobileNavPanel`), `sitemap.ts`'s `INTERIOR_SLUGS`, and needs a redirect shim — real, separable work with its own test surface, same reasoning T-105 used to defer it in the first place. Building the actual Facility & Quality content (this task's substantive deliverable) doesn't require the URL to change first; doing both in one pass this late into a long session risked rushing the higher-blast-radius change (a site-wide nav label + URL rename) alongside a lower-risk content edit. | Verified: D-14 wording present, "in-house IQF" (the exact overclaim D-14 forbids) absent, traceability/audit paragraphs render, full `manufacturing.spec.ts`/`chrome-consistency.spec.ts`/`page-routing.spec.ts` regression (70 tests) clean. Content applied to production via the same one-off-script-in-buildCommand pattern as D-31/D-35 (reverted after). The `/facility` rename is still open — pick up alongside T-108 (About) or as a dedicated pass whenever nav-label/IA changes are being reviewed together, not silently forgotten.

D-37 | 2026-08-02 | Found and fixed a real false-claim bug while starting T-108: production's live `/company` page said "Star Agrevolution... operates under a valid Importer-Exporter Code (IEC) registration... including APEDA registration for agricultural exports" — a direct claim of HELD registrations, when both are actually still in progress (PROJECT_MEMORY §3, T-106's own status board), under the pre-rebrand company name ("Star Agrevolution", not "VNP Global"). `seed-content.ts` already had the correct T-008-fixed wording ("IEC and APEDA registration in progress") — production was simply never re-synced after that fix landed, same staleness class as D-27 (catalog)/D-36 (manufacturing page), just with a materially worse consequence: a live, false regulatory claim, for however long it had been up. | This is the exact failure mode D-01/D-22's "verify against reality" discipline exists to catch — a page nobody was actively working on quietly drifted out of sync with an already-fixed source of truth. Found only because T-108's own prep work happened to involve reading `/company` (CONTENT_PLAYBOOK routes leadership/compliance there, not to About). | Fixed via the established one-off-script-in-buildCommand pattern (`scripts/fix-company-page-false-claims.ts`, idempotent — matches on the false-claim text, not a hardcoded assumption about which blocks need touching), verified removed from the live page and the correct VNP Global/in-progress wording confirmed present. Commit 07a6437. Followed up immediately with a live check of every public page (`/`, `/about`, `/company`, `/manufacturing`, `/export`, `/certifications`, `/contact`, `/products`) for "Star Agrevolution", "operates under a valid", and fabricated-stat patterns (`\d+ years/countries/buyers/customers/shipments`) — all clean, this was an isolated miss on `/company` specifically, not a wider rebrand-sync gap.

D-38 | 2026-08-02 | T-108 added CONTENT_PLAYBOOK §4's exact group/facility honesty sentence to `/about` ("VNP Global is a new export company. Our products are manufactured at the Kavita Facility Management (Agro Division) plant...") and a "What We Promise Buyers" de-risk FeatureGrid (the same 4 tiles as the homepage's T-104 section — reused, not reinvented). Corrected the intro's "sources, processes, and prepares" to "sources and exports" (VNP doesn't process — D-27). Deliberately did NOT include CONTENT_PLAYBOOK §4's founder section (items 1 "why VNP exists" first-person + 3 founder photo/bio, `{{FOUNDER_NAME}}`) — no real founder name, story, or quote exists anywhere in PROJECT_MEMORY or this log. | Putting invented words in a real person's mouth (a first-person "why I started this company" narrative) is a materially worse fabrication than an invented statistic — there's no honest placeholder version of a founder's personal story the way there is for, say, a cert status badge. Omit entirely rather than fake it, matching the same CAT-04 discipline used for certifications/product photos all session; blocked on T-110. | While fixing `company.spec.ts` for D-37's text change, also fixed a test that was checking for "Importer-Exporter Code" spelled out — that exact phrase only ever existed in the false-claim copy D-37 removed, never in the honest seed source (which only ever said "IEC", abbreviated). Rewritten to assert the actual current text ("IEC and APEDA registrations are in progress"). Full regression (`company.spec.ts`/`chrome-consistency.spec.ts`/`page-routing.spec.ts`, 74 tests) clean. Content applied to production via the established one-off-script-in-buildCommand pattern, reverted after. Follow-up (same day): a post-deploy live check caught the update script had fixed the intro richText's "sources, processes, and prepares" but missed the hero subhead carrying the identical phrase — the script's blanket early-exit ("group/facility paragraph present, skip everything") meant a second run wouldn't have retried the missed piece either. Rewrote each fix (hero, intro richText, new-blocks insertion) to check its own state independently, reran, verified the hero fixed too. Reinforces: after any content-sync script, actually read back the specific field it targeted, not just check for the new content's presence — a script "succeeding" only proves the parts it touched worked, not that it touched everything it should have.

D-39 | 2026-08-02 | T-109 extended the RFQ form to match COMPONENT_LIBRARY C-18's LOCKED field schema, which the existing (already-good, per TASK_BACKLOG's own note) engineering was missing 3 fields from: `buyer_type` (select), `products` (multi-select, real 8-SKU catalog), `timeline` (select). Kept everything else as-is — same schema/Server Action/spam-defense stack, per the backlog's explicit "extend rather than rewrite" instruction. Added a Checkbox UI primitive (`radix-ui`'s Checkbox is already bundled in the installed package, no new dependency) for the products multi-select, built the same way `accordion.tsx` already wraps its Radix primitive. C-18's own text says "Honeypot + time-trap antispam (no CAPTCHA friction)" — the form actually uses Cloudflare Turnstile (a CAPTCHA) + honeypot, per CLAUDE.md's explicit stack choice ("Cloudflare Turnstile + honeypot for spam"). | CLAUDE.md's own preamble states project instructions override default/imported-doc behavior, and TASK_BACKLOG's own T-109 note anticipated this exact CAPTCHA-vs-honeypot question as an open implementation decision to log, not a rigid LOCKED constraint to enforce against the resident stack choice — Turnstile was already built, tested, and is live; ripping it out now for a stale playbook line would be a regression, not a fix. | `Products of Interest` pre-checks whichever product a buyer arrived via (the existing `?product=` deep link from product-page CTAs) but stays schema-optional — a general inquiry with zero product context is still valid, same "optional at schema, UI-driven required-in-context" convention the existing quantity/incoterm RFQ fields already use. Email subject line now matches PROMPT_LIBRARY's `[RFQ] {products} — {company}, {country}` format when real product context exists. Full contact-form e2e regression (26 tests: `contact.spec.ts`/`contact-rfq-mode.spec.ts`/`contact-error-state.spec.ts`) clean — the one failure on first run was a 3ms-margin animation-timing flake in an unrelated accordion test, confirmed via isolated rerun.

D-40 | 2026-08-02 | T-206 (performance pass) investigated the open Lighthouse gap from D-32 (51/100 production, real bug fixed but short of the ≥90 gate). Found a real, confirmed root cause: `react-hook-form` (~90KB, the single largest JS chunk site-wide) was executing on the HOMEPAGE, which has no contact form — traced to `RenderBlocks.tsx` statically importing `ContactBlockView` (which imports `ContactForm` -> react-hook-form/zod/Turnstile) into its `BLOCK_MAP`; since `RenderBlocks` itself is imported by every Pages-collection route, that whole dependency tree got pulled along regardless of a given page's actual block content. Converted to `next/dynamic()`. This did NOT fix the measured issue: empirical comparison of `<script>` tags across `/`, `/certifications`, and `/contact` showed byte-identical lists on all three — this Next.js 16 + Turbopack production build currently ships one unified shared script set to every route, a bundler-level characteristic that an app-level dynamic-import hint can't override (Turbopack decides client-component chunk boundaries from "use client" directives during Flight compilation, not from how the importing Server Component is loaded). | Kept the dynamic-import anyway — it's correct code-splitting hygiene regardless of whether Turbopack currently respects it, harmless, and may start working automatically as Turbopack's production bundler matures. Did NOT pursue forcing webpack as the production bundler (a real possible workaround) — that's a bundler-level infrastructure change for the whole site, materially different in risk/blast-radius from every other fix this session, and not something to decide unilaterally this deep into a long session without the owner weighing in. | T-206 stays open. Two concrete next steps for whoever picks this up: (1) confirm whether forcing webpack (`next.config.ts` bundler override, if Next 16 still supports it) actually restores per-route code-splitting and is worth the trade-off, or (2) wait for/verify against a newer Next.js/Turbopack release that may resolve this natively. Full contact-form + homepage e2e regression (15 tests) clean — no functional regression from the dynamic-import change itself.

D-41 | 2026-08-02 | T-201 rebuilt `/export` from one broad ExportMap (16 highlighted countries — Gulf + Europe + US + Singapore + South Africa — plus a "16+ Target Export Markets" stat tile) to CONTENT_PLAYBOOK §4's actual Gulf-first structure: Gulf & Middle East ExportMap first (6 countries: AE/SA/QA/KW/BH/OM, capability copy — reefer logistics to Jebel Ali/Dammam, Halal, Arabic labeling, GSO awareness), Southeast Asia ExportMap second (Singapore only — the sole SEA country actually drawn on `world-map-svg.ts`; Malaysia/Indonesia/Vietnam mentioned in the intro copy as expansion capacity rather than falsely highlighted on a map that can't render them), then a plain "other markets on request" richText note, zero stat tiles anywhere. | This is the exact same over-claiming shape D-31 already fixed on the homepage, just never applied to `/export` — a new exporter with zero shipment history showing a wide "countries we're in" map/count reads as a track-record claim it doesn't have. Caught only because starting T-201 meant actually reading the live page first. | `export-map-a11y.spec.ts` needed one assertion fixed (was checking for a "Germany" chip that no longer exists after this content change) — full regression (16 tests) clean otherwise. Same one-off-script-in-buildCommand deploy pattern as prior content updates. This is the second time this session a page's content was found silently drifted from the playbook's own honesty rules while working on something adjacent (D-37's `/company` false-claim fix was the first) — worth a full site-wide content audit against CONTENT_PLAYBOOK §4 at some point rather than only catching these opportunistically.

D-42 | 2026-08-02 | T-204 (technical SEO pass) found a real bug: `robots.ts`'s blanket `Disallow: /api` also caught `/api/media/*`, which is where every uploaded image is actually served from (`Media.url` in `payload-fetch.ts`) — hero, logo, cert logos, all 8 real product photos. Google Image Search could not index a single site image. Carved out an explicit `Allow: /api/media` (crawlers match the longest/most-specific rule, so it wins over the broader `Disallow: /api` for that one subpath) while keeping `/admin` and the rest of `/api` (REST/GraphQL endpoints) disallowed. Added a regression test (`robots.spec.ts`, none existed before). Also audited alt text (zero missing across all public pages, no action needed) and JSON-LD (Organization/Product/BreadcrumbList all parse valid on `/`, a product page, `/certifications`, `/about`). | This is a launch-blocking-class SEO gap that had nothing to do with content quality or copy — pure infrastructure, would have silently suppressed all image-search traffic indefinitely without ever surfacing as an error anywhere (robots.txt returns 200, the site "works" fine for human visitors, no test or build step would ever catch it). Found only because T-204 was actually scoped to check technical SEO infra rather than assuming it was already correct. | Filenames (`GreenPeas.png`, `fssai-01 (quillbot.com).svg`, "ChatGPT Image Jul 30, 2026...png") are messy and not SEO-descriptive, but NOT fixed — renaming an already-uploaded Payload/Vercel-Blob media file means re-uploading it (a new URL, a real content-admin action, not a code fix), and alt text — the stronger of the two ranking signals — is already solid everywhere. Logging as a known minor gap, not a task to silently skip.

D-43 | 2026-08-02 | T-205 ran a real automated accessibility audit (`@axe-core/playwright`, WCAG 2.0/2.1 A+AA tags — the industry-standard tool, not a manual checklist pass) across every public page type in both locales (9 Pages-collection routes + one product detail page + `/ar` for RTL). Zero violations on every page, first run. | This reflects a11y discipline already built in throughout the whole session (visible-label associations, aria-live regions, focus-visible rings, logical-property RTL layout, keyboard-accessible mega-menu/mobile-nav, alt text audited clean in T-204) rather than something newly fixed here — T-205 found nothing to fix. | Locked the clean result in as a permanent regression test (`a11y-audit.spec.ts`) rather than just reporting it and moving on, so a future change that breaks a label/contrast/landmark fails CI instead of shipping silently. 3 of 10 tests timed out on the very first local run — confirmed via isolated rerun to be the same known cold-Turbopack-dev-compile pattern already characterized for T-105/T-106 (not a real issue, production build has no such cost), not counted as findings.

D-44 | 2026-08-02 | T-207 found the site's entry-animation duration was 2x DESIGN_SYSTEM §6's ceiling: spec says "fade+8px rise on section entry (200-300ms, ease-out, once)", `Reveal.tsx` (section-level) was `duration-600` and `RevealItem.tsx` (per-item stagger) was `duration-500` — been that way since the original Phase 9 motion build, well before this session. Both changed to `duration-300`. | A slower-than-spec reveal isn't a functional bug, but it's a real, measurable deviation from a written design rule, caught the same way every other T-20x pass caught something: actually reading the current implementation against its own spec instead of assuming it was already correct. | Checked the rest of §6/§7 while in here rather than fixing one number and stopping: hover micro-transitions (ProductCard/InsightCard raise-on-hover) already correctly at `duration-150` per spec — no fix needed; no carousels/parallax anywhere (confirmed via the "no lightbox/carousel" anti-pattern comments already present in MediaGalleryBlock/ProductGallery from earlier phases); `motion-reduce` fallbacks already present on both components. Full RTL + homepage + manufacturing regression clean.

D-45 | 2026-08-02 | T-208 ran an actual grep-verify pass against CONTENT_PLAYBOOK §1's banned-word/banned-pattern list (not a skim) across every public page and found two systemic issues: (1) the homepage hero used "Premium" as a bare adjective — the banned list's own first example — fixed to "Export-Ready Agricultural Products, Direct From India" using language already present in the subhead; (2) every CTA band heading site-wide used the banned rhetorical-question pattern ("Ready to Source With Confidence?", "Want a Facility Walkthrough?", "Have a Compliance Question?", etc.) — 6 in `seed-content.ts` plus 4 more hardcoded directly in route components (products listing/detail, insights listing/detail — "Ready to Source With Confidence?" was literally duplicated 3x rather than shared as a constant). All 10 converted to declarative statements with the same intent. | This is the kind of finding that only surfaces from actually running the rule against the content rather than assuming voice guidelines were followed because the content "sounds fine" — every one of these headings reads naturally as a question in isolation, which is exactly why the rule exists (a page full of them reads as a listicle/ad-copy voice, not the direct, confident voice CONTENT_PLAYBOOK §1 wants). | Deliberately did NOT touch `/contact`'s FAQ headings ("What should I include in my first inquiry?" etc.) — those are real FAQ Q&A content, a different, legitimate use of question-form headings, not the CTA-heading pattern the rule targets. Banned words (world-class, leading, cutting-edge, unparalleled, one-stop, synergy) and exclamation marks: already clean everywhere, verified via the same audit rather than assumed. Full regression clean (2 timeouts on first parallel-worker run were SQLITE_BUSY lock contention from concurrent local-dev DB writes, confirmed via single-worker rerun, not real failures).

D-46 | 2026-08-03 | T-209 was four owner bug reports against live production (vnpglobal.in), not planned playbook work. (1) Logo looked small despite repeated CSS height increases — real root cause was the uploaded canvas having ~70% empty transparent padding around the actual mark (confirmed via `sharp().trim()`), so any box-height increase scaled the padding proportionally along with the mark; cropped the source asset to its real content bounding box and re-uploaded, plus added the DESIGN_SYSTEM §6-spec 150ms hover micro-transition while in there. (2) Header WhatsApp button was a 44px override sitting next to a 32px sibling button — dropped the override so it uses the same 36px icon-button size as everywhere else in the design system (a compromise between the old mismatch and the ≥44px tap-target guidance, since shrinking to exactly 32px would itself violate that rule). (3) Mega-menu: clicking a product made the whole menu vanish before the click landed — the panel was offset from its trigger with a CSS `margin` (a real, unrendered gap); a mouse path from trigger to link crossed that gap, briefly left every DOM descendant of the `onMouseLeave` hover root, and unmounted the panel mid-transit. Fixed by switching to `padding` so the panel's own hit-box starts flush against the trigger. (4) Found while investigating, not reported: `/export`'s world-map graphic was disconnected gray rectangles with no canvas or labels, reading as an unfinished loading skeleton rather than a map — added a canvas background, per-block strokes, and ISO-code labels; also fixed a real WCAG AA contrast failure this introduced (dark label text on the highlighted primary-500 green fill measures ~3.2:1, below the 4.5:1 normal-text threshold T-205 locked in as a regression gate — labels now flip to white when their country is highlighted) and a stale `PRIMARY_500` hex (`#3D8266`) that had drifted from the current design token (`#1E7A38`). | Every fix required finding the actual root cause rather than the symptom described: the logo "size" complaint was actually a canvas-padding problem no CSS change could ever fix; the mega-menu "disappears" complaint was a specific CSS-gap/event-handler interaction, not a general hover-flakiness issue. | The mega-menu's e2e test (`mega-menu.spec.ts`) initially failed consistently under the real Playwright test runner despite the underlying fix being manually confirmed correct against a production build (`next build && next start`) — root cause was `next dev`'s Turbopack Fast-Refresh swallowing the simulated click mid-flight, a known dev-mode-only artifact unrelated to the fix (confirmed via a raw script with no test-runner involved, after first ruling out a stale long-running dev server and a too-fast synthetic click as red herrings). Rewrote the test's assertion to check the actual regression invariant (the intended link is still under the cursor via `elementFromPoint` after the mouse transits the old gap) instead of depending on dev-mode navigation completing. Separately, the first full local e2e regression run showed 14 failures (`ERR_NETWORK_IO_SUSPENDED`, assorted timeouts) that traced to the laptop idle-sleeping mid-run (confirmed via `pmset -g log`) plus manual verification scripts hitting the same dev server concurrently — re-ran isolated with `caffeinate` and all passed, except `certifications.spec.ts`, which was a genuine local-only gap (certifications aren't part of `db:seed`'s scope, so a prior DB reset this session left them unseeded) fixed by re-running `populate-certifications-t106.ts`, unrelated to any T-209 code change. First prod deploy attempt hit the known transient Vercel "fetch failed" at the Deploying-outputs stage before creating a deployment record; the retry succeeded. Standard temporary-buildCommand-piggyback pattern used again for the logo-crop script (needs prod `DATABASE_URL` + `BLOB_READ_WRITE_TOKEN`), reverted after confirming the new logo URL was live.

D-47 | 2026-08-03 | T-209 follow-up: after D-46's map-legibility fix shipped, the owner's actual reaction was "I thought it would be an actual beautiful world map" — the rectangle-tile placeholder (`world-map-svg.ts`) could never deliver that regardless of polish, since it wasn't real geography. Replaced it entirely: `world-map-geo.ts` computes real country paths server-side (d3-geo + topojson-client against `world-atlas`'s Natural Earth 50m data — 50m confirmed necessary over 110m for the small Gulf states to exist at all) inside the async `ExportMapBlock` Server Component, so no map-rendering library ships to the client (avoids adding to T-206's still-open bundle-size gap). `WorldMapInteractive.tsx` (client) owns only the interactive touches: a once-per-view staggered reveal of served countries and a 150ms hover-brightness + native `<title>` tooltip per country. Two real bugs found and fixed while building this: (1) the "compact" regional variant (Southeast Asia) was fitting the whole-world projection and shrinking the box, so Singapore rendered as an invisible dot — fixed by fitting the projection to the highlighted countries' own extent, which surfaced (2) a centering bug (scaling a `fitSize()` projection after the fact without recomputing `translate` shifts the effective center — confirmed via the Maldives rendering cropped off-frame) fixed by rebuilding the projection with an explicit centroid + translate instead. Also added minimum-visible marker dots for any highlighted country projecting to sub-pixel size at its map's zoom (Bahrain, Maldives, Singapore all hit this — Maldives specifically because `min(width,height)` is what indicates true visual size, not `max`, since a thin elongated archipelago has a tall bounding box but is still a near-zero-width sliver). | The owner then made two real business-fact decisions, not code decisions: confirmed (via explicit yes/no question) that Vietnam, Indonesia, and the Maldives are current served markets, not "capacity to extend as demand grows" — updated `/export`'s highlighted set and copy accordingly, which is a genuine claim-strength change, not a bug fix, and is logged as such rather than silently treated like one. Then asked to combine the Gulf and Southeast Asia maps into one "Our Primary Focus" map (10 countries) instead of two sections — implemented within the existing `ExportMap` block shape (one intro paragraph that still names each region's specific operational detail, no schema change needed). | Homepage's own map was still Gulf-only with copy saying "Southeast Asia... available on request" — now contradicted `/export`'s fuller claim, so brought it into sync (same 10 countries, updated intro) rather than leaving two pages making different claims about the same company. `homepage.spec.ts`'s "Gulf countries only" test assertion updated to match (was checking presence, not absence, of Gulf codes, so it would have stayed green even with the drift — the stale test *title* was the only thing masking the inconsistency). Full regression (a11y-audit, export-map-a11y, homepage, mega-menu, en+ar) clean. Deploy status corrected 2026-08-08: verified live directly against production via curl — real map + combined "Our Primary Focus" set confirmed present on both `/export` and homepage. Backlog note previously said "awaiting go-ahead", which was stale; the commits had been on `origin/main` since 2026-08-03.

D-48 | 2026-08-08 | Vercel sent two usage-limit emails (75% then 100% of the free-tier 200,000/mo ISR Writes quota) — investigated instead of just upgrading, since the site is pre-launch (T-306 gate still open, no real buyer traffic yet) and 200k writes before launch is a red flag, not organic growth. Root cause: all 6 route files (`page.tsx`, `[slug]/page.tsx`, `products/page.tsx`, `products/[category]/[slug]/page.tsx`, `insights/page.tsx`, `insights/[slug]/page.tsx`) had `export const revalidate = 60` — a 60-second time-based ISR fallback that's redundant with the on-demand `revalidatePath` hooks (`revalidateCatalog.ts`, `revalidatePage.ts`, `revalidateInsights.ts`, `revalidateSiteSettings.ts`) already pushing fresh content instantly on every CMS save. Checked and ruled out the two other suspects: all 13 scripts that write via the Payload local API (`payload.update`/`create`) already pass `context: { disableRevalidate: true }`, so seed/one-off scripts weren't the source; Playwright (`playwright.config.ts`) defaults `BASE_URL` to `localhost:3000` and spins up a local dev server, which never touches Vercel's cache at all — only a handful of manual post-deploy verification runs (e.g. T-102's follow-up fix) pointed it at production. The actual multiplier: ~20 tasks' worth of dev-cycle redeploys (many shipped as ship-then-revert temporary-buildCommand pairs, ~40+ total deploys), each of which resets the ISR cache, so the next visit to any of ~50+ page × locale combinations became a fresh write again — compounded by the 60s window re-arming on every subsequent visit. Fixed by bumping all 6 to `revalidate = 3600` (1hr); content freshness is unaffected since the on-demand hooks are the real freshness mechanism, this was only ever the staleness-fallback timer. Cross-checked against Vercel's own Data Cache docs (linked in the warning email) before committing to the fix — confirms time-based revalidation is the write generator and on-demand invalidation is the intended low-cost mechanism for freshness, which is exactly the pattern already built here. | Chose 3600s over disabling time-based revalidation entirely (`revalidate = false`) so pages still self-heal if a hook ever fails to fire or misses a path, without reintroducing the write-storm at a short interval. | Vercel's "paused" behavior on exceeding a Hobby-plan quota is a temporary availability outage, not data loss — code/git history/CMS/Postgres data are untouched; serving resumes on upgrade or next billing-cycle reset. Not yet re-verified against a live Vercel usage dashboard post-fix (would need 24-48h of normal traffic to confirm the write rate actually drops) — flagged as a follow-up, not silently assumed fixed.

D-49 | 2026-08-08 | Owner shared an external "VNP Global SEO & AEO Growth Plan" consultant doc for review before adoption. It's a generic B2B SEO template with VNP's product names dropped in — written with no visibility into this codebase, SEO_PLAYBOOK.md's locked decisions, or D-47 (same day). Reviewed against actual code/decisions rather than adopted wholesale. **Rejected outright** (4 direct conflicts): (1) 10 separate per-country market pages (`/markets/uae/`, `/markets/saudi-arabia/`...) — contradicts D-47's explicit merge of Gulf+SEA into one "Our Primary Focus" map, deployed live the same day; following the doc would reverse an owner decision from this session. (2) Split `/quality/` + `/manufacturing/` pages — contradicts T-107's combined Facility & Quality page, already built and shipped. (3) Flat product URL structure (`/products/frozen-green-peas/`) — contradicts SEO_PLAYBOOK §2's **LOCKED (E-01)** nested structure (`/products/frozen-vegetables/green-peas`), which is what's actually implemented. (4) Treats multilingual SEO as "P3, after validation" — lowest priority — contradicts CLAUDE.md's hard constraint that 4-locale i18n is architected in from day one, not deferred. Doc's Phase 0/1 "technical foundation" is also largely redundant — sitemap.ts/robots.ts, Organization/Product/BreadcrumbList/FAQPage JSON-LD, image alt audit, and most of the Product schema fields it recommends (grade/moisture/packaging/shelf-life/MOQ/certifications/FAQ/downloads) already exist (T-103, T-204). **Merged in** (genuinely new, non-conflicting, added as SEO_PLAYBOOK §10-11): a structured keyword-tracking table shape (keyword/intent/product/market/priority/target-URL/conversion-value/current-ranking columns), seeded from existing §1 clusters and explicitly marked "needs validation with real search data" per the source doc's own caveat — no invented volumes or rankings; an off-page/entity-consistency checklist (LinkedIn/Google Business Profile/APEDA/FSSAI/GST name-logo-contact consistency), explicitly warning against directory-listing/backlink packages, consistent with this project's existing anti-fabrication stance. | The doc's own Principle 4 ("evidence beats marketing claims... avoid stock photos as proof of facilities") undercuts its own 90-day timeline — Phase 2 "Commercial Pages" (days 16-35) assumes real photos/certs/specs that don't exist yet (T-301/303/304/305 all `BLOCKED(owner)`, T-306 pre-launch gate still open). Running this doc's roadmap now means building SEO infrastructure around placeholder evidence, the exact thing it tells readers not to do. | While reviewing this doc's analytics recommendation ("GA4 or Plausible — decide"), discovered SEO_PLAYBOOK §9 still had this framed as an open decision. It is not — ANALY-01 was decided and fully implemented in Phase 4 (checkpoint 04-05): Plausible script is mounted in `src/app/(site)/[locale]/layout.tsx`, guarded by `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`, `trackEvent` dispatches to it, 4/4 unit tests pass, verified in `04-VERIFICATION.md`. Corrected §9 to state this as decided, not open. The actual remaining gap isn't code — `.env.example` was never updated with `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` (a documented sandbox-permission block at the time, per `04-05-SUMMARY.md`) and no Plausible site/env var has been created in Vercel production, so zero analytics data has ever flowed despite the code being launch-ready. Fixed `.env.example` this session (see below) and flagging the Plausible account creation + Vercel env var as an owner action, not a dev task.

D-50 | 2026-08-08 | While auditing technical SEO coverage (following up on T-204's "DONE" claim), found a real, previously-unflagged gap: home, all 6 interior pages (about/certifications/manufacturing/export/company/contact), `/products`, and `/insights` had **zero page-specific metadata** — every one of these 9 route types × 4 locales silently inherited the root layout's generic site-wide title/description. Only product-detail and insight-detail pages had `generateMetadata`. T-204 audited robots/alt-text/JSON-LD but missed per-page metadata coverage entirely. Fixed: (1) added a `seo` group (title/description/ogImage, localized) to the `Pages` collection — same shape as `Products.seo`, added in T-103 — with a hand-written Postgres migration (`20260808_t204_pages_seo_overrides.ts`) mirroring that migration's exact column/constraint/index pattern, since `payload migrate:create` hit a pre-existing Drizzle-Kit snapshot version-mismatch error unrelated to this change (the stored `.json` snapshots are stale relative to the installed tooling version — a known gap, not fixed here, flagged for a future session since it blocks any future `migrate:create` diffing until resolved). (2) Home and the 6 interior pages now call `generateMetadata`, CMS `seo` override first, falling back to copy reused verbatim/near-verbatim from each page's own already-approved hero headline+subhead in `seed-content.ts` — repackaging existing approved copy into meta tags, not inventing new content, same boundary as writing a page's own `<title>`. (3) `/products` and `/insights` have no backing CMS Pages doc (they render from i18n messages + live catalog/insight data directly), so their `generateMetadata` is static, describing the page's real function (a catalog/resource listing), not a new company claim. `npx tsc --noEmit` clean, `npm run lint` shows zero new errors (pre-existing errors in `insights/not-found.tsx`/`RenderBlocks.tsx` untouched), `homepage.spec.ts` + `nav-links.spec.ts` full regression (en+ar) 12/12 pass. | Chose per-page CMS override + hardcoded-copy fallback over leaving pages metadata-less until an editor fills them in, because a blank/generic `<title>` on 9 page types indexed by Google is an active SEO cost every day it's live, not a neutral default — same reasoning as T-103's "schema built, content empty is still better than the field not existing." | `seo.ogImage` falls back to nothing (no image) when unset, same as `Products.seo` — not the site logo — since a wrong/generic OG image on a social share is worse than none, matching the existing per-product pattern.

D-51 | 2026-08-08 | While fixing D-50, found a more severe bug one layer down: `buildAlternates()` (`src/lib/seo/alternates.ts`) hardcoded `canonical` to the English URL for **every** call, regardless of which locale's page was rendering — so `/ar/about`'s own `<link rel="canonical">` pointed at `/about`, telling Google the Arabic page is not the authoritative version of itself. This is a real duplicate-content signal that suppresses a localized page from ranking at all once it has real content, not a cosmetic gap — and directly contradicts SEO_PLAYBOOK §5's own written rule ("Canonical on every page (self)"). Root cause: `buildAlternates`/`buildMetadata` never received the current-request locale, only the translated-locale list and path, so canonical had no way to know which page it was for and defaulted to English every time. Confirmed this isn't theoretical: `src/i18n/messages/fr.json`/`ru.json` are already fully translated (528 lines total across all 4 locale files, similarly sized) — this bug has been live and actively wrong for any fr/ru page that ships real CMS content, and would immediately affect Arabic the moment T-402 lands. Fixed: `buildAlternates` and `buildMetadata` both now take a required `locale`/`currentLocale` parameter (compile error if a caller omits it, so it can't silently default wrong again) and canonical is computed as `localeUrl(currentLocale, path)`; the `languages`/`x-default` hreflang map is unaffected (still lists every translated variant, unchanged). Threaded through all 6 production call sites (home, 6 interior pages via `[slug]/page.tsx`, `/products`, `/insights`, product-detail, insight-detail) plus `sitemap.ts`'s own call (which discards `canonical`, only uses `languages` — passed `routing.defaultLocale` as an arbitrary valid placeholder there, output byte-identical to before). Updated the 2 existing unit test files (`seo-alternates.spec.ts`, `seo-metadata.spec.ts`) that had encoded the bug as expected behavior, and added 2 new regression tests proving a non-English `currentLocale` yields its own self-referencing canonical, not English. `npx tsc --noEmit` clean; `seo-alternates.spec.ts` + `seo-metadata.spec.ts` 11/11 pass including the 2 new regression tests. | Ran the full unit + int suite before declaring done — found 5 pre-existing failures (`sitemap.spec.ts` ×3, `products-revalidate-hook.spec.ts` ×1, `contact-action.spec.ts` ×1) unrelated to this change. Confirmed via `git stash` + re-run against clean HEAD (commit `4c73053`, before any of this session's D-50/D-51 work) that all 5 fail identically without my changes — pre-existing test-DB-state/drift issues, not introduced here. Not fixed (out of scope, and blind-fixing a shared integration-test DB or an unrelated RFQ-email-subject-format mismatch risks masking a real regression elsewhere) — flagged honestly rather than silently left out of this entry. | e2e regression (`homepage.spec.ts`, `nav-links.spec.ts`, `product-detail.spec.ts`, `company.spec.ts`, `language-switcher.spec.ts`, en): 26/31 passed, 5 failed — all 5 confirmed pre-existing via the same `git stash` + re-run-against-clean-HEAD method (product-detail's Basmati cold-nav timeout, both language-switcher client-nav timeouts, nav-links footer) — dev-server flake under this session's sustained multi-hour load (same class as the D-46 precedent: "laptop idle-sleeping mid-run"), not a regression from this change.

D-52 | 2026-08-08 | Continuing the same SEO_PLAYBOOK §4 audit that found D-51: two more requirements from that section were never implemented despite being written since the playbook's creation. (1) `contactPoint` (tel/email/availableLanguage) on the Organization schema — `email`/`phone` were already fetched by every caller via `getSiteBrand()` but never passed into `organizationJsonLd()`. (2) `WebSite` schema "on home" — no such builder existed anywhere in `src/lib/seo/json-ld.tsx`. Fixed both: `organizationJsonLd()` takes optional `email`/`phone`, emits a `ContactPoint` (contactType "sales", `availableLanguage: ["English","Arabic","French","Russian"]` matching the 4 shipped locales) only when at least one is present — omitted, not a placeholder value, when both are blank; `websiteJsonLd()` is a new builder, rendered only in the home page component (`page.tsx`), not site-wide in the layout, matching the spec's "on home" scoping exactly (Organization stays site-wide in `layout.tsx`, unchanged). `npx tsc --noEmit` clean; extended `seo-json-ld.spec.tsx` with 3 new tests (contactPoint present/absent, WebSite shape) — 8/8 pass; `homepage.spec.ts` (en+ar) 8/8 pass. | Checked `npm run lint` before and after via `git stash` comparison — the one warning surfaced in `json-ld.tsx` ("unused eslint-disable directive") is pre-existing, confirmed identical on clean HEAD, not introduced here. | This closes out SEO_PLAYBOOK §4's structured-data checklist as fully implemented — Organization (now complete with contactPoint), Product, BreadcrumbList, FAQPage, and WebSite are all live; only the Rich Results Test manual validation step from §4's last line remains unautomated (a QA action, not a code gap).

D-53 | 2026-08-08 | T-202 Resources Hub, built per the existing P-09/C-16 spec (never actually planned/executed before, despite being spec'd since this playbook's creation). Deliberately skipped the GSD numbered-phase workflow (`/gsd-plan-phase`) for this — T-202 lives in `playbook/TASK_BACKLOG.md`'s separate T-0xx/T-1xx/T-2xx tracking system (per D-22's documented dual-track split), not `.planning/ROADMAP.md`'s 10 already-completed numbered phases; forcing a new task into that unrelated numbering system would mean fabricating a Phase 11 that doesn't fit the roadmap's own "six phases" framing, for zero benefit — the spec was already unambiguous (P-09 + C-16), no research/discussion/verification-loop ceremony was going to add anything a careful direct build + full test rigor wouldn't. Built `/resources`: aggregates 3 existing data sources rather than duplicating any of them — `SiteSettings.resourceDocuments` (new localized array field: title/description/file, same shape as `Products.seo`/`DocumentCard`, for site-level docs like company profile/sample COA/export checklist), `Certifications.certificatePdf` (existing field, was never surfaced anywhere before), `Products.downloads` (existing T-103 field, was only ever rendered per-product, never hub-aggregated). New `ResourceDocumentList` client component (row: icon, title, type+size derived from Payload's standard `mimeType`/`filesize` upload metadata, ungated download link) fires the new `spec_download` analytics event (added to `EventName`, `src/lib/analytics.ts` — that file's own header comment claiming the Plausible-vs-GA4 decision was "still deferred" was also stale, same root cause as the SEO_PLAYBOOK §9 note D-49 already fixed; corrected here too). Every document row honestly shows "available on request" (reusing the exact CertCard/DocumentCard wording convention) rather than a broken/placeholder link when a file hasn't been uploaded — which is every row right now, since all 3 sources are genuinely empty pending T-110/owner content, same pattern as everywhere else in this project. Added `/resources` to the footer nav (`GlobalFooter.tsx`'s own comment already states "Footer is the comprehensive discovery surface... so nothing is orphaned" — an unlinked page contradicts that explicit design intent) and the `nav.resources` i18n key across all 4 locales (English placeholder in ar/fr/ru, consistent with this codebase's existing precedent of leaving several nav keys — home/about/company/insights/export — in English pending professional translation, not machine-translating them). | Noticed while building this: ar/fr/ru's `nav`/`insights` namespaces are more incompletely translated than previously visible (5 of 9 nav keys, the entire `insights` namespace) — pre-existing, not this task's scope to fix, flagged here rather than silently worked around. | Full verification: `npx tsc --noEmit` clean; `npm run lint`/`lint:rtl` zero new issues; unit suite 92/97 pass (same 5 pre-existing failures as D-51, unchanged); e2e — `homepage.spec.ts`/`nav-links.spec.ts`/`company.spec.ts` fully clean (28 total), `language-switcher.spec.ts` ×2 and `product-detail.spec.ts` ×1 hit the same pre-existing dev-server cold-compile/client-nav flake already confirmed via `git stash` comparison earlier this session (identical test names/errors). The new `/resources` route itself hit this same flake on its own first-ever navigation (`nav-links.spec.ts`'s footer-links test) — verified directly via `curl` against a running dev server (200, real rendered content, correct empty-state copy) before re-running the Playwright test, which then passed clean once the route was warm — not a code bug, confirmed rather than assumed.

D-54 | 2026-08-08 | Investigated the 5 "pre-existing" test failures flagged (not fixed) across D-51/D-52/D-53, rather than leaving them as permanent background noise. 2 of 5 turned out to be genuinely fixable stale tests, not flakes: (1) `contact-action.spec.ts`'s RFQ subject assertion expected `"New RFQ: Basmati Rice"` but the real `buildSubject()` (`src/lib/contact-action.ts`, unchanged, correct) has always built `"[RFQ] {products} — {company}, {country}"` — a richer, more useful subject for sales triage; the test just predates that format and was never updated. Fixed the assertion to match real (better) behavior, not the code. (2) `products-revalidate-hook.spec.ts` expected `revalidatePath` called 8 times on a product save, but the real `revalidateCatalog.ts` (confirmed correct, same file D-48 already analyzed) calls it 12 times — 3 `revalidateAllLocales` invocations (index, old-flat-redirect-shim path, new-nested path) × 4 locales — because the old flat product URL still needs to stay fresh as a 301-redirect target post-T-105. The test only asserted the pre-T-105 8-call shape and never got updated when the redirect-shim revalidation was added. Fixed to assert all 12 calls explicitly. Both were caught by `it.toHaveBeenCalledTimes(N)`/`toBe(string)` assertions that fail loud on any drift — exactly the kind of stale-but-strict test that's worth fixing rather than leaving red forever, since a failing suite trains everyone to ignore failures. | The 3rd bucket (`sitemap.spec.ts` ×3) is a genuine environment/harness flake, not a code bug — went further than a stash-comparison this time: wrote a standalone reproduction script calling the actual `sitemap()` export (`src/app/sitemap.ts`, untouched by any of today's work) against a fresh isolated sqlite db with the exact same create-product-then-query pattern as the failing test, and it worked correctly (product included in output, category populated via `depth:1` as expected). Deleted and rebuilt `tests/int/.test.db` from scratch too — same 3 failures persisted, ruling out stale/corrupt test-db state. Root cause not fully isolated (likely resource contention from this session's many hours of concurrent dev servers/test runs), but the shipped `sitemap.ts` logic itself is proven correct by direct reproduction — not silently left as "probably fine," actually checked. | Result: unit+int suite improved from 92/97 to 94/97; the 3 remaining failures are the same confirmed-unrelated sitemap harness flake, unchanged.

D-55 | 2026-08-08 | T-401 first 3 Insights posts, at owner's explicit direction to research and write complete, publish-ready content rather than just a topic list. Chose 3 of SEO_PLAYBOOK §6's pre-approved seed topics, all deliberately generic/industry-level rather than VNP-specific: "How to Import Agricultural Products from India: A Documentation Checklist", "Incoterms for Food Buyers: FOB, CIF, and CFR Explained", "IQF vs Block-Frozen Vegetables: What the Difference Means for Buyers". Researched each via web search before writing (phytosanitary/COO/HS-code requirements, Incoterms 2020 FOB/CFR/CIF risk-transfer points, IQF vs block-freeze ice-crystal/cell-damage mechanics) rather than writing from assumed knowledge — sources: CBI/godigit/jkinternational/indiafilings for documentation, global-trade-solution/peitrade/mightyshipping for Incoterms, Wikipedia/nutrada/jooeverfoods/flexfoodsltd for IQF mechanics. Deliberately avoided any VNP-specific claim (no invented Brix values, no invented capacity numbers, no "our process") — every fact is generic industry/regulatory knowledge, safe to publish without owner fact-check, unlike a product-spec article would be. Checked every draft against CONTENT_PLAYBOOK §1's banned-word list and rhetorical-heading/exclamation-mark rules before seeding — zero hits. Full structured articles (H2 sections + bullet lists via Lexical heading/list nodes, not the existing `richText()` helper's single-paragraph-only shape — extended it locally in the new seed script) — 500-800 words each, not thin stubs. Cover images: 3 new generated SVG placeholders (`scripts/seed-assets/insight-*.svg`), same "self-authored placeholder, not real photography" convention already documented in `scripts/seed-assets/README.md` for the existing facility/logo/avatar placeholders — explicitly labeled "editorial cover — generated placeholder graphic" in the SVG itself. New `scripts/seed-insights-t401.ts`, kept separate from `scripts/seed-insights.ts` (whose 2 existing articles are thin e2e-test fixtures referencing the stale rice/spice placeholder catalog per D-23, not real editorial content — did not count those toward the "≥3 substantive posts" bar). | Insights isn't gated behind a literal show/hide toggle (checked `GlobalHeader`/`GlobalFooter`/`MobileNavPanel` — footer/mobile nav already link it unconditionally, only the trimmed 4-item desktop primary nav omits it, same "5-item nav" tradeoff as Facility & Quality/Resources) — "unhide at ≥3 posts" in the backlog is an intent bar (worth visiting), not a literal code flag to flip. | Verified: `npx tsc --noEmit` clean; ran the seed script against local dev db, all 3 created; direct curl + grep confirmed real structured HTML (H2 headings, 17 `<li>` across 3 `<ul>` lists, not flattened to paragraphs) and working cover-image URLs; `insights.spec.ts` 4/4 pass (en+ar); `a11y-audit.spec.ts` 8/9 pass — zero WCAG violations on every real page checked including `/insights`, only the 1 already-established Basmati product-detail cold-nav flake (same class confirmed multiple times earlier this session, unrelated).

D-56 | 2026-08-08 | T-401's deploy (piggybacking `scripts/seed-insights-t401.ts` onto the Vercel buildCommand, same pattern as T-207/T-208/T-209) failed in production. Root cause: my own T-202 migration (`20260808_t202_site_settings_resource_documents.ts`, same day, D-53) had a real schema bug — it put `title`/`description`/`_locale` directly as columns on the array-row table, copying `products_specs_*`'s pattern (T-103) where every sub-field on that row genuinely is locale-specific. `SiteSettings.resourceDocuments` mixes localized (title/description) and non-localized (file) sub-fields — the wrong pattern to copy. The right pattern was sitting in the same T-103 migration two field-groups over: `products_downloads`/`products_downloads_locales` (`Products.downloads` has the identical localized-label/non-localized-file shape) — didn't check that closely enough before writing the original migration, local sqlite dev never caught it because dev uses schema-push, not the migration file, so the bug was invisible until it hit real Postgres in Vercel's build. The build failed prerendering `/en/contact` with `relation "site_settings_resource_documents_locales" does not exist` — a query Payload's runtime generates expecting the correct child-table shape, which the broken migration never created. Fixed forward (couldn't edit the already-applied migration): new `20260808_t202b_fix_resource_documents_locales.ts` drops the wrong columns and creates the correct `_locales` child table, verified schema-for-schema against `products_downloads_locales` before writing it, not re-guessed. `resourceDocuments` was empty in every environment when the broken migration ran (T-110 still blocked) — nothing to migrate forward, safe to drop and recreate. Also found and fixed a real, separate issue in the same failed build: the seed script's `payload.destroy()` didn't let the Node process exit for ~11 minutes (all 3 inserts had completed and logged within seconds), burning most of the build's time before `npm run build` even started. Added a hard `process.exit(0)` after `destroy()` resolves. | The failed deployment never went live — Vercel does not promote a failed build to production, so the site kept serving the previous deployment throughout, zero visitor-facing impact. Caught before any user saw a broken page, but a real mistake nonetheless, logged plainly rather than folded quietly into D-53/D-55's "success" framing. | Lesson for future schema work in this codebase: local dev's sqlite schema-push means a hand-written Postgres migration is genuinely untested until it runs against real Postgres — there is no local verification step that catches a migration bug before either a teammate runs `db:seed`/`migrate` against a real Postgres or, worse, a production deploy does. Should have cross-checked the new field's shape against the closest existing analog (`Products.downloads`, already in the same file I was copying from) before writing, not after a failure forced it. Fix deployed and verified live via direct curl against `vnpglobal.in`: `/contact` (the page that failed to prerender before), `/resources`, `/insights`, and all 3 new T-401 articles all return 200 on the real production domain. `vercel.json`'s buildCommand reverted to normal (piggyback removed) in the same commit as this log entry.

D-57 | 2026-08-08 | T-206 (performance), reopened: D-40's diagnosis was real but the fix was wrong. D-40 found the react-hook-form/zod/Turnstile chunk (~90KB estimated, 367KB actual) loading on every page and wrapped `ContactBlockView` in `next/dynamic()` inside `RenderBlocks.tsx` to scope it to `/contact` only — measured no improvement, concluded Turbopack was shipping one unified bundle to every route regardless of bundler, logged as an open infra question (webpack vs wait-for-Turbopack). Re-investigated instead of accepting that conclusion. Tested forcing `next build --webpack`: failed outright first (Payload's GraphQL route can't resolve the `graphql` peer dependency under webpack, works under Turbopack — fixed by installing it), then succeeded but showed the identical bundle overlap D-40 found — ruling out "wrong bundler" as the cause. Found the real root cause via a documented Next.js issue (vercel/next.js#54935): `next/dynamic()` only actually code-splits when the call site is a Client Component — `RenderBlocks.tsx` has no `"use client"` directive, it's a Server Component, so the dynamic() wrapper D-40 added never did anything; ContactBlockView's whole tree stayed statically bundled regardless. `ContactBlockView` itself also can't be the client boundary (it uses `next-intl/server`'s `getTranslations`, must stay server-rendered). Fixed by moving the dynamic boundary one level deeper: new `ContactFormLazy.tsx` (`"use client"`) wraps just `ContactForm` — the actual heavy subtree — in `dynamic()`; `ContactBlockView` imports that client wrapper statically (fine, crossing server→client via static import doesn't break splitting, only a server-side `dynamic()` call does) and is itself back to a plain static import in `RenderBlocks.tsx`. Verified via direct chunk inspection, not just script-tag counting like D-40's original check: `grep`'d which built chunk actually contains `react-hook-form`, confirmed a 367KB chunk now appears in `/contact`'s script list and is absent from `/`'s — under both webpack and Turbopack, ruling out bundler-dependence definitively. Reverted the `graphql` dependency and stayed on Turbopack (default, stable) since webpack wasn't actually the fix. | Found and fixed a genuine UX gap surfaced by the fix itself: the `Suspense` fallback around `ContactForm` was `null` — harmless when the component was already bundled (no real wait), but now a real ~360KB network fetch happens where none did before, and this site's own target audience (CLAUDE.md: "varied networks", Gulf/SEA buyers) is exactly who'd notice a blank gap during that fetch. Added a minimal inline pulse-skeleton (`ContactFormLoading`, sized to the form's real field count) rather than a new shared Skeleton primitive — single call site, doesn't warrant a design-system component yet. | Chased what looked like a regression before confirming it wasn't: `contact-error-state.spec.ts`'s 2 tests (both exercise error/rate-limit paths with `page.route()` network mocking) failed intermittently after the fix. Ran them 3× against clean HEAD (git stash) before concluding anything — found the SAME 2 tests fail intermittently on unmodified code too (2/3, 2/3, 1/3 runs), conclusively pre-existing flakiness (likely the rate-limiter's own documented shared-IP-key limitation across repeated runs against one dev server), not caused by this fix. Verified via full regression: unit+int suite 94/97 unchanged baseline; broad e2e sweep first showed 8 failures, all sharing one signature (cold-navigation timeout on first hit) across unrelated pages — traced to ~15 accumulated stray `next`/`playwright` processes from this session's many hours of testing competing for system resources, not a code issue; killed them, re-ran clean: 17/18 pass, the 1 remaining (`/resources` footer-nav-links cold-nav) already independently verified live and correct via direct curl earlier the same session (D-53).

D-58 | 2026-08-09 | T-206 Lighthouse re-measurement, on a genuinely fresh machine (laptop rebooted, zero accumulated session load — ruling out D-57's "exhausted local Chrome" concern explicitly, not assumed). Result: **29/100 mobile, LCP 13.8s, TBT 1,890ms** — materially worse than D-32's original 51/100 baseline, not improved. Server itself is fast (TTFB 87ms via direct curl, ruled out first) — the entire cost is client-side JS bootup (`mainthread-work-breakdown`: 7.3s, `bootup-time`: 3.6s per Lighthouse diagnostics). D-57's fix (367KB react-hook-form chunk off non-contact pages) is real and confirmed still deployed correctly, but it was never going to single-handedly fix this — homepage still ships **840KB of JS across 17 chunks**, and the single largest (226KB) contains Payload CMS code (`grep` confirmed literal `"payload"` string in the bundle) leaking into a client chunk that loads on the public homepage, which has no business shipping any Payload code at all. Root cause of THIS leak not yet isolated (candidates: `@payloadcms/richtext-lexical`'s React renderer needed for `RichTextBlock`'s hydration, or some other shared import pulling Payload's client runtime along) — flagged as the real next step, not guessed at or fixed blind with the session's remaining time. | T-206 stays open. What's proven: (1) the D-57 fix is real, correctly deployed, and necessary but not sufficient; (2) the performance problem is bigger than the one chunk that fix addressed; (3) 840KB total / 226KB single chunk is the next concrete lead, with "why does homepage's bundle contain Payload code" as the specific question the next session should answer first. | Explicitly did not attempt a blind fix here — diagnosing "why Payload code is in the client bundle" needs its own investigation (likely an import-chain trace through `RenderBlocks.tsx`'s block components), not a guess under time pressure to "conclude." TASK_BACKLOG T-206 updated to reflect this precisely rather than overstating D-57 as the resolution.

D-59 | 2026-08-09 | Owner-observed: "text on the pages is not well formatted." Confirmed real, not vague — `RichTextBlock.tsx` (every CMS Page's rich-text sections), the Insight article body (`insights/[slug]/page.tsx`), and the product description (`products/[category]/[slug]/page.tsx`) all render Payload's Lexical output via `<RichText>` with only a bare `max-w-[720px] text-body` wrapper and zero styling for the elements it actually emits — confirmed by grepping the live rendered HTML: bare `<h2>`, bare `<p>`, no classes at all, falling back to the browser's default stylesheet. This directly explains why yesterday's T-401 articles (the most heading/list-heavy content on the site) would have looked wrong — DESIGN_SYSTEM §3's "Section H2s in Fraunces; H3 in Archivo 600" and the spacing scale were wired into every hand-built component but never into CMS-authored rich text. Fixed with one shared `.cms-richtext` class (globals.css, `:where()` selectors for zero specificity) targeting `p`/`h2`/`h3`/`ul`/`ol`/`li`/`a`/`strong` via the project's own real tokens (`--font-display`/`--text-heading` for h2, spacing-scale margins, logical properties throughout for RTL), applied at all 3 call sites. Chose hand-written CSS over installing `@tailwindcss/typography` — not present in the codebase, and its defaults would still need full customization to match Fraunces/the token scale, so it wouldn't have saved real work over ~50 lines of scoped CSS, consistent with how the rest of this design system is hand-rolled in `globals.css` rather than plugin-sourced. Verified via Playwright: screenshotted rendered output, checked computed styles on a live h2 (`fontFamily: Fraunces`, `fontSize: 28px`, `fontWeight: 600`, correct margins) — confirmed working, not just "CSS exists in the file." Unit suite 94/97 unchanged baseline (same 3 pre-existing sitemap flakes).

D-60 | 2026-08-09 | Owner-observed: WhatsApp button should use the same number on every page, centralized. Investigated every WhatsApp CTA site-wide rather than guessing which one was wrong: `WhatsAppFloatingButton`, `GlobalHeader`, `MobileNavPanel`, `ContactBlockView`, and `HeroBlock` all already correctly call `getSiteBrand()` for the live `waHref` (`HeroBlock` even has an explicit comment noting it deliberately ignores a "stale seeded href"). `CTABandBlock.tsx` — rendered at the bottom of nearly every page (home, products, insights, resources, product/insight detail, and every CMS Page via `RenderBlocks`) — was the one exception: it read `block.secondaryCta.href` straight from CMS-seeded content, a value baked in at seed time from `seed-content.ts`'s `WHATSAPP_PLACEHOLDER_LINK` constant and frozen per-page from then on. Practical effect: if the owner ever updates the real number in SiteSettings, every WhatsApp button on the site would follow except this one, which would keep showing whatever number existed when that page's content was first seeded — a real, silent, hard-to-notice drift bug (all pages currently show the same placeholder number today, so nothing visibly diverges yet — the bug is latent until the real number is set). Fixed to match `HeroBlock`'s existing pattern exactly: `CTABandBlock` is now `async`, calls `getSiteBrand()`, uses `waHref` instead of `block.secondaryCta.href`. Verified via curl across all 6 page types (`/`, `/about`, `/certifications`, `/products`, `/insights`, `/resources`) — identical number on every one. `npx tsc --noEmit` clean (async Server Component in `RenderBlocks.tsx`'s `BLOCK_MAP` works the same way `HeroBlock` already proved it does).

D-61 | 2026-08-09 | Owner asked for real narrative content per-page (excluding media/technical specs), interview-driven rather than invented, per CONTENT_PLAYBOOK's anti-fabrication discipline. Confirmed real facts via direct interview (`AskUserQuestion`, never asserted unilaterally): 3 founders — Virendra Patil (strategy, buyer relationships, compliance/documentation), Piyush Chavan (runs Piyush Farms, Karad — production/QC/sourcing/cold storage/traceability/packaging), Nilesh Kalbhor (farmer relationships/contract farming, harvest planning, export freight). Real founding story: Piyush's factory already had repeat business from other exporters before VNP Global existed; the three set up a dedicated export entity to capitalize on that track record. Written into About page (merged single richText block, replaces the old founder-section-intentionally-omitted gap) and Company page leadership tiles (real names replace the 3 generic role-only placeholders). Owner also gave explicit house style rule, applies sitewide, not just this content: **no em dashes** — "clearly indicates this was AI generated." Swept and removed from all existing published copy, not just new content: 45 instances in `seed-content.ts` + 24 in `scripts/seed-insights-t401.ts` (69 total across 5 Pages + 3 Insights), replaced with period/comma/colon per natural sentence break, never a blind regex substitution. Second, larger finding while auditing "is anything left stale": **SiteSettings and the Contact page were serving fake placeholder contact info live in production** — `sales@example.com`, all-zeros phone/WhatsApp, and a wrong "Nashik" address inconsistent with the real Karad facility mentioned everywhere else on the site. Replaced with owner-confirmed real values: email `info@vnpglobal.in`, phone/WhatsApp `+91 84088 07241`, registered office address (Pune, per Registrar of Companies record) on SiteSettings.address + the Contact page's contactBlock, factory address (Kavita Facility Management, Tasawade MIDC, Karad) on SiteSettings.factoryAddress. IEC and APEDA confirmed registered (owner has real numbers, adding later) — Certifications collection entries flipped to `status: "registered"`, Company page copy updated from "in progress" to registered wording. Placeholder tokens for the numbers initially used ad-hoc names, caught on a playbook revisit and corrected to match the already-established Open Placeholders convention: `{{CERT_NUMBER_IEC}}`/`{{CERT_NUMBER_APEDA}}`/`{{CERT_NUMBER_GST}}` on Company page + SiteSettings.legalIdentity, plus a new `{{CERT_NUMBER_CIN}}` (CIN wasn't in the original table). Open Placeholders table updated: those 4 marked confirmed-registered/pending-number, `{{FOUNDER_NAME}}`/`{{FOUNDER_BIO}}` marked resolved. Global Markets page got a real-facts-only shipping/logistics FAQ (−18°C transit temp, FCL-only, per-shipment doc set) — explicitly did NOT add testimonials: `seed-content.ts`'s `testimonials()` helper is unused sitewide on purpose (no real client quotes exist yet, D-01/D-31's zero-export-history discipline), fabricating even an anonymized quote would be a bigger Pitfall-5 violation than the page reading thin. Process findings worth keeping for next time: (1) direct-DB one-off scripts (Payload local API against `DATABASE_URL`) reach the same Postgres the live site/admin use, confirmed by cross-checking after each write — but MUST NOT call `revalidatePath` from that context (`context: { disableRevalidate: true }`), since it throws ("Invariant: static generation store missing") outside a live Next.js request; the tradeoff is on-demand ISR never fires for these writes, so a manual redeploy is required afterward. (2) Vercel's Redeploy dialog defaults to "Use existing Build Cache" checked, which reuses stale ISR/route-cache output and silently no-ops a redeploy for exactly this kind of direct-DB content fix — must be unchecked for a clean rebuild. (3) `Certifications` collection had zero `afterChange` hooks (unlike Pages/Insights/Products) — any edit, admin or script, needed a manual redeploy to ever go live. Added `revalidateCertifications.ts` (busts `/certifications` + `/` per locale), same pattern as `revalidatePage.ts`/`revalidateInsight.ts`. Also renamed `seed-content.ts`'s `WHATSAPP_PLACEHOLDER_LINK` → `WHATSAPP_LINK` (real number now) so the seed source of truth matches production instead of drifting stale — same fix applied to the seed file's hardcoded Contact address. **See D-62: most of the "pushed to production" claims in this entry were wrong at the time of writing — the writes went to a local SQLite file. They were re-applied to real production later the same day.**

D-62 | 2026-08-09 | **Every direct-DB script in D-61 wrote to a local SQLite file, not production.** `.env.local` has no `DATABASE_URL`, and `payload.config.ts` does `usePostgres = Boolean(process.env.DATABASE_URL)` with an `sqliteAdapter({ url: process.env.DATABASE_URI ?? "file:./payload.db" })` fallback (`DATABASE_URI` is `file:///payload.db`). A standalone `tsx` script calling `loadEnvConfig()` + `getPayload({ config })` therefore silently binds to that local file. It fails silently in the worst way: `payload.db` holds a near-complete copy of the real content, so reads return plausible data and writes report success. Cost: roughly an hour of telling the owner their Payload admin saves were "failing silently and never reaching the database," walking them through log-out/log-in cycles, and asking them to re-enter a founder story and re-add a FAQ block — **the admin had been working correctly the entire time**. The owner ended it by pasting the raw admin API response (`"message": "Updated successfully."` echoing the changed doc back), which is the evidence that should have been requested in the first two minutes. Everything the owner did through the admin UI had landed in production correctly; everything "pushed by script" had not. Diagnostic lesson recorded separately as a standing behavioural rule: when a check contradicts what the user reports seeing, the check is the prime suspect, and results must be stated as "the local SQLite copy shows X," never the source-agnostic "the DB shows X" that hid this for so long. 

Fix: production credentials are marked **Sensitive** in Vercel, so `vercel env pull` writes `DATABASE_URL="[SENSITIVE]"` and the CLI genuinely cannot retrieve them — the owner pasted the real value into `.env.vercel` (already gitignored at line 61) by hand. Deliberately NOT `.env.local`: that would point local `npm run dev` at the production database. Added `scripts/prod-env.ts`, a bootstrap that must be imported *first* (the adapter is chosen at config-build time, so setting `DATABASE_URL` after `payload.config` loads is too late), reads `.env.vercel`, hard-fails if `DATABASE_URL` is absent rather than silently falling back to SQLite, prints the resolved Postgres host on every run so the target is never ambiguous again, and forces `NODE_ENV=production` — `@payloadcms/db-postgres` only skips drizzle's schema push under that value, so a prod-pointed script without it can mutate the live schema. Added `scripts/prod-audit.ts` (read-only production state dump: em-dash counts per doc, certification statuses, SiteSettings, key copy) as the standing verification tool. 

Re-applied to real production, verified by re-running the audit: em dashes now 0 across all 7 pages and all 3 insights (25 instances, matched as exact old→new string pairs, never a blind regex); Contact page + `SiteSettings.address` now carry the real Registrar-of-Companies registered office (the live values had been `Plot 14, MIDC Industrial Area, Pune 422010` and a `street` field containing the company name with postal code 411035); IEC and APEDA certification entries flipped to `registered`; Company page copy moved off "registrations are in progress" to registered wording with `{{CERT_NUMBER_IEC}}`/`{{CERT_NUMBER_APEDA}}` tokens; `SiteSettings.legalIdentity` seeded with `{{CERT_NUMBER_CIN}}`/`{{CERT_NUMBER_GST}}`/`{{CERT_NUMBER_IEC}}`. Two things deliberately left alone: production's `factoryAddress` already had *more* detail than the local copy (real plot numbers `B-134 & B-172`), and the Company hero subhead's "leadership with decades of trade experience" is an unverified claim that is the owner's call, not a content-cleanup decision. Also corrected: D-61's claim that production was serving fake `sales@example.com`/all-zeros contact details was itself a local-SQLite artifact — production had the real email/phone/WhatsApp throughout.







