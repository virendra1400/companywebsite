{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "buildCommand": "CI=true npx payload generate:importmap && CI=true npx payload migrate && CI=true npx tsx scripts/fix-company-page-false-claims.ts && npm run build"
}
