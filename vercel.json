{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "buildCommand": "CI=true npx payload migrate:create init --force-accept-warning || true; CI=true npx payload migrate:fresh --force-accept-warning --forceAcceptWarning; npm run db:seed && npm run build"
}
