{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "buildCommand": "CI=true npx payload generate:importmap && CI=true npx payload migrate && CI=true npx tsx scripts/update-about-page-t108.ts && npm run build"
}
