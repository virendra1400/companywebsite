{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "buildCommand": "CI=true npx payload generate:importmap && CI=true npx payload migrate && npx tsx scripts/fix-logo-crop-t209.ts && npm run build"
}
