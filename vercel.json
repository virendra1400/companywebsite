{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "buildCommand": "CI=true npx payload migrate:create init --force-accept-warning && CI=true npx payload migrate && npm run db:seed && npm run build"
}
