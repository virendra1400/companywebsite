---
phase: 06-design-system-elevation-premium-type-scale-display-tokens-rh
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/blocks/CTABandBlock.tsx
  - src/components/blocks/ContactBlockView.tsx
  - src/components/blocks/CertStripBlock.tsx
  - src/components/blocks/DocumentCardBlock.tsx
  - src/components/blocks/FeatureGridBlock.tsx
  - src/components/blocks/MediaGalleryBlock.tsx
  - src/components/blocks/ExportMapBlock.tsx
  - src/components/blocks/RichTextBlock.tsx
  - src/components/blocks/StatsBandBlock.tsx
  - src/components/products/ProductCard.tsx
  - src/components/insights/InsightCard.tsx
  - src/components/blocks/CertCard.tsx
autonomous: true
requirements: [D-04]
must_haves:
  truths:
    - "All 9 CMS block section wrappers (CTABand, Contact, CertStrip, DocumentCard, FeatureGrid, MediaGallery, ExportMap, RichText, StatsBand) step desktop vertical padding up to 96px via a trailing xl:py-4xl on the existing recipe; HeroBlock is untouched (Phase 7 scope) (D-04)."
    - "ProductCard and InsightCard converge on rounded-card (10px) resting radius, shadow-card resting elevation, and shadow-card-hover on hover/focus — replacing rounded-lg and group-hover:shadow-md / group-focus-visible:shadow-md (D-04)."
    - "CertCard converges on rounded-card + shadow-card (resting only — it has no hover group), replacing its reliance on the Card primitive's default rounded-xl / shadow-sm (D-04)."
    - "The shadcn Card primitive (src/components/ui/card.tsx) is NOT hand-edited; all card overrides happen at the call site via the existing cn() / tailwind-merge pattern."
    - "npm run lint:rtl stays green — every new utility (xl:py-4xl, rounded-card, shadow-card, shadow-card-hover) is direction-agnostic and matches no banned physical-direction pattern."
    - "Homepage and one trust page render with no layout regression after the rhythm/card changes (CONTEXT non-negotiable visual check)."
  artifacts:
    - path: "src/components/blocks/StatsBandBlock.tsx"
      provides: "representative of the 9-block section-rhythm recipe update"
      contains: "xl:py-4xl"
    - path: "src/components/products/ProductCard.tsx"
      provides: "card recipe: rounded-card + shadow-card + shadow-card-hover via cn() override"
      contains: "shadow-card-hover"
    - path: "src/components/insights/InsightCard.tsx"
      provides: "card recipe applied identically to insight cards"
      contains: "shadow-card-hover"
    - path: "src/components/blocks/CertCard.tsx"
      provides: "card recipe (resting radius + shadow, no hover) on both ternary branches"
      contains: "rounded-card"
  key_links:
    - from: "src/components/blocks/*Block.tsx (9 section blocks)"
      to: "src/app/globals.css --spacing-4xl (96px)"
      via: "xl:py-4xl utility auto-generated from --spacing-4xl"
      pattern: "xl:py-4xl"
    - from: "src/components/{products/ProductCard,insights/InsightCard,blocks/CertCard}.tsx"
      to: "src/app/globals.css --radius-card / --shadow-card / --shadow-card-hover"
      via: "rounded-card / shadow-card / shadow-card-hover utilities auto-generated from the card tokens, applied through cn()/tailwind-merge overriding the Card primitive defaults"
      pattern: "shadow-card"
  prohibitions:
    - statement: "MUST NOT hand-edit src/components/ui/card.tsx — it is vendor-owned shadcn registry output (RTL-lint exempt); all card overrides happen at the call site via cn()."
      status: enforced
      verification: automated
    - statement: "MUST NOT touch HeroBlock padding/sizing — its hero rhythm is Phase 7 'Hero & Homepage Narrative' scope."
      status: enforced
      verification: automated
    - statement: "MUST NOT apply display tokens (text-display-lg / text-display-xl / font-display / tracking-display) to any component this phase — that is Phase 7."
      status: enforced
      verification: judgment
    - statement: "MUST NOT wire tabular-nums into StatsBandBlock or any spec table this phase — that is Phase 8."
      status: enforced
      verification: judgment
    - statement: "MUST NOT touch GlobalFooter or LocaleFallbackNotice — they are chrome, not section blocks, and are outside the block-recipe contract even though the footer uses py-2xl."
      status: enforced
      verification: judgment
---

<objective>
Apply the amended design system from Plan 06-01 across the component layer: bump desktop section rhythm to 96px on the 9 CMS block wrappers, and converge the three card-family components on the new hairline radius + subtle ambient shadow system. This realizes the "section rhythm 64-96px" and "hairline card system" halves of the Phase 6 roadmap goal (D-04). It depends on 06-01 because Tailwind v4 generates the `p-4xl` / `rounded-card` / `shadow-card` utilities FROM the `@theme` tokens — the classes are no-ops until those tokens exist.

Purpose: D-04 — tighter, more premium rhythm and card elevation, using the single-source-of-truth tokens from 06-01, applied through the codebase's existing recipe strings and `cn()` override pattern (no new mechanism, no primitive edits).
Output: 9 block components with the extended rhythm recipe, 3 card components on the converged card recipe, lint:rtl green, and a homepage + trust-page visual-regression sign-off.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-CONTEXT.md
@.planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-UI-SPEC.md
@.planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-01-SUMMARY.md
@src/components/blocks/StatsBandBlock.tsx
@src/components/products/ProductCard.tsx
@src/components/blocks/CertCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the section-rhythm recipe to 96px desktop padding across the 9 CMS blocks</name>
  <files>src/components/blocks/CTABandBlock.tsx, src/components/blocks/ContactBlockView.tsx, src/components/blocks/CertStripBlock.tsx, src/components/blocks/DocumentCardBlock.tsx, src/components/blocks/FeatureGridBlock.tsx, src/components/blocks/MediaGalleryBlock.tsx, src/components/blocks/ExportMapBlock.tsx, src/components/blocks/RichTextBlock.tsx, src/components/blocks/StatsBandBlock.tsx</files>
  <read_first>
    - src/components/blocks/StatsBandBlock.tsx (representative: its `<section>` className ends the responsive padding recipe with `md:py-3xl xl:px-xl` — this is the string to extend; 7 other blocks share the identical `${sectionBg(index)} px-md py-2xl md:px-lg md:py-3xl xl:px-xl` recipe)
    - src/components/blocks/CTABandBlock.tsx (the one variant: its recipe is `bg-primary-900 px-md py-2xl text-center text-white md:px-lg md:py-3xl xl:px-xl` — same tail, extend it the same way)
    - src/components/blocks/HeroBlock.tsx (DO NOT EDIT — read only to confirm it uses `py-3xl` and is excluded per Phase 7 scope)
    - .planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-UI-SPEC.md (section "Section rhythm recipe" carries the old→new recipe contract and the HeroBlock exclusion)
  </read_first>
  <action>
    In each of the 9 listed block components, extend the section wrapper's responsive padding recipe by appending ` xl:py-4xl` immediately after the existing `xl:px-xl`, so desktop vertical section padding steps from 64px (3xl) up to 96px (the new `--spacing-4xl` from 06-01) at the xl breakpoint (D-04). Mobile (`py-2xl` = 48px) and tablet (`md:py-3xl` = 64px) steps are unchanged — this only adds the desktop step, covering the full 64-96px range across the responsive scale. The transformed tail reads `… md:py-3xl xl:px-xl xl:py-4xl` in every file.
    HeroBlock is EXPLICITLY EXCLUDED (its padding is Phase 7 scope) — do not open or edit it. Do not edit GlobalFooter or LocaleFallbackNotice (chrome, not section blocks). Change nothing else in these files.
  </action>
  <acceptance_criteria>
    - Each of the 9 named block files contains `xl:py-4xl` exactly once — a recursive grep of `src/components/blocks/`, `src/components/products/`, `src/components/insights/` for `xl:py-4xl` returns 9 block hits (StatsBand/CTABand/Contact/CertStrip/DocumentCard/FeatureGrid/MediaGallery/ExportMap/RichText).
    - `src/components/blocks/HeroBlock.tsx` still contains `py-3xl` and does NOT contain `xl:py-4xl` (untouched — positive check that Hero rhythm is unchanged).
    - `src/components/chrome/GlobalFooter.tsx` is unchanged (no `xl:py-4xl`).
    - `npm run lint:rtl` exits 0 (`py-*` is not a banned physical-direction utility).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -rl 'xl:py-4xl' src/components/blocks src/components/products src/components/insights | wc -l | tr -d ' ')" = "9" && grep -F 'py-3xl' src/components/blocks/HeroBlock.tsx && ! grep -q 'xl:py-4xl' src/components/blocks/HeroBlock.tsx && ! grep -q 'xl:py-4xl' src/components/chrome/GlobalFooter.tsx && node scripts/check-physical-direction.mjs</automated>
  </verify>
  <done>All 9 CMS block section wrappers apply `xl:py-4xl` (96px desktop vertical padding); HeroBlock and the chrome components are untouched; lint:rtl green.</done>
</task>

<task type="auto">
  <name>Task 2: Converge ProductCard, InsightCard, and CertCard on the hairline card recipe</name>
  <files>src/components/products/ProductCard.tsx, src/components/insights/InsightCard.tsx, src/components/blocks/CertCard.tsx</files>
  <read_first>
    - src/components/products/ProductCard.tsx (the `<Card className="…">` currently reads `rounded-lg border … group-hover:shadow-md … group-focus-visible:shadow-md …` — this is the string to converge)
    - src/components/insights/InsightCard.tsx (identical Card className to ProductCard — same change)
    - src/components/blocks/CertCard.tsx (its `<Card className={ … }>` is a ternary with two branches, neither carrying a rounded-/shadow- class — both rely on the Card primitive default; add resting radius + shadow to both branches)
    - src/components/ui/card.tsx (READ ONLY — vendor-owned; note its base is `… rounded-xl border … shadow-sm`; tailwind-merge lets call-site classes override these — do NOT edit this file)
    - .planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-UI-SPEC.md (section "Card recipe" carries the converge-on contract)
  </read_first>
  <action>
    Converge the three card-family components on the 06-01 card tokens via the existing call-site `cn()` / className override (D-04). Do NOT edit `src/components/ui/card.tsx`.
    - ProductCard.tsx and InsightCard.tsx (identical Card className): change the resting radius from the current `rounded-lg` to `rounded-card` (10px), add `shadow-card` as the resting elevation (the primitive's default resting shadow is otherwise the neutral shadow-sm), and change the two hover/focus elevation utilities from `group-hover:shadow-md` / `group-focus-visible:shadow-md` to `group-hover:shadow-card-hover` / `group-focus-visible:shadow-card-hover`. Keep everything else identical (border-neutral-300, the -translate-y hover lift, ring-accent-600 focus ring, p-md/md:p-lg padding, gap-sm).
    - CertCard.tsx: in BOTH branches of the className ternary, add `rounded-card` and `shadow-card` (converging it off the primitive's implicit rounded-xl / shadow-sm). CertCard has no group-hover wrapper, so do NOT add shadow-card-hover to it — resting radius + shadow only. Keep the existing border/border-2 accent treatment and padding untouched.
    All three overrides ride the same tailwind-merge mechanism ProductCard/InsightCard already use to override the Card primitive — no new utility mechanism, no primitive edit.
  </action>
  <acceptance_criteria>
    - `src/components/products/ProductCard.tsx` and `src/components/insights/InsightCard.tsx` each contain `rounded-card`, `shadow-card`, `group-hover:shadow-card-hover`, and `group-focus-visible:shadow-card-hover`.
    - `src/components/blocks/CertCard.tsx` contains `rounded-card` and `shadow-card` (both ternary branches).
    - `src/components/ui/card.tsx` is unchanged (`git diff --quiet src/components/ui/card.tsx` succeeds).
    - `npm run lint:rtl` exits 0 (`rounded-card` / `shadow-card` / `shadow-card-hover` match no banned pattern — `\brounded-l\b` etc. do not match `rounded-card`).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'shadow-card-hover' src/components/products/ProductCard.tsx && grep -F 'shadow-card-hover' src/components/insights/InsightCard.tsx && grep -F 'rounded-card' src/components/products/ProductCard.tsx && grep -F 'rounded-card' src/components/insights/InsightCard.tsx && test "$(grep -c 'rounded-card' src/components/blocks/CertCard.tsx)" -ge "2" && grep -F 'shadow-card' src/components/blocks/CertCard.tsx && git diff --quiet src/components/ui/card.tsx && node scripts/check-physical-direction.mjs</automated>
  </verify>
  <done>ProductCard/InsightCard use rounded-card + shadow-card resting + shadow-card-hover on hover/focus; CertCard uses rounded-card + shadow-card (resting) on both branches; the shadcn Card primitive is untouched; lint:rtl green and build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design token → rendered CMS pages | The rhythm/card recipe changes alter padding and elevation on every CMS-driven page; the risk is layout regression on editor-authored content, not untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering (layout integrity) | CMS-driven pages regressing from the rhythm/card recipe change | medium | mitigate | Recipe change is additive (one appended `xl:py-4xl`; card overrides via tailwind-merge). CONTEXT-mandated human visual-regression check on homepage + one trust page before done; `npm run build` gate. |
| T-06-04 | Elevation of Privilege (scope creep) | applying display tokens or tabular-nums outside this phase's scope | low | mitigate | Prohibitions in must_haves forbid touching HeroBlock, the Card primitive, display-token application (Phase 7), and tabular-nums wiring (Phase 8); enforced by the HeroBlock/card.tsx unchanged assertions. |
| T-06-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No package is installed this phase — only existing components and existing Tailwind utilities are touched. |
</threat_model>

<verification>
- `npm run lint:rtl` exits 0 (all new utilities direction-agnostic; verified against the banned-pattern list in scripts/check-physical-direction.mjs).
- `npm run build` succeeds (all 12 edited components compile with the new utilities resolving against 06-01's tokens).
- `npm test` (vitest) passes — no existing component/integration test regresses.
- Human visual-regression check (CONTEXT.md NON-NEGOTIABLE, per 06-UI-SPEC backstop): with the dev server running, load the homepage (`/en`) and one trust page (e.g. `/en/certifications` or a product detail page) and confirm no layout regression — sections show the increased desktop vertical rhythm (96px) and cards show the 10px radius + subtle ambient shadow, with no broken/overflowing layout. If skipped, this is insufficient_spec at verify time, not a silent pass.
</verification>

<success_criteria>
- 9 CMS block wrappers apply `xl:py-4xl`; HeroBlock and chrome untouched (D-04).
- ProductCard/InsightCard/CertCard converged on rounded-card + shadow-card (+ shadow-card-hover on the two hover cards); Card primitive unedited (D-04).
- lint:rtl green, build succeeds, existing tests pass.
- Homepage + one trust page pass visual-regression sign-off.
</success_criteria>

<output>
Create `.planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-02-SUMMARY.md` when done.
</output>
