---
phase: 06-design-system-elevation-premium-type-scale-display-tokens-rh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/(site)/[locale]/layout.tsx
  - .planning/milestones/v2.0-phases/01-foundation-cms-decision/01-UI-SPEC.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "globals.css @theme exposes display-lg (52px / weight 300 / line-height 1.1) and display-xl (56px / weight 300 / line-height 1.05) tiers plus --tracking-display (-0.025em), so Tailwind v4 auto-generates text-display-lg / text-display-xl / tracking-display utilities for Phase 7 hero use (D-01)."
    - "A Latin-only display face (Geist, weight 300) loads via next/font/google and exposes --font-geist-display; --font-display resolves to it on en/fr/ru and falls back to --font-sans (Plex) on ar — the Latin display face never reaches Arabic content (D-02)."
    - "Section rhythm gains --spacing-4xl (96px) and the card system gains --radius-card (10px) plus subtle primary-900-tinted --shadow-card / --shadow-card-hover ambient elevation tokens (D-04)."
    - "No @theme color token is added or changed: the 11 --color-* tokens and every body-tier --text-label/body/heading/display and --spacing-xs..3xl key stay byte-identical (D-03 + body-tier lock)."
    - "Tabular figures are available via Tailwind's framework-native tabular-nums utility — no new token is added because the utility already exists in Tailwind v4 (D-05)."
    - "The archived Phase-1 UI-SPEC carries an amendment note so future agents read the display-tier lock as lifted, not the old exactly-4-sizes/2-weights rule as still-binding."
  artifacts:
    - path: "src/app/globals.css"
      provides: "8 new @theme tokens: --text-display-lg/xl (+ line-height/font-weight modifiers), --tracking-display, --font-display, --spacing-4xl, --radius-card, --shadow-card, --shadow-card-hover"
      contains: "--text-display-xl: 56px"
    - path: "src/app/(site)/[locale]/layout.tsx"
      provides: "Geist locale-scoped Latin display font loader; --font-geist-display set on <html> for en/fr/ru only"
      contains: "--font-geist-display"
    - path: ".planning/milestones/v2.0-phases/01-foundation-cms-decision/01-UI-SPEC.md"
      provides: "Phase 6 display-tier amendment note lifting the 4-sizes/2-weights lock for display only"
      contains: "Phase 6"
  key_links:
    - from: "src/app/(site)/[locale]/layout.tsx"
      to: "src/app/globals.css"
      via: "--font-geist-display CSS variable set on <html> is consumed by --font-display's nested fallback chain"
      pattern: "font-geist-display"
    - from: "src/app/globals.css"
      to: "Tailwind v4 auto-generated utilities (text-display-lg/xl, tracking-display, font-display, p-4xl, rounded-card, shadow-card, shadow-card-hover)"
      via: "@theme namespace convention (--text-*, --spacing-*, --radius-*, --shadow-*, --tracking-*, --font-*) drives utility generation with zero extra CSS"
      pattern: "spacing-4xl"
  prohibitions:
    - statement: "MUST NOT touch any @theme color token (the --color-* ramp) — colors are unchanged this phase (D-03 non-negotiable)."
      status: enforced
      verification: automated
    - statement: "MUST NOT change body-tier typography (--text-label/body/heading/display sizes, weights, line-heights) or --spacing-xs..3xl — only the display tier is amended (D-01)."
      status: enforced
      verification: automated
    - statement: "MUST NOT apply the new display tokens to any component (Hero headline is Phase 7); this plan only makes the tokens/utilities exist."
      status: enforced
      verification: judgment
---

<objective>
Elevate the existing launch-ready design-token system to premium tier by amending `src/app/globals.css` `@theme` with a display-tier type scale, a locale-scoped Latin display face, a desktop rhythm step, and a tightened card radius/shadow system — the gating foundation for Phase 7 (premium hero) and Phase 8 (component polish). This plan makes the tokens and font wiring exist; applying them to components is Plan 06-02 (rhythm/card recipes) and Phases 7/8 (display headline, stat cells).

Purpose: Deliver D-01 (display-tier type scale amendment), D-02 (Geist Latin display face, never on `ar`), D-03 (colors untouched), D-04 (96px rhythm step + hairline card tokens), and D-05 (tabular figures confirmed framework-native) — without a color overhaul, without breaking RTL, and without body-tier changes.
Output: Amended `globals.css` `@theme` block, Geist font loader wired locale-scoped into the site layout, and a display-tier amendment note appended to the archived Phase-1 UI-SPEC.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-CONTEXT.md
@.planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-UI-SPEC.md
@src/app/globals.css
@src/app/(site)/[locale]/layout.tsx
</context>

<artifacts_this_phase_produces>
New CSS custom properties (append to `globals.css` `@theme`, no existing key touched):
- `--text-display-lg: 52px` + `--text-display-lg--line-height: 1.1` + `--text-display-lg--font-weight: 300`
- `--text-display-xl: 56px` + `--text-display-xl--line-height: 1.05` + `--text-display-xl--font-weight: 300`
- `--tracking-display: -0.025em` (one proportional em token shared by both tiers: -1.3px@52px / -1.4px@56px, matching the Stripe 56px/-1.4px reference at the xl tier)
- `--font-display: var(--font-geist-display, var(--font-sans))` (nested fallback → Plex on `ar`)
- `--spacing-4xl: 96px`
- `--radius-card: 10px`
- `--shadow-card: 0 1px 2px 0 rgb(15 46 34 / 0.04), 0 1px 1px 0 rgb(15 46 34 / 0.03)`
- `--shadow-card-hover: 0 8px 20px -4px rgb(15 46 34 / 0.10), 0 2px 6px -2px rgb(15 46 34 / 0.05)`

Tailwind v4 utilities these auto-generate (zero extra CSS): `text-display-lg`, `text-display-xl`, `tracking-display`, `font-display`, `p-4xl` / `py-4xl` / `px-4xl` (+ responsive variants), `rounded-card`, `shadow-card`, `shadow-card-hover`.

Font-loader export (`layout.tsx`): `geistDisplay` loader exposing the `--font-geist-display` CSS variable; `displayVar` locale-gated class string (empty on `ar`).

Reused framework-native (NO artifact created — ladder rung 3/4, reuse not reinvent): `font-light` (Tailwind default weight-300 utility, never removed by this file) reserved for the two display tiers; `tabular-nums` (Tailwind v4 built-in `font-variant-numeric` utility) for D-05 — its wiring into StatsBand/SpecTable is Phase 8, this phase only confirms it exists.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Amend globals.css @theme with display-tier, rhythm, and card tokens</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (read the full @theme block first — note the existing color ramp lines, --spacing-xs..3xl, the four --text-* keys, --font-weight-regular/semibold, and the --font-sans nested-fallback pattern with its comment; these are the patterns to mirror and the keys to leave byte-identical)
    - .planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-UI-SPEC.md (section "Token Amendment — globals.css @theme" carries the exact token block and naming rationale)
  </read_first>
  <action>
    Append a new comment-delimited block INSIDE the existing `@theme { … }` block (place it after the `--font-sans` declaration, before `--breakpoint-xl`). Add exactly these declarations, following the file's existing `--text-*` / `--spacing-*` naming convention (D-01, D-04):
    - `--text-display-lg: 52px;` with modifiers `--text-display-lg--line-height: 1.1;` and `--text-display-lg--font-weight: 300;`
    - `--text-display-xl: 56px;` with modifiers `--text-display-xl--line-height: 1.05;` and `--text-display-xl--font-weight: 300;`
    - `--tracking-display: -0.025em;` (one proportional token shared by both tiers)
    - `--font-display: var(--font-geist-display, var(--font-sans));` (mirrors the existing --font-sans nested-fallback pattern so Arabic, which never sets --font-geist-display, falls back to Plex — D-02)
    - `--spacing-4xl: 96px;` (new 4th rhythm step for the xl breakpoint — D-04)
    - `--radius-card: 10px;` (D-04)
    - `--shadow-card: 0 1px 2px 0 rgb(15 46 34 / 0.04), 0 1px 1px 0 rgb(15 46 34 / 0.03);` (subtle brand-tinted ambient elevation — the rgb triplet is the existing primary-900 hex re-expressed for alpha syntax, NOT a new color — D-04)
    - `--shadow-card-hover: 0 8px 20px -4px rgb(15 46 34 / 0.10), 0 2px 6px -2px rgb(15 46 34 / 0.05);`
    Do NOT add a new `--font-weight-*` token: weight 300 is carried by the per-tier `--font-weight: 300` modifiers above plus Tailwind's built-in `font-light`; `font-light` is reserved for these two display tiers only (body/heading/label keep regular/semibold).
    Also revise the existing typography comment above `--text-label` so it no longer claims the whole scale is locked to four sizes / two weights: scope that lock to BODY tiers only and note that the display tier is amended per Phase 6 (D-01) — leaving a stale in-code claim that contradicts the new tokens is not acceptable.
    Do NOT touch the `--color-*` ramp (D-03), the `--spacing-xs..3xl` keys, or any `--text-label/body/heading/display` key including `--text-display: 40px` / `--text-display--font-weight: 600` (body-tier lock). Do NOT add a tabular-figures token — `tabular-nums` is already a Tailwind v4 built-in (D-05).
  </action>
  <acceptance_criteria>
    - `grep -F -- "--text-display-lg: 52px" src/app/globals.css` matches, and `--text-display-lg--line-height: 1.1` and `--text-display-lg--font-weight: 300` are present.
    - `grep -F -- "--text-display-xl: 56px" src/app/globals.css` matches, and `--text-display-xl--line-height: 1.05` and `--text-display-xl--font-weight: 300` are present.
    - `--tracking-display: -0.025em`, `--font-display: var(--font-geist-display, var(--font-sans))`, `--spacing-4xl: 96px`, and `--radius-card: 10px` are each present verbatim.
    - `--shadow-card:` and `--shadow-card-hover:` declarations are present, each using `rgb(15 46 34 / …)` (primary-900, no new color literal introduced).
    - `grep -c "^\s*--color-" src/app/globals.css` still equals 11 (color ramp untouched — D-03).
    - `--text-display: 40px` and `--text-display--font-weight: 600` are still present (existing display tier + body-tier lock untouched — D-01).
    - `npm run lint:rtl` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-physical-direction.mjs && grep -F -- "--text-display-xl: 56px" src/app/globals.css && grep -F -- "--tracking-display: -0.025em" src/app/globals.css && grep -F -- "--spacing-4xl: 96px" src/app/globals.css && grep -F -- "--radius-card: 10px" src/app/globals.css && test "$(grep -c '^\s*--color-' src/app/globals.css)" = "11" && grep -F -- "--text-display: 40px" src/app/globals.css</automated>
  </verify>
  <done>All 8 new display/rhythm/card tokens exist in the @theme block with the exact values above; the color ramp (11 tokens) and every body-tier type/spacing key are byte-identical; the stale typography comment is corrected to scope the 4-sizes/2-weights lock to body tiers; lint:rtl is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Geist Latin display font, locale-scoped so it never reaches Arabic</name>
  <files>src/app/(site)/[locale]/layout.tsx</files>
  <read_first>
    - src/app/(site)/[locale]/layout.tsx (read the full file — note the `IBM_Plex_Sans, IBM_Plex_Sans_Arabic` import from next/font/google, the two module-scope loaders with `variable`/`display: "swap"`, the `fontVar = locale === "ar" ? plexSansArabic.variable : plexSans.variable` gate, and the `<html lang={locale} dir={dir} className={fontVar}>` line — mirror this exact pattern)
    - .planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-UI-SPEC.md (section "Font loading wiring" carries the exact Geist loader options and the displayVar gating snippet)
    - src/app/globals.css (confirm --font-display consumes --font-geist-display via nested fallback — Task 1)
  </read_first>
  <action>
    Add `Geist` to the existing `next/font/google` import alongside `IBM_Plex_Sans, IBM_Plex_Sans_Arabic`. At module scope, beside the two Plex loaders, add a Geist loader with these exact options (D-02): `subsets: ["latin"]`, `weight: ["300"]`, `variable: "--font-geist-display"`, `display: "swap"` (swap prevents FOIT; latin-only subset keeps the download small; single 300 weight is all the display tiers use).
    Inside `LocaleLayout`, immediately after the existing `const fontVar = …` line, add `const displayVar = locale === "ar" ? "" : geistDisplay.variable;` — the empty string on `ar` is the entire locale-scoping mechanism: Arabic never sets `--font-geist-display`, so `--font-display` falls back to `--font-sans` (Plex Sans Arabic) and the Latin display face never reaches Arabic content (D-02, Arabic integrity non-negotiable).
    Change the opening `<html>` tag's className from `className={fontVar}` to interpolate both variables and trim so `ar` yields a single class with no trailing space: className becomes the trimmed join of `fontVar` and `displayVar`.
    Do NOT add Geist to the Arabic branch. Do NOT change the two Plex loaders, the body `font-family: var(--font-sans)` in globals.css, or any other file — this is the entire locale-scoped display-font deliverable.
  </action>
  <acceptance_criteria>
    - The `next/font/google` import includes `Geist`.
    - A `geistDisplay = Geist({ … })` loader exists with `subsets: ["latin"]`, `weight: ["300"]`, `variable: "--font-geist-display"`, and `display: "swap"`.
    - `const displayVar = locale === "ar" ? "" : geistDisplay.variable;` is present (empty on ar).
    - The `<html>` className applies both `fontVar` and `displayVar` and is trimmed (no trailing space on ar).
    - `npx tsc --noEmit` passes (the Geist loader type-checks under next/font).
    - `npm run lint:rtl` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'Geist' 'src/app/(site)/[locale]/layout.tsx' && grep -F -- '--font-geist-display' 'src/app/(site)/[locale]/layout.tsx' && grep -F 'locale === "ar" ? "" : geistDisplay.variable' 'src/app/(site)/[locale]/layout.tsx' && npx tsc --noEmit && node scripts/check-physical-direction.mjs</automated>
  </verify>
  <done>Geist loads via next/font/google exposing --font-geist-display; displayVar is empty on ar and set on en/fr/ru; the html className includes both font variables trimmed; tsc and lint:rtl pass. Latin display face is structurally impossible to leak onto ar (variable never set at html scope for Arabic).</done>
</task>

<task type="auto">
  <name>Task 3: Append display-tier amendment note to the archived Phase-1 UI-SPEC</name>
  <files>.planning/milestones/v2.0-phases/01-foundation-cms-decision/01-UI-SPEC.md</files>
  <read_first>
    - .planning/milestones/v2.0-phases/01-foundation-cms-decision/01-UI-SPEC.md (read the `## Typography` section — the table and the line stating the scale is exactly 4 sizes / 2 weights; the amendment note anchors here)
    - .planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-CONTEXT.md (Locked Decision 1 — the exact scope of what is lifted vs still-binding)
  </read_first>
  <action>
    Append a short "Phase 6 amendment" note to the `## Typography` section of the archived Phase-1 UI-SPEC (immediately after the line stating the scale is exactly 4 sizes / 2 weights). The note must state, so a future agent does not re-read the old lock as still-binding (D-01):
    - The Phase-1 lock (exactly 4 sizes / 2 weights / 40px max) is AMENDED by Phase 6 for the DISPLAY tier only.
    - Phase 6 adds `text-display-lg` (52px, weight 300, tracking -0.025em) and `text-display-xl` (56px, weight 300, tracking -0.025em), rendered in a Latin-only display face (Geist via `--font-display`), locale-scoped to en/fr/ru and NEVER applied to `ar` (D-02).
    - Body tiers (label/body/heading and the existing 40px display at weight 600) remain locked at 2 weights (400/600) — unchanged.
    - Cross-reference the authoritative sources: Phase 6 CONTEXT.md Locked Decision 1 and 06-UI-SPEC.md.
    Keep it concise (a short paragraph or a few bullets). Do not rewrite the existing Phase-1 typography table or other sections.
  </action>
  <acceptance_criteria>
    - The archived 01-UI-SPEC.md now contains an amendment note referencing "Phase 6".
    - The note names both `display-lg` (or `text-display-lg`) and `display-xl` (or `text-display-xl`) and the weight 300 / -0.025em amendment.
    - The note states the Latin display face is never applied to `ar`.
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'Phase 6' .planning/milestones/v2.0-phases/01-foundation-cms-decision/01-UI-SPEC.md && grep -Fi 'display-xl' .planning/milestones/v2.0-phases/01-foundation-cms-decision/01-UI-SPEC.md && grep -Fi 'display-lg' .planning/milestones/v2.0-phases/01-foundation-cms-decision/01-UI-SPEC.md</automated>
  </verify>
  <done>The archived Phase-1 UI-SPEC carries a concise Phase 6 amendment note lifting the type-scale lock for display tiers only, documenting the two new tiers (52/56px, weight 300, -0.025em), the Latin-display-face-never-on-ar rule, and cross-references to CONTEXT.md/06-UI-SPEC.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → rendered locale layout | next/font resolves and self-hosts Geist at build time; the only cross-boundary risk is which locale receives the Latin display face. |
| CSS token → Arabic content | `--font-display` fallback chain decides whether Arabic text could ever render in a Latin face. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Latin display face leaking into `ar` locale (Arabic integrity) | medium | mitigate | `displayVar` is empty string on `ar`, so `--font-geist-display` is never set on `<html>` for Arabic; `--font-display` falls back to `--font-sans` (Plex Sans Arabic). Verified by the `locale === "ar" ? ""` gate assertion in Task 2. |
| T-06-02 | Denial (readability) | FOIT / invisible display headings if Geist fails to load | low | mitigate | `display: "swap"` on the Geist loader + the `var(--font-geist-display, var(--font-sans))` nested fallback guarantee visible text (Plex) if the font is slow or fails. |
| T-06-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No package is installed this phase. Geist is consumed via `next/font/google`'s built-in `Geist` export (Next 16.2.10, already in node_modules) — no `npm install`, no new dependency, so the package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `npm run lint:rtl` exits 0 (all new tokens/utilities are direction-agnostic; no banned physical-direction class introduced).
- `npm run build` succeeds — the definitive gate that the Geist `next/font/google` config is valid and the amended `@theme` compiles.
- `npm test` (vitest) passes — no existing test regresses from the token/font additions.
- `npx tsc --noEmit` passes.
- Human spot-check (backstop, per 06-UI-SPEC UI Considerations): with the dev server running, view page source on `/ar` and confirm the `<html class>` does NOT contain `--font-geist-display`; view `/en` and confirm it does. (No component uses the display tokens yet — visual application is Phase 7.)
</verification>

<success_criteria>
- 8 new @theme tokens exist with exact values; color ramp and body-tier keys byte-identical (D-01, D-03, D-04).
- Geist Latin display face wired locale-scoped; structurally cannot reach `ar` (D-02).
- Tabular figures confirmed available via framework-native `tabular-nums`; no redundant token added (D-05).
- Archived Phase-1 UI-SPEC carries the display-tier amendment note.
- lint:rtl green, build succeeds, existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/06-design-system-elevation-premium-type-scale-display-tokens-rh/06-01-SUMMARY.md` when done.
</output>
