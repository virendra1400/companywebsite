---
phase: 05-seo-infrastructure-insights-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/int/insights-fallback.spec.ts
  - tests/int/insights-revalidate-hook.spec.ts
  - src/collections/Insights.ts
  - src/hooks/revalidateInsights.ts
  - src/payload.config.ts
autonomous: true
requirements: [BLOG-02]
must_haves:
  truths:
    - "Staff can create/edit/publish an Insights article per locale via the CMS; localized fields (title/excerpt/body/author) cascade and slug stays one canonical unlocalized value (D-01/D-04)."
    - "The Insights afterChange hook fires revalidatePath for all 4 locale variants of /insights and /insights/<slug> on publish, and skips entirely when context.disableRevalidate is set (BLOG-02)."
    - "getPageContent-style existence check applied to the insights collection returns isTranslated:false for a locale with only fallback content and true once real translated content exists (SEO-02/BLOG-02)."
    - "A draft (published:false) Insights article is excluded from every public query (list, detail, sitemap) exactly as Products.published already is (Pitfall 2)."
    - "The generated payload-types.ts exports an Insight type reflecting the new collection so downstream pages/hooks typecheck."
  artifacts:
    - path: "src/collections/Insights.ts"
      provides: "Insights Payload collection (D-01..D-04): title/slug/excerpt/coverImage/category/author/body/published, public read"
      contains: "slug: \"insights\""
    - path: "src/hooks/revalidateInsights.ts"
      provides: "revalidateInsight afterChange hook — per-locale revalidatePath for /insights + /insights/<slug>"
      exports: ["revalidateInsight"]
    - path: "src/payload.config.ts"
      provides: "Insights registered in the collections array"
      contains: "Insights"
    - path: "tests/int/insights-fallback.spec.ts"
      provides: "isTranslated existence-check coverage for the insights collection"
    - path: "tests/int/insights-revalidate-hook.spec.ts"
      provides: "revalidateInsight per-locale path assertions"
  key_links:
    - from: "src/collections/Insights.ts"
      to: "src/hooks/revalidateInsights.ts"
      via: "hooks.afterChange: [revalidateInsight]"
      pattern: "afterChange.*revalidateInsight"
    - from: "src/payload.config.ts"
      to: "src/collections/Insights.ts"
      via: "import { Insights } and add to collections array"
      pattern: "Insights"
  prohibitions:
    - statement: "MUST NOT populate Insights article fields (title/excerpt/body/author) in non-English locales with machine-translated or auto-generated copy — published non-English copy must be professional human translation (CLAUDE.md project constraint: MT for published copy destroys B2B credibility). English-only is the correct v1 state; the localized:true architecture supports human translation later with zero rework."
      status: unresolved
      verification: judgment
---

<objective>
Create the new `Insights` Payload collection (the one genuinely new piece of application code in Phase 5) so staff can author editorial articles per locale via the CMS, with the same translation-status/fallback and per-locale revalidation behavior the rest of the site already uses.

Purpose: BLOG-02 — "Staff can publish articles via CMS per locale as content is written." This is the data backbone every visitor-facing Insights page (05-04) and the sitemap/hreflang enumeration (05-05) build on.
Output: `Insights.ts` collection, `revalidateInsights.ts` hook, registration in `payload.config.ts`, regenerated `payload-types.ts`, and passing integration tests.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-infrastructure-insights-blog/05-CONTEXT.md
@.planning/phases/05-seo-infrastructure-insights-blog/05-RESEARCH.md
@.planning/phases/05-seo-infrastructure-insights-blog/05-PATTERNS.md
@src/collections/Products.ts
@src/collections/Media.ts
@src/hooks/revalidateCatalog.ts
@src/payload.config.ts
@src/lib/payload-fetch.ts
@tests/int/products-revalidate-hook.spec.ts
@tests/int/pages-fallback.spec.ts
@tests/int/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — failing integration tests for the Insights collection</name>
  <read_first>
    - tests/int/pages-fallback.spec.ts (fallback-detection test shape to mirror)
    - tests/int/products-revalidate-hook.spec.ts (revalidate-hook mock shape to mirror)
    - tests/int/config.ts (getTestPayload fixture)
    - src/lib/payload-fetch.ts (getPageContent/getProduct dual-query existence check being generalized)
  </read_first>
  <action>
    Create two Wave 0 integration specs that FAIL until Tasks 2-3 land (RED-first, per VALIDATION.md Wave 0).

    `tests/int/insights-revalidate-hook.spec.ts` — mirror `products-revalidate-hook.spec.ts` exactly, retargeted at the `insights` collection: `vi.mock("next/cache", () => ({ revalidatePath }))` BEFORE `const { getTestPayload } = await import("./config")`; create an article via `payload.create({ collection: "insights", data: { title, slug: "revalidate-test-insight", excerpt, coverImage: <media id>, body: <lexical root> }, overrideAccess: true, context: { disableRevalidate: true } })`. Assert an update WITHOUT disableRevalidate calls `revalidatePath` with exactly the 8 paths `/insights`, `/ar/insights`, `/fr/insights`, `/ru/insights`, `/insights/revalidate-test-insight`, `/ar/...`, `/fr/...`, `/ru/...` (toHaveBeenCalledTimes(8)); and that `context.disableRevalidate` skips revalidation entirely. Coverage image: create a Media doc first via `payload.create({ collection: "media", data: { alt }, filePath: "scripts/seed-assets/product-basmati-rice.svg" })` (coverImage is required).

    `tests/int/insights-fallback.spec.ts` — mirror `pages-fallback.spec.ts` exactly, retargeted at `insights`: seed an en article, assert `en` existence-check is true; assert an untranslated `fr` read returns fallback content with the native (fallbackLocale:false) existence check false; after writing real `fr` title, existence check true. Exercise the check through `getTranslatedLocales("insights", slug)` from `src/lib/seo/translated-locales.ts` (created in 05-02) — import it; this spec is expected to stay RED until BOTH this collection (Task 2) and that helper (05-02) exist, which is correct Wave 0 behavior. If 05-02 has not landed at run time, assert against the raw `payload.find({ collection: "insights", locale, fallbackLocale: false })` existence shape instead so this spec is self-contained on the collection alone.
  </action>
  <verify>
    <automated>npx vitest run --project=int tests/int/insights-revalidate-hook.spec.ts tests/int/insights-fallback.spec.ts 2>&1 | grep -Eq 'FAIL|No test files|Cannot find|fail' && echo "RED as expected" || echo "expected RED (tests must fail before Task 2/3)"</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files exist under tests/int/ and are syntactically valid TypeScript.
    - Running them BEFORE Task 2 fails (collection "insights" does not exist yet) — RED-first confirmed.
    - The revalidate spec asserts exactly 8 per-locale paths and the disableRevalidate skip, matching products-revalidate-hook.spec.ts's assertion style.
  </acceptance_criteria>
  <done>Two integration specs exist, are RED before implementation, and encode the BLOG-02 behavior (per-locale revalidation + translation-status existence check).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insights collection + revalidate hook + config registration</name>
  <read_first>
    - src/collections/Products.ts (field-shape + localized-cascade + published flag + afterChange wiring to copy)
    - src/collections/Media.ts (public-read access shape to copy: read: () => true, write admin-gated)
    - src/hooks/revalidateCatalog.ts (revalidateAllLocales helper + revalidateProduct shape to copy verbatim)
    - src/payload.config.ts (collections array + Products import line to mirror)
    - .planning/phases/05-seo-infrastructure-insights-blog/05-PATTERNS.md (exact field mapping table)
  </read_first>
  <behavior>
    - Insights collection exposes fields title(text,required,localized), slug(text,required,unique,index, NOT localized), excerpt(textarea,required,localized), coverImage(upload→media,required), category(relationship→categories,required:false), author(text,localized,defaultValue "Export Team"), body(richText,required,localized), publishedDate(date), published(checkbox,defaultValue true).
    - access.read is () => true (public); create/update/delete are Boolean(user) (admin-gated) — matching Media/SiteSettings, deviating from Products' admin-gated read.
    - revalidateInsight fires per-locale revalidatePath for /insights and /insights/<slug>, guarded by context.disableRevalidate.
  </behavior>
  <action>
    Create `src/collections/Insights.ts` (slug "insights", admin.useAsTitle "title") with the field list per D-01..D-04 and the exact field mapping in 05-PATTERNS.md §Insights.ts. Copy the PUBLIC access block from `Media.ts` (read: () => true; create/update/delete Boolean(user)) — NOT the admin-gated read from Products; the collection is served through Payload's auto-generated public REST endpoint. Add `published: { type: "checkbox", defaultValue: true }` (RESEARCH Open Question 1 — required so drafts are hard-filterable from every public query, mirroring Products.published). Set `localized: true` on parent fields only (title/excerpt/author/body) — do NOT re-set on nested fields (cascade pitfall). Keep `slug` and `category` unlocalized. Wire `hooks: { afterChange: [revalidateInsight] }`.

    Create `src/hooks/revalidateInsights.ts` exporting `revalidateInsight: CollectionAfterChangeHook` — copy `revalidateCatalog.ts`'s `revalidateAllLocales` helper verbatim and retarget paths to `/insights` and `/insights/${doc.slug}`, guarded by `!context.disableRevalidate`, returning `doc`.

    Edit `src/payload.config.ts`: add `import { Insights } from "@/collections/Insights";` alongside the existing Products import, and add `Insights` to the `collections: [...]` array (after Products).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -Ei 'Insights|insights' || echo "no Insights type errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/collections/Insights.ts exports `Insights` with slug "insights" and access.read === () => true.
    - Fields exactly match the D-01 set; title/excerpt/author/body carry localized:true; slug carries unique:true and NO localized; category has required:false; published defaults true.
    - src/hooks/revalidateInsights.ts exports revalidateInsight and references paths "/insights" and "/insights/" via revalidateAllLocales.
    - src/payload.config.ts imports Insights and includes it in the collections array (grep "Insights" matches both the import and the array).
  </acceptance_criteria>
  <done>The Insights collection, its revalidate hook, and its config registration exist and typecheck against the existing Products/Media/hook patterns.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply schema to the database + regenerate types + green integration tests</name>
  <read_first>
    - src/payload.config.ts (adapter selection: postgres push:true in prod, SQLite auto-sync locally)
    - tests/int/config.ts (int tests boot Payload against the isolated test DB, creating tables on connect)
  </read_first>
  <action>
    [BLOCKING — MUST run after Task 2 and BEFORE any verification of downstream plans. TypeScript/build checks pass without this because Payload's generated types come from config, not the live DB; the live schema and payload-types.ts must actually gain the `insights` table/type.]

    1. Regenerate Payload types so `payload-types.ts` gains the `Insight` interface used by 05-04's article page: run `npx payload generate:types`.
    2. Apply the schema to the database. This repo's adapters use `push: true` (Postgres prod) and SQLite auto-sync locally, so the `insights` table is created on the next Payload connect; run the documented push command `CI=true PAYLOAD_MIGRATING=true npx payload migrate` to force/confirm the migration path. If that command reports no migrations directory (expected for a push:true repo with no committed migrations), that is acceptable — the authoritative proof is the integration tests below CRUD-ing the collection against a live table.
    3. Confirm the collection is live by running the Task 1 integration specs to GREEN.
  </action>
  <verify>
    <automated>npx payload generate:types && npx vitest run --project=int tests/int/insights-revalidate-hook.spec.ts tests/int/insights-fallback.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - payload-types.ts contains an `Insight` interface (grep "Insight" in payload-types.ts matches a generated interface, not only imports).
    - Both Task 1 integration specs pass GREEN (the insights table exists and accepts create/update/find).
    - The revalidate spec proves exactly 8 per-locale paths fire on a real published update and none fire under disableRevalidate.
  </acceptance_criteria>
  <done>The insights schema is live in the DB, payload-types.ts exports Insight, and the BLOG-02 integration tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → Payload auto-generated REST/GraphQL (`(payload)/api/insights`) | Insights.access.read: () => true exposes every published article document to unauthenticated callers, same as Media already does. |
| CMS editor → Insights write | create/update/delete gated behind Boolean(user) (existing Payload admin auth, unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Insights.access.read: () => true | low | accept | Intentional, reviewed public-read matching the Media/SiteSettings precedent — articles are public editorial marketing content. Write stays admin-gated (Boolean(user)). Draft leakage handled by the `published` flag + public-query filtering (owned by 05-04/05-05). |
| T-05-SC | Tampering | npm installs | low | accept | No new package installed this plan (schema-dts is optional and NOT adopted — JSON-LD types are hand-rolled in 05-03). No install task, so no supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run --project=int tests/int/insights-fallback.spec.ts tests/int/insights-revalidate-hook.spec.ts` passes.
- `npx payload generate:types` succeeds and payload-types.ts exports Insight.
- `npx tsc --noEmit` reports no Insights-related type errors.
</verification>

<success_criteria>
- BLOG-02 backbone live: staff can create/edit/publish per-locale Insights articles in /admin; the collection is public-read, drafts are excludable, and per-locale revalidation fires on publish.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (excluded from drift verification as newly-created):
- `src/collections/Insights.ts` — `Insights` collection config (slug "insights")
- `src/hooks/revalidateInsights.ts` — `revalidateInsight` afterChange hook
- `src/payload.config.ts` — edited to import and register `Insights`
- `payload-types.ts` — regenerated to add the `Insight` interface
- `tests/int/insights-fallback.spec.ts`, `tests/int/insights-revalidate-hook.spec.ts` — new integration specs

## Probe assumptions (surfaced, not dropped)

- The prohibition "no machine-translated published non-English copy" (values/judgment tier) is authored descriptor-less → flagged-unverified at verify time (never auto-dismissed). It records the CLAUDE.md project constraint against MT for published copy; English-first v1 satisfies it by construction.

<output>
Create `.planning/phases/05-seo-infrastructure-insights-blog/05-01-SUMMARY.md` when done.
</output>
