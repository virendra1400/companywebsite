---
phase: 02-core-marketing-pages-trust-surfaces
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - src/blocks/MediaGallery.ts
  - src/blocks/index.ts
  - src/components/blocks/MediaGalleryBlock.tsx
  - src/components/blocks/RenderBlocks.tsx
  - src/collections/Pages.ts
  - src/lib/seed-content.ts
  - scripts/seed-pages.ts
  - payload-types.ts
  - tests/e2e/manufacturing.spec.ts
autonomous: true
requirements: [TRUST-03]
must_haves:
  truths:
    - "The Manufacturing page (/[locale]/manufacturing) renders a process-overview RichText, a MediaGallery of facility photos, and a StatsBand of capacity/QC/cold-chain figures"
    - "Each gallery cell holds a fixed aspect ratio (no CLS/layout break) with an optional caption below the image (never overlaid), lazy-loaded, no lightbox"
    - "An empty gallery items[] renders the generic empty-state line, not a crash"
  artifacts:
    - path: "src/blocks/MediaGallery.ts"
      provides: "mediaGallery block config: sectionTitle + items[] (image/caption/optional video URL)"
      contains: "items"
    - path: "src/components/blocks/MediaGalleryBlock.tsx"
      provides: "Responsive AspectRatio grid render, captions below, lazy next/image"
  key_links:
    - from: "src/components/blocks/RenderBlocks.tsx"
      to: "MediaGalleryBlock"
      via: "BLOCK_MAP mediaGallery entry"
      pattern: "mediaGallery"
    - from: "src/lib/seed-content.ts"
      to: "manufacturing page layout"
      via: "richText + mediaGallery + statsBand block sequence"
      pattern: "mediaGallery"
---

## Phase Goal

**As a** first-time B2B buyer, **I want to** see the exporter's processing facilities, capacity and quality controls, **so that** I trust it can reliably fulfil my order.

<objective>
Manufacturing/process trust slice: the MediaGallery block plus a complete Manufacturing page (RichText process overview + MediaGallery facility photos + StatsBand capacity/QC/cold-chain figures). RichText and StatsBand already exist (02-02, 02-04); this plan adds the one missing block and composes the page.

Purpose: TRUST-03.
Output: mediaGallery block config + render component, composed Manufacturing page, seeded facility-photo placeholders + capacity stats, e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-RESEARCH.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-CONTEXT.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-VALIDATION.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-04-PLAN.md
@./CLAUDE.md

<interfaces>
Consume from 02-02/02-04 (extend, don't redefine): src/blocks/index.ts barrel, src/collections/Pages.ts blocks array, src/components/blocks/RenderBlocks.tsx BLOCK_MAP + sectionBg(index), the statsBand + richText blocks, PAGES_EN_SEED + scripts/seed-pages.ts, the `blocks` i18n namespace empty-state string (02-04).

Binding spec: UI-SPEC §6 MediaGallery — grid-cols-2 md:grid-cols-3 gap-md, each cell `aspect-[4/3]` via shadcn AspectRatio + object-cover, lazy next/image, caption as a Label line BELOW the image (not overlaid), NO lightbox/modal (anti-feature — skip). Page Composition row "Manufacturing/process". Media guard (Pitfall 3) on each item image.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MediaGallery block config + render component + registration</name>
  <read_first>
    - 02-UI-SPEC.md §6 MediaGallery (fields, aspect ratio, caption-below rule, no-lightbox note)
    - src/components/blocks/RenderBlocks.tsx + src/blocks/index.ts + src/collections/Pages.ts (registration points)
    - src/components/ui/aspect-ratio.tsx (02-01)
    - src/components/blocks/StatsBandBlock.tsx (02-04 — pattern for an items-array server block)
  </read_first>
  <action>
    Create src/blocks/MediaGallery.ts (slug 'mediaGallery', NO localized on nested fields): sectionTitle (text, localized, optional), items array — image (upload→media, required), caption (text, localized, optional), videoUrl (text, optional — future embed hook, render as a caption-linked note for now, do NOT build an embed player this phase).
    Append to src/blocks/index.ts and src/collections/Pages.ts blocks array.
    Create src/components/blocks/MediaGalleryBlock.tsx (server component, {block, index}, section via sectionBg(index)): grid grid-cols-2 md:grid-cols-3 gap-md; each cell shadcn AspectRatio ratio 4/3 with next/image object-cover (lazy by default), Media typeof-object guard (skip a cell with no populated image rather than render a broken box); caption (if present) as a Label-token line below the cell, not overlaid. Empty items[] → the blocks-namespace empty-state line. Logical properties only. No lightbox.
    Add the mediaGallery entry to BLOCK_MAP.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl</automated>
  </verify>
  <acceptance_criteria>
    - src/blocks/MediaGallery.ts slug 'mediaGallery' with items[] (image/caption/videoUrl); exported from index.ts AND present in Pages.ts blocks array
    - MediaGalleryBlock uses AspectRatio 4/3, caption below (not overlaid), lazy next/image; no lightbox/modal code; empty items[] → empty-state line
    - RenderBlocks BLOCK_MAP has mediaGallery
    - npx tsc --noEmit exits 0; npm run lint:rtl passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Compose Manufacturing page + seed facility photos & capacity stats + build; e2e</name>
  <read_first>
    - 02-UI-SPEC.md Page Composition row Manufacturing + placeholder Hero copy table
    - 02-CONTEXT.md D-03 (realistic placeholders)
    - src/lib/seed-content.ts + scripts/seed-pages.ts (structures to extend)
    - tests/e2e/rtl-arabic.spec.ts + tests/e2e/locale-routing.spec.ts (en+ar conventions)
    - 02-VALIDATION.md row TRUST-03
  </read_first>
  <action>
    Update PAGES_EN_SEED manufacturing layout, replacing the thin 02-02 stub with: compact hero ("Manufacturing & Process") → richText (process overview, realistic long-string prose) → mediaGallery (facility photos, ≥4 items to exercise the grid) → statsBand (capacity / QC labs / cold-chain figures, realistic "shaped" values) → ctaBand.
    Extend scripts/seed-pages.ts to seed ≥4 gallery photo Media placeholders (self-authored generic placeholder images via the scripts/seed-assets/ pattern from 02-03 — no third-party facility photos). Idempotent, en only.
    Regenerate types; run seed + build.
    Create tests/e2e/manufacturing.spec.ts (Playwright, en+ar): /[locale]/manufacturing renders the process RichText, a MediaGallery with ≥4 aspect-ratio cells (captions below), and the StatsBand tiles; assert no layout break under dir=rtl and captions are not overlaid.
    Run the RTL gate.
  </action>
  <verify>
    <automated>npx payload generate:types && npm run db:seed && npx tsc --noEmit && npm run lint:rtl && npm run build && npx playwright test tests/e2e/manufacturing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - manufacturing layout: hero(compact) → richText → mediaGallery(≥4) → statsBand → ctaBand
    - ≥4 gallery photo Media placeholders seeded (generic, non-third-party); npm run db:seed idempotent
    - npm run build exits 0; /manufacturing prerenders
    - tests/e2e/manufacturing.spec.ts passes en+ar; node scripts/check-physical-direction.mjs passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public browser → Next RSC → Payload Local API | Manufacturing page + gallery Media read server-side, overrideAccess:true; no public REST/GraphQL exposure |
| public browser → Media URL | Facility photos served as static Media URLs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | Gallery photo Media exposure | accept | Self-authored generic placeholder images only; same Media access model as prior plans; no confidential facility imagery in placeholders |
| T-02-12 | Repudiation/Legal | Capacity/QC/cold-chain stats mistaken as verified | mitigate | Realistic-SHAPED placeholder values (D-03), not presented as audited figures; no fabricated named certification or client |
</threat_model>

<verification>
- `npx playwright test tests/e2e/manufacturing.spec.ts` green (en + ar)
- `npm run build` exit 0; RTL gate passes
- Gallery uses fixed aspect ratios (no CLS), captions below, no lightbox
</verification>

<success_criteria>
The Manufacturing page shows facility photos, process narrative, and capacity/QC/cold-chain figures with stable layout across locales. TRUST-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-05-SUMMARY.md`
</output>
