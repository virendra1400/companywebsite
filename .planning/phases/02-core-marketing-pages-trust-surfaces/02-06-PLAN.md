---
phase: 02-core-marketing-pages-trust-surfaces
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - public/maps/world.svg
  - public/maps/README.md
  - src/blocks/ExportMap.ts
  - src/blocks/index.ts
  - src/components/blocks/ExportMapBlock.tsx
  - src/components/blocks/RenderBlocks.tsx
  - src/collections/Pages.ts
  - src/lib/seed-content.ts
  - scripts/seed-pages.ts
  - src/lib/country-names.ts
  - payload-types.ts
  - tests/e2e/export-map-a11y.spec.ts
autonomous: true
requirements: [TRUST-04]
must_haves:
  truths:
    - "The Export Track Record page (/[locale]/export) renders a StatsBand (years exporting / shipment volume / incoterms) and an ExportMap showing served countries highlighted"
    - "The map is a static SVG (no map library, no interactivity/3D) wrapped in role=img with an aria-label summarizing the country count"
    - "The served-country NAMES are always rendered as a visible text chip list below the map — the map never gatekeeps information a screen-reader user cannot otherwise get"
    - "The map SVG renders identically in ltr and rtl (geography does not mirror); it uses a self-authored/license-safe asset, swappable later"
    - "The homepage renders the compact ExportMap variant, completing the PAGE-01 sequence"
  artifacts:
    - path: "public/maps/world.svg"
      provides: "Static self-authored/license-safe world map SVG with ISO-coded path ids for highlight fills"
      contains: "svg"
    - path: "src/blocks/ExportMap.ts"
      provides: "exportMap block: sectionTitle + highlightedCountryCodes[] + stats[]"
      contains: "highlightedCountryCodes"
    - path: "src/components/blocks/ExportMapBlock.tsx"
      provides: "role=img SVG + highlight fills + always-visible country-name chip list"
  key_links:
    - from: "src/components/blocks/ExportMapBlock.tsx"
      to: "public/maps/world.svg + src/lib/country-names.ts"
      via: "code fills SVG paths by ISO code + renders country-name chips"
      pattern: "highlightedCountryCodes"
    - from: "src/components/blocks/RenderBlocks.tsx"
      to: "ExportMapBlock"
      via: "BLOCK_MAP exportMap entry"
      pattern: "exportMap"
---

## Phase Goal

**As a** first-time B2B buyer, **I want to** see verifiable evidence of the exporter's global reach and track record, **so that** I trust it has real export experience before I inquire.

<objective>
Export Track Record trust slice: a static, non-interactive ExportMap block (D-06) plus a complete Export page (StatsBand + ExportMap with a visible country-name chip list). The homepage compact ExportMap variant is added here too, completing the PAGE-01 sequence.

Purpose: TRUST-04. Map is structural trust evidence with an accessible text fallback — the SVG illustrates, never gatekeeps.
Output: license-safe/self-authored world.svg asset, exportMap block + render component, ISO→name lookup, composed Export page + homepage compact map, seeded highlighted countries + stats, a11y e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-RESEARCH.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-CONTEXT.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-VALIDATION.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-04-PLAN.md
@./CLAUDE.md

<interfaces>
Consume from 02-02/02-04 (extend, don't redefine): src/blocks/index.ts barrel, src/collections/Pages.ts blocks array, src/components/blocks/RenderBlocks.tsx BLOCK_MAP + sectionBg(index), the statsBand block, PAGES_EN_SEED + scripts/seed-pages.ts.

Binding specs:
- UI-SPEC §8 ExportMap: two variants driven by page (homepage compact = map + 3-4 stats side-by-side; Export page full = larger map max-w-[900px] + country-name chip list below). Highlight fills: served = fill-primary-500, others = fill-neutral-300, ocean transparent/neutral-100. NOT gold. Map does NOT mirror in RTL.
- UI-SPEC RTL Extensions: geographic maps do not mirror (add to no-mirror list).
- A11y (mandatory): SVG in role=img + aria-label with count; served-country NAMES always rendered as visible chips below the map.
- CONTEXT D-06: static SVG, no heavy map lib, interactive/3D globe is an anti-feature.

Map license (RESEARCH Open Question 1 + Assumptions A1/A2, VALIDATION Manual-Only "Export-map SVG license clearance"): DO NOT check in a third-party SVG whose license is uncleared (CC BY-SA share-alike is unresolved for this commercial site). This plan ships a SELF-AUTHORED, license-safe schematic world SVG as the swappable placeholder (blocky/simplified continents with ISO-coded path ids) — geographic precision is NOT required because the chip list carries the real information. Record in public/maps/README.md that swapping in a precise third-party map requires legal-owner license clearance first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-authored world.svg + ISO→name lookup + exportMap block + render component + registration</name>
  <read_first>
    - 02-UI-SPEC.md §8 ExportMap (variants, fill colors, a11y chip-list rule) + RTL Extensions (map no-mirror)
    - 02-RESEARCH.md Open Question 1 + Assumptions A1/A2 (why self-authored/license-safe, not third-party)
    - src/components/blocks/StatsBandBlock.tsx (02-04 — reuse for the stats within ExportMap or render StatsBand alongside)
    - src/components/blocks/RenderBlocks.tsx + src/blocks/index.ts + src/collections/Pages.ts (registration points)
  </read_first>
  <action>
    Create public/maps/world.svg — a SELF-AUTHORED, license-safe schematic world map: simplified/blocky continent shapes, each highlightable region a `<path>`/`<g>` with an `id` = ISO alpha-2 code, default fill neutral-300, transparent ocean/background. Precision is not required; it is a swappable placeholder. Add public/maps/README.md stating: self-authored placeholder, swapping to a precise third-party map (e.g. any CC BY-SA source) requires legal-owner license clearance first (RESEARCH Open Question 1 / VALIDATION Manual-Only).
    Create src/lib/country-names.ts: a small ISO alpha-2 → English display-name map covering the codes the seed will highlight (not all 249 — only the served set + a reasonable buffer). Used to render the visible chip list.
    Create src/blocks/ExportMap.ts (slug 'exportMap', NO localized nested fields except localized text): sectionTitle (text, localized, optional), variant (select compact|full, NOT localized, default full), highlightedCountryCodes (array of text ISO codes, NOT localized), stats (array value/label — same shape as StatsBand, value text + localized label).
    Append to src/blocks/index.ts and src/collections/Pages.ts blocks array.
    Create src/components/blocks/ExportMapBlock.tsx (server component, {block, index}, section via sectionBg(index)): inline/import the world.svg and apply fill-primary-500 to paths whose id is in highlightedCountryCodes (others stay neutral-300) — via server-side string transform or an inline SVG component reading the code set. Wrap the SVG in `role="img"` with an aria-label summarizing the count (e.g. "World map highlighting N countries Star Agrevolution exports to"). ALWAYS render the served-country NAMES (from country-names.ts) as a visible wrapped chip list below the map — present in both variants, mandatory. Map element carries no dir-dependent transform (does not mirror). variant compact → map + stats side-by-side (grid-cols-1 lg:grid-cols-2 gap-xl); variant full → larger map (max-w-[900px] mx-auto) + stats as a full band + chip list below. Logical properties for surrounding layout only (the SVG itself is direction-neutral). No interactivity/JS map lib.
    Add the exportMap entry to BLOCK_MAP.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl</automated>
  </verify>
  <acceptance_criteria>
    - public/maps/world.svg is self-authored with ISO-alpha-2 path ids; public/maps/README.md flags the legal-clearance requirement for a third-party swap
    - src/lib/country-names.ts exports an ISO→name lookup covering the served set
    - src/blocks/ExportMap.ts slug 'exportMap' with variant/highlightedCountryCodes/stats; exported from index.ts AND present in Pages.ts blocks array
    - ExportMapBlock wraps the SVG in role=img + aria-label with count; renders a visible country-name chip list in BOTH variants; served countries fill primary-500 (not gold); no map library imported; no dir-based mirror transform on the SVG
    - RenderBlocks BLOCK_MAP has exportMap; npx tsc --noEmit exits 0; npm run lint:rtl passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Compose Export page + homepage compact map + seed; a11y e2e</name>
  <read_first>
    - 02-UI-SPEC.md Page Composition rows Export Track Record + Homepage + placeholder Hero copy
    - src/lib/seed-content.ts + scripts/seed-pages.ts (structures to extend)
    - tests/e2e/rtl-arabic.spec.ts + tests/e2e/locale-routing.spec.ts (en+ar conventions)
    - 02-VALIDATION.md row TRUST-04 (role=img + visible country list)
  </read_first>
  <action>
    Update PAGES_EN_SEED:
    - export layout, replacing the 02-02 stub: compact hero ("Export Track Record") → statsBand (years exporting / shipment volume / incoterms handled, realistic shaped values) → exportMap (variant full, highlightedCountryCodes = a realistic served set e.g. GCC + Europe + a few others matching country-names.ts, plus its own stats) → ctaBand.
    - home layout: insert exportMap (variant compact) into the ExportMap slot left by 02-04 (after StatsBand, before CTABand) — completing the UI-SPEC homepage sequence.
    Highlighted country codes are seed data (English-shaped, no localization needed on codes). No fabricated precise shipment counts presented as audited fact (D-03 shaped placeholders only).
    Regenerate types; run seed + build.
    Create tests/e2e/export-map-a11y.spec.ts (Playwright, en+ar): /[locale]/export — assert the SVG container has role="img" and a non-empty aria-label; assert the visible country-name chip list is present and contains ≥1 served country name; assert the StatsBand tiles render; assert the map is present in BOTH the export page (full) and homepage (compact). On /ar, assert the map is not mirrored (its bounding transform has no scaleX(-1) / the served paths are in the same positions) and surrounding layout still passes.
    Run the RTL gate.
  </action>
  <verify>
    <automated>npx payload generate:types && npm run db:seed && npx tsc --noEmit && npm run lint:rtl && npm run build && npx playwright test tests/e2e/export-map-a11y.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - export layout: hero(compact) → statsBand → exportMap(full) → ctaBand; home layout ExportMap slot filled with exportMap(compact)
    - Seeded highlightedCountryCodes are all present in country-names.ts; no fabricated audited shipment figures
    - tests/e2e/export-map-a11y.spec.ts passes en+ar (role=img + aria-label, visible country chips, stats, map present on both pages, not mirrored on /ar)
    - npm run build exits 0; node scripts/check-physical-direction.mjs passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public browser → Next RSC → Payload Local API | Export page + highlighted-country data read server-side, overrideAccess:true |
| repo → shipped static asset | world.svg is a code asset (public/), not editor-managed Media (D-06) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Repudiation/Legal | Third-party map SVG license (CC BY-SA share-alike) shipped uncleared | mitigate | Ship a SELF-AUTHORED license-safe schematic SVG placeholder; public/maps/README.md requires legal-owner clearance before any third-party swap (RESEARCH Open Question 1 / VALIDATION Manual-Only) |
| T-02-14 | Denial of Service / a11y | Information locked behind a decorative SVG only | mitigate | Served-country NAMES always rendered as visible text chips (role=img + aria-label + text list); map never the sole channel |
| T-02-15 | Repudiation/Legal | Export stats presented as audited fact | accept | Realistic-shaped placeholder values (D-03); no fabricated audited shipment counts |
</threat_model>

<verification>
- `npx playwright test tests/e2e/export-map-a11y.spec.ts` green (en + ar)
- `npm run build` exit 0; RTL gate passes
- Map is static (no map lib), role=img + aria-label, visible country chip list present, not mirrored in RTL
- public/maps/README.md documents the third-party-swap legal-clearance requirement
</verification>

<success_criteria>
The Export Track Record page shows served countries on a static, accessible map with an always-visible country-name list plus track-record stats; the homepage compact map completes the PAGE-01 sequence; the shipped SVG is license-safe. TRUST-04 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-06-SUMMARY.md`
</output>
