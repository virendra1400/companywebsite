---
phase: 02-core-marketing-pages-trust-surfaces
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/blocks/CertStrip.ts
  - src/blocks/index.ts
  - src/components/blocks/CertCard.tsx
  - src/components/blocks/CertStripBlock.tsx
  - src/components/blocks/RenderBlocks.tsx
  - src/collections/Pages.ts
  - src/lib/payload-fetch.ts
  - src/lib/seed-content.ts
  - scripts/seed-pages.ts
  - scripts/seed-assets/README.md
  - src/i18n/messages/en.json
  - src/i18n/messages/ar.json
  - src/i18n/messages/fr.json
  - src/i18n/messages/ru.json
  - payload-types.ts
  - tests/int/certifications.spec.ts
  - tests/e2e/certifications.spec.ts
autonomous: true
requirements: [TRUST-01, TRUST-02]
must_haves:
  truths:
    - "A visitor on /[locale]/certifications sees every published Certification as a card with its logo, name and issuing body"
    - "A cert whose halal flag is true renders visually elevated (2px accent-600 border + Halal Certified badge + spans 2 grid columns), ahead of standard certs"
    - "A cert WITH a certificatePdf shows a working Download PDF link (download + target=_blank + rel=noopener); a cert WITHOUT one shows the muted 'Certificate available on request' line and NO dead link"
    - "The homepage shows a compact CertStrip of the same certification logos, each linking to /certifications"
  artifacts:
    - path: "src/components/blocks/CertCard.tsx"
      provides: "Shared cert/document card primitive (generic prop shape: name/subtitle/logo/pdf/halal) driving both the grid and later the company-profile doc card"
      contains: "halal"
    - path: "src/blocks/CertStrip.ts"
      provides: "certStrip Payload block config with variant strip|grid + optional sectionTitle/intro"
      contains: "variant"
    - path: "src/lib/payload-fetch.ts"
      provides: "getCertifications(locale) helper — sorted by displayOrder, halal-first"
      exports: ["getCertifications"]
  key_links:
    - from: "src/components/blocks/CertStripBlock.tsx"
      to: "getCertifications(locale)"
      via: "async RSC data fetch"
      pattern: "getCertifications\\("
    - from: "src/components/blocks/RenderBlocks.tsx"
      to: "CertStripBlock"
      via: "BLOCK_MAP certStrip entry"
      pattern: "certStrip"
---

## Phase Goal

**As a** first-time B2B buyer evaluating a supplier, **I want to** see the exporter's certifications with real downloadable proof (and its Halal credential featured), **so that** I can verify compliance before I send an inquiry.

<objective>
First trust vertical slice on top of the 02-02 block system: render the Certifications collection (schema already added in 02-02) as visible cards on the Certifications page and as a compact logo strip on the homepage. Halal certs get the elevated treatment (D-05). PDF-present and PDF-absent states both render honestly.

Purpose: TRUST-01 + TRUST-02. The Certifications collection exists but nothing consumes it yet — this plan queries it, builds the CertCard shared primitive, and composes the cert page + homepage strip end-to-end.
Output: certStrip block (variant strip|grid), CertCard primitive, getCertifications helper, seeded sample certs (with self-authored placeholder logos/PDF), composed Certifications page + homepage CertStrip, int + e2e tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-RESEARCH.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-CONTEXT.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-VALIDATION.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-02-PLAN.md
@src/collections/Media.ts
@./CLAUDE.md

<interfaces>
Consume these 02-02 outputs (already built by the plan this depends on — do NOT redefine):
- `src/collections/Certifications.ts` — fields: name (text, req, localized), issuingBody (text, req, localized), logo (upload→media, required), certificatePdf (upload→media, optional), validityNotes (text, localized), halal (checkbox, default false), displayOrder (number, default 0). Consume; do not re-add.
- `src/collections/Pages.ts` — `layout` blocks field, `blocks: [Hero, RichText, CTABand]` imported from `@/blocks`. Append the new block config to this array.
- `src/blocks/index.ts` — barrel re-exporting block configs. Append CertStrip.
- `src/components/blocks/RenderBlocks.tsx` — `BLOCK_MAP` keyed by blockType + `sectionBg(index)` helper. Add `certStrip` entry.
- `src/lib/payload-fetch.ts` — `getPageContent(slug, locale)` dual-query fallback helper (add `getCertifications` beside it, same `getPayload`/`overrideAccess:true` pattern).
- `src/lib/seed-content.ts` — `PAGES_EN_SEED` (array keyed by slug, each { title, layout }). Insert certStrip into home + certGrid into the certifications page layout.
- `scripts/seed-pages.ts` — idempotent per-slug upsert (en only), `@next/env` default-import + runtime-destructure pattern, `context.disableRevalidate:true`.

Media relation guard (repeat in CertCard for logo + pdf — RESEARCH Pitfall 3):
`const logo = cert.logo && typeof cert.logo === "object" ? (cert.logo as Media) : null`

RESEARCH "CertCard shared primitive" + "Certifications collection" code examples are the reference shape. UI-SPEC §5 (CertStrip/CertCard) is the binding visual contract (halal elevation, PDF states, empty state copy).

Locale-aware links: import { Link } from "@/i18n/navigation" (auto locale-prefixes). Locale type: import type { Locale } from "@/i18n/routing".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CertCard primitive + certStrip block (variant strip|grid) + getCertifications + registration + i18n keys</name>
  <read_first>
    - 02-RESEARCH.md "CertCard shared primitive" and "Certifications collection" code examples (exact prop/field shapes)
    - 02-UI-SPEC.md §5 CertStrip/CertCard (halal elevation rule, PDF present/absent states, CertStrip = logos-only compact) + Copywriting Contract (PDF-absent line, empty-state copy)
    - src/collections/Certifications.ts (the schema being consumed — from 02-02)
    - src/components/blocks/RenderBlocks.tsx + src/blocks/index.ts + src/collections/Pages.ts (the registration points — from 02-02)
    - src/lib/payload-fetch.ts (getPageContent pattern to mirror for getCertifications)
    - src/i18n/messages/en.json (nav/hero key structure to extend with a certs block)
    - src/components/ui/{card,badge,aspect-ratio}.tsx (shadcn primitives from 02-01)
  </read_first>
  <action>
    Create src/components/blocks/CertCard.tsx (server component). Accept a GENERIC prop shape — `{ name: string; subtitle?: string; logo: Media | null; pdf: Media | null; halal?: boolean; t: (key: string) => string }` — NOT a raw Certification, so 02-04's company-profile document card reuses this exact component without a new component (UI-SPEC "not a new component"). Repeat the typeof-object Media guard for logo and pdf. Standard card: shadcn Card, logo in `aspect-[3/2]` AspectRatio box (`object-contain`, bg-white), Body-600 name, Label neutral-600 subtitle. Halal (`halal: true`): `border-2 border-accent-600`, `bg-accent-100 text-accent-800` shadcn Badge "Halal Certified", card spans 2 columns (`col-span-2`) / first + full-width on mobile. PDF present: `<a href={pdf.url} download target="_blank" rel="noopener noreferrer" aria-label={...}>` + lucide Download icon (no-mirror) + visible label `t("downloadPdf")`. PDF absent: muted neutral-600 Label line `t("pdfUnavailable")` — never a dead link. Logical properties only.
    Create src/blocks/CertStrip.ts (slug 'certStrip'): fields `variant` (select strip|grid, default grid, NOT localized), `sectionTitle` (text, localized, optional), `intro` (textarea, localized, optional). No cert data lives on the block — it references the Certifications collection at render.
    Append CertStrip to src/blocks/index.ts barrel and to the `blocks: []` array in src/collections/Pages.ts (do not touch existing entries).
    Create src/components/blocks/CertStripBlock.tsx (async server component consuming {block, index}): call getCertifications(locale) — derive locale from params via the same mechanism sibling block components use (accept locale as a prop passed down from the page, matching how 02-02 blocks receive request context; if 02-02 blocks read locale internally, follow that). variant 'grid' → CertCard grid `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-lg`, halal card first + spanning; wrap section with sectionBg(index). variant 'strip' → compact logos-only wrapped row (no name/PDF), each logo wrapped in `<Link href="/certifications">`, halal logo keeps border-accent-600. Empty-cert state → render the UI-SPEC "Certifications coming soon" heading/body (Copywriting Contract), never a blank grid.
    Add the `certStrip` entry to BLOCK_MAP in RenderBlocks.tsx.
    Add getCertifications(locale) to src/lib/payload-fetch.ts: payload.find on 'certifications', overrideAccess:true, locale + fallbackLocale (en for non-en, matching getPageContent), sort by displayOrder; return docs with halal:true ordered first (stable sort). Default depth populates logo/pdf one level (Pitfall 3).
    Add i18n message keys under a new `certs` namespace to all 4 message catalogs (en/ar/fr/ru — en real, others copy the en value as placeholder, matching the fallback-ready convention): `halalBadge` ("Halal Certified"), `downloadPdf` ("Download PDF"), `pdfUnavailable` ("Certificate available on request"), `comingSoonHeading`, `comingSoonBody` (from Copywriting Contract). These are UI chrome strings (next-intl), NOT CMS content.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl</automated>
  </verify>
  <acceptance_criteria>
    - src/components/blocks/CertCard.tsx exists and takes a generic prop shape (name/subtitle/logo/pdf/halal/t), not a raw Certification
    - src/blocks/CertStrip.ts slug is 'certStrip' with a variant select (strip|grid); it is exported from src/blocks/index.ts AND present in Pages.ts blocks array
    - RenderBlocks BLOCK_MAP has a certStrip entry; getCertifications is exported from payload-fetch.ts and orders halal first
    - The 4 message catalogs contain certs.halalBadge/downloadPdf/pdfUnavailable/comingSoonHeading/comingSoonBody
    - npx tsc --noEmit exits 0; npm run lint:rtl passes (no physical-direction classes)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Compose Certifications page + homepage CertStrip; seed sample certs with self-authored placeholder assets</name>
  <read_first>
    - 02-UI-SPEC.md Page Composition row "Certifications" and "Homepage" (block sequences) + §5 states
    - 02-CONTEXT.md D-03 (realistic-shaped placeholders), D-05 (halal prominence), specifics (legal caution — no fabricated cert numbers, no real cert-body logos)
    - src/lib/seed-content.ts + scripts/seed-pages.ts (from 02-02 — the seed structures to extend)
    - src/collections/Media.ts (upload via payload.create({collection:'media', filePath, data:{alt}}); mimeTypes image/* + application/pdf)
  </read_first>
  <action>
    Add self-authored placeholder assets under scripts/seed-assets/ (create the dir + a short README.md noting these are generated placeholders, NOT real certification-body logos/documents — legal caution, Pitfall 5/9). Assets: 3 generic placeholder logo images (plain neutral labeled boxes, e.g. simple SVG or small PNG — NOT ISO/Halal/FSSAI trademarked marks) and 1 single-page placeholder PDF. The executor may generate these programmatically (e.g. a minimal SVG string written to file, a trivial PDF) — do not download third-party branded assets.
    Extend scripts/seed-pages.ts to idempotently seed the Certifications collection (en only, overrideAccess:true, context.disableRevalidate:true, skip-if-exists by name): create the 3 logo Media docs + 1 PDF Media doc via payload.create with filePath, then create 3 certifications:
      (1) halal:true, WITH certificatePdf → exercises halal elevation + PDF-present;
      (2) halal:false, WITH certificatePdf → standard + PDF-present;
      (3) halal:false, WITHOUT certificatePdf → standard + PDF-absent ("available on request").
    Use realistic-length placeholder names/issuing bodies per UI-SPEC §5 examples (long strings to stress wrapping) — NEVER a fabricated IEC/FSSAI/certificate/registration number or named client (Pitfall 5).
    Update PAGES_EN_SEED in src/lib/seed-content.ts: insert a certStrip block (variant 'grid') into the certifications page layout (after the existing compact hero, before the ctaBand) and a certStrip block (variant 'strip') into the home page layout (after hero — CertStrip is the homepage trust opening beat per UI-SPEC focal rule).
    Regenerate types (`npx payload generate:types`) if the new block changed the Page union. Then run the seed + local build to prove no empty-content prerender error.
  </action>
  <verify>
    <automated>npx payload generate:types && npm run db:seed && npx tsc --noEmit && npm run lint:rtl && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-assets/ exists with a README flagging placeholders as non-trademark generated assets; 3 placeholder logos + 1 placeholder PDF are produced by the seed run
    - Re-running `npm run db:seed` does not duplicate certs or Media (idempotent by name)
    - Seeded certs: exactly one halal:true; one standard with PDF; one standard without PDF; no fabricated cert/IEC/registration numbers or named clients in any string
    - PAGES_EN_SEED home layout contains a certStrip variant 'strip'; certifications layout contains a certStrip variant 'grid'
    - npm run build exits 0 locally (SQLite); /certifications and / prerender with cert content
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: int + e2e tests for cert rendering, halal elevation, PDF states</name>
  <read_first>
    - tests/int/payload-fallback.spec.ts + tests/int/config.ts (Local API int test conventions)
    - tests/e2e/rtl-arabic.spec.ts + tests/e2e/locale-routing.spec.ts (en+ar project conventions, dir assertions)
    - 02-VALIDATION.md Per-Task Verification Map rows TRUST-01/TRUST-02
    - 02-UI-SPEC.md §5 (exact selectors: accent-600 border on halal, Download link attrs, "available on request" copy)
  </read_first>
  <action>
    Create tests/int/certifications.spec.ts (Vitest, Payload Local API): assert getCertifications returns docs ordered halal-first; assert the halal doc has halal===true; assert a cert with no certificatePdf yields a null pdf relation (drives the absent state); assert isTranslated-style fallback behaves (untranslated locale falls back to en) mirroring payload-fallback.spec.ts.
    Create tests/e2e/certifications.spec.ts (Playwright, en+ar): visit /[locale]/certifications; assert the halal cert renders its Badge (text = certs.halalBadge) and carries the border-accent-600 elevation class / spans 2 columns; assert a Download PDF link exists with download + target=_blank + rel containing noopener for a PDF-present cert; assert the "Certificate available on request" line renders for the PDF-absent cert and NO anchor is present for it; assert the homepage CertStrip renders logo links to /certifications. Include the /ar run to confirm the grid auto-flows under dir=rtl (no layout break).
    Run the RTL gate as part of verification.
  </action>
  <verify>
    <automated>npx vitest run tests/int/certifications.spec.ts && node scripts/check-physical-direction.mjs && npx playwright test tests/e2e/certifications.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/int/certifications.spec.ts passes (halal-first ordering, PDF-absent = null relation, fallback)
    - tests/e2e/certifications.spec.ts passes on both en and ar projects (halal badge + elevation, PDF-present download link attrs, PDF-absent copy with no link, homepage strip links)
    - node scripts/check-physical-direction.mjs passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public browser → Next RSC → Payload Local API | Certifications read server-side via Local API with overrideAccess:true; never the public REST/GraphQL surface (unchanged from Phase 1 / 02-02) |
| public browser → Media/PDF URL | Cert logos + certificate PDFs served as static Media URLs (Blob prod / disk dev) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Certifications public exposure | mitigate | No drafts/versions (02-02 anti-pattern held); access all Boolean(user) for REST/GraphQL; public reads only via overrideAccess Local API |
| T-02-06 | Tampering | PDF download anchor (`download`/`target=_blank`) | mitigate | `rel="noopener noreferrer"` on every external/download anchor (UI-SPEC contract), preventing reverse-tabnabbing |
| T-02-07 | Repudiation/Legal | Placeholder cert content mistaken for real credentials | accept | Self-authored non-trademark placeholder logos + PDF-absent "available on request"; NO fabricated cert/IEC numbers or real cert-body marks (Pitfall 5/9); flagged in scripts/seed-assets/README.md |
</threat_model>

<verification>
- `npx vitest run tests/int/certifications.spec.ts` green
- `npx playwright test tests/e2e/certifications.spec.ts` green (en + ar)
- `npm run build` exit 0; `npm run lint:rtl` / `node scripts/check-physical-direction.mjs` pass
- No fabricated cert/IEC/registration number or real trademarked cert-body logo in seed content or assets
</verification>

<success_criteria>
The Certifications page lists every published cert with logo + name + issuing body; the Halal cert is visually elevated; PDF-present and PDF-absent states both render correctly and honestly; the homepage carries a compact CertStrip. TRUST-01 and TRUST-02 are satisfied end-to-end, all placeholder content legally safe.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-03-SUMMARY.md`
</output>
