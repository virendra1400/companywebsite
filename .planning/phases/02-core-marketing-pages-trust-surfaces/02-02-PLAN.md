---
phase: 02-core-marketing-pages-trust-surfaces
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/collections/Pages.ts
  - src/collections/Certifications.ts
  - src/blocks/Hero.ts
  - src/blocks/RichText.ts
  - src/blocks/CTABand.ts
  - src/blocks/index.ts
  - src/components/blocks/RenderBlocks.tsx
  - src/components/blocks/HeroBlock.tsx
  - src/components/blocks/RichTextBlock.tsx
  - src/components/blocks/CTABandBlock.tsx
  - src/lib/payload-fetch.ts
  - src/hooks/revalidatePage.ts
  - src/payload.config.ts
  - "src/app/(site)/[locale]/page.tsx"
  - "src/app/(site)/[locale]/[slug]/page.tsx"
  - src/lib/seed-content.ts
  - scripts/seed-home.ts
  - scripts/seed-pages.ts
  - package.json
  - payload-types.ts
  - tests/int/pages-fallback.spec.ts
  - tests/e2e/page-routing.spec.ts
autonomous: true
requirements: [PAGE-01, PAGE-02, TRUST-06]
must_haves:
  truths:
    - "The homepage renders from a Pages collection doc (slug 'home') through RenderBlocks, not from the retired Home global"
    - "All 6 interior slugs (about, certifications, manufacturing, export, company, contact) resolve at /[locale]/[slug] and render at least a compact Hero; an unknown slug returns 404"
    - "An untranslated locale shows the LocaleFallbackNotice above the blocks, mirroring the proven Phase 1 fallback rule (D-06)"
    - "npm run build succeeds locally against SQLite with all 7 pages seeded (no empty-content prerender error)"
  artifacts:
    - path: "src/collections/Pages.ts"
      provides: "Pages collection: text slug + localized blocks layout field, revalidatePage afterChange hook"
      contains: "type: 'blocks'"
    - path: "src/collections/Certifications.ts"
      provides: "Certifications collection schema (name/issuingBody/logo/certificatePdf/validityNotes/halal/displayOrder)"
      contains: "halal"
    - path: "src/components/blocks/RenderBlocks.tsx"
      provides: "blockType->component switch + sectionBg alternation"
      contains: "sectionBg"
    - path: "src/lib/payload-fetch.ts"
      provides: "getPageContent(slug, locale) dual-query fallback-detection helper"
      exports: ["getPageContent"]
    - path: "src/app/(site)/[locale]/[slug]/page.tsx"
      provides: "Explicit single-segment route for the 6 interior pages with generateStaticParams"
  key_links:
    - from: "src/app/(site)/[locale]/page.tsx"
      to: "getPageContent('home', locale)"
      via: "RSC data fetch replacing getHomeContent"
      pattern: "getPageContent\\(\\s*['\"]home['\"]"
    - from: "src/collections/Pages.ts"
      to: "revalidatePage hook"
      via: "afterChange"
      pattern: "afterChange"
    - from: "src/components/blocks/RenderBlocks.tsx"
      to: "src/blocks (block configs) blockType values"
      via: "BLOCK_MAP keyed by blockType"
      pattern: "BLOCK_MAP"
---

<objective>
Generalize the Phase 1 single-global pipeline into a real Payload Blocks page-builder: a `Pages` collection (slug + localized `layout` blocks) replaces the `Home` global, one `RenderBlocks` switch renders block-type -> component, and the homepage plus all 6 interior routes render end-to-end. Ship the thinnest working slice (Hero + RichText + CTABand blocks) so the homepage stays live through the migration; remaining blocks layer on in later plans. Add the `Certifications` collection schema now so its schema syncs in this plan's single [BLOCKING] step. This is the MVP walking-slice for the block system.

Purpose: RESEARCH D-02 resolution / Pattern 1-6. Retiring the `Home` global and repointing the homepage is atomic — it must all land together to keep the build green.
Output: Pages + Certifications collections, RenderBlocks, 3 real blocks, generic fetch/revalidate helpers, [slug] route, retired Home global, re-seeded 7 pages, green local SQLite build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-RESEARCH.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-CONTEXT.md
@.planning/phases/01-foundation-cms-decision/01-02-SUMMARY.md
@.planning/phases/01-foundation-cms-decision/01-03-SUMMARY.md
@src/payload.config.ts
@src/globals/Home.ts
@src/hooks/revalidateHome.ts
@src/lib/payload-fetch.ts
@src/lib/seed-content.ts
@scripts/seed-home.ts
@src/components/Hero.tsx
@src/app/(site)/[locale]/page.tsx

<interfaces>
Existing getHomeContent (to be generalized) — src/lib/payload-fetch.ts:
```ts
export async function getHomeContent(locale: Locale): Promise<{ content: Home; isTranslated: boolean }>
```
Target generalization (RESEARCH Pattern 4) — dual query: display (fallbackLocale en for non-en) + existence check (fallbackLocale:false); isTranslated = locale==='en' || Boolean(nativeCheck.docs[0]?.title):
```ts
export async function getPageContent(slug: string, locale: Locale): Promise<{ page: Page | null; isTranslated: boolean }>
```

Media relation guard already used in Hero.tsx (repeat this exact pattern in every media-bearing block — RESEARCH Pitfall 3):
```ts
const image = content.heroImage && typeof content.heroImage === "object" ? (content.heroImage as Media) : null
```

RenderBlocks contract (RESEARCH Pattern 3) — sectionBg alternates bg-white (even) / bg-neutral-100 (odd); CTABand ignores alternation and hardcodes bg-primary-900:
```tsx
export function sectionBg(index: number) { return index % 2 === 0 ? "bg-white" : "bg-neutral-100" }
```

Locale type: import type { Locale } from "@/i18n/routing"; RTL_LOCALES set also there.
Existing chrome to reuse unchanged: LocaleFallbackNotice, GlobalHeader/Footer (injected by [locale]/layout.tsx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pages + Certifications collections, generic fetch + revalidate helpers, retire Home global</name>
  <read_first>
    - src/globals/Home.ts, src/hooks/revalidateHome.ts, src/lib/payload-fetch.ts (the exact patterns being generalized)
    - src/payload.config.ts (collections/globals arrays, localization block)
    - src/collections/Media.ts (access-function shape to mirror)
    - 02-RESEARCH.md Pattern 1 (Pages config), Pattern 2 (localization cascade), Pattern 4 (fetch), Pattern 5 (revalidate), and the Certifications collection example
    - 02-CONTEXT.md D-02, D-04, D-05
  </read_first>
  <action>
    Create src/collections/Pages.ts: slug 'pages', admin.useAsTitle 'title', access functions all `({ req: { user } }) => Boolean(user)` (mirror Media). Fields: `title` (text, required, localized) admin-facing only; `slug` (text, required, unique, index) NOT localized (one canonical URL per page, D-08); `layout` (type 'blocks', localized: true, minRows 1, blocks: [Hero, RichText, CTABand] imported from '@/blocks'). Per RESEARCH Pattern 2 set localized ONLY on the top-level `layout` field — cascade handles nested block fields; do NOT re-set localized on any nested block field. hooks.afterChange: [revalidatePage].
    Create src/collections/Certifications.ts exactly per the RESEARCH example: slug 'certifications', useAsTitle 'name', Boolean(user) access; fields name (text required localized), issuingBody (text required localized), logo (upload relationTo media, required), certificatePdf (upload relationTo media, optional), validityNotes (text localized), halal (checkbox defaultValue false — drives D-05 prominence), displayOrder (number defaultValue 0). No versions/drafts (RESEARCH anti-pattern), no custom SQL (Pitfall 4).
    Create src/hooks/revalidatePage.ts (CollectionAfterChangeHook<Page>): if context.disableRevalidate return doc; compute path = doc.slug === 'home' ? '/' : `/${doc.slug}`; call revalidatePath for the en root + '/ar'+path, '/fr'+path, '/ru'+path (mirror revalidateHome's 4-locale loop; for home use '/', '/ar', '/fr', '/ru'). Return doc.
    Generalize src/lib/payload-fetch.ts: add getPageContent(slug, locale) using payload.find on 'pages' where slug equals, limit 1, dual query per the interfaces block (display + fallbackLocale:false existence check), overrideAccess:true; isTranslated = locale==='en' || Boolean(nativeCheck.docs[0]?.title). Delete getHomeContent.
    Update src/payload.config.ts: add Pages, Certifications to `collections`; remove Home from `globals` (leave globals: [] or drop the key). Delete src/globals/Home.ts and src/hooks/revalidateHome.ts. Do NOT change the db/localization/storage blocks (already prod-ready per Phase 1 Plan 04).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/collections/Pages.ts and Certifications.ts exist; payload.config.ts collections includes both; globals no longer includes Home
    - src/globals/Home.ts and src/hooks/revalidateHome.ts are deleted
    - getHomeContent is removed; getPageContent is exported from payload-fetch.ts
    - `layout` blocks field has localized:true; NO nested block field sets localized (grep src/blocks shows no `localized: true` inside block field defs)
    - npx tsc --noEmit exits 0 (types will fully resolve after payload-types regen in Task 3; if tsc references generated Page/Certification types not yet present, run `npx payload generate:types` first)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: RenderBlocks + Hero/RichText/CTABand blocks + [slug] route + homepage repoint</name>
  <read_first>
    - src/components/Hero.tsx (the component being generalized into HeroBlock — reuse image guard, gradient, PrimaryButton pattern)
    - src/app/(site)/[locale]/page.tsx and layout.tsx (routing + setRequestLocale pattern)
    - 02-UI-SPEC.md Block Library sections 1 (Hero), 2 (RichText), 7 (CTABand); Section rhythm rule; Copywriting Contract
    - 02-RESEARCH.md Pattern 3 (RenderBlocks), Pattern 6 (explicit [slug] route)
  </read_first>
  <action>
    Create block config files (Payload Block definitions — field schemas only, NO localized on nested fields):
    - src/blocks/Hero.ts (slug 'hero'): variant (select full|compact, default compact), eyebrow (text), headline (text required), subhead (textarea), primaryCta (group: label text, href text), secondaryCta (group: label text, href text).
    - src/blocks/RichText.ts (slug 'richText'): content (richText, lexical).
    - src/blocks/CTABand.ts (slug 'ctaBand'): heading (text required), body (textarea), primaryCta (group label/href), secondaryCta (group label/href).
    - src/blocks/index.ts: barrel re-exporting Hero, RichText, CTABand (and reserve comment noting later blocks append here).
    Create React render components under src/components/blocks/ (server components, consume the generated Page['layout'][number] discriminated union; each derives its own section bg from `index` via sectionBg except CTABand):
    - RenderBlocks.tsx: BLOCK_MAP { hero: HeroBlock, richText: RichTextBlock, ctaBand: CTABandBlock }, sectionBg helper (per interfaces), map with fail-soft on unknown blockType (return null), key by block.id ?? index, pass {block, index}.
    - HeroBlock.tsx: generalize Hero.tsx — full variant keeps min-h-[70dvh]; compact variant uses min-h-[320px] md:min-h-[400px]; render eyebrow (Label, tracking-wide) when present, Display headline, Body subhead; primaryCta as PrimaryButton (shadcn Button default + hover:bg-primary-500 focus-visible:ring-accent-600); secondaryCta as Button variant="outline" (white border on dark hero) positioned inline-end desktop / stacked mobile; keep the Media image guard + gradient. Keep the existing latn sample-count line ONLY on the homepage full-variant hero so the Phase 1 FOUND-03 e2e assertion stays green (per 01-03-SUMMARY decision).
    - RichTextBlock.tsx: render Payload Lexical content via @payloadcms/richtext-lexical's React JSX converter (RESEARCH: do NOT hand-roll dangerouslySetInnerHTML); container max-w-[720px] mx-auto; Body token; wrap section with sectionBg(index).
    - CTABandBlock.tsx: full-width bg-primary-900 band (ignores alternation — documented UI-SPEC exception), white text, max-w-2xl mx-auto text-center, PrimaryButton + optional outline secondary.
    Create src/app/(site)/[locale]/[slug]/page.tsx: generateStaticParams returns the 6 interior slugs [about, certifications, manufacturing, export, company, contact]; the page awaits params, setRequestLocale, calls getPageContent(slug, locale); if page is null call notFound(); render LocaleFallbackNotice when !isTranslated, then RenderBlocks with page.layout.
    Update src/app/(site)/[locale]/page.tsx (homepage): replace getHomeContent + direct Hero with getPageContent('home', locale) + LocaleFallbackNotice + RenderBlocks. Do NOT delete src/components/Hero.tsx yet if RenderBlocks/HeroBlock fully supersede it — remove Hero.tsx only once HeroBlock covers the homepage render and no import references remain (verify by grep).
    All layout uses logical properties only (RTL gate).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl</automated>
  </verify>
  <acceptance_criteria>
    - src/blocks/{Hero,RichText,CTABand}.ts and index.ts exist; block slugs are hero/richText/ctaBand
    - src/components/blocks/{RenderBlocks,HeroBlock,RichTextBlock,CTABandBlock}.tsx exist
    - RenderBlocks exports sectionBg and fails soft (returns null) on an unknown blockType
    - [locale]/[slug]/page.tsx has generateStaticParams with the 6 interior slugs and calls notFound() on a missing doc
    - [locale]/page.tsx calls getPageContent('home', locale) (grep pattern matches); no getHomeContent reference remains anywhere (grep clean)
    - HeroBlock compact variant uses min-h-[320px] md:min-h-[400px]; full uses min-h-[70dvh]
    - npm run lint:rtl passes; npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] schema sync + re-seed all 7 pages + green local build + Wave-0 test scaffolds</name>
  <read_first>
    - scripts/seed-home.ts, src/lib/seed-content.ts (current Home-global seed to replace)
    - vercel.json (prod build runs `payload migrate` then `npm run db:seed && npm run build` — the seed must populate Pages so prod prerender/prod build does not hit empty content)
    - tests/int/payload-fallback.spec.ts (mirror this for pages-fallback.spec.ts)
    - tests/e2e/rtl-arabic.spec.ts and locale-routing.spec.ts (e2e conventions, en+ar projects)
    - 02-VALIDATION.md Per-Task Verification Map; 02-RESEARCH.md Wave 0 Gaps; Pitfall 5 (no fabricated cert/IEC numbers)
  </read_first>
  <action>
    This is the [BLOCKING] schema-sync + seed step — it must complete before verification and before any later plan runs. Replace src/lib/seed-content.ts's HOME_EN_SEED with a PAGES_EN_SEED structure: an array of page objects keyed by slug, each with title + layout blocks. Seed all 7 pages so both the local SQLite build and the prod Neon build prerender without empty-content errors:
    - home: title "Home", layout [hero(full, eyebrow "ISO / HACCP / APEDA CERTIFIED", headline+subhead from UI-SPEC placeholder table, primaryCta "Request a Quote" -> /contact, secondaryCta "Chat on WhatsApp" -> the wa.me link), ctaBand]. (FeatureGrid/CertStrip/StatsBand/ExportMap inserted by later plans.)
    - about, certifications, manufacturing, export, company, contact: each title + a compact hero (headline/subhead from the UI-SPEC placeholder table) + a ctaBand (except contact, which per UI-SPEC has no CTABand — seed contact with just the compact hero for now; ContactBlock lands in Plan 06). These thin stubs guarantee every generateStaticParams slug has a doc to prerender; later plans enrich each page's layout.
    Use realistic-shaped placeholder copy (long strings) per D-03; NEVER fabricate a specific IEC/FSSAI/certificate number or named client (Pitfall 5 legal caution).
    Rewrite scripts/seed-home.ts into scripts/seed-pages.ts: idempotent per-slug upsert into the `pages` collection (find by slug; create if absent, else skip or update the en locale), overrideAccess:true, context.disableRevalidate:true, seed en only (fr/ar/ru left unseeded so the fallback path stays exercisable). Keep the @next/env default-import runtime-destructure pattern from the existing script (01-03 Deviation #4). Update package.json `db:seed` to run scripts/seed-pages.ts, and update vercel.json's buildCommand only if the script filename change requires it (db:seed npm script indirection means vercel.json likely needs no edit — verify).
    Run `npx payload generate:types` to regenerate payload-types.ts with Page + Certification types (SQLite dev auto-push creates the new tables on Payload init — no manual migration; prod uses the existing payload.config push:true / vercel.json migrate, unchanged).
    Create Wave-0 test scaffolds: tests/int/pages-fallback.spec.ts (mirror payload-fallback.spec.ts against the Pages collection — assert isTranslated false for an untranslated locale, true once en content exists) and tests/e2e/page-routing.spec.ts (each of the 6 interior slugs returns 200 and renders a hero at /[locale]/[slug]; unknown slug returns 404; en+ar).
    Then run the seed and the local build to prove no empty-content prerender error.
  </action>
  <verify>
    <automated>npx payload generate:types && npm run db:seed && npx tsc --noEmit && npm run lint:rtl && npx vitest run tests/int/pages-fallback.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - payload-types.ts contains a Page interface and a Certification interface (grep)
    - src/lib/seed-content.ts exports PAGES_EN_SEED covering all 7 slugs; no fabricated IEC/certificate/license numbers or named clients in the copy
    - scripts/seed-pages.ts is idempotent (re-running does not duplicate docs) and seeds en only
    - package.json db:seed points at seed-pages.ts; `npm run db:seed` succeeds
    - tests/int/pages-fallback.spec.ts passes
    - `npm run build` exits 0 locally against SQLite (all 7 pages prerender; no empty-required-field / empty-content error)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public browser -> Next RSC -> Payload Local API | Public site reads Pages/Certifications via server-side Local API with overrideAccess:true (never the public REST/GraphQL surface) — same as Phase 1 |
| CMS editor (admin, authenticated) -> DB | Content authoring; unchanged Payload auth from Phase 1 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | Pages/Certifications public exposure of unpublished content | mitigate | No drafts/versions feature introduced (RESEARCH anti-pattern) — all docs are effectively published once created, same as the retired Home global; access functions locked to Boolean(user) for REST/GraphQL, public reads only via overrideAccess Local API |
| T-02-03 | Tampering | Lexical RichText rendered on RichTextBlock | mitigate | Render via @payloadcms/richtext-lexical's own React JSX converter; NO dangerouslySetInnerHTML path (RESEARCH Security Domain) |
| T-02-04 | Tampering | Access control on new collections | mitigate | Pages/Certifications access all `Boolean(user)` for create/read/update/delete, mirroring Media/Home (RESEARCH V4) |
</threat_model>

<verification>
- `npx payload generate:types` regenerates Page + Certification types
- `npm run build` exit 0 locally (SQLite), all 7 pages prerendered
- `npx vitest run tests/int/pages-fallback.spec.ts` green
- `npx tsc --noEmit` + `npm run lint:rtl` clean
- grep: no `getHomeContent` / `src/globals/Home` references remain
</verification>

<success_criteria>
The homepage and all 6 interior routes render through the Pages collection + RenderBlocks; the Home global is retired; the fallback rule works for untranslated locales; the local SQLite build is green with all pages seeded. The block-system contract (block config -> RenderBlocks -> page) is proven end-to-end, ready for later plans to append blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-02-SUMMARY.md`
</output>
