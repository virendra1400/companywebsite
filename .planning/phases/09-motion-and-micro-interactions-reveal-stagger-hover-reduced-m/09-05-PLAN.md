---
phase: 09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m
plan: 05
type: execute
wave: 3
depends_on: ["09-02", "09-03", "09-04"]
files_modified: []
autonomous: false
requirements: [PERF-01, PERF-03]
user_setup: []

must_haves:
  truths:
    - "The full suite (typecheck, lint, lint:rtl, vitest, playwright en + ar, production build) is green on the merged Wave 2 tree, not just on each plan's isolated branch"
    - "CLS stays at or near zero and LCP is unchanged on the most block-dense page after every non-hero block gained a reveal wrapper — the phase's stated zero-regression bar (D-03, PERF-01)"
    - "A human has seen the motion in both LTR and Arabic and confirms it reads as restrained Stripe/Linear-tier polish, not agency choreography — judged against that benchmark alone, since no other motion reference was adopted for this phase (D-01, D-02)"
    - "Every locked decision D-01 through D-13 is observably delivered in the running site, verified against the merged tree rather than inferred from plan summaries"
  artifacts:
    - "Verification findings recorded in 09-05-SUMMARY.md (no source artifact — this plan is a gate)"
  key_links:
    - "Merged Wave 2 output -> full suite -> human sign-off -> phase seal"
---

<objective>
Verify the merged output of Wave 2 as one tree and get the human sign-off that a motion phase cannot be sealed without.

Purpose: plans 09-02, 09-03 and 09-04 each pass in isolation by construction. What no isolated run can check is their composition — a section reveal wrapping a staggered grid wrapping a hover-lifting card, all at once, on a page with eleven blocks, in two reading directions. Phase 8's P04 established this merged-verification step for exactly this reason, and motion is the phase where "each part passed" is least persuasive.

Output: a green full suite, a Lighthouse/CWV spot-check on the homepage, and a recorded human approval covering both locales.

Planning note (recorded, visible skip): the spec-less edge/prohibition probe fallback did not run this phase — Phase 9 has zero mapped requirement IDs, so there was no requirement text to derive probe predicates from. `must_haves.truths` come from 09-CONTEXT.md D-01..D-13 and 09-UI-SPEC.md `## UI Considerations` instead.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-CONTEXT.md
@.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-UI-SPEC.md
@.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-VALIDATION.md
</context>

## Artifacts this phase produces

| Artifact | Created by | Kind |
|----------|-----------|------|
| `useInView(threshold?)` -> `{ ref, inView }` | 09-01 | client hook, `src/components/motion/useInView.ts` |
| `Reveal({ children })` | 09-01 | client component, `src/components/motion/Reveal.tsx` |
| `RevealItem({ children, index, direction?, className? })` | 09-01 | client component, `src/components/motion/RevealItem.tsx` |
| `STAGGER_MS = 50`, `STAGGER_CAP = 8` | 09-01 | module constants in `RevealItem.tsx` |
| `--animate-float-in` + `@keyframes float-in` | 09-01 | `@theme` token in `src/app/globals.css` |
| `@import "tw-animate-css"` + `@media (scripting: none)` guard | 09-01 | `src/app/globals.css` |
| `data-motion` / `data-direction` / `data-revealed` DOM contract | 09-01 | DOM contract |
| `tests/unit/motion-reveal.spec.tsx` | 09-01 | vitest unit spec |
| `tests/e2e/reduced-motion.spec.ts` | 09-02 | playwright spec |
| Hero-excluded `Reveal` wiring in `RenderBlocks.tsx` | 09-02 | edit |
| `RevealItem` wiring in 4 grid blocks + 2 catalog grids | 09-03 | edits |
| Directional-slide RTL assertions in `tests/e2e/rtl-arabic.spec.ts` | 09-03 | edit |
| Accordion `grid-template-rows` animation + closed-state a11y guard | 09-04 | edit, `src/components/ui/accordion.tsx` |
| `active:scale-[0.98]` in the `buttonVariants` base cva | 09-04 | edit, `src/components/ui/button.tsx` |
| `animate-float-in` entrance + hover lift on the floating WhatsApp CTA | 09-04 | edit |
| Deepened card hover lift + image hover scale | 09-04 | edits, ProductCard / InsightCard |
| Accordion + tap-feedback assertions in `tests/e2e/contact.spec.ts` | 09-04 | edit |
| **Merged-tree verification findings** | **09-05 (this plan)** | recorded in `09-05-SUMMARY.md` |

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate suite on the merged Wave 2 tree</name>
  <files>(no source files — verification only; any fix required is scoped and recorded, not open-ended)</files>
  <read_first>
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-VALIDATION.md` ("Full suite command" and "Sampling Rate" — this task is its "Before /gsd-verify-work" row)
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-01-SUMMARY.md`, `09-02-SUMMARY.md`, `09-03-SUMMARY.md`, `09-04-SUMMARY.md` (what each plan actually landed, including any deviation the executors recorded)
    - `.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-04-SUMMARY.md` (Phase 8's merged-verification precedent, including its list of pre-existing unrelated failures — use it to tell an inherited failure apart from one this phase caused)
    - `playwright.config.ts` (the `webServer` block starts `npm run dev`; Phase 8 P04 recorded dev-server contention as a source of spurious e2e failures, so run the e2e suites without competing dev servers)
  </read_first>
  <action>
On the merged tree, run in order and capture the result of each: `npm run typecheck`, `npm run lint`, `npm run lint:rtl`, `npm run test`, `npm run build`, then `npx playwright test --project=en` and `npx playwright test --project=ar`.

Include `npm run build` even though 09-VALIDATION.md's "full suite command" omits it: this phase adds a new npm dependency and three new CSS constructs to `globals.css`, and a Tailwind `@theme` keyframe or an `@import` that resolves in dev but not in a production build is a failure mode that only `next build` surfaces.

For every failure, classify before fixing — Phase 8 P04 traced 16 e2e failures to three pre-existing unrelated bugs plus dev-server contention, none of them caused by that phase. Use `git stash` or a scratch checkout to determine whether the same test fails on the pre-phase-9 tree. Classify each as: (a) caused by Phase 9, (b) pre-existing and unrelated, (c) infrastructure/contention. Fix only (a), with the smallest change in the plan that owns the file. Record (b) and (c) in the summary with evidence rather than fixing them here — out of scope, and silently absorbing unrelated fixes into a motion phase is how phase boundaries rot.

Then run these composition checks that no single Wave 2 plan could have run, because each needs the others' changes present:

- **Grid double-layering.** On `/` with no reduced-motion preference, scroll a grid block (FeatureGrid or Testimonials) into view and confirm items arrive staggered inside a section that is itself revealing — the two layers compose rather than fighting, and the section-level 600ms plus a capped 400ms item delay does not leave the last card visibly lagging the section.
- **Equal-height rows survived.** Confirm cards in the same FeatureGrid/Testimonials row still share a height after the `RevealItem` wrapper became the grid child.
- **Reduced motion end to end.** With `prefers-reduced-motion: reduce` set at the OS or browser level, load `/`, `/products`, `/contact`: nothing is stuck invisible, the accordion still opens and closes, buttons still respond, the WhatsApp button is present in its final state.
- **Decision sweep.** Walk D-01 through D-13 against the running site and record one line of evidence per decision in the summary. D-08 in particular has no component edit behind it — its evidence is the mobile nav Sheet actually transitioning on open/close, which is the visible consequence of the `tw-animate-css` import.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run lint:rtl &amp;&amp; npm run test &amp;&amp; npm run build &amp;&amp; npx playwright test --project=en &amp;&amp; npx playwright test --project=ar</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0 (or fails only on the 5 pre-existing unrelated errors already logged to deferred-items.md in Phase 8 P01 — cite them explicitly if so)
    - `npm run lint:rtl` prints "RTL guard: no physical-direction classes under src/." and exits 0
    - `npm run test` exits 0 across both vitest projects
    - `npm run build` exits 0 with no Tailwind or CSS resolution warning naming `tw-animate-css`, `float-in`, or `animate-float-in`
    - `npx playwright test --project=en` and `--project=ar` both exit 0, or every remaining failure is documented in the summary with a reproduction on the pre-phase-9 tree proving it is inherited
    - `09-05-SUMMARY.md` contains a D-01..D-13 table with one evidence line per decision, and no decision marked "assumed" or "not checked"
    - The four composition checks above are each recorded with a stated observed result
  </acceptance_criteria>
  <done>Every gate is green on the merged tree, each residual failure is classified with evidence, and all thirteen locked decisions have a recorded observation against the running site.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on motion quality, CLS/LCP, and Arabic mirroring</name>
  <files>(none — human verification)</files>
  <read_first>
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-CONTEXT.md` — D-01/D-02 set the motion register (restrained Stripe/Linear tier, no reference site beyond that benchmark). This checkpoint is the only place that register is judged; no grep can score "does this feel busy."
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-UI-SPEC.md` **Motion Contract §1-§9** — the locked values (0.6s section reveal, 50ms stagger capped at 8, 2px card lift, 0.98 tap dip) the human is checking the live render against.
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-VALIDATION.md` **Manual-Only Verifications** — the CLS/LCP spot-check row is explicitly reserved for a human because CWV scoring is not in this phase's automated suite.
    - Task 1's recorded results in `09-05-SUMMARY.md` — do not present this checkpoint until the gate suite is green, so the human is judging taste rather than debugging.
  </read_first>
  <action>
    Stop and hand control to the human. Present the `<what-built>` summary and the numbered `<how-to-verify>` steps below verbatim, then wait for the `<resume-signal>`. Do not self-approve, do not infer a pass from Task 1's green gates, and do not edit source from this task.

    This checkpoint exists because three things in this phase are unprovable by automation: (a) whether the motion register reads as restrained Stripe/Linear-tier polish or as agency choreography for a B2B procurement audience (D-01, D-02 — no reference site beyond that benchmark, so this is a judgement the human owns), (b) the CLS/LCP figures, which need Lighthouse and are the manual-only row in 09-VALIDATION.md, and (c) whether the Arabic directional slide arrives from the visually correct edge — `npm run lint:rtl` cannot catch a semantically inverted `translate-x` sign (RESEARCH Pitfall 6).

    If the human reports a defect, record it in `09-05-SUMMARY.md`, name the owning Wave 2 plan and file (09-02 RenderBlocks, 09-03 grid/catalog wiring, 09-04 component micro-interactions, 09-01 the motion primitives and tokens), fix it there rather than here, re-run Task 1's gate suite, and re-present this checkpoint.
  </action>
  <what-built>
Sitewide motion: every non-hero CMS block fades and rises into view once per page load; grid items stagger 50ms apart with the delay capped at the 8th item; ExportProcess steps slide in from the inline-start edge; product and insight cards lift 2px with a subtly scaling photo on hover; every button dips to 0.98 on press; the FAQ accordion finally animates open and closed; the floating WhatsApp button floats in on paint. Under `prefers-reduced-motion: reduce` every scroll-reveal is disabled and the rest becomes instant. Automated coverage: unit spec for the class contract, `reduced-motion.spec.ts`, RTL directional-mirroring assertions in `rtl-arabic.spec.ts`, accordion and tap-feedback assertions in `contact.spec.ts` — all green.
  </what-built>
  <how-to-verify>
1. Start the dev server (`npm run dev`) and open `http://localhost:3000/`.
2. Scroll the homepage top to bottom at a normal reading pace. The hero must be fully painted the instant the page loads, with no fade. Every section below it should fade and rise as you reach it. Judgement call: does this read as restrained Stripe/Linear-tier polish, or does it feel busy? D-01 sets the bar deliberately low-key for a B2B procurement audience — say so if any surface overshoots.
3. Scroll back up and down again over the same sections. Nothing should re-animate. Any section that fades in a second time is a D-06 failure.
4. Open `http://localhost:3000/products`. Cards should arrive in a tight left-to-right ripple, not a slow one-by-one march, and the last card in a large category must not lag noticeably behind the first. Hover a card: it should lift slightly, its shadow deepen, and its photo scale a touch inside the frame.
5. Open `http://localhost:3000/contact`, scroll to the FAQ. Open a question, then close it. BOTH directions must animate — a smooth open followed by an instant snap shut is the exact bug this phase was meant to fix. Press and hold any button: it should dip slightly.
6. Shrink the window to mobile width and open the hamburger menu. The panel should slide in rather than appear abruptly.
7. Open `http://localhost:3000/ar` and repeat steps 2 and 4. In the export-process section, steps must slide in from the RIGHT (Arabic's inline-start edge). If any element arrives from the wrong side, that is the RTL mirroring failure this phase's e2e is guarding against — report which surface.
8. Enable reduced motion at the OS level (macOS: System Settings > Accessibility > Display > Reduce motion) and hard-reload `/`, `/products`, `/contact`. Nothing should be invisible or stuck. Hover and press states should still visibly change, just without animating. The FAQ should still open and close, instantly.
9. Run Lighthouse (Chrome DevTools > Lighthouse > Performance, mobile preset) against `http://localhost:3000/` and report the CLS and LCP figures. CLS should be at or near zero and LCP should be unchanged versus before this phase — that is the whole point of the transform/opacity-only constraint, and Phase 10's hardening pass starts from whatever this reads.
  </how-to-verify>
  <resume-signal>Type "approved" to seal the phase, or describe what looked wrong — naming the page, the locale, and the surface.</resume-signal>
  <verify>
    <automated>MISSING — human-judgement checkpoint by design. Automated coverage for the mechanical half of these steps already exists and is asserted in Task 1: `npx playwright test --project=en &amp;&amp; npx playwright test --project=ar` (covers reduced-motion, zero-replay, accordion open+close, RTL mirroring). Steps 2, 4 and 9 above (motion register, ripple pace, Lighthouse CLS/LCP) have no automated equivalent in this phase — Phase 10 owns CWV tooling.</automated>
    <human-check>All nine `<how-to-verify>` steps performed on a live dev server in both `en` and `ar`, with the CLS and LCP figures from step 9 reported as numbers.</human-check>
  </verify>
  <acceptance_criteria>
    - The human has responded with the resume-signal — the checkpoint is never self-approved and never inferred from Task 1's green gates
    - Step 2 answered explicitly: the hero paints with no fade, sections below it fade and rise, and the human has stated whether the register reads as restrained (D-01, D-02) or overshoots on a named surface
    - Step 3 answered explicitly: no section re-animates on scroll-up-then-down (D-06)
    - Step 5 answered explicitly: the FAQ accordion animates in BOTH directions — an animated open followed by an instant snap shut is a fail, not a pass (D-13)
    - Step 7 answered explicitly: in `ar`, the export-process steps arrive from the right; any surface arriving from the wrong edge is named (D-10)
    - Step 8 answered explicitly: under OS-level reduced motion nothing is invisible or stuck on `/`, `/products`, `/contact`, and hover/press/accordion state changes still occur (D-12)
    - Step 9 reported as two numbers: CLS at or near zero, and an LCP figure with a stated comparison against the pre-phase-9 baseline (D-03, PERF-01)
    - Every reported defect is recorded in `09-05-SUMMARY.md` against its owning plan and file, fixed there, and re-verified — not fixed inside this checkpoint
  </acceptance_criteria>
  <done>A human has performed all nine verification steps across both locales, reported the CLS/LCP numbers, and either signed off or reported defects that have been fixed in their owning plan's files and re-verified through Task 1's gate suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan runs existing test and build commands against the merged tree and writes no source code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-09 | Tampering | Dependency tree at merge time | low | mitigate | Task 1's `npm run build` runs against the merged `package-lock.json`, re-exercising the `tw-animate-css` install that plan 09-01's Package Legitimacy Gate cleared (verdict OK, no postinstall). No package is added by this plan. |
| T-09-10 | Repudiation | Phase seal without evidence | low | mitigate | Task 1 requires a recorded D-01..D-13 evidence table and forbids "assumed"/"not checked" entries; Task 2 is a blocking human gate. Neither can be satisfied by inference from prior plan summaries alone. |

## ASVS L1 Category Applicability (per 09-RESEARCH.md Security Domain)

| ASVS Category | Applies | Rationale |
|---------------|---------|-----------|
| V2 Authentication | No | No auth surface touched |
| V3 Session Management | No | No session/cookie logic touched |
| V4 Access Control | No | No access-gated route touched |
| V5 Input Validation | No | This plan writes no code |
| V6 Cryptography | No | Not applicable |

Conclusion: verification-only plan, no code, no new dependency, no new data flow. No `high`-severity threat present, so the `block_on: high` gate passes.
</threat_model>

<verification>
- Every command in Task 1's automated verify exits 0, or each residual failure is documented with a pre-phase-9 reproduction
- `09-05-SUMMARY.md` carries the D-01..D-13 evidence table, the four composition-check results, and the Lighthouse CLS/LCP figures
- Task 2's human approval is recorded verbatim, including any caveats raised
</verification>

<success_criteria>
- Full suite green on the merged tree in both locales, with a clean production build
- CLS at or near zero and LCP unchanged on the homepage after sitewide reveal wrapping
- All thirteen locked decisions observed in the running site, not inferred
- Human sign-off obtained for both LTR and Arabic, and for the reduced-motion pass
</success_criteria>

<output>
Create `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-05-SUMMARY.md` when done
</output>
