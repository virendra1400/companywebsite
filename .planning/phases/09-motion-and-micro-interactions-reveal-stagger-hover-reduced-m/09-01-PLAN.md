---
phase: 09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/globals.css
  - src/components/motion/useInView.ts
  - src/components/motion/Reveal.tsx
  - src/components/motion/RevealItem.tsx
  - tests/unit/motion-reveal.spec.tsx
autonomous: true
requirements: [PERF-01, PERF-03]
user_setup: []

must_haves:
  truths:
    - "Reveal renders its children hidden on first paint (opacity 0 + 24px downward offset) and animates only opacity and translate — no layout-affecting property is ever transitioned (D-03)"
    - "Under prefers-reduced-motion: reduce the reveal hook reports in-view immediately on mount and attaches no IntersectionObserver at all, so content is never gated behind a trigger that intentionally never animates (D-12, UI Considerations: Reduced-motion state)"
    - "A revealed element never re-hides or re-animates: the observer stops observing on first intersection (D-06, UI Considerations: Zero-replay / stateful reveal)"
    - "Per-item stagger delay plateaus at 400ms (STAGGER_CAP 8 x STAGGER_MS 50) no matter how large the grid, so a large catalog never produces a slow item-by-item march (UI Considerations: Overflow / large-collection)"
    - "A single grid item reveals at 0ms delay (index 0 x 50ms) so small-N grids degrade gracefully (UI Considerations: Zero / one item count)"
    - "RevealItem direction=start emits both -translate-x-4 and rtl:translate-x-4 so a directional slide arrives from the inline-start edge in both LTR and RTL (D-10, UI Considerations: RTL / directional mirroring)"
    - "No scroll event listener exists anywhere in the new motion code (D-04)"
  artifacts:
    - src/components/motion/useInView.ts
    - src/components/motion/Reveal.tsx
    - src/components/motion/RevealItem.tsx
    - "src/app/globals.css: --animate-float-in theme variable + @keyframes float-in"
    - "src/app/globals.css: @import \"tw-animate-css\""
    - "src/app/globals.css: @media (scripting: none) no-JS reveal guard"
    - tests/unit/motion-reveal.spec.tsx
    - "package.json devDependency tw-animate-css"
  key_links:
    - "Reveal/RevealItem -> useInView (single shared IntersectionObserver hook, one instance per wrapper)"
    - "globals.css --animate-float-in -> the animate-float-in utility class consumed by WhatsAppFloatingButton in plan 09-04"
    - "globals.css @import \"tw-animate-css\" -> the already-present but currently dead animate-in / fade-in-0 / zoom-in-95 / slide-in-from-* classes in src/components/ui/sheet.tsx, dropdown-menu.tsx, select.tsx (D-08 mobile nav polish, zero component edits needed)"
---

<objective>
Build the motion foundation this whole phase composes from: one IntersectionObserver hook, two client wrapper components, and the three CSS additions in `globals.css` that later plans consume.

Purpose: every other plan in Phase 9 is a thin wiring change on top of these five artifacts. Nothing here is user-visible on its own — that is deliberate, it keeps the sitewide wiring plans (09-02, 09-03, 09-04) small enough to execute correctly in parallel.

Output: `src/components/motion/{useInView.ts,Reveal.tsx,RevealItem.tsx}`, three additions to `src/app/globals.css`, `tw-animate-css` devDependency, and a fast unit test that locks the class contract.

Planning note (recorded, visible skip): the spec-less edge/prohibition probe fallback did not run for this phase — Phase 9 has zero requirement IDs mapped in REQUIREMENTS.md, so there was no requirement text to derive probe predicates from. `must_haves.truths` are instead derived directly from 09-CONTEXT.md's D-01..D-13 and 09-UI-SPEC.md's `## UI Considerations` covered rows, which is a more specific source than a generic probe would have produced.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-CONTEXT.md
@.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-RESEARCH.md
@.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-UI-SPEC.md
@.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-PATTERNS.md
@src/app/globals.css
</context>

## Artifacts this phase produces

Symbols and files created by Phase 9 as a whole (this plan creates the first group):

| Artifact | Created by | Kind |
|----------|-----------|------|
| `useInView(threshold?)` -> `{ ref, inView }` | 09-01 | client hook, `src/components/motion/useInView.ts` |
| `Reveal({ children })` | 09-01 | client component, `src/components/motion/Reveal.tsx` |
| `RevealItem({ children, index, direction?, className? })` | 09-01 | client component, `src/components/motion/RevealItem.tsx` |
| `STAGGER_MS = 50`, `STAGGER_CAP = 8` | 09-01 | module constants in `RevealItem.tsx` |
| `--animate-float-in` + `@keyframes float-in` | 09-01 | `@theme` token in `src/app/globals.css` -> `animate-float-in` utility |
| `@import "tw-animate-css"` | 09-01 | `src/app/globals.css` — resolves the pre-existing dead shadcn animation utilities |
| `@media (scripting: none)` reveal guard | 09-01 | `src/app/globals.css` |
| `data-motion="reveal"` / `data-motion="reveal-item"` / `data-direction` / `data-revealed` | 09-01 | DOM contract used by CSS variants and by the e2e specs in 09-02 / 09-03 |
| `tests/unit/motion-reveal.spec.tsx` | 09-01 | vitest unit spec |
| `tests/e2e/reduced-motion.spec.ts` | 09-02 | playwright spec |
| Reveal wiring in `RenderBlocks.tsx` (hero-excluded) | 09-02 | edit |
| `RevealItem` wiring in 4 grid blocks + 2 catalog grids | 09-03 | edits |
| Accordion `grid-template-rows` open/close animation | 09-04 | edit, `src/components/ui/accordion.tsx` |
| `active:scale-[0.98]` in `buttonVariants` base cva | 09-04 | edit, `src/components/ui/button.tsx` |

<tasks>

<task type="auto">
  <name>Task 1: Add tw-animate-css and the three globals.css motion additions</name>
  <files>package.json, package-lock.json, src/app/globals.css</files>
  <read_first>
    - `src/app/globals.css` (full file — you are editing the `@theme` block that starts at line 9 and adding two top-level rules; note the second `@theme inline` block at line 136 is the shadcn bridge and is NOT where these go)
    - `package.json` (confirm `tw-animate-css` is absent and note that Tailwind lives in `devDependencies`)
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-RESEARCH.md` sections "Standard Stack", "Package Legitimacy Audit", "Pitfall 1"
  </read_first>
  <action>
Install `tw-animate-css` as a devDependency with `npm install -D tw-animate-css` (RESEARCH.md Package Legitimacy Audit already cleared it: verdict OK, 35.3M weekly downloads, no postinstall script, actively maintained — no human legitimacy checkpoint is required, the audit table is present and the verdict is OK, not ASSUMED or SUS). Expected resolved version is `1.4.0` or newer.

Then make exactly three additions to `src/app/globals.css`:

(a) Immediately after the existing `@import "tailwindcss";` on line 1, add the import line for `tw-animate-css`. Order matters — it must come after the Tailwind import so its utilities land in the same cascade position the shadcn files expect. This single line is the entire fix for D-08's abrupt mobile-nav open/close: `src/components/ui/sheet.tsx`, `dropdown-menu.tsx` and `select.tsx` already carry the `animate-in` / `animate-out` / `fade-in-0` / `zoom-in-95` / `slide-in-from-*` utility references, they simply resolve to nothing today. Do not edit those three files.

(b) Inside the FIRST `@theme { ... }` block (the one opening at line 9), after the `--shadow-card-hover` declaration on line 97 and before the `/* Container */` comment, add the float-in animation token plus its keyframes. The theme variable is `--animate-float-in` with value `float-in 0.5s cubic-bezier(0.16, 1, 0.3, 1) both`, and the `@keyframes float-in` rule must be nested INSIDE the same `@theme` block (Tailwind v4 requires keyframes for an `--animate-*` variable to be declared within `@theme` to be emitted). Keyframe content: `from` sets `opacity: 0` and `transform: translateY(16px) scale(0.9)`; `to` sets `opacity: 1` and `transform: translateY(0) scale(1)`. This generates the `animate-float-in` utility consumed by plan 09-04.

(c) After the closing brace of the first `@theme` block and before the `:root` block, add a top-level no-JS safety rule: `@media (scripting: none) { [data-motion] { opacity: 1 !important; translate: none !important; } }`. Rationale to put in a short comment above it: reveal wrappers ship with `opacity-0` in the server-rendered HTML and only become visible once the client hook runs; without this guard a scripting-disabled agent would see permanently blank sections on a trust-critical B2B site. Note the property is `translate`, not `transform` — Tailwind v4 emits `translate-*` utilities as the standalone `translate` CSS property (verified against the installed compiler, tailwindcss 4.3.2).

Do not add any other token, color, spacing value or type size — this phase introduces no new visual tokens (09-UI-SPEC scope note).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint:rtl &amp;&amp; node -e "const p=require('./package.json');if(!p.devDependencies['tw-animate-css'])process.exit(1)" &amp;&amp; grep -c 'tw-animate-css' src/app/globals.css &amp;&amp; grep -c 'animate-float-in' src/app/globals.css &amp;&amp; grep -c 'scripting: none' src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');process.exit(p.devDependencies['tw-animate-css']?0:1)"` exits 0
    - `npm view tw-animate-css scripts.postinstall` prints nothing (no postinstall script in the installed package)
    - `grep -n 'tw-animate-css' src/app/globals.css` reports exactly one line, and its line number is greater than the line number reported by `grep -n '@import "tailwindcss"' src/app/globals.css`
    - `grep -c -- '--animate-float-in' src/app/globals.css` == 1 and `grep -c '@keyframes float-in' src/app/globals.css` == 1
    - `awk '/^@theme \{/,/^\}/' src/app/globals.css | grep -c '@keyframes float-in'` == 1 (the keyframes rule is nested inside the first `@theme` block, not at top level)
    - `grep -c 'scripting: none' src/app/globals.css` == 1 and the same rule block contains `[data-motion]`
    - `git diff --name-only` lists only `package.json`, `package-lock.json`, `src/app/globals.css`
    - `npm run lint:rtl` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>`tw-animate-css` is a devDependency, `globals.css` carries the import, the `--animate-float-in` theme token with nested keyframes, and the `@media (scripting: none)` guard — and nothing else changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the useInView hook and the Reveal / RevealItem client wrappers</name>
  <files>src/components/motion/useInView.ts, src/components/motion/Reveal.tsx, src/components/motion/RevealItem.tsx</files>
  <read_first>
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-PATTERNS.md` sections for `useInView.ts`, `Reveal.tsx` and the "RTL logical properties for directional slide" shared pattern — these carry the reference implementations
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-RESEARCH.md` Patterns 1-3 and Pitfalls 2, 3, 5, 6
    - `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-UI-SPEC.md` Motion Contract sections 1, 2, 3, 6, 10 (the locked numeric values)
    - `src/lib/utils.ts` (the `cn` helper — `RevealItem` merges an optional `className` through it, matching every other component in this codebase)
    - `src/components/products/ProductCard.tsx` (existing transition/className convention to match in tone)
  </read_first>
  <behavior>
    - `useInView()` under `prefers-reduced-motion: reduce` sets `inView` true on mount and constructs no IntersectionObserver
    - `useInView()` otherwise observes its element, sets `inView` true on the first intersection, then stops observing that element so a later scroll-out/scroll-in does not re-fire
    - `Reveal` server-renders with `data-motion="reveal"`, `opacity-0`, `translate-y-6`, and NO `data-revealed` attribute
    - `RevealItem` at index 0 renders `transition-delay: 0ms`; at index 3 renders `150ms`; at index 20 renders `400ms` (capped)
    - `RevealItem` with `direction="start"` renders both `-translate-x-4` and `rtl:translate-x-4`; with `direction="end"` renders both `translate-x-4` and `rtl:-translate-x-4`; default `direction="up"` renders `translate-y-4` and neither x-axis class
  </behavior>
  <action>
Create `src/components/motion/` (the directory does not exist). It must NOT go in `src/hooks/` — that directory is reserved for Payload CMS collection lifecycle hooks (`revalidateCatalog.ts` and siblings), a different concept, and CLAUDE.md documents it that way.

`src/components/motion/useInView.ts` — `"use client"`. Export `function useInView(threshold = 0.2)` returning `{ ref, inView }` where `ref` is a `useRef<HTMLDivElement>(null)` and `inView` is `useState(false)`. Inside a single `useEffect` keyed on `[threshold]`: first check `window.matchMedia("(prefers-reduced-motion: reduce)").matches` and, when true, set `inView` true and return immediately without constructing an observer (D-12 — reduced-motion users must never be gated behind a trigger that will never animate). Otherwise read `ref.current`, bail if null, construct an `IntersectionObserver` with `{ threshold }`, and in the callback, when the entry is intersecting, set `inView` true and then stop observing that element (D-06 — once per page load, no replay). Return a cleanup that disconnects the observer. Use only the IntersectionObserver API for scroll detection; registering a scroll event listener on `window` is forbidden by D-04 and would fail this plan's gate.

`src/components/motion/Reveal.tsx` — `"use client"`. Export `function Reveal({ children }: { children: React.ReactNode })`. Renders a single `<div>` carrying `ref` from the hook, `data-motion="reveal"`, `data-revealed={inView || undefined}` (so the attribute is absent, not `"false"`, when not revealed), and this exact className string: `translate-y-6 opacity-0 transition-[opacity,translate] duration-600 ease-[cubic-bezier(0.16,1,0.3,1)] data-[revealed]:translate-y-0 data-[revealed]:opacity-100 motion-reduce:translate-y-0 motion-reduce:opacity-100 motion-reduce:transition-none`. It takes no other props.

The transition property list is `opacity,translate` and NOT `opacity,transform` — this is load-bearing and differs from the draft snippet in 09-RESEARCH.md Pattern 2. Verified against the project's installed Tailwind (4.3.2): `translate-y-6` compiles to `--tw-translate-y: ...; translate: var(--tw-translate-x) var(--tw-translate-y);` — it sets the standalone `translate` CSS property, so a transition list naming only `transform` animates nothing and the reveal would snap instead of fade. Add a short source comment recording this, citing D-03/D-05.

`src/components/motion/RevealItem.tsx` — `"use client"`. Module constants `const STAGGER_MS = 50` and `const STAGGER_CAP = 8` (09-UI-SPEC Motion Contract section 3; the cap is what keeps a large catalog grid from a multi-second item-by-item march — CAT-03 scalability). Export `function RevealItem({ children, index, direction = "up", className })` with prop types `{ children: React.ReactNode; index: number; direction?: "up" | "start" | "end"; className?: string }`. Compute `const delay = Math.min(index, STAGGER_CAP) * STAGGER_MS`. Pick the initial offset class by direction: `"up"` -> `translate-y-4`; `"start"` -> `-translate-x-4 rtl:translate-x-4`; `"end"` -> `translate-x-4 rtl:-translate-x-4`. Render one `<div>` with `ref`, `data-motion="reveal-item"`, `data-direction={direction}`, `data-revealed={inView || undefined}`, `style={{ transitionDelay: `${delay}ms` }}`, and a className built with `cn(...)` from: the direction-specific offset class, the shared base string `opacity-0 transition-[opacity,translate] duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] data-[revealed]:translate-x-0 data-[revealed]:translate-y-0 data-[revealed]:opacity-100 motion-reduce:translate-x-0 motion-reduce:translate-y-0 motion-reduce:opacity-100 motion-reduce:transition-none`, and the caller-supplied `className` last (call sites in plan 09-03 pass `h-full` to preserve equal-height card rows).

The `rtl:` variant is what makes a directional slide mirror correctly in Arabic (D-10 / RESEARCH Pitfall 6): verified against the installed compiler, `rtl:translate-x-4` emits `:where(:dir(rtl), [dir="rtl"], [dir="rtl"] *)` and is ordered after the base offset utility, so it wins under `dir="rtl"`. `npm run lint:rtl` does not and cannot catch a sign mistake here (`translate-x-*` is not in its banned list) — the e2e assertion added in plan 09-03 is the real gate. Record that in a source comment.

None of the three files may import from, or be imported by, any existing block component — these are new leaves only. Do not add `"use client"` to any existing file (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint:rtl &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Files `src/components/motion/useInView.ts`, `src/components/motion/Reveal.tsx`, `src/components/motion/RevealItem.tsx` exist and each begins with a `"use client"` directive
    - `grep -rn 'useInView' src/components/motion/Reveal.tsx src/components/motion/RevealItem.tsx` shows both wrappers import the shared hook (no second observer implementation)
    - `cat src/components/motion/*.ts src/components/motion/*.tsx | grep -v '^\s*//' | grep -c 'addEventListener'` == 0 (D-04: IntersectionObserver only)
    - `grep -c 'unobserve' src/components/motion/useInView.ts` >= 1 (D-06: no replay)
    - `grep -c 'prefers-reduced-motion' src/components/motion/useInView.ts` == 1 (D-12)
    - `grep -c 'transition-\[opacity,translate\]' src/components/motion/Reveal.tsx` == 1 and same for `RevealItem.tsx` (NOT `transition-[opacity,transform]`, which would animate nothing under Tailwind v4)
    - `grep -c 'STAGGER_MS = 50' src/components/motion/RevealItem.tsx` == 1 and `grep -c 'STAGGER_CAP = 8' src/components/motion/RevealItem.tsx` == 1
    - `grep -c 'rtl:translate-x-4' src/components/motion/RevealItem.tsx` >= 1 and `grep -c 'rtl:-translate-x-4' src/components/motion/RevealItem.tsx` >= 1
    - `grep -rlc '"use client"' src/components/blocks/ src/components/products/ src/components/insights/ 2>/dev/null | wc -l` is unchanged from before this task (no existing component gained a client directive)
    - `npm run typecheck`, `npm run lint`, `npm run lint:rtl` all exit 0
  </acceptance_criteria>
  <done>The hook and both wrappers exist under `src/components/motion/`, compile clean, use IntersectionObserver only, unobserve after the first intersection, short-circuit under reduced motion, and carry the exact locked class strings from 09-UI-SPEC sections 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the motion class contract with a unit spec</name>
  <files>tests/unit/motion-reveal.spec.tsx</files>
  <read_first>
    - `tests/unit/seo-json-ld.spec.tsx` (the existing precedent for a `.tsx` unit spec rendering a component via `react-dom/server` in the node-environment `unit` vitest project)
    - `vitest.config.ts` (confirm the `unit` project's include globs already cover `tests/unit/**/*.spec.tsx` — they do, no config change needed)
    - `src/components/motion/Reveal.tsx` and `src/components/motion/RevealItem.tsx` as written in Task 2
  </read_first>
  <action>
Create `tests/unit/motion-reveal.spec.tsx` using `renderToStaticMarkup` from `react-dom/server`, matching the import and structure style of `tests/unit/seo-json-ld.spec.tsx`. No config change is needed and no new dependency is needed. `useEffect` does not run under `renderToStaticMarkup`, so this spec deterministically captures the pre-reveal (server-rendered) state, which is exactly the contract worth locking: what a user sees before any JS runs.

Cover these cases:
- `<Reveal>` markup contains `data-motion="reveal"`, `opacity-0`, `translate-y-6`, `transition-[opacity,translate]`, and does NOT contain `data-revealed` (attribute absent before the observer fires)
- `<Reveal>` markup contains the children passed to it (proving it wraps rather than replaces server-rendered output)
- `<RevealItem index={0}>` renders `transition-delay:0ms`
- `<RevealItem index={3}>` renders `transition-delay:150ms`
- `<RevealItem index={20}>` renders `transition-delay:400ms` — the STAGGER_CAP plateau, this is the large-catalog guard and the single most valuable assertion in the file
- `<RevealItem index={0} direction="start">` markup contains both `-translate-x-4` and `rtl:translate-x-4`
- `<RevealItem index={0} direction="end">` markup contains both `translate-x-4` and `rtl:-translate-x-4`
- `<RevealItem index={0} className="h-full">` markup contains `h-full` (proves the call-site className passthrough plan 09-03 depends on)

Note when asserting the `start` case that `translate-x-4` is a substring of `-translate-x-4`; assert on the full expected class fragments so the two directions cannot pass each other's assertion.
  </action>
  <verify>
    <automated>npm run test -- tests/unit/motion-reveal.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- tests/unit/motion-reveal.spec.tsx` exits 0 with at least 8 passing assertions and 0 failures
    - The spec asserts `transition-delay:400ms` for an index well above 8 (STAGGER_CAP plateau proven, not just implied)
    - The spec asserts the absence of `data-revealed` in the pre-reveal markup
    - `npm run test` (full vitest run, both projects) exits 0 — no pre-existing spec regressed
  </acceptance_criteria>
  <done>A fast unit spec pins the pre-reveal class contract, the stagger cap, and the RTL direction mapping; the whole vitest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | A new devDependency (`tw-animate-css`) enters the build via `npm install -D` |
| CMS/render -> DOM style attribute | `RevealItem` writes a computed value into an inline `style` attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering | `npm install -D tw-animate-css` | high | mitigate | Package Legitimacy Gate already executed in 09-RESEARCH.md via `gsd-tools query package-legitimacy check --ecosystem npm tw-animate-css` -> verdict **OK** (published 2025-09-24, 35.3M weekly downloads, repo `github.com/Wombosvideo/tw-animate-css`, not deprecated, `postinstall: null`). Verdict is OK — not `[ASSUMED]` and not `[SUS]` — so no blocking human legitimacy checkpoint is required. Task 1 re-asserts `npm view tw-animate-css scripts.postinstall` is empty at install time as a point-in-time re-check. Package ships CSS only: zero JS runtime footprint, no code execution path. |
| T-09-01 | Injection (V5 Input Validation) | `RevealItem` inline `style={{ transitionDelay }}` | low | accept | The only value reaching a style attribute is `Math.min(index, 8) * 50`, where `index` is an internal `Array.prototype.map` index — a number, never CMS-authored or user-supplied text. React escapes style values regardless. No validation control is warranted. |

## ASVS L1 Category Applicability (per 09-RESEARCH.md Security Domain)

| ASVS Category | Applies | Rationale |
|---------------|---------|-----------|
| V2 Authentication | No | Phase touches no auth surface |
| V3 Session Management | No | No session/cookie logic touched |
| V4 Access Control | No | No access-gated route touched |
| V5 Input Validation | No (marginal) | Covered by T-09-01 above — numeric internal index only |
| V6 Cryptography | No | Not applicable |

Conclusion: CSS/DOM-presentation phase with no applicable ASVS L1 threats beyond standard dependency supply-chain hygiene, which is already satisfied. No `high`-severity threat is left unmitigated, so the `block_on: high` gate passes.
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run lint:rtl` exits 0 ("RTL guard: no physical-direction classes under src/.")
- `npm run test` exits 0 (both vitest projects, including the new unit spec)
- `git diff --stat` touches only the seven files listed in `files_modified`
</verification>

<success_criteria>
- `src/components/motion/` exists with exactly three files, all client-only leaves, no existing component converted to a client component
- The reveal transition property list names `translate` (not `transform`), matching how Tailwind v4 actually compiles `translate-*` utilities
- Reduced-motion short-circuit and once-only unobserve are both present in `useInView.ts`
- `STAGGER_MS`/`STAGGER_CAP` produce a 400ms delay plateau, proven by an assertion, not by inspection
- `globals.css` carries the tw-animate-css import, the `--animate-float-in` token with nested keyframes, and the `@media (scripting: none)` no-JS guard
- Full vitest suite green
</success_criteria>

<output>
Create `.planning/phases/09-motion-and-micro-interactions-reveal-stagger-hover-reduced-m/09-01-SUMMARY.md` when done
</output>
