# Deferred Items — Phase 09

Out-of-scope discoveries logged during execution, not fixed per the executor scope boundary rule (only auto-fix issues directly caused by the current task's changes).

| Plan | Discovered during | File | Issue | Status |
|------|-------------------|------|-------|--------|
| 09-04 | Task 3 verify (`npx playwright test tests/e2e/contact-error-state.spec.ts`) | `src/components/blocks/ContactForm.tsx` | Same pre-existing issue already logged in `08-component-polish-pass-apply-amended-design-system-across-car/deferred-items.md` (row 11): all 3 cases in `contact-error-state.spec.ts` time out on `getByLabel("Company", { exact: true })` because the required-field asterisk markup makes the label's computed accessible name not equal the exact string "Company". Re-confirmed present here, unrelated to load (reproduced identically at system load average 37-43 and again at 16.95), and unrelated to 09-04's diff — `ContactForm.tsx` is not in 09-04's `files_modified` (`accordion.tsx`, `button.tsx`, `WhatsAppFloatingButton.tsx`, `ProductCard.tsx`, `InsightCard.tsx`, `tests/int/blocks-placeholder.spec.ts`, `tests/e2e/contact.spec.ts`) and no commit in this plan touches it or its labels | Not fixed — out of scope for 09-04, pre-existing test/markup mismatch predating this phase |
| 09-03 | Task 2 verify (`npx playwright test tests/e2e/insights.spec.ts`) | `src/app/(site)/[locale]/insights/[slug]/page.tsx` | `en: /insights/<slug> returns 200...` intermittently exceeds Playwright's default 30s test timeout on `page.goto` ("waiting until load"). Reproduced with a real Vercel Blob-backed `payload.db` copied into the worktree for verification; passes reliably (31.3s) with `--timeout=60000`. Root cause is real-network image fetch latency to Vercel Blob in dev mode, not app logic — confirmed by isolating the run (single worker, warm server) and by the fact this route (`[slug]/page.tsx`) is not in 09-03's `files_modified` and received zero edits from this plan | Not fixed — out of scope for 09-03, which only touches the `insights` list grid (`insights/page.tsx`), not the detail route |
| 09-05 | Task 1 gate suite (`npm run lint`) | `src/app/(site)/[locale]/insights/not-found.tsx`, `src/components/blocks/RenderBlocks.tsx` | Reconfirmed on the merged Wave 2 tree: same 5 pre-existing lint errors already logged in `08-component-polish-pass.../deferred-items.md` (4x `@next/next/no-html-link-for-pages` in `not-found.tsx`, 1x `@typescript-eslint/no-explicit-any` in `RenderBlocks.tsx`'s `BLOCK_MAP`) — count unchanged, no new errors introduced by any Phase 9 plan | Not fixed — out of scope for 09-05 (verification-only plan) |
| 09-05 | Task 1 gate suite (`npx playwright test tests/e2e/responsive-rtl.spec.ts`, isolated single-worker rerun) | `src/components/chrome/GlobalFooter.tsx` | Reconfirmed on the merged Wave 2 tree: `sm (ltr) — header/footer/hero render without overflow` fails deterministically — two footer legal-links overflow the 640px viewport (`scrollWidth` 723px vs `clientWidth` 640px, offending elements are `<a class="text-neutral-100 underline-offset-4 hover:text-accent-600 hover:underline">`). This is the exact same bug already root-caused and logged in `08-component-polish-pass.../deferred-items.md` row 10. Confirmed unrelated to Phase 9 via `git log 70a362e..HEAD -- src/components/chrome/GlobalFooter.tsx` (zero commits since the Phase 9 baseline; last touching commit `e3baaaa` predates Phase 8) | Not fixed — out of scope for 09-05, unrelated file not in any Phase 9 plan's `files_modified` |
| 09-05 | Task 1 gate suite (`npx playwright test tests/e2e/nav-links.spec.ts`, isolated single-worker rerun on a warm dev server) | `tests/e2e/nav-links.spec.ts` | Reconfirmed on the merged Wave 2 tree, deterministically (not contention — reproduced with `--workers=1` against an already-warm server): the Primary/Footer nav-link cases each perform 7-9 sequential `page.goto` calls inside one 30s test budget and intermittently exceed it (different route times out each run — `/certifications`, `/export`, `/ar/company`, `/contact`, `/ar/manufacturing` observed across three separate runs). Same pre-existing test-design fragility already logged in `08-component-polish-pass.../deferred-items.md` row 12. `tests/e2e/nav-links.spec.ts` last touched by commit `7482727` (Phase 2), zero Phase 9 commits | Not fixed — out of scope for 09-05, pre-existing test-design fragility predating this phase |
| 09-05 | Task 1 gate suite (`npx playwright test tests/e2e/contact-error-state.spec.ts`, both `en`/`ar` projects) | `src/components/blocks/ContactForm.tsx` | Reconfirmed on the merged Wave 2 tree: all 3 cases time out on `getByLabel("Company", { exact: true })`, same root cause already logged by 09-04 above and by `08-component-polish-pass.../deferred-items.md` row 11 — `ContactForm.tsx` is not in any Phase 9 plan's `files_modified` | Not fixed — out of scope for 09-05, pre-existing test/markup mismatch predating this phase |
| 09-05 | Task 1 environment setup (`npm run test`/`npx playwright test`) | `payload.db` (gitignored, dev-only, worktree + main checkout) | This worktree's copy of `payload.db` (copied from the main checkout per established precedent) had 0 rows in `pages_blocks_export_process` — the same stale-seed issue 09-03 already diagnosed and fixed in its own worktree, but the *main checkout's* `payload.db` had independently drifted stale again (0 rows) since then, so the copy inherited the gap. Confirmed via direct `sqlite3` inspection of both the worktree and main-checkout copies. Fixed by deleting the worktree's `payload.db` and re-running `npx tsx scripts/seed-pages.ts` (+ `seed-products.ts`/`seed-insights.ts` for the catalog/insights e2e specs), which recreated it from current `seed-content.ts` (5 export-process steps now present). Data-only fix, gitignored, not committed | Fixed (Rule 3 - blocking, dev-environment/data-only, no source file changed) |
