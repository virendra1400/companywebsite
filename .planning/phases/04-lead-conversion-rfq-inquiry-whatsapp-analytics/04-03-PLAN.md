---
phase: 04-lead-conversion-rfq-inquiry-whatsapp-analytics
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/components/blocks/ContactForm.tsx
  - src/i18n/messages/en.json
  - src/i18n/messages/ar.json
  - src/i18n/messages/fr.json
  - src/i18n/messages/ru.json
  - .env.example
  - tests/e2e/contact.spec.ts
  - tests/e2e/contact-error-state.spec.ts
autonomous: true
requirements: [LEAD-01, LEAD-03, LEAD-04, LEAD-07]

user_setup:
  - service: cloudflare-turnstile
    why: "LEAD-03 client widget renders and produces the token the Server Action verifies"
    env_vars:
      - name: NEXT_PUBLIC_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard -> Turnstile -> your widget -> Site Key (public by design — the ONLY Turnstile key that carries NEXT_PUBLIC_)"

must_haves:
  truths:
    - "Fresh page load renders all form fields blank with no success/error banner until a real submit attempt occurs. [UI-SPEC empty — covered]"
    - "On submit the button shows an inline spinner and 'Sending...' label and is disabled, and all fields switch to readOnly (not disabled) until the Server Action resolves. [UI-SPEC loading — covered]"
    - "A server/network failure renders a destructive-styled banner above the submit button, form field values are retained (no data loss), and submit re-enables for retry. [UI-SPEC error — covered / LEAD-04]"
    - "A legitimate user tripping the rate limit sees the distinct 'submitting too quickly' copy, not the generic failure copy. [UI-SPEC error/rate-limit — covered / LEAD-03]"
    - "A honeypot-triggered submission renders the SAME success state as a legitimate submission (detection never revealed). [UI-SPEC honeypot — covered / LEAD-03]"
    - "When email and phone are both empty a single combined error string is anchored under the Phone field (not two conflicting per-field required errors). [UI-SPEC partial — covered / D-01]"
    - "The Message textarea wraps naturally and is bounded by the existing max(2000) schema limit. [UI-SPEC long-text message — covered]"
    - "trackEvent fires inquiry_submit only after a success result (the rfq_submit branch is wired here and exercised by the RFQ plan). [LEAD-07/D-06]"
    - "The submit button is disabled during the in-flight request so a single form instance cannot double-submit from one interaction. [LEAD-01 concurrency — covered client-side]"
    - "The honeypot field is always rendered, always empty for humans (aria-hidden, tabIndex -1, autoComplete off, off-screen positioned), bound to a schema key distinct from the real company field. [LEAD-03]"
    - statement: "If the Turnstile script fails to load or times out, the submit button stays disabled and the form-level error banner explains the failure with a WhatsApp/email fallback rather than leaving a silently-broken submit button."
      verification: backstop
  prohibitions:
    - statement: "Visitor PII (name, email, phone, company, message) must never be passed as analytics event parameters; trackEvent parameters carry only non-PII values (event name, product slug)."
      status: unverified
      flagged: true
  artifacts:
    - "src/components/blocks/ContactForm.tsx (wired to submitContactForm: email/phone fields, honeypot, Turnstile widget, loading/success/error/rate-limit states, inquiry_submit)"
    - tests/e2e/contact-error-state.spec.ts
    - ".env.example (NEXT_PUBLIC_TURNSTILE_SITE_KEY + server env var documentation)"
  key_links:
    - "ContactForm.onSubmit awaits submitContactForm and only calls trackEvent on a success result."
    - "The Turnstile widget onSuccess writes turnstileToken into the form value consumed by the Server Action's siteverify step; without a token the submit stays disabled."
---

<objective>
Wire the Phase 2 client-only `ContactForm` stub to the real `submitContactForm` Server Action (04-02) for the general inquiry happy path, and deliver the full interactive state machine the form has never had: email/phone reachability fields (D-01), the honeypot field + Turnstile widget (LEAD-03 client half), and loading / success / error / rate-limit banners (UI-SPEC state contract). Fire `inquiry_submit` on success (LEAD-07).

After this plan a real user can fill and submit a general inquiry at `/contact` and it is delivered (via the 04-02 pipeline), with honest loading/error/success feedback and spam defense active. The RFQ-mode fields are added in 04-04.

Purpose: LEAD-01 (user-visible inquiry), LEAD-03 (client honeypot + widget), LEAD-04 (error surfacing), LEAD-07 (inquiry_submit).
Output: modified `ContactForm.tsx`, new catalog copy, error-state e2e, updated stale stub assertions.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-UI-SPEC.md
@.planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-RESEARCH.md
@.planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-PATTERNS.md
@src/components/blocks/ContactForm.tsx
</context>

## Artifacts this phase produces (this plan's slice)

- Modified `src/components/blocks/ContactForm.tsx` — email/phone fields, honeypot field, Turnstile widget, onSubmit → Server Action, loading/success/error/rate-limit states, `inquiry_submit`.
- New catalog keys: `emailLabel`, `phoneLabel`, `loadingSubmit`, `errorBanner`, `rateLimitBanner`, and the updated `successMessage` (inquiry) per the Copywriting Contract.
- New `.env.example` documenting `NEXT_PUBLIC_TURNSTILE_SITE_KEY` (client) and the server env vars from 04-02.
- New `tests/e2e/contact-error-state.spec.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Wire ContactForm inquiry path — fields, spam UI, state machine, inquiry_submit</name>
  <files>src/components/blocks/ContactForm.tsx, src/i18n/messages/en.json, src/i18n/messages/ar.json, src/i18n/messages/fr.json, src/i18n/messages/ru.json, .env.example</files>
  <read_first>
    - src/components/blocks/ContactForm.tsx (existing FormField pattern, honeypot insertion-point comment, success-state panel)
    - src/lib/contact-action.ts (submitContactForm signature + SubmitResult union — from 04-02)
    - src/lib/analytics.ts (trackEvent — from 04-01)
    - .planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-UI-SPEC.md (Component Inventory §3 field table + loading/success/error cycle + Copywriting Contract)
    - .planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-RESEARCH.md (client wiring excerpt; @marsidev/react-turnstile usage)
  </read_first>
  <action>
    In src/components/blocks/ContactForm.tsx add imports for `submitContactForm` (@/lib/contact-action), `trackEvent` (@/lib/analytics), `useSearchParams` (next/navigation), `Turnstile` (@marsidev/react-turnstile), and `Loader2` (lucide-react). Extend `defaultValues` to include `email: ""`, `phone: ""`, `companyWebsite: ""`, `turnstileToken: ""`. Replace the single `submitted` boolean with a `status` state of `"idle" | "loading" | "success" | "error" | "rate-limited"`. Add Email + Phone FormFields as an adjacent pair (`grid grid-cols-1 sm:grid-cols-2 gap-md`) following the existing FormField → FormItem → FormLabel → FormControl(Input) → FormMessage structure, using `t("emailLabel")`/`t("phoneLabel")`; the combined email-or-phone error surfaces under Phone (schema `path:["phone"]` from 04-02). At the marked spam-defense insertion point add: (a) the honeypot field — an Input registered as `companyWebsite` with label "Company Website", `aria-hidden`, `tabIndex={-1}`, `autoComplete="off"`, off-screen class `absolute -left-[9999px] top-auto h-px w-px overflow-hidden`; and (b) the `<Turnstile siteKey={process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY!} onSuccess={(token)=>form.setValue("turnstileToken", token)} onError=/onExpire=(reset token to "")>` widget in normal `gap-md` rhythm above the submit button. Keep the submit `disabled` until `turnstileToken` is non-empty; on Turnstile error/timeout keep it disabled and set `status="error"` so the error banner (with WhatsApp/email fallback copy) explains it (backstop). Replace `onSubmit`: set `status="loading"`, make fields readOnly during flight, `await submitContactForm(values)`; on `success` set `status="success"` and call `trackEvent(values.product ? "rfq_submit" : "inquiry_submit", { product: values.product ?? "" })` (only the product slug — no PII); on `message === "rate-limited"` set `status="rate-limited"`; else `status="error"`. Submit button shows `Loader2` `animate-spin` + `t("loadingSubmit")` while loading. Success replaces the form with the existing `role="status"` panel (inquiry success copy); error/rate-limit render a destructive banner (`bg-destructive/10 text-destructive border border-destructive`) ABOVE the submit button with the form retained. Add catalog keys to all four locales per the Copywriting Contract: `emailLabel`, `phoneLabel`, `loadingSubmit` ("Sending..."), `errorBanner`, `rateLimitBanner`, and update `successMessage` to the inquiry success copy (EN source of truth; ar/fr/ru professional equivalents). Create/update .env.example documenting `NEXT_PUBLIC_TURNSTILE_SITE_KEY` plus the 04-02 server vars.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/blocks/ContactForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tabIndex" src/components/blocks/ContactForm.tsx` is 1 or more and the honeypot Input is aria-hidden.
    - onSubmit calls trackEvent only inside the success branch and passes only `{ product }` (no name/email/phone/company/message).
    - Submit button is disabled while `turnstileToken` is empty.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Inquiry form submits through the Server Action with honeypot + Turnstile, full loading/success/error/rate-limit UI, and inquiry_submit on success.</done>
</task>

<task type="auto">
  <name>Task 2: e2e — error/rate-limit/Turnstile-fail states + repair stale stub assertions</name>
  <files>tests/e2e/contact-error-state.spec.ts, tests/e2e/contact.spec.ts</files>
  <read_first>
    - tests/e2e/contact.spec.ts (empty-submit "no network" assertion + submit-button locator)
    - .planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-RESEARCH.md (Wave 0 Gaps; Pitfall 6 — Playwright covers client-observable states only)
    - .planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-VALIDATION.md (Per-Task Verification Map — LEAD-04 error e2e row)
  </read_first>
  <action>
    Create tests/e2e/contact-error-state.spec.ts covering CLIENT-OBSERVABLE states only (Pitfall 6): (1) intercept the Server Action's browser-visible POST with `page.route(...)` and fulfil it as a failure, submit a valid form (stub the Turnstile token via the widget's test mode or by setting the hidden field), and assert the destructive error banner is visible, field values are retained, and the submit button re-enables; (2) a rate-limit case where the intercepted response maps to the rate-limited result and assert the distinct "submitting too quickly" copy; (3) a backstop case where the Cloudflare Turnstile challenge script is aborted via `page.route` and assert the submit button stays disabled and the error banner shows the WhatsApp/email fallback. Update tests/e2e/contact.spec.ts: confirm the empty-submit test still asserts client-side zod validation short-circuits with NO network POST (validation blocks before the Server Action) — adjust the assertion/comment to reflect the wired-but-still-client-validated reality rather than the D-07 "no handler exists" wording, and keep the submit-button locator matching "Send Inquiry".
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/contact-error-state.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - contact-error-state.spec.ts asserts the destructive banner AND that inputs retain their typed values after a failed submit.
    - The Turnstile-abort case asserts the submit button remains disabled.
    - `npx playwright test tests/e2e/contact.spec.ts` passes with the updated empty-submit assertion.
  </acceptance_criteria>
  <done>Error, rate-limit, and Turnstile-load-failure states are e2e-verified; the stale D-07 stub assertion is repaired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot → public form | Automated submissions attempt to bypass the client widget |
| client bundle | Only the public Turnstile SITE key may ship to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service / Repudiation | ContactForm honeypot + Turnstile widget | high | mitigate | Always-rendered off-screen honeypot bound to a decoy key + Turnstile widget producing a token the server verifies; submit disabled until token present |
| T-04-04 | Information Disclosure | client env exposure | high | mitigate | Only `NEXT_PUBLIC_TURNSTILE_SITE_KEY` (public site key) is referenced client-side; the secret key stays server-only in 04-02 |
| T-04-06 | Information Disclosure | trackEvent params | medium | mitigate | Success-path trackEvent passes only the product slug; never name/email/phone/company/message |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx eslint src/components/blocks/ContactForm.tsx` clean.
- `npx playwright test tests/e2e/contact.spec.ts tests/e2e/contact-error-state.spec.ts` green.
</verification>

<success_criteria>
- General inquiry submits end-to-end through the Server Action with honeypot + Turnstile active.
- Loading, success, error, and rate-limit states all render per the UI-SPEC contract; failed submits retain input.
- inquiry_submit fires on success with a non-PII payload.
</success_criteria>

<output>
Create `.planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-03-SUMMARY.md` when done.
</output>
