---
phase: 04-lead-conversion-rfq-inquiry-whatsapp-analytics
plan: 05
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-04]
files_modified:
  - package.json
  - src/app/(site)/[locale]/layout.tsx
  - src/components/chrome/ConsentBanner.tsx
  - src/i18n/messages/en.json
  - src/i18n/messages/ar.json
  - src/i18n/messages/fr.json
  - src/i18n/messages/ru.json
  - .env.example
autonomous: false

requirements: [ANALY-01, LEAD-07]

user_setup:
  - service: analytics-vendor
    why: "ANALY-01 — the chosen vendor's script needs a container id / domain to actually collect data"
    env_vars:
      - name: NEXT_PUBLIC_GTM_ID
        source: "Google Tag Manager -> Container -> GTM-XXXX (only if GA4+GTM is chosen at the decision checkpoint)"
      - name: NEXT_PUBLIC_PLAUSIBLE_DOMAIN
        source: "Plausible -> Site settings -> domain (only if Plausible is chosen)"

must_haves:
  truths:
    - "A web analytics vendor script (GA4+GTM or Plausible, per the recorded decision) is mounted in the root site layout and receives the rfq_submit / inquiry_submit / whatsapp_click conversion events via trackEvent. [ANALY-01/LEAD-07]"
    - "The consent banner renders ONLY when GA4+GTM is the chosen vendor; cookieless Plausible ships no banner and no dead banner code. [UI-SPEC Consent Banner — conditional]"
    - "The vendor script guards against a missing container-id / domain env var so local dev and CI render without an analytics key. [ANALY-01 graceful]"
    - "The trackEvent dispatch branch (dataLayer for GTM, window.plausible for Plausible) matches the vendor actually mounted in the layout."
    - "The consent banner is a non-blocking fixed bottom bar (no modal overlay), persists the Accept/Decline choice so it does not re-appear every page, and contains no link to a non-existent /privacy page. [UI-SPEC Consent Banner]"
  prohibitions:
    - statement: "Analytics must never capture visitor PII (name, email, phone, company, message) as event parameters or custom dimensions; only non-PII conversion signals (event name, product slug) flow to the vendor."
      status: unverified
      flagged: true
  artifacts:
    - "src/components/chrome/ConsentBanner.tsx (conditional, GA4+GTM path only)"
    - "src/app/(site)/[locale]/layout.tsx vendor script mount"
    - "analytics env vars (NEXT_PUBLIC_GTM_ID or NEXT_PUBLIC_PLAUSIBLE_DOMAIN)"
  key_links:
    - "trackEvent (src/lib/analytics.ts, 04-01) dispatch branch must match the vendor mounted here (dataLayer <-> GTM, window.plausible <-> Plausible)."
    - "The vendor decision at the checkpoint determines whether ConsentBanner and @next/third-parties are added at all."
---

<objective>
Make ANALY-01 real: choose the analytics vendor (a genuinely deferred business decision per STACK.md / RESEARCH Open Question #1 / Pitfall 7), then mount that vendor's script in the root site layout so the vendor-agnostic `trackEvent()` events built in 04-01/03/04 actually reach a real analytics backend. If GA4+GTM is chosen, also ship the conditional consent banner (UI-SPEC); if Plausible is chosen (cookieless), ship no banner and no dead code.

The `trackEvent()` wrapper already works vendor-agnostically, so no conversion-tracking work is wasted regardless of the decision — this plan only wires the vendor-specific script/banner once the choice is confirmed.

Purpose: ANALY-01 (analytics installed) + the vendor-mount half of LEAD-07 (events reach a real backend).
Output: decision record, layout vendor mount, conditional `ConsentBanner.tsx`, analytics env vars.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-UI-SPEC.md
@.planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-RESEARCH.md
@src/app/(site)/[locale]/layout.tsx
@src/lib/analytics.ts
</context>

## Artifacts this phase produces (this plan's slice)

- Recorded analytics vendor decision (GA4+GTM vs Plausible).
- Vendor script mount in `src/app/(site)/[locale]/layout.tsx` (guarded against a missing env key).
- `src/components/chrome/ConsentBanner.tsx` — GA4+GTM path only.
- `@next/third-parties` dependency — GA4+GTM path only.
- Analytics env var (`NEXT_PUBLIC_GTM_ID` or `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`) documented in `.env.example`.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the analytics vendor (GA4+GTM vs Plausible)</name>
  <action>Halt and present the decision below to the user. Do not mount any vendor script or build the consent banner until the user selects an option. Record the chosen option id in the plan SUMMARY so Task 2 (and any later phase) knows which vendor path was taken. This is a blocking, non-auto-approvable decision because it has GDPR/consent and reporting-stakeholder implications (RESEARCH Pitfall 7).</action>
  <decision>Which web analytics vendor mounts for launch — GA4 + Google Tag Manager, or Plausible?</decision>
  <context>
    STACK.md frames GA4+GTM as the default expectation for a B2B team (Google Ads attribution, segmentation depth) with Plausible as the cookieless / no-consent-banner alternative; CONTEXT.md and RESEARCH Open Question #1 confirm this is still a genuinely deferred decision with legal/GDPR and reporting-stakeholder implications, not a purely technical one. The trackEvent wrapper (04-01) already dispatches to whichever vendor is mounted, so this choice only affects the vendor script + whether a consent banner ships.
  </context>
  <options>
    <option id="ga4-gtm">
      <name>GA4 + Google Tag Manager</name>
      <pros>Google Ads attribution, richer segmentation, familiar to most B2B marketing stakeholders; uses @next/third-parties GoogleTagManager + sendGTMEvent</pros>
      <cons>Sets cookies -> requires the consent banner + GTM consent state; more moving parts</cons>
    </option>
    <option id="plausible">
      <name>Plausible</name>
      <pros>Cookieless -> no consent banner needed, simpler, privacy-friendly, lighter script</pros>
      <cons>No Google Ads attribution, shallower segmentation than GA4</cons>
    </option>
  </options>
  <resume-signal>Select: ga4-gtm or plausible</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Mount the chosen vendor script + conditional consent banner</name>
  <files>package.json, src/app/(site)/[locale]/layout.tsx, src/components/chrome/ConsentBanner.tsx, src/i18n/messages/en.json, src/i18n/messages/ar.json, src/i18n/messages/fr.json, src/i18n/messages/ru.json, .env.example</files>
  <read_first>
    - src/app/(site)/[locale]/layout.tsx (root site layout — where the script mounts, inside NextIntlClientProvider)
    - src/lib/analytics.ts (trackEvent dispatch branches — must match the mounted vendor)
    - .planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-UI-SPEC.md (Consent Banner section — bg-primary-900, Accept/Decline, no modal, no /privacy link, persist choice)
    - .planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-RESEARCH.md (Standard Stack @next/third-parties; Pitfall 7 consent-banner conditionality)
  </read_first>
  <action>
    Implement per the checkpoint decision. IF ga4-gtm: install `@next/third-parties`; in the root site layout mount `GoogleTagManager` with `gtmId={process.env.NEXT_PUBLIC_GTM_ID}`, guarding so nothing renders when the id is unset (dev/CI safe); create src/components/chrome/ConsentBanner.tsx as a `"use client"` non-blocking fixed bottom bar (`bg-primary-900 text-white`, full width, `px-md py-md md:px-lg`) with body copy (Label token) and two Buttons — Accept (filled primary) sets GTM consent state to granted, Decline (outline) sets it to denied and suppresses the GA4 tag — both persisting the choice in localStorage so the banner does not re-appear, with NO modal overlay and NO link to a /privacy page (that route does not exist); render `ConsentBanner` in the layout; add the consent catalog keys (`consentBody`, `consentAccept`, `consentDecline`) to all four locales. IF plausible: add the Plausible script tag in the layout with `data-domain={process.env.NEXT_PUBLIC_PLAUSIBLE_DOMAIN}` (guarded when unset), do NOT install @next/third-parties, do NOT create ConsentBanner, do NOT add consent copy (no dead code). In BOTH cases confirm the trackEvent dispatch branch in src/lib/analytics.ts matches the mounted vendor (dataLayer.push for GTM, window.plausible for Plausible) and update .env.example with the chosen vendor's env var.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/app/(site)/[locale]/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The layout renders without error when the vendor env var is unset (guarded — no crash in dev/CI).
    - GA4+GTM path: ConsentBanner exists, is non-blocking, has no /privacy link, and persists the choice; Plausible path: no ConsentBanner file and no consent catalog keys exist.
    - trackEvent's dispatch branch matches the mounted vendor.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The chosen analytics vendor is mounted site-wide, conversion events reach it, and the consent banner ships iff GA4+GTM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → analytics vendor | Conversion events + (for GTM) cookies flow to a third party |
| visitor consent | GDPR consent state governs whether GA4 cookies are set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Information Disclosure | vendor event stream | medium | mitigate | Only non-PII conversion signals (event name, product slug) are emitted by trackEvent; no form-field PII is sent as params/dimensions |
| T-04-08 | Repudiation / Compliance | GA4 cookie consent | medium | mitigate | GA4+GTM path ships a consent banner gating cookies (Decline suppresses the tag); Plausible path is cookieless so no consent gate is required. (Generic GDPR consent-banner requirement — canon; implemented, not re-derived as a project-specific control) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; eslint clean on the layout.
- Manual smoke: the site renders in all locales with the vendor env var unset (guarded) and with it set; consent banner appears only under the GA4+GTM path.
</verification>

<success_criteria>
- Analytics vendor decision recorded and its script mounted site-wide.
- Conversion events (rfq_submit / inquiry_submit / whatsapp_click) reach the mounted vendor.
- Consent banner present iff GA4+GTM; no dead code on the Plausible path; dev/CI render without keys.
</success_criteria>

<output>
Create `.planning/phases/04-lead-conversion-rfq-inquiry-whatsapp-analytics/04-05-SUMMARY.md` when done.
</output>
