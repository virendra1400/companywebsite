---
phase: 07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/lib/seed-content.ts
  - scripts/seed-pages.ts
requirements: [D-03, D-04, D-05]
autonomous: true
must_haves:
  truths:
    - "The homepage `home` Pages document composes the 11-block narrative in this exact order: Hero(full) → TrustBar → FeatureGrid(icon, Why Choose Us) → MediaGallery(Manufacturing Excellence) → StatsBand(capacity/QC/cold-chain) → CertStrip(strip) → StatsBand(15+/40+/500+) → ExportProcess → ExportMap(compact) → Testimonials → CTABand (D-03)."
    - "TrustBar is seeded with 5 logo-less region descriptors (Gulf & Middle East Importers, European Distributors, North American Buyers, African Trade Partners, Southeast Asian Wholesalers) — no fabricated named clients (Pitfall 5); ExportProcess with the 5 inquiry→delivery steps; Testimonials with 3 role+buyer-category+country quotes flagged in-code as placeholder (D-05)."
    - "Manufacturing Excellence is a condensed on-page composition reusing the existing MediaGallery (Processing Floor / Quality Control Lab / Cold Storage captions — Packing & Dispatch dropped) + StatsBand (500+ Metric Tons Monthly Capacity / 3 In-House QC Checkpoints / 24/7 Cold-Chain Monitoring) with verbatim /manufacturing copy — no new block, no schema change (D-03)."
    - "scripts/seed-pages.ts injectFacilityPhotos() attaches the existing FACILITY_PHOTOS placeholder images to the home page's new MediaGallery items (keyed by caption), not only the manufacturing page — reusing upsertMediaByAlt as-is, no duplicated upload logic (D-05)."
    - "The existing tests/e2e/homepage.spec.ts monotonic order (Hero < FeatureGrid < CertStrip < StatsBand'Years Exporting' < ExportMap < CTABand) and tests/e2e/rtl-arabic.spec.ts sample-count assertion both still pass; HOME_EN_SEED heroHeadline/heroSubhead stay derived from the unchanged homeHero so fallback-notice.spec.ts stays green."
    - "All new homepage copy lives in seed-content.ts (CMS-authored block content), not en.json — no new i18n key; no color/body-tier/globals.css change (D-04, D-05)."
  artifacts:
    - path: "src/lib/seed-content.ts"
      provides: "trustBar/exportProcess/testimonials seed helpers + rewritten home layout composing the 11-block narrative with reused Manufacturing MediaGallery+StatsBand"
      contains: "Trusted by Importers Across the Globe"
    - path: "scripts/seed-pages.ts"
      provides: "injectFacilityPhotos generalized to attach facility photos to every page's mediaGallery block (home + manufacturing), keyed by caption"
      contains: "mediaGallery"
  key_links:
    - "The home MediaGallery item captions ('Processing Floor'/'Quality Control Lab'/'Cold Storage') MUST match FACILITY_PHOTOS keys in seed-pages.ts, or injectFacilityPhotos leaves image=0 and MediaGalleryBlock renders nothing for those cells."
    - "The new manufacturing StatsBand on home carries capacity/QC labels — distinct from the existing 'Years Exporting' StatsBand — so homepage.spec.ts's getByText('Years Exporting').first() still resolves to the later (row 7) band and the order assertions stay monotonic."
    - "seed-pages.ts is skip-by-slug idempotent: the existing `home` doc must be deleted (or payload.db reset) before re-seeding, or the new layout is never written."
---

<objective>
Compose the homepage `home` Pages document into the full 11-block trust narrative per 07-UI-SPEC.md Part 3 (D-03) and seed it with realistic English placeholder content (D-05), then generalize the facility-photo seed helper so the homepage's reused Manufacturing MediaGallery gets its placeholder images. This is page-content/seed work on the existing block library — no schema change (that shipped in 07-02), no new i18n keys.

Purpose: This is the payoff of the phase — the homepage actually tells the trust→process→social-proof story an international buyer needs, with every section CMS-editable. Manufacturing Excellence is delivered as a condensed on-page composition (MediaGallery + StatsBand with verbatim /manufacturing copy) rather than a bare teaser link, per CONTEXT.md discretion.

Output: A rewritten `home` layout in `src/lib/seed-content.ts` (with 3 new seed helpers) and a generalized `injectFacilityPhotos()` in `scripts/seed-pages.ts`.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-CONTEXT.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-UI-SPEC.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-02-SUMMARY.md
@src/lib/seed-content.ts
@scripts/seed-pages.ts
@tests/e2e/homepage.spec.ts
@tests/e2e/rtl-arabic.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 3 seed helpers and rewrite the home layout to the 11-block narrative</name>
  <files>src/lib/seed-content.ts</files>
  <read_first>
    - src/lib/seed-content.ts (the existing block helper functions `featureGrid`/`statsBand`/`mediaGallery`/`certStrip`/`exportMap`/`ctaBand`, the `homeHero` const, the current `home` entry's `layout` array and its head comment, the manufacturing page's MediaGallery captions + StatsBand figures to reuse, and the `HOME_EN_SEED` export at the bottom)
    - .planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-UI-SPEC.md (Part 2a/2b/2c seed content and Part 3 composition table — the exact copy strings and block order)
    - scripts/seed-pages.ts (the FACILITY_PHOTOS caption keys the home MediaGallery must match)
    - tests/e2e/homepage.spec.ts (the order-anchor texts the new composition must preserve: "Quality", "Years Exporting", the /certifications link, the countries role=img, "Ready to Source With Confidence?")
  </read_first>
  <action>
    In `src/lib/seed-content.ts`, add three seed helper functions next to the existing ones (each returning the `blockType: "<slug>" as const` object shape that matches the 07-02 configs):

    - `trustBar(sectionTitle: string, items: { name: string }[])` → `{ blockType: "trustBar" as const, sectionTitle, items }`.
    - `exportProcess(sectionTitle: string, steps: { title: string; body: string }[])` → `{ blockType: "exportProcess" as const, sectionTitle, steps }`.
    - `testimonials(sectionTitle: string, items: { quote: string; name: string; company?: string; country?: string }[])` → `{ blockType: "testimonials" as const, sectionTitle, items }`. Add a comment flagging the seeded quotes as placeholder pending real client testimonials (Pitfall 5 — role + buyer-category + real served country, never a fabricated named endorsement).

    Then replace the `home` entry's `layout` array (and update its head comment to describe the new order) so it composes exactly these 11 blocks in order:
    1. `homeHero` (unchanged).
    2. `trustBar("Trusted by Importers Across the Globe", [{name:"Gulf & Middle East Importers"},{name:"European Distributors"},{name:"North American Buyers"},{name:"African Trade Partners"},{name:"Southeast Asian Wholesalers"}])`.
    3. The existing `featureGrid("icon", [ ...the current 4 Quality/Reliability/Compliance/Global Reach value props... ])` — keep its 4 items verbatim.
    4. `mediaGallery([{caption:"Processing Floor"},{caption:"Quality Control Lab"},{caption:"Cold Storage"}], "Manufacturing Excellence")` — 3 captions only (drop "Packing & Dispatch" for the condensed homepage teaser); captions MUST match FACILITY_PHOTOS keys exactly.
    5. `statsBand([{value:"500+",label:"Metric Tons Monthly Capacity"},{value:"3",label:"In-House QC Checkpoints"},{value:"24/7",label:"Cold-Chain Monitoring"}])` — no sectionTitle (reads as continuation of row 4); copy reused verbatim from the /manufacturing StatsBand.
    6. `certStrip("strip")` (unchanged).
    7. `statsBand([{value:"15+",label:"Years Exporting"},{value:"40+",label:"Countries Served"},{value:"500+",label:"Container Shipments"}])` — the existing homepage StatsBand, unchanged, no sectionTitle (its "Years Exporting" label is what homepage.spec.ts anchors on, and it stays after CertStrip).
    8. `exportProcess("How an Order Moves From Inquiry to Delivery", [5 steps])` with the exact step {title, body} pairs from 07-UI-SPEC.md Part 2b: Inquiry, Quote, Production & QC, Export Documentation, Delivery (use the UI-SPEC body strings verbatim).
    9. `exportMap("compact", [{ value: `${SERVED_COUNTRY_CODES.length}+`, label: "Countries We Currently Export To" }])` (unchanged from the current home).
    10. `testimonials("What Our Buyers Say", [3 items])` with the exact quote/name/company/country from 07-UI-SPEC.md Part 2c (Procurement Lead / GCC-Based Food Importer / United Arab Emirates; Import Operations Manager / European Distribution Partner / Germany; Category Buyer / Wholesale Import Group / Singapore).
    11. `ctaBand("Ready to Source With Confidence?")` (unchanged).

    Leave the `HOME_EN_SEED` export deriving `heroHeadline`/`heroSubhead` from `homeHero` exactly as-is (homeHero is unchanged, so fallback-notice.spec.ts stays green). Do NOT add any en.json i18n key — all copy is CMS block content. Do NOT change any other page's layout.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the new home layout type-checks against the 07-02-regenerated payload-types.ts).
    - `grep -F 'Trusted by Importers Across the Globe' src/lib/seed-content.ts`, `grep -F 'How an Order Moves From Inquiry to Delivery' ...`, `grep -F 'What Our Buyers Say' ...` each return a match.
    - `grep -F 'Manufacturing Excellence' src/lib/seed-content.ts` returns a match, and the home MediaGallery captions "Processing Floor"/"Quality Control Lab"/"Cold Storage" are all present in the home layout (they now appear for both home and manufacturing).
    - `grep -Fc 'blockType: "trustBar"' src/lib/seed-content.ts`, `... "exportProcess"`, `... "testimonials"` — the helpers exist (each returns >= 1); and the home layout invokes `trustBar(`, `exportProcess(`, `testimonials(` once each.
    - The 5 TrustBar items are region/segment descriptors, not invented company names (verify by reading — Pitfall 5, D-03).
    - `HOME_EN_SEED` still references `homeHero.headline`/`homeHero.subhead` (unchanged) — grep confirms the export block is intact.
    - No new i18n key added to any file under src/i18n/messages/.
  </acceptance_criteria>
  <done>The home layout composes the 11-block narrative in the UI-SPEC Part 3 order with realistic non-fabricated placeholder copy; the 3 seed helpers exist; types check; HOME_EN_SEED and other pages are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize injectFacilityPhotos to the home MediaGallery, re-seed, and run the homepage/RTL e2e regression</name>
  <files>scripts/seed-pages.ts</files>
  <read_first>
    - scripts/seed-pages.ts (the current `injectFacilityPhotos()` — it finds only `PAGES_EN_SEED.find((p) => p.slug === "manufacturing")`; the `FACILITY_PHOTOS` caption→path map; and `upsertMediaByAlt`)
    - src/lib/seed-content.ts (the home layout's new MediaGallery captions — confirm they match FACILITY_PHOTOS keys)
    - tests/e2e/homepage.spec.ts and tests/e2e/rtl-arabic.spec.ts (the assertions this re-seed must keep green)
  </read_first>
  <action>
    Generalize `injectFacilityPhotos()` in `scripts/seed-pages.ts` so it attaches facility placeholder photos to EVERY page's `mediaGallery` block(s), not just `manufacturing`. Iterate over all `PAGES_EN_SEED` pages; for each, find its `mediaGallery` block(s) in `layout`; for each item, look up `FACILITY_PHOTOS[item.caption]` and, when present, `upsertMediaByAlt(`${item.caption} placeholder photo`, assetPath)` and assign `item.image = media.id`. Reuse `FACILITY_PHOTOS` and `upsertMediaByAlt` exactly as they are — do not duplicate the upload logic and do not add new assets (the home gallery reuses the same 3 caption keys as manufacturing, so no new SVG placeholder is needed). Keep it running BEFORE the create loop and keep it idempotent (upsertMediaByAlt already de-dupes by alt).

    Then, for local verification only, re-seed so the new home layout is written: because `seed-pages.ts` is skip-by-slug idempotent, first delete the existing `home` Pages document (or reset the dev `payload.db`), then run `npx tsx scripts/seed-pages.ts`. (On prod this is handled by a fresh migrate+seed; do not add reseed-on-build logic — SESSION-HANDOFF: "Build no longer reseeds.")

    Run the e2e regression to confirm no homepage/RTL regression from the new composition.
  </action>
  <verify>
    <automated>npm run lint:rtl && npx tsc --noEmit && npm run test:e2e -- homepage.spec.ts rtl-arabic.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `injectFacilityPhotos()` no longer hardcodes a single slug — it iterates `PAGES_EN_SEED` and handles every `mediaGallery` block (verify by reading; the home gallery items get real Media ids after seeding).
    - After deleting the `home` doc and re-running `npx tsx scripts/seed-pages.ts`, the seed logs "Seeded page 'home' (en)." and completes without error.
    - `npm run test:e2e -- homepage.spec.ts` passes: the homepage still renders Hero → FeatureGrid("Quality") → CertStrip(/certifications link) → StatsBand("Years Exporting") → ExportMap(countries img) → CTABand("Ready to Source With Confidence?") in strictly increasing vertical order (the new blocks are inserted without breaking this monotonic chain), and all 4 FeatureGrid value props are visible.
    - `npm run test:e2e -- rtl-arabic.spec.ts` passes: the Arabic homepage `sample-count` shows a Western digit and no Arabic-Indic digit.
    - `npm run lint:rtl` exits 0 and `npx tsc --noEmit` exits 0.
    - The home MediaGallery ("Manufacturing Excellence") renders its 3 placeholder facility photos on the live homepage (not empty cells) after the re-seed.
  </acceptance_criteria>
  <done>injectFacilityPhotos attaches photos to the home MediaGallery; a clean re-seed writes the 11-block home layout; homepage.spec.ts and rtl-arabic.spec.ts pass; lint:rtl and tsc are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → dev/prod DB | The seed writes English placeholder page content and uploads placeholder Media into the database. |
| Placeholder copy → public homepage | Seeded trust/testimonial copy is shown to prospective international buyers on a lead-gen site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Tampering | seed-pages.ts idempotency | low | mitigate | The generalized `injectFacilityPhotos` reuses `upsertMediaByAlt` (de-dupes Media by alt) and re-injects the same ids on every run; the create loop stays skip-by-slug, so re-seeding never duplicates pages or Media. |
| T-07-03-02 | Repudiation/Integrity | Fabricated client claims on a trust site | medium | mitigate | Per Pitfall 5, TrustBar uses region/segment descriptors, Testimonials use role + generic buyer-category + real served country (no invented named clients/endorsements), and the testimonial copy is flagged in-code as placeholder pending real client content. |
| T-07-03-03 | Denial of service | Home MediaGallery renders empty cells | low | mitigate | Home gallery captions match FACILITY_PHOTOS keys so injectFacilityPhotos populates real image ids; MediaGalleryBlock's Media guard already skips (not crashes) any unpopulated cell. |
| T-07-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — seed-content/seed-pages edits only; nothing to audit. |
</threat_model>

<verification>
- `npx tsc --noEmit` green (home layout type-checks against 07-02 types).
- Clean re-seed writes the new `home` layout without error.
- `npm run test:e2e -- homepage.spec.ts rtl-arabic.spec.ts` green (order + RTL preserved).
- `npm run lint:rtl` green.
- No en.json / color-token / body-tier / globals.css change; no new npm dependency.
</verification>

<success_criteria>
The homepage document composes the full 11-block trust narrative in UI-SPEC Part-3 order with realistic non-fabricated placeholder content, the Manufacturing MediaGallery shows placeholder photos, and both the homepage order e2e and the Arabic sample-count e2e still pass.
</success_criteria>

## Artifacts This Phase Produces (Plan 07-03)

| Artifact | Kind | Status |
|----------|------|--------|
| `src/lib/seed-content.ts` | Seed source (modified) | +3 helpers (trustBar/exportProcess/testimonials); `home` layout rewritten to 11 blocks |
| `scripts/seed-pages.ts` | Seed script (modified) | `injectFacilityPhotos` generalized to all pages' MediaGallery blocks |
| `home` Pages document (data) | Seeded content | 11-block homepage narrative, all CMS-editable |

No new files, no new Payload block, no new block slug, no new dependency in this plan (blocks/migration shipped in 07-02).

<output>
Create `.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-03-SUMMARY.md` when done.
</output>
