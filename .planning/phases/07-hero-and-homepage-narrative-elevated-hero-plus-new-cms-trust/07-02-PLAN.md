---
phase: 07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/TrustBar.ts
  - src/blocks/ExportProcess.ts
  - src/blocks/Testimonials.ts
  - src/blocks/index.ts
  - src/collections/Pages.ts
  - src/components/blocks/TrustBarBlock.tsx
  - src/components/blocks/ExportProcessBlock.tsx
  - src/components/blocks/TestimonialsBlock.tsx
  - src/components/blocks/RenderBlocks.tsx
  - tests/int/blocks-placeholder.spec.ts
  - payload-types.ts
  - src/migrations/index.ts
requirements: [D-03, D-04, D-05]
autonomous: true
must_haves:
  truths:
    - "Three new Payload Block configs exist and are selectable in the /admin layout builder: trustBar (sectionTitle + items[{logo?, name}]), exportProcess (sectionTitle + steps[{title, body}]), testimonials (sectionTitle + items[{quote, name, company?, country?}]) — each with NO field-level localized (cascades from Pages.layout, RESEARCH Pattern 2), matching the StatsBand/MediaGallery config style (D-03, D-05)."
    - "TrustBar does NOT reuse or extend CertStrip's Certifications-collection binding — it is a standalone editor-authored logo/label block with its own optional media-upload array and zero collection dependency (D-03)."
    - "Each new renderer is registered in RenderBlocks.tsx BLOCK_MAP under its slug and renders without crashing on empty items/steps (the shared blocks.emptyState fallback) — TrustBar as a centered wrap row with grayscale logo tiles or text-chip fallback, ExportProcess as a semantic <ol> of numbered steps, Testimonials as a 3-up Card grid with an accent Quote glyph (D-03, D-05)."
    - "ExportProcess step badges render as literal two-character strings 01–05 (String(i+1).padStart(2,'0')), never passed through Intl.NumberFormat, so they cannot leak Arabic-Indic digits on ar (D-05)."
    - "Every new block/renderer uses logical properties only (no ml-/mr-/pl-/pr-/left-/right-); npm run lint:rtl stays green. No color token, no body-tier typography token, no globals.css/@theme edit is added (D-04)."
    - "A committed Postgres migration adds the three new block tables so prod (Vercel + Neon) picks them up via vercel.json's payload migrate build step (D-05)."
  artifacts:
    - path: "src/blocks/TrustBar.ts"
      provides: "Payload Block config, slug 'trustBar', array items[{logo upload optional, name required}]"
      contains: "slug: \"trustBar\""
    - path: "src/blocks/ExportProcess.ts"
      provides: "Payload Block config, slug 'exportProcess', array steps[{title required, body textarea required}]"
      contains: "slug: \"exportProcess\""
    - path: "src/blocks/Testimonials.ts"
      provides: "Payload Block config, slug 'testimonials', array items[{quote required, name required, company, country}]"
      contains: "slug: \"testimonials\""
    - path: "src/components/blocks/TrustBarBlock.tsx"
      provides: "Async server renderer — centered flex-wrap logo/chip row"
      contains: "grayscale"
    - path: "src/components/blocks/ExportProcessBlock.tsx"
      provides: "Async server renderer — semantic <ol> numbered-step grid capped at 5 columns"
      contains: "<ol"
    - path: "src/components/blocks/TestimonialsBlock.tsx"
      provides: "Async server renderer — 3-up Card grid with lucide Quote accent glyph"
      contains: "text-accent-600"
    - path: "src/components/blocks/RenderBlocks.tsx"
      provides: "BLOCK_MAP entries trustBar/exportProcess/testimonials + their imports"
      contains: "trustBar: TrustBarBlock"
    - path: "src/collections/Pages.ts"
      provides: "TrustBar/ExportProcess/Testimonials added to layout.blocks array + @/blocks import"
      contains: "TrustBar"
    - path: "src/blocks/index.ts"
      provides: "barrel re-exports for the three new configs"
      contains: "export { TrustBar }"
    - path: "tests/int/blocks-placeholder.spec.ts"
      provides: "resilience + basic-content render cases for the three new blocks"
      contains: "exportProcess"
    - path: "src/migrations/index.ts"
      provides: "reference to the new phase-7 block-tables migration"
      contains: "phase7"
  key_links:
    - "config slug (e.g. 'trustBar') === Pages.layout.blocks entry === RenderBlocks BLOCK_MAP key === the blockType literal renderers Extract on — all four must match exactly or the block silently fails to render."
    - "Renderers narrow their prop via Extract<NonNullable<Page['layout']>[number], { blockType: '<slug>' }> from payload-types.ts, so `payload generate:types` MUST run after Pages.ts registration and before the renderers type-check."
    - "The Postgres migration is generated from the config schema diff after the blocks are registered in Pages.ts."
---

<objective>
Build the three genuinely-new CMS blocks the homepage narrative needs — **TrustBar** (partner/importer logo-label row), **ExportProcess** (inquiry→delivery timeline), and **Testimonials** (buyer quotes) — as Payload `Block` configs + async server renderers, wired through the exact 4-point registration pattern the 9 existing blocks use (`src/blocks/index.ts` barrel, `Pages.ts` `layout.blocks` array, `RenderBlocks.tsx` `BLOCK_MAP`, regenerated `payload-types.ts`), plus the committed Postgres migration prod needs. Per CONTEXT.md D-03: reuse-before-build already covered "Why Choose Us" (FeatureGrid), "Manufacturing Excellence" (MediaGallery+StatsBand) — these three are the only new block shapes, and TrustBar must NOT extend `CertStrip`'s Certifications binding.

Purpose: These blocks are the CMS vehicles for the trust/process/social-proof beats of the homepage narrative. They must be non-technical-editable (D-05) and follow the established convention so they behave identically to the existing block library.

Output: 3 config files, 3 renderer files, edits to `index.ts`/`Pages.ts`/`RenderBlocks.tsx`, extended `blocks-placeholder.spec.ts`, regenerated `payload-types.ts`, and one new committed Postgres migration.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-CONTEXT.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-UI-SPEC.md
@src/blocks/StatsBand.ts
@src/blocks/MediaGallery.ts
@src/blocks/index.ts
@src/collections/Pages.ts
@src/components/blocks/RenderBlocks.tsx
@src/components/blocks/FeatureGridBlock.tsx
@src/components/blocks/ExportMapBlock.tsx
@src/components/blocks/MediaGalleryBlock.tsx
@src/components/ui/card.tsx
@tests/int/blocks-placeholder.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 3 block configs, register them, regenerate types, create the migration</name>
  <files>src/blocks/TrustBar.ts, src/blocks/ExportProcess.ts, src/blocks/Testimonials.ts, src/blocks/index.ts, src/collections/Pages.ts, payload-types.ts, src/migrations/index.ts</files>
  <read_first>
    - src/blocks/StatsBand.ts and src/blocks/MediaGallery.ts (the config style to match: `import type { Block } from "payload"`, the head comment noting "Field-level localization NOT set here — cascades from Pages.layout (RESEARCH Pattern 2)", array field with `required: true` sub-fields, `upload` field with `relationTo: "media"`)
    - src/blocks/index.ts (the barrel — append exports; the file comment already says "Append later blocks here as later plans add them")
    - src/collections/Pages.ts (the `@/blocks` destructured import list AND the `layout.blocks` array — both need the 3 new names)
    - .planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-UI-SPEC.md (Part 2a/2b/2c — the exact field shapes)
  </read_first>
  <action>
    1. Create `src/blocks/TrustBar.ts` exporting `const TrustBar: Block` with slug `trustBar`, labels {singular:"Trust Bar", plural:"Trust Bars"}, fields: a `sectionTitle` text field, and an `items` array whose fields are `{ name: "logo", type: "upload", relationTo: "media" }` (OPTIONAL — no `required`, so an item with no logo yet renders as an honest text chip) and `{ name: "name", type: "text", required: true }`. Head comment (matching StatsBand.ts style) must note (a) localization cascades from Pages.layout / RESEARCH Pattern 2, and (b) this is a standalone editor-authored logo/label block with no Certifications-collection dependency — it deliberately does not reuse CertStrip (D-03).

    2. Create `src/blocks/ExportProcess.ts` exporting `const ExportProcess: Block` with slug `exportProcess`, labels {singular:"Export Process", plural:"Export Processes"}, fields: a `sectionTitle` text field, and a `steps` array whose fields are `{ name: "title", type: "text", required: true }` and `{ name: "body", type: "textarea", required: true }`. No icon field (numbered badges instead). Same localization-cascade head comment.

    3. Create `src/blocks/Testimonials.ts` exporting `const Testimonials: Block` with slug `testimonials`, labels {singular:"Testimonials", plural:"Testimonials Sections"}, fields: a `sectionTitle` text field, and an `items` array whose fields are `{ name: "quote", type: "textarea", required: true }`, `{ name: "name", type: "text", required: true }`, `{ name: "company", type: "text" }`, `{ name: "country", type: "text" }`. No avatar/photo field. Same localization-cascade head comment.

    4. In `src/blocks/index.ts`, append three barrel re-exports: TrustBar from "./TrustBar", ExportProcess from "./ExportProcess", Testimonials from "./Testimonials".

    5. In `src/collections/Pages.ts`, add TrustBar, ExportProcess, Testimonials to the destructured import from `@/blocks` AND append all three to the `layout.blocks` array (after ContactBlock). Do not set `localized` on any block field (the array's `localized: true` cascades).

    6. Run `npx payload generate:types` to regenerate `payload-types.ts` so `Page["layout"]` gains the three new block union members.

    7. Generate the prod Postgres migration for the new block tables (dev SQLite auto-pushes; prod needs a committed migration per SESSION-HANDOFF): run `DATABASE_URL=postgres://u:p@localhost:5432/none npx payload migrate:create phase7_homepage_narrative_blocks` (the dummy URL selects the pg dialect offline; the command emits a new `src/migrations/<timestamp>_phase7_homepage_narrative_blocks.ts/.json` and appends it to `src/migrations/index.ts`). The generated DDL must be additive only (CREATE TABLE for the new block/array tables) — review it, do not hand-edit existing migrations.
  </action>
  <verify>
    <automated>npx payload generate:types && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx payload generate:types` succeeds and `grep -F 'blockType: "trustBar"' payload-types.ts`, `grep -F 'blockType: "exportProcess"' payload-types.ts`, `grep -F 'blockType: "testimonials"' payload-types.ts` each return a match.
    - `grep -F 'export { TrustBar }' src/blocks/index.ts` (and ExportProcess, Testimonials) return matches.
    - `grep -F 'TrustBar' src/collections/Pages.ts` appears in BOTH the import and the blocks array (grep count >= 2 for each new name).
    - TrustBar.ts contains `slug: "trustBar"` and its `logo` field has no `required: true`; ExportProcess.ts contains `slug: "exportProcess"`; Testimonials.ts contains `slug: "testimonials"`.
    - None of the three config files imports from or references `CertStrip` or `certifications` (TrustBar is standalone — verify by reading the files, D-03).
    - A new migration file matching `src/migrations/*phase7_homepage_narrative_blocks.ts` exists and `grep -F 'phase7_homepage_narrative_blocks' src/migrations/index.ts` returns a match; the migration's `up()` only CREATEs new tables (no DROP/ALTER of existing block tables).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Three new block configs exist, are exported from the barrel, are registered in Pages.layout, are present in regenerated payload-types.ts, and a committed additive Postgres migration covers their tables.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 3 renderer components, wire BLOCK_MAP, extend the resilience test</name>
  <files>src/components/blocks/TrustBarBlock.tsx, src/components/blocks/ExportProcessBlock.tsx, src/components/blocks/TestimonialsBlock.tsx, src/components/blocks/RenderBlocks.tsx, tests/int/blocks-placeholder.spec.ts</files>
  <read_first>
    - src/components/blocks/FeatureGridBlock.tsx (the reference renderer: `getTranslations("blocks")`, `sectionBg(index)`, the `items.length === 0 ? t("emptyState") : ...` pattern, the `lgColsClass(count)` count→column lookup, the `item.photo && typeof item.photo === "object"` Media guard, and the `<Card className="gap-sm p-lg">` override pattern)
    - src/components/blocks/ExportMapBlock.tsx (the text-chip fallback: `rounded-full bg-neutral-100 px-md py-xs text-label text-neutral-900`)
    - src/components/blocks/MediaGalleryBlock.tsx (the section-rhythm wrapper `${sectionBg(index)} px-md py-2xl md:px-lg md:py-3xl xl:px-xl xl:py-4xl`, the centered `mb-lg text-center text-heading font-semibold` title, and the "Known stub"/placeholder comment convention)
    - src/components/blocks/RenderBlocks.tsx (BLOCK_MAP + imports — append 3 entries)
    - src/components/ui/card.tsx (the shadcn Card primitive the Testimonials renderer reuses)
    - tests/int/blocks-placeholder.spec.ts (the render harness that mocks next-intl + RenderBlocks.sectionBg and uses renderToStaticMarkup — extend it, don't rewrite it)
    - .planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-UI-SPEC.md (Part 2a/2b/2c render treatments — the exact className strings)
  </read_first>
  <action>
    Create three async server components under `src/components/blocks/`, each importing `getTranslations` from "next-intl/server", `sectionBg` from "./RenderBlocks", and typing its `block` prop via `Extract<NonNullable<Page["layout"]>[number], { blockType: "<slug>" }>` from `../../../payload-types`. Each uses the shared section wrapper `<section className={`${sectionBg(index)} px-md py-2xl md:px-lg md:py-3xl xl:px-xl xl:py-4xl`}>` with an inner `<div className="mx-auto max-w-[1280px]">`, an optional centered `<h2 className="mb-lg text-center text-heading font-semibold">{block.sectionTitle}</h2>`, and the shared empty-state fallback `<p className="text-center text-body text-neutral-600">{t("emptyState")}</p>` when the array is empty (namespace "blocks", key "emptyState" — already exists, no new i18n key).

    - **TrustBarBlock.tsx** (slug trustBar): items row = `<div className="flex flex-wrap items-center justify-center gap-x-xl gap-y-lg">`. For each item, guard the logo with `item.logo && typeof item.logo === "object" ? (item.logo as Media) : null`. Logo present → `<div className="relative h-12 w-28 shrink-0 grayscale opacity-70">` containing a next/image `Image` with `fill`, `sizes`, `className="object-contain"`, alt from Media.alt. Logo absent → the exact ExportMap chip: `<span className="rounded-full bg-neutral-100 px-md py-xs text-label text-neutral-900">{item.name}</span>`.

    - **ExportProcessBlock.tsx** (slug exportProcess): container = a semantic `<ol className={`grid grid-cols-1 gap-lg sm:grid-cols-2 ${colsClass(steps.length)}`}>` where `colsClass` is a count→`md:grid-cols-N` lookup capped at 5 (same technique as FeatureGridBlock's `lgColsClass`, extended: 5→md:grid-cols-5, 4→md:grid-cols-4, 3→md:grid-cols-3, 2→md:grid-cols-2, else md:grid-cols-1). Each step is an `<li>` with a numbered badge `<span className="flex size-10 items-center justify-center rounded-full bg-primary-700 text-body font-semibold text-white">` whose text is `String(i + 1).padStart(2, "0")` (literal "01".."05" — never Intl.NumberFormat, so no Arabic-Indic leak on ar), then `<p className="mt-md text-body font-semibold">{step.title}</p>` and `<p className="mt-xs text-label text-neutral-600">{step.body}</p>`. No connecting line/arrow between steps (direction-agnostic by design).

    - **TestimonialsBlock.tsx** (slug testimonials): import `Card` from "@/components/ui/card" and `Quote` from "lucide-react" (both already installed — no new dependency). Grid = `<div className="grid grid-cols-1 gap-lg md:grid-cols-3">`. Each item = `<Card className="gap-sm p-lg rounded-card border border-neutral-300 bg-white shadow-card">` containing `<Quote aria-hidden="true" className="size-6 text-accent-600" />`, then `<p className="text-body text-neutral-900">{item.quote}</p>`, then an attribution block: `<p className="text-label font-semibold text-neutral-900">{item.name}</p>` and a `text-label text-neutral-600` line joining company and country with " · " when both are present (render whichever single value is present otherwise; render nothing if neither). Add a head comment flagging that seed copy is placeholder pending real client testimonials (matching MediaGalleryBlock's stub-comment convention).

    Register all three in `src/components/blocks/RenderBlocks.tsx`: add the three imports and add `trustBar: TrustBarBlock`, `exportProcess: ExportProcessBlock`, `testimonials: TestimonialsBlock` to `BLOCK_MAP`.

    Extend `tests/int/blocks-placeholder.spec.ts` (reuse its existing next-intl + RenderBlocks.sectionBg mocks and `renderToStaticMarkup`): import the three new renderers and add cases asserting (a) each renders the `EMPTY_STATE_TEXT` substring when given an empty items/steps array without throwing, and (b) ExportProcess given two steps renders the literal badge text "01" and "02" in its output. These blocks render server-safe (Card, lucide SVG, next/image all produce static markup) so no jsdom is needed.

    Use logical properties only throughout (the classes above are all logical/flex/grid — no ml-/mr-/pl-/pr-/left-/right-). Add no color token and no globals.css edit; `text-accent-600` and `bg-primary-700` etc. are existing tokens.
  </action>
  <verify>
    <automated>npx vitest run tests/int/blocks-placeholder.spec.ts && npm run lint:rtl && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/int/blocks-placeholder.spec.ts` passes, including the new empty-state cases for trustBar/exportProcess/testimonials and the ExportProcess "01"/"02" badge assertion.
    - `npm run lint:rtl` exits 0 (no physical-direction utilities in any new renderer).
    - `npx tsc --noEmit` exits 0.
    - `grep -F 'trustBar: TrustBarBlock' src/components/blocks/RenderBlocks.tsx`, `grep -F 'exportProcess: ExportProcessBlock' ...`, `grep -F 'testimonials: TestimonialsBlock' ...` each return a match.
    - `grep -F '<ol' src/components/blocks/ExportProcessBlock.tsx` returns a match (semantic ordered list) and `grep -F 'padStart(2, "0")' src/components/blocks/ExportProcessBlock.tsx` returns a match (literal badge digits).
    - `grep -F 'grayscale' src/components/blocks/TrustBarBlock.tsx` and `grep -F 'rounded-full bg-neutral-100 px-md py-xs text-label text-neutral-900' src/components/blocks/TrustBarBlock.tsx` each return a match (logo tile + chip fallback).
    - `grep -F 'shadow-card' src/components/blocks/TestimonialsBlock.tsx` and `grep -F 'text-accent-600' src/components/blocks/TestimonialsBlock.tsx` each return a match (Phase-6 card recipe + accent Quote glyph).
    - No new import of any package outside the existing dependency set (only next/image, next-intl/server, @/components/ui/card, lucide-react, ./RenderBlocks, ../../../payload-types).
  </acceptance_criteria>
  <done>All three renderers exist, are registered in BLOCK_MAP, render their seeded and empty states without throwing under the int test harness, and pass lint:rtl + tsc; ExportProcess badges are locale-proof literal strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS editor → new block renders | Editor-authored TrustBar names, ExportProcess titles/body, Testimonials quotes, and uploaded logo Media cross into rendered pages. |
| Config schema → prod DB | The new block schema reaches Neon Postgres via a committed migration applied at build time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | Tampering/Info (XSS) | TrustBar/ExportProcess/Testimonials renderers | low | mitigate | All CMS text rendered as escaped JSX children; no `dangerouslySetInnerHTML` in any new block (unlike ExportMapBlock's self-authored SVG); TrustBar logos rendered via next/image using only the populated `Media.url`. |
| T-07-02-02 | Tampering/DoS | phase7 Postgres migration | medium | mitigate | Migration generated by `payload migrate:create` from the schema diff after registration; DDL is additive-only (CREATE TABLE for new block/array tables), reviewed before commit, and applied by vercel.json's non-destructive `payload migrate` step — no existing table dropped/altered. |
| T-07-02-03 | Denial of service | Renderer crash on empty/partial CMS data | low | mitigate | Every renderer guards empty arrays with the shared blocks.emptyState fallback and guards optional Media with the `typeof === "object"` check; covered by the extended blocks-placeholder int test. |
| T-07-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — reuses already-installed `lucide-react` (Quote) and the existing shadcn `Card`; nothing to audit. |
</threat_model>

<verification>
- `npx payload generate:types` regenerates payload-types.ts with the 3 new block union members.
- `npx vitest run tests/int/blocks-placeholder.spec.ts` green (incl. new blocks).
- `npm run lint:rtl` green.
- `npx tsc --noEmit` green.
- A committed additive Postgres migration for the 3 block tables exists and is referenced in src/migrations/index.ts.
- No color-token / body-tier typography / globals.css change; no new npm dependency.
</verification>

<success_criteria>
Three new blocks (trustBar, exportProcess, testimonials) are selectable in /admin, render their content and empty states without crashing, follow the established convention exactly, do not extend CertStrip, keep lint:rtl/tsc green, and ship with a committed Postgres migration for prod.
</success_criteria>

## Artifacts This Phase Produces (Plan 07-02)

| Artifact | Kind | Slug |
|----------|------|------|
| `src/blocks/TrustBar.ts` | Payload Block config (new) | `trustBar` |
| `src/blocks/ExportProcess.ts` | Payload Block config (new) | `exportProcess` |
| `src/blocks/Testimonials.ts` | Payload Block config (new) | `testimonials` |
| `src/components/blocks/TrustBarBlock.tsx` | Renderer component (new) | renders `trustBar` |
| `src/components/blocks/ExportProcessBlock.tsx` | Renderer component (new) | renders `exportProcess` |
| `src/components/blocks/TestimonialsBlock.tsx` | Renderer component (new) | renders `testimonials` |
| `src/blocks/index.ts` | Barrel (modified) | +3 exports |
| `src/collections/Pages.ts` | Collection (modified) | +3 blocks in `layout.blocks` |
| `src/components/blocks/RenderBlocks.tsx` | Dispatcher (modified) | +3 BLOCK_MAP entries |
| `tests/int/blocks-placeholder.spec.ts` | Int test (modified) | +3 block resilience cases |
| `payload-types.ts` | Generated types (regenerated) | +3 block union members |
| `src/migrations/<ts>_phase7_homepage_narrative_blocks.ts` + `src/migrations/index.ts` | Postgres migration (new) | 3 block tables |

<output>
Create `.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-02-SUMMARY.md` when done.
</output>
