---
phase: 7
slug: hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust
status: closed
threats_open: 0
asvs_level: 1
created: 2026-07-24
---

# Phase 7 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| CMS editor → Hero render | Editor-authored headline/subhead/eyebrow text crosses into the rendered page. | Rendered typography |
| CMS editor → new block renders | Editor-authored TrustBar names, ExportProcess titles/body, Testimonials quotes, and uploaded logo Media cross into rendered pages. | CMS text/media |
| Config schema → prod DB | New block schema reaches Neon Postgres via a committed migration applied at build time. | Schema/DDL |
| Seed script → dev/prod DB | Seed writes English placeholder page content and uploads placeholder Media into the database. | Content/media |
| Placeholder copy → public homepage | Seeded trust/testimonial copy is shown to prospective international buyers on a lead-gen site. | Public copy |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-07-01-01 | Tampering/Info (XSS) | HeroBlock headline/subhead/eyebrow | low | mitigate | Text rendered as escaped JSX children (`{block.headline}` etc.); no `dangerouslySetInnerHTML` introduced. Verified: grep for `dangerouslySetInnerHTML` in `HeroBlock.tsx` returns zero matches. | closed |
| T-07-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase. Verified: `git diff` on `package.json` across the full phase range is empty. | closed |
| T-07-02-01 | Tampering/Info (XSS) | TrustBar/ExportProcess/Testimonials renderers | low | mitigate | All CMS text rendered as escaped JSX children; no `dangerouslySetInnerHTML` in any new block; TrustBar logos rendered via `next/image` using only the populated `Media.url`. Verified: grep across all 3 new renderer files returns zero matches. | closed |
| T-07-02-02 | Tampering/DoS | phase7 Postgres migration | medium | mitigate | Migration generated by `payload migrate:create`, additive-only (CREATE TABLE for new block/array tables), reviewed before commit, applied by vercel.json's non-destructive `payload migrate` step. Verified: manual review of `20260724_093617_phase7_homepage_narrative_blocks.ts` — 13 ALTER statements, all ADD CONSTRAINT on new tables; 0 references to `site_settings`; no DROP/ALTER of pre-existing tables (an unrelated schema-drift line was manually stripped by the executor before commit). | closed |
| T-07-02-03 | Denial of service | Renderer crash on empty/partial CMS data | low | mitigate | Every renderer guards empty arrays with the shared `blocks.emptyState` fallback and guards optional Media with a `typeof === "object"` check. Verified: `tests/int/blocks-placeholder.spec.ts` — 11/11 passing, including 6 new resilience cases for the 3 new blocks. | closed |
| T-07-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — reuses already-installed `lucide-react` (Quote) and existing shadcn `Card`. Verified: no `package.json` diff. | closed |
| T-07-03-01 | Tampering | seed-pages.ts idempotency | low | mitigate | Generalized `injectFacilityPhotos` reuses `upsertMediaByAlt` (de-dupes by alt); create loop stays skip-by-slug. Verified live: the prod reseed operation (delete + recreate `home` by id) executed cleanly with no duplicate Media or Page docs created. | closed |
| T-07-03-02 | Repudiation/Integrity | Fabricated client claims on a trust site | medium | mitigate | TrustBar uses region/segment descriptors, Testimonials use role + generic buyer-category + real served country (no invented named clients/endorsements); testimonial copy flagged in-code as placeholder. Verified: UI-SPEC/CONTEXT review confirmed no fabricated company names; live homepage screenshot review (UAT Test 2) confirmed the seeded copy matches this constraint. | closed |
| T-07-03-03 | Denial of service | Home MediaGallery renders empty cells | low | mitigate | Home gallery captions match `FACILITY_PHOTOS` keys so `injectFacilityPhotos` populates real image ids; `MediaGalleryBlock`'s Media guard skips (not crashes) any unpopulated cell. Verified: live homepage screenshot (UAT) shows all 3 Manufacturing Excellence gallery cells populated with placeholder images, no broken cells. | closed |
| T-07-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — seed-content/seed-pages edits only. Verified: no `package.json` diff. | closed |

*Status: open · closed · open — below {block_on} threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-07-01 | T-07-01-SC | No new dependency installed this phase; supply-chain gate does not apply. | orchestrator (grep-level verification, ASVS L1) | 2026-07-24 |
| R-07-02 | T-07-02-SC | No new dependency installed this phase; supply-chain gate does not apply. | orchestrator (grep-level verification, ASVS L1) | 2026-07-24 |
| R-07-03 | T-07-03-SC | No new dependency installed this phase; supply-chain gate does not apply. | orchestrator (grep-level verification, ASVS L1) | 2026-07-24 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-07-24 | 10 (T-07-01-01, T-07-01-SC, T-07-02-01/02/03/SC, T-07-03-01/02/03/SC) | 10 | 0 | orchestrator — short-circuit path (threats_open:0, register plan-time-authored, ASVS L1 grep-depth verification; auditor spawn not required per gate rule) |

**Note:** This phase also involved a temporary, token-protected `/api/reseed-home` route used once to migrate prod's existing `home` Pages document to the new 11-block layout (since `scripts/seed-pages.ts` is skip-by-slug and would not touch an already-existing document). The route checked a header token against a Vercel Sensitive env var (`RESEED_TOKEN`, generated locally, never logged/committed), was deployed, invoked once via `curl`, then both the route file and the `RESEED_TOKEN` env var were removed and a clean redeploy shipped. Verified post-cleanup: `curl` to `/api/reseed-home` returns 404 in production.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
