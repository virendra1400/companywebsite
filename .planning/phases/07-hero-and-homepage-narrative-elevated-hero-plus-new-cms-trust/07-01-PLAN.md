---
phase: 07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/blocks/HeroBlock.tsx
autonomous: true
requirements: [D-01, D-02, D-04]
must_haves:
  truths:
    - "The homepage (full variant) Hero headline renders at the display-xl tier — text-display-xl font-display font-light tracking-display text-white (56px / weight 300 / -0.025em, Geist on en/fr/ru, Plex on ar) — replacing the old text-display font-semibold (40px/600) treatment (D-01)."
    - "The compact variant (every interior page) Hero is byte-identical to before: headline stays max-w-[42rem] text-display font-semibold text-white, padding stays px-md py-3xl md:px-lg xl:px-xl, gap stays gap-lg, overlay stays from-primary-900/80 via-primary-900/30 (D-01)."
    - "The full-variant atmospheric overlay is softened to from-primary-900/75 via-primary-900/25 to-transparent (single vertical bg-gradient-to-t, no heavier scrim, no CSS filter, no new dependency) so the image reads through more (D-02)."
    - "The data-testid=\"hero\" section hook and the data-testid=\"sample-count\" line (full variant only) are preserved verbatim, and the full/compact variant split and eyebrow/subhead/CTA/WhatsApp structure are unchanged, so tests/e2e/rtl-arabic.spec.ts stays green (D-01)."
    - "No color token, no body-tier typography token, and no @theme/globals.css edit is introduced — only existing Phase-6 utilities (text-display-xl, font-display, tracking-display, xl:py-4xl, gap-xl) are applied (D-04)."
  artifacts:
    - path: "src/components/blocks/HeroBlock.tsx"
      provides: "isFull-conditional heroPadding / stackGap / headlineClass / subheadClass / overlayClass driving the elevated full-variant hero; compact branch unchanged"
      contains: "text-display-xl font-display font-light tracking-display"
  key_links:
    - "The existing isFull boolean (block.variant === 'full') selects every elevated class — full gets display-xl/gap-xl/xl:py-4xl/softened-overlay/max-w-[46rem]; compact keeps every current class."
    - "data-testid=\"hero\" (section) and data-testid=\"sample-count\" (full-only line) remain the exact selectors tests/e2e/rtl-arabic.spec.ts asserts on."
---

<objective>
Elevate the homepage Hero (`full` variant only) to the premium display tier using the tokens Phase 6 shipped: swap the headline to `text-display-xl font-display font-light tracking-display` (56px/300, Geist on Latin locales, Plex on `ar`), apply the Phase-6 96px desktop rhythm step (`xl:py-4xl`), widen the headline/subhead columns, loosen the content-stack gap, and soften the atmospheric gradient — all per CONTEXT.md Locked Decisions 1 & 2 (D-01/D-02) and 07-UI-SPEC.md Part 1. The `compact` variant (every interior page) stays byte-identical.

Purpose: The homepage hero is the first trust signal an international buyer sees; the large, thin, editorial headline is the visual centerpiece of the v2.0 premium redesign. Phase 6 built the tokens but explicitly left Hero untouched — this plan is the first application.

Output: A modified `src/components/blocks/HeroBlock.tsx` with `isFull`-conditional classes. No new files, no new dependencies, no schema change.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-CONTEXT.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-UI-SPEC.md
@src/components/blocks/HeroBlock.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Phase-6 display tokens to the full-variant Hero (compact untouched)</name>
  <files>src/components/blocks/HeroBlock.tsx</files>
  <read_first>
    - src/components/blocks/HeroBlock.tsx (the file being modified — note the existing `const isFull = block.variant === "full"`, the hardcoded section padding `px-md py-3xl md:px-lg xl:px-xl`, the overlay div classes, the content-stack `gap-lg`, the `h1` and subhead `p` classes, and both `data-testid` hooks)
    - .planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-UI-SPEC.md (Part 1 — the element table and the "Implementation shape" variable definitions; these are the exact literal class strings to apply)
  </read_first>
  <action>
    Introduce five `isFull`-conditional class variables at the top of the component body (just after the existing `isFull` declaration), then reference each in the JSX. Use these exact literal values (from 07-UI-SPEC.md Part 1):

    - heroPadding: full = "px-md py-3xl md:px-lg md:py-3xl xl:px-xl xl:py-4xl"; compact = "px-md py-3xl md:px-lg xl:px-xl". Replace the currently-hardcoded padding segment in the section `className` with this variable (keep the rest of the section className — `relative flex flex-col justify-center overflow-hidden bg-primary-900 text-white` and the existing `isFull ? "min-h-[70dvh]" : "min-h-[320px] md:min-h-[400px]"` min-height ternary — exactly as-is).
    - overlayClass: full = "bg-gradient-to-t from-primary-900/75 via-primary-900/25 to-transparent"; compact = "bg-gradient-to-t from-primary-900/80 via-primary-900/30 to-transparent". Apply to the existing aria-hidden overlay div (keep `absolute inset-0`).
    - stackGap: full = "gap-xl"; compact = "gap-lg". Apply to the content-stack div (keep `relative mx-auto flex w-full max-w-[1280px] flex-col items-start text-start`).
    - headlineClass: full = "max-w-[46rem] text-display-xl font-display font-light tracking-display text-white"; compact = "max-w-[42rem] text-display font-semibold text-white". Apply to the `h1`.
    - subheadClass: full = "max-w-[38rem] text-body text-primary-100"; compact = "max-w-[36rem] text-body text-primary-100". Apply to the subhead `p`.

    Change ONLY those five elements. Do NOT touch: the `data-testid="hero"` attribute on the section, the eyebrow `p`, the primary/secondary CTA buttons, the WhatsAppTrackedLink, or the `data-testid="sample-count"` line (kept inside the existing `isFull` guard). Add no CSS `filter`, no parallax, no video, no new import — the softened gradient alone is the "better image treatment" per D-02. `font-light` is used ONLY on the display-xl headline (Phase 6's reserved-utility contract); do not apply it anywhere else. Introduce no color token and no globals.css/@theme edit (D-04).
  </action>
  <verify>
    <automated>npm run lint:rtl && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:rtl` exits 0 (the full-variant overlay stays `bg-gradient-to-t` — vertical, not the banned `-to-r`/`-to-l`; no `ml-/mr-/pl-/pr-/left-/right-` introduced).
    - `npx tsc --noEmit` exits 0.
    - `grep -F 'text-display-xl font-display font-light tracking-display' src/components/blocks/HeroBlock.tsx` returns a match (full-variant headline elevated).
    - `grep -F 'text-display font-semibold text-white' src/components/blocks/HeroBlock.tsx` returns a match (compact-variant headline unchanged).
    - `grep -F 'xl:py-4xl' src/components/blocks/HeroBlock.tsx` returns a match (full-variant 96px desktop rhythm applied).
    - `grep -F 'from-primary-900/75 via-primary-900/25' src/components/blocks/HeroBlock.tsx` returns a match (softened full-variant overlay).
    - `grep -c 'data-testid="hero"' src/components/blocks/HeroBlock.tsx` and `grep -c 'data-testid="sample-count"' src/components/blocks/HeroBlock.tsx` each return 1 (both test hooks preserved).
    - `npm run test:e2e -- rtl-arabic.spec.ts` passes — the Arabic homepage `sample-count` still shows a Western digit and no Arabic-Indic digit (needs the existing seeded dev DB; the elevation changes no seed data, so the current seed renders the elevated hero directly).
    - No edit to src/app/globals.css and no new import statement added to HeroBlock.tsx.
  </acceptance_criteria>
  <done>The full-variant homepage hero renders the 56px thin Geist display headline with the softened gradient and wider rhythm; the compact interior-page hero is visually unchanged; both `data-testid` hooks and the variant split are intact; lint:rtl and tsc are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS editor → Hero render | Editor-authored `headline`/`subhead`/`eyebrow` text crosses into the rendered page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | Tampering/Info (XSS) | HeroBlock headline/subhead/eyebrow | low | mitigate | Text rendered as escaped JSX children (`{block.headline}` etc.); this plan adds no `dangerouslySetInnerHTML` and changes only Tailwind class strings, so the existing escape posture is unchanged. |
| T-07-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — pure Tailwind-class change on an existing component; nothing to audit. |
</threat_model>

<verification>
- `npm run lint:rtl` green.
- `npx tsc --noEmit` green.
- `npm run test:e2e -- rtl-arabic.spec.ts` green (sample-count digit assertion).
- Manual/visual (optional, folded into Phase-level review): the homepage hero headline is visibly larger and thinner; an interior page (e.g. /about) hero is unchanged.
- No globals.css / color-token / body-tier typography change.
</verification>

<success_criteria>
Homepage `full` hero displays the elevated `text-display-xl font-display font-light tracking-display` headline with `xl:py-4xl` rhythm and softened overlay; `compact` hero byte-identical; `data-testid="hero"` + `data-testid="sample-count"` preserved; lint:rtl + tsc + rtl-arabic e2e green.
</success_criteria>

## Artifacts This Phase Produces (Plan 07-01)

| Artifact | Kind | Status |
|----------|------|--------|
| `src/components/blocks/HeroBlock.tsx` | Renderer component (modified) | Elevated `full` variant; `compact` unchanged |

No new files, no new Payload block, no new block slug, no new dependency in this plan.

<output>
Create `.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-01-SUMMARY.md` when done.
</output>
