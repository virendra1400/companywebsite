---
phase: 01-foundation-cms-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - components.json
  - src/app/globals.css
  - src/i18n/routing.ts
  - src/i18n/navigation.ts
  - src/i18n/request.ts
  - src/middleware.ts
  - src/i18n/messages/en.json
  - src/i18n/messages/ar.json
  - src/i18n/messages/fr.json
  - src/i18n/messages/ru.json
  - src/app/(site)/[locale]/layout.tsx
  - src/app/(site)/[locale]/page.tsx
  - vitest.config.ts
  - playwright.config.ts
  - tests/e2e/locale-routing.spec.ts
  - tests/e2e/rtl-arabic.spec.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "Visitor can reach the home page at /, /ar, /fr, /ru and each returns 200"
    - "The /ar page renders <html dir=\"rtl\"> and every other locale renders dir=\"ltr\", set server-side"
    - "A formatted number rendered in the ar locale uses Western (latn) digits, not Arabic-Indic"
    - "The IBM Plex Sans Arabic subset loads only for ar; IBM Plex Sans (latin) for en/fr/ru"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "next-intl path-prefix routing (locales en/ar/fr/ru, defaultLocale en, localePrefix as-needed)"
      contains: "defineRouting"
    - path: "src/middleware.ts"
      provides: "next-intl middleware excluding admin/api/_next"
      contains: "createMiddleware"
    - path: "src/app/(site)/[locale]/layout.tsx"
      provides: "server-set dir/lang, next/font/google IBM Plex fonts, setRequestLocale, generateStaticParams"
      min_lines: 30
    - path: "vitest.config.ts"
      provides: "Vitest test harness"
    - path: "playwright.config.ts"
      provides: "Playwright e2e harness with en+ar project matrix"
  key_links:
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware\\(routing\\)"
    - from: "src/app/(site)/[locale]/layout.tsx"
      to: "<html dir>"
      via: "RTL_LOCALES check on locale param"
      pattern: "dir=\\{"
---

<objective>
Scaffold the Next.js 16 + Tailwind v4 + TypeScript-strict application, wire next-intl path-prefix locale routing for en/ar/fr/ru, set RTL correctly server-side, load the IBM Plex font pairing per script, and install the Vitest + Playwright test harness. This is the foundation slice of the Walking Skeleton: after this plan a visitor can browse all four locale URLs with correct direction and fonts, verified by e2e tests. CMS-driven content arrives in Plan 03; the home page here renders next-intl chrome strings as realistic-shaped placeholder content so the routing/RTL pipeline is provable end-to-end without Payload.

Purpose: Every later slice (CMS, chrome, deploy) builds on this locale/RTL foundation. RTL is architected from the first component per FOUND-03 and UI-SPEC — never retrofitted.
Output: A bootable, locale-routed, RTL-correct app skeleton with a green test harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cms-decision/01-CONTEXT.md
@.planning/phases/01-foundation-cms-decision/01-RESEARCH.md
@.planning/phases/01-foundation-cms-decision/01-UI-SPEC.md
@.planning/phases/01-foundation-cms-decision/01-VALIDATION.md

<interfaces>
<!-- Load-bearing contracts for downstream plans. Extracted from 01-RESEARCH.md Patterns 1-4. -->
- routing.ts exports `routing` from defineRouting({ locales:['en','ar','fr','ru'], defaultLocale:'en', localePrefix:'as-needed' }).
- navigation.ts exports Link, usePathname, useRouter, redirect from createNavigation(routing) — Plan 03 LanguageSwitcher consumes usePathname + useRouter.
- request.ts (getRequestConfig) exposes a named number format `latn` = { numberingSystem: 'latn' } — used for FOUND-03 non-mirrored numerals.
- layout.tsx sets <html lang={locale} dir={RTL_LOCALES.has(locale) ? 'rtl' : 'ltr'}> and applies the per-script font CSS variable. RTL_LOCALES = new Set(['ar']).
- messages/{locale}.json holds ONLY chrome strings (nav labels, switcher endonyms, fallback-notice copy) — never CMS content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold app + design tokens + install test harness + author failing e2e specs (RED)</name>
  <files>package.json, next.config.ts, tsconfig.json, postcss.config.mjs, components.json, src/app/globals.css, vitest.config.ts, playwright.config.ts, tests/e2e/locale-routing.spec.ts, tests/e2e/rtl-arabic.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation-cms-decision/01-RESEARCH.md (Standard Stack: pinned versions; Installation; Validation Architecture; Environment Availability — Node v24.15.0, npm 11.17.0)
    - .planning/phases/01-foundation-cms-decision/01-UI-SPEC.md (Spacing Scale, Typography, Color token ramp, Breakpoints — feed these into the @theme block)
    - .planning/phases/01-foundation-cms-decision/01-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
  </read_first>
  <action>
    Scaffold with `npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*"` into the working directory (greenfield, no existing package.json). Pin next to 16.2.x, react/react-dom to 19.x, tailwindcss to 4.3.x. Enable TypeScript strict mode in tsconfig.json. Initialize shadcn via `npx shadcn@latest init` on the neutral base preset (do NOT add components yet — Plan 03 adds Button/DropdownMenu/Sheet). In src/app/globals.css define the Tailwind v4 `@theme` block using the UI-SPEC token ramp: primary-900 #0F2E22, primary-700 #1B5E45, primary-500 #3D8266, primary-100 #E3EEE8, accent-800 #8A6612, accent-600 #C9A227, accent-100 #F7EFDD, neutral-900 #1A1F1C, neutral-600 #5B655F, neutral-300 #D7DBD8, neutral-100 #F5F6F4; spacing xs/sm/md/lg/xl/2xl/3xl = 4/8/16/24/32/48/64px; the four type sizes (Label 14/Body 16/Heading 28/Display 40) at weights 400 and 600 only; map shadcn --primary to primary-700, --background to #FFFFFF, --foreground to neutral-900. Install the test harness as devDependencies: `npm install -D vitest @playwright/test` then `npx playwright install`. Create vitest.config.ts (node environment, a `tests/int/**` project for later Payload Local API tests). Create playwright.config.ts with a projects matrix covering en and ar, webServer running `npm run dev`. Author tests/e2e/locale-routing.spec.ts asserting GET /, /ar, /fr, /ru each return 200 and render the home hero, and tests/e2e/rtl-arabic.spec.ts asserting `/ar` has `html[dir="rtl"]`, `/` has `html[dir="ltr"]`, and a rendered sample number in ar uses latn digits (regex on a `[data-testid="sample-count"]` node — no Arabic-Indic ٠-٩). These specs MUST fail now (no routing/layout yet) — this is the RED step.
  </action>
  <acceptance_criteria>
    - `npm run build` (or `npx tsc --noEmit`) exits 0 with strict mode on.
    - `src/app/globals.css` contains an `@theme` block with all token names above (grep `--color-primary-700` and `#1B5E45` present).
    - `npx playwright test tests/e2e/locale-routing.spec.ts` and `tests/e2e/rtl-arabic.spec.ts` RUN and FAIL (RED) — a run error other than assertion failure does not count.
    - No physical-direction Tailwind classes in globals.css: `grep -v '^\s*/\*' src/app/globals.css | grep -cE '\b(ml-|mr-|text-left|text-right)\b'` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test tests/e2e/rtl-arabic.spec.ts --reporter=line || echo "RED expected"</automated>
  </verify>
  <done>App scaffolds and type-checks; Vitest + Playwright installed and configured; both e2e specs fail as RED placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: next-intl path-prefix routing, middleware, request config, chrome messages</name>
  <files>src/i18n/routing.ts, src/i18n/navigation.ts, src/i18n/request.ts, src/middleware.ts, src/i18n/messages/en.json, src/i18n/messages/ar.json, src/i18n/messages/fr.json, src/i18n/messages/ru.json</files>
  <read_first>
    - .planning/phases/01-foundation-cms-decision/01-RESEARCH.md (Pattern 1 routing/middleware, Pattern 4 numberingSystem latn + the #-token limitation in Pitfall 2)
    - .planning/phases/01-foundation-cms-decision/01-UI-SPEC.md (Copywriting Contract — exact fallback-notice strings; LanguageSwitcher endonyms)
    - src/app/globals.css (tokens created in Task 1)
  </read_first>
  <behavior>
    - GET /ar routes to the ar locale segment; GET / routes to en (no prefix, localePrefix as-needed).
    - Requesting /admin or /api/* is NOT intercepted by the locale middleware.
    - A number formatted via the `latn` format in the ar locale yields Western digits.
    - messages/*.json each contain keys: nav.*, switcher.ariaLabel, fallbackNotice.heading, fallbackNotice.body for all 4 locales.
  </behavior>
  <action>
    Create src/i18n/routing.ts exporting `routing` per the interfaces block (D-05 path-prefix, en at root). Create src/i18n/navigation.ts exporting Link/usePathname/useRouter/redirect from createNavigation(routing). Create src/i18n/request.ts (getRequestConfig) that loads the matching messages/{locale}.json and declares `formats.number.latn = { numberingSystem: 'latn' }` so ar renders non-mirrored numerals on formatter calls (FOUND-03). Create src/middleware.ts as `createMiddleware(routing)` with matcher `/((?!api|admin|_next|_vercel|.*\\..*).*)` — the added `admin` exclusion keeps the Payload admin route group (Plan 02) out of locale rewriting. Populate messages/en.json with realistic-shaped chrome strings and translate the fallback-notice into ar/fr/ru per the UI-SPEC Copywriting Contract (Arabic heading "هذه الصفحة غير متوفرة باللغة العربية بعد"). Add a `sample.count` message used only to prove latn numerals in the ar e2e test. Do NOT use the ICU `#` plural token for any Arabic count — use an explicit format.number(...,'latn') call in the consuming component per Pitfall 2.
  </action>
  <acceptance_criteria>
    - `src/middleware.ts` matcher excludes `admin` and `api` (grep both tokens present in the matcher string).
    - `src/i18n/request.ts` defines a `latn` number format containing `numberingSystem` and value `'latn'`.
    - All four messages files parse as JSON and each contain `fallbackNotice.heading` and `switcher.ariaLabel`: `for f in en ar fr ru; do node -e "JSON.parse(require('fs').readFileSync('src/i18n/messages/'+process.argv[1]+'.json'))" $f; done` exits 0.
    - No `#`-token plural for Arabic counts: `grep -rE '\{[a-zA-Z]+, *plural' src/i18n/messages/ar.json` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>node -e "['en','ar','fr','ru'].forEach(l=>JSON.parse(require('fs').readFileSync('src/i18n/messages/'+l+'.json')))" && grep -q "numberingSystem" src/i18n/request.ts</automated>
  </verify>
  <done>Routing, navigation, request config, middleware, and all four chrome-string message files exist and are valid; admin/api excluded from locale middleware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Locale layout (dir/lang/fonts) + placeholder home page — turn e2e GREEN</name>
  <files>src/app/(site)/[locale]/layout.tsx, src/app/(site)/[locale]/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-cms-decision/01-RESEARCH.md (Pattern 2 server-set dir/lang + next/font/google IBM Plex, generateStaticParams, setRequestLocale)
    - .planning/phases/01-foundation-cms-decision/01-UI-SPEC.md (Hero placeholder spec, Typography Display token, RTL contract)
    - src/i18n/routing.ts, src/i18n/request.ts (created Task 2)
    - tests/e2e/locale-routing.spec.ts, tests/e2e/rtl-arabic.spec.ts (created Task 1 — these must pass now)
  </read_first>
  <behavior>
    - /, /ar, /fr, /ru all render the home hero and return 200.
    - <html> carries lang={locale} and dir=rtl for ar, ltr otherwise, set server-side (no client toggle).
    - The ar page's `[data-testid="sample-count"]` shows latn digits.
    - IBM_Plex_Sans loads subset ['latin'] weights 400/600; IBM_Plex_Sans_Arabic loads subset ['arabic'] only for ar.
  </behavior>
  <action>
    Create src/app/(site)/[locale]/layout.tsx per Pattern 2: await params, `hasLocale(routing.locales, locale)` → notFound() on invalid, `setRequestLocale(locale)`, `generateStaticParams()` from routing.locales. Load IBM_Plex_Sans (subsets ['latin'], variable --font-plex-sans, weight ['400','600']) and IBM_Plex_Sans_Arabic (subsets ['arabic'], variable --font-plex-sans-arabic, weight ['400','600']) from next/font/google; apply the Arabic font variable class only when locale==='ar', latin otherwise. Render `<html lang dir>` (dir from RTL_LOCALES) wrapping NextIntlClientProvider. Do NOT add GlobalHeader/GlobalFooter here — Plan 03 injects chrome (this keeps file ownership clean; layout gains chrome in wave 3). Create src/app/(site)/[locale]/page.tsx rendering a placeholder Hero: Display-token headline + Body subhead + a "Request a Quote" PrimaryButton placeholder (plain styled link for now; shadcn Button arrives Plan 03), all copy from next-intl chrome strings, plus a `[data-testid="sample-count"]` node formatting a number via the `latn` format to prove FOUND-03. Use only logical Tailwind utilities (ms-/me-/ps-/pe-/text-start/text-end). This makes the Task 1 RED specs GREEN.
  </action>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/locale-routing.spec.ts tests/e2e/rtl-arabic.spec.ts` passes (GREEN) for both en and ar projects.
    - `src/app/(site)/[locale]/layout.tsx` calls `setRequestLocale` and `generateStaticParams` (grep both).
    - No physical-direction classes in the two new files: `grep -REn '\b(ml-|mr-|pl-|pr-|left-|right-|text-left|text-right|bg-gradient-to-r|bg-gradient-to-l)\b' "src/app/(site)"` returns 0 matches.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test tests/e2e/rtl-arabic.spec.ts tests/e2e/locale-routing.spec.ts --reporter=line</automated>
  </verify>
  <done>All four locale URLs render with correct server-set dir/lang and per-script fonts; routing + RTL e2e specs are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → Next.js middleware/SSR | Untrusted URL path (locale segment) crosses here; only the routing surface exists in this plan (no data writes, no auth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | locale param in [locale] route | mitigate | `hasLocale(routing.locales, locale)` → `notFound()` on any value outside the 4-locale allowlist (Task 3) — no unvalidated locale reaches rendering. |
| T-01-02 | Denial of Service | middleware matcher over-matching static/api paths | mitigate | Matcher explicitly excludes `api`, `admin`, `_next`, `_vercel`, and dotted files so the locale middleware never intercepts the Payload admin/api surfaces (Task 2). |
| T-01-03 | Information Disclosure | source maps / env in client bundle | accept | No secrets or data sources exist in this plan (pure routing/layout); env-handling threats are dispositioned in Plan 02. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (strict mode).
- `npx playwright test` green for locale-routing + rtl-arabic across en and ar projects.
- Grep gate: zero physical-direction Tailwind classes under `src/`.
- Manual sanity: `npm run dev`, visit `/`, `/ar`, `/fr`, `/ru` — each renders the placeholder hero; `/ar` is visibly RTL.
</verification>

<success_criteria>
Contributes to ROADMAP success criteria 1 (locale URLs + dir + non-mirrored numerals + responsive shell) and the launch part of 2/3 (four locales exist from day one). Fully met when all four locale routes render with correct server-set direction, per-script fonts, latn numerals in ar, and the routing/RTL e2e suite is green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cms-decision/01-01-SUMMARY.md`.
</output>
