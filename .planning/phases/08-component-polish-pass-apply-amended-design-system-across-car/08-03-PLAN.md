---
phase: 08-component-polish-pass-apply-amended-design-system-across-car
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/Faq.ts
  - src/blocks/index.ts
  - src/collections/Pages.ts
  - payload-types.ts
  - src/migrations/index.ts
  - src/components/ui/accordion.tsx
  - src/components/blocks/FaqBlock.tsx
  - src/components/blocks/RenderBlocks.tsx
  - src/lib/seed-content.ts
  - tests/int/blocks-placeholder.spec.ts
requirements: [FAQ-01]
autonomous: true
must_haves:
  truths:
    - "A non-technical editor can add an FAQ section to any page from the /admin layout builder, authoring a sectionTitle plus any number of question/answer pairs, with per-locale values cascading from Pages.layout's localized flag exactly like the other 13 blocks (08-CONTEXT.md 'FAQ scope: build it this phase' / 08-UI-SPEC Contract §6)."
    - "Scope resolved: the FAQ block now exists in shipped code, closing the 08-UI-SPEC Component Audit row #9 gap where ROADMAP.md, PROJECT.md and REDESIGN-PLAN.md all named FAQ as Phase 8 scope but nothing existed to polish (08-UI-SPEC UI Considerations: Scope — resolved to 'build' by 08-CONTEXT.md, not deferred)."
    - "The FAQ renders as a shadcn Accordion in single-collapsible mode with NO item pre-opened, using the accordion's own row dividers rather than a card wrapper — the StatsBandBlock lightweight-band precedent, not the flat spec-sheet anti-pattern (08-UI-SPEC Contract §6)."
    - "Empty state verified: a zero-item FAQ block renders the shared blocks.emptyState fallback string, never a blank accordion shell — identical handling to every other block (08-UI-SPEC UI Considerations: Empty, covered)."
    - "The section wrapper uses the identical rhythm string every other block uses, so the FAQ participates in the sectionBg alternation and the 96px xl rhythm with no special-casing."
    - "The FAQ is live and visitor-reachable, not dead registered code: the seeded Contact page carries an FAQ section whose answers restate only process facts already published elsewhere on the site, inventing no certifications, volumes or client names."
    - "npm run lint:rtl stays green including the newly generated accordion primitive — the shadcn accordion ships a physical-direction text alignment utility out of the box and must be converted to its logical equivalent before commit."
    - "No new npm dependency is added: radix-ui@1.6.2 (already installed, already the import source for sheet.tsx) exports Accordion, so the generated primitive imports from the existing unified package."
  artifacts:
    - path: "src/blocks/Faq.ts"
      provides: "Payload Block config, slug 'faq', fields sectionTitle (text, optional) + items array of { question: text, answer: textarea }"
      contains: "slug: \"faq\""
    - path: "src/components/blocks/FaqBlock.tsx"
      provides: "Async server renderer — section wrapper + single-collapsible Accordion + shared empty-state fallback"
      contains: "type=\"single\""
    - path: "src/components/ui/accordion.tsx"
      provides: "shadcn Accordion/AccordionItem/AccordionTrigger/AccordionContent over the existing radix-ui package, RTL-corrected"
      contains: "AccordionTrigger"
    - path: "src/components/blocks/RenderBlocks.tsx"
      provides: "BLOCK_MAP entry faq -> FaqBlock plus its import"
      contains: "faq: FaqBlock"
    - path: "src/collections/Pages.ts"
      provides: "Faq added to the @/blocks import list and to layout.blocks"
      contains: "Faq"
    - path: "src/blocks/index.ts"
      provides: "barrel re-export for the Faq config"
      contains: "export { Faq }"
    - path: "src/migrations/index.ts"
      provides: "reference to the new additive phase-8 FAQ block-tables migration"
      contains: "phase8_faq_block"
    - path: "src/lib/seed-content.ts"
      provides: "faq() block helper + an FAQ section on the seeded Contact page"
      contains: "blockType: \"faq\""
    - path: "tests/int/blocks-placeholder.spec.ts"
      provides: "FaqBlock empty-state and populated-render resilience cases"
      contains: "FaqBlock"
  key_links:
    - "The slug string must match in four places or the block silently fails to render with no error: `slug: \"faq\"` in Faq.ts === the entry in Pages.ts layout.blocks === the BLOCK_MAP key `faq` in RenderBlocks.tsx === the `blockType: \"faq\"` literal FaqBlock Extracts on from payload-types.ts."
    - "FaqBlock narrows its prop via Extract<NonNullable<Page['layout']>[number], { blockType: 'faq' }>, so `npx payload generate:types` MUST run after Pages.ts registration and before FaqBlock type-checks — Task 1 strictly precedes Task 2."
    - "The Postgres migration is a diff against the live config schema, so it can only be generated after Faq.ts is registered in Pages.ts. Prod (Vercel + Neon) applies it via vercel.json's `payload migrate` build step; dev SQLite auto-pushes and needs nothing."
    - "seed-pages.ts post-processes only mediaGallery and photo-variant featureGrid blocks; a plain faq block passes straight through with no seed-script change needed."
---

<objective>
Build the FAQ block end-to-end — Payload config, shadcn Accordion primitive, server renderer, four-point registration, additive Postgres migration, seeded Contact-page content, and integration tests — per `08-UI-SPEC.md` Contract §6 and the `08-CONTEXT.md` locked decision to build (not defer) it this phase.

Purpose: This is the one item in the phase that is genuinely additive rather than a polish fix. `08-UI-SPEC` Component Audit row #9 found that ROADMAP.md, PROJECT.md and REDESIGN-PLAN.md all name FAQ as Phase 8 scope while a full-repo grep found no FAQ collection, block or component anywhere in shipped code. `08-CONTEXT.md` resolved the open scope question: build it, following the exact Phase 7 block-registration pattern.

Output: 2 new source files + 1 generated shadcn primitive + registration edits in 3 files + regenerated types + 1 new migration + seed content + test cases.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-CONTEXT.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-UI-SPEC.md
@.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-02-SUMMARY.md
@src/blocks/Testimonials.ts
@src/blocks/index.ts
@src/collections/Pages.ts
@src/components/blocks/RenderBlocks.tsx
@src/components/blocks/TestimonialsBlock.tsx
@src/components/ui/sheet.tsx
@tests/int/blocks-placeholder.spec.ts
</context>

## Artifacts this phase produces

Full phase-wide symbol list (this plan owns rows marked **P03**):

| Symbol / file | Kind | Owning plan |
|---------------|------|-------------|
| `FeatureGridBlock` / `SpecTable` card hairline recipe | modified class strings | P01 |
| `SpecTable` / `StatsBandBlock` / `ExportMapBlock` `tabular-nums` | modified class strings | P01 |
| `buttonVariants` base cva — accent focus ring as the default | modified primitive | P02 |
| `buttonVariants` `default` variant — brand hover baked in | modified primitive | P02 |
| `buttonVariants.variant.outlineOnDark` | **new Button variant** | P02 |
| `src/components/ui/accordion.tsx` — `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent` | **new shadcn primitive** | **P03** |
| `src/blocks/Faq.ts` — `Faq` (Payload `Block`, slug `faq`) | **new file** | **P03** |
| `src/components/blocks/FaqBlock.tsx` — `FaqBlock` | **new file** | **P03** |
| `src/blocks/index.ts` — `export { Faq }` | modified barrel | **P03** |
| `src/collections/Pages.ts` — `Faq` in `layout.blocks` | modified collection | **P03** |
| `RenderBlocks.tsx` — `BLOCK_MAP.faq` entry | modified registry | **P03** |
| `payload-types.ts` — `blockType: "faq"` union member | regenerated types | **P03** |
| `src/migrations/<ts>_phase8_faq_block.ts` / `.json` | **new migration** | **P03** |
| `src/lib/seed-content.ts` — `faq()` helper + Contact-page FAQ section | new seed content | **P03** |
| `tests/int/blocks-placeholder.spec.ts` — FaqBlock cases | extended test | **P03** |

<!-- planner-discipline-allow: text-left -->

<tasks>

<task type="auto">
  <name>Task 1: Create the Faq block config, register it, regenerate types, generate the additive migration</name>
  <files>src/blocks/Faq.ts, src/blocks/index.ts, src/collections/Pages.ts, payload-types.ts, src/migrations/index.ts</files>
  <read_first>
    - `src/blocks/Testimonials.ts` — the config style to match verbatim: `import type { Block } from "payload"`, the `labels: { singular, plural }` pair, an optional `sectionTitle` text field, an `items` array with `required: true` sub-fields, and the head comment noting that field-level localization is NOT set here because it cascades from `Pages.layout` (RESEARCH Pattern 2).
    - `src/blocks/index.ts` — the barrel; its head comment already instructs appending later blocks here rather than pre-scaffolding stubs.
    - `src/collections/Pages.ts` lines 1-60 — the destructured `@/blocks` import list AND the `layout.blocks` array; both need `Faq`.
    - `08-UI-SPEC.md` **Contract §6** (the Payload config bullet: `slug: "faq"`, `sectionTitle` text optional, `items` array of `{ question: text, answer: textarea }`).
    - `.planning/phases/07-hero-and-homepage-narrative-elevated-hero-plus-new-cms-trust/07-02-SUMMARY.md` — specifically the migration-generation note about `payload migrate:create` capturing unrelated pre-existing schema drift, which must be stripped from the generated diff.
  </read_first>
  <action>
    1. Create `src/blocks/Faq.ts` exporting `const Faq: Block` with `slug: "faq"`, `labels: { singular: "FAQ", plural: "FAQ Sections" }`, and fields: `{ name: "sectionTitle", type: "text" }` (optional — matches every other block's convention) and `{ name: "items", type: "array", fields: [{ name: "question", type: "text", required: true }, { name: "answer", type: "textarea", required: true }] }`. Add a head comment in the project's citation style referencing `08-UI-SPEC Contract §6` and stating that field-level localization is deliberately not set because it cascades from `Pages.layout`. Do not add an icon, category, or `defaultOpen` field.

    2. In `src/blocks/index.ts`, append one barrel re-export for `Faq` from `"./Faq"`, in the same style as the existing lines.

    3. In `src/collections/Pages.ts`, add `Faq` to the destructured `@/blocks` import AND append `Faq` to the `layout.blocks` array (after `Testimonials`). Do NOT set `localized` on any field inside the block — the array's `localized: true` cascades.

    4. Run `npx payload generate:types` so `payload-types.ts` gains the `blockType: "faq"` union member on `Page["layout"]`.

    5. Generate the prod Postgres migration (dev SQLite auto-pushes; prod needs a committed migration): run `DATABASE_URL=postgres://u:p@localhost:5432/none npx payload migrate:create phase8_faq_block` — the dummy URL selects the pg dialect offline. This emits `src/migrations/<timestamp>_phase8_faq_block.ts` + `.json` and appends the entry to `src/migrations/index.ts`.

    6. **Review the generated DDL before committing.** It must be strictly additive — `CREATE TABLE` (plus index/FK statements) for the new faq block and faq items tables and their locale variants only. If the diff picks up any unrelated pre-existing schema drift on an existing table (the Phase 7 executor hit exactly this), strip those statements from BOTH `up()` and `down()` so the migration stays scoped to the new tables. Never hand-edit an already-committed migration file.

    7. This task requires DB/tooling access for `generate:types` and `migrate:create`. If the environment lacks the `.env` / local `payload.db` needed to run them, stop and report the blocker — do not hand-write `payload-types.ts` or the migration SQL.
  </action>
  <verify>
    <automated>grep -Fc 'slug: "faq"' src/blocks/Faq.ts && grep -Fc 'export { Faq }' src/blocks/index.ts && test "$(grep -Fc 'Faq' src/collections/Pages.ts)" -ge 2 && npx payload generate:types && grep -Fc 'blockType: "faq"' payload-types.ts && grep -Fc 'phase8_faq_block' src/migrations/index.ts && ls src/migrations/*phase8_faq_block.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'slug: "faq"' src/blocks/Faq.ts` returns `1`; the file declares exactly two top-level fields (`sectionTitle`, `items`) and the `items` array's sub-fields are `question` (text, required) and `answer` (textarea, required).
    - `grep -Fc 'localized' src/blocks/Faq.ts` returns `0` (localization cascades from `Pages.layout`, never re-set on block fields).
    - `grep -Fc 'export { Faq }' src/blocks/index.ts` returns `1`.
    - `grep -Fc 'Faq' src/collections/Pages.ts` returns `>= 2` (present in both the import list and the `layout.blocks` array).
    - `npx payload generate:types` exits 0 and `grep -Fc 'blockType: "faq"' payload-types.ts` returns `>= 1`.
    - `ls src/migrations/*phase8_faq_block.ts` matches exactly one file, and `grep -Fc 'phase8_faq_block' src/migrations/index.ts` returns `>= 1`.
    - The new migration's `up()` contains `CREATE TABLE` statements and contains zero `DROP TABLE` and zero `ALTER TABLE` statements against pre-existing tables — verify by reading the generated file; `grep -c 'DROP TABLE' src/migrations/*phase8_faq_block.ts` returns `0` for the `up()` body.
    - `git diff --stat` shows no modification to any pre-existing migration file under `src/migrations/`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The `faq` block config exists, is exported from the barrel, is selectable in `Pages.layout`, appears as a `blockType: "faq"` union member in regenerated `payload-types.ts`, and a strictly-additive committed Postgres migration covers its tables.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Accordion primitive, build FaqBlock, register it in BLOCK_MAP, extend the int tests</name>
  <files>src/components/ui/accordion.tsx, src/components/blocks/FaqBlock.tsx, src/components/blocks/RenderBlocks.tsx, tests/int/blocks-placeholder.spec.ts</files>
  <read_first>
    - `src/components/blocks/TestimonialsBlock.tsx` — the renderer template to mirror: async server component, `getTranslations("blocks")`, `Extract<...>` prop narrowing, the section wrapper string, the `mx-auto max-w-[1280px]` inner div, the optional `sectionTitle` heading, and the zero-items `t("emptyState")` fallback.
    - `src/components/ui/sheet.tsx` line 5 — the project's Radix import convention: named primitive pulled from the unified `"radix-ui"` package, NOT from a per-primitive `@radix-ui/react-*` package.
    - `src/components/blocks/RenderBlocks.tsx` — the `BLOCK_MAP` object and the import block; also `sectionBg`, which `FaqBlock` imports.
    - `tests/int/blocks-placeholder.spec.ts` lines 1-40 and 92-160 — the `next-intl/server` mock, the `@/components/blocks/RenderBlocks` mock supplying the real `sectionBg`, the dynamic `await import(...)` pattern, and the Phase 7 describe-block style to copy for the FAQ cases.
    - `08-UI-SPEC.md` **Contract §6** (renderer, primitive, no-card-wrapper and empty-state bullets), the **RTL & Locale Contract** row for FAQ, and the **Registry Safety** table.
  </read_first>
  <action>
    1. Generate the Accordion primitive: `npx shadcn@latest add accordion`. It writes `src/components/ui/accordion.tsx` (official shadcn registry — no vetting gate required per 08-UI-SPEC Registry Safety).

    2. **No new dependency may be introduced.** `radix-ui@1.6.2` is already installed and already exports `Accordion` (verified: `sheet.tsx` uses the same unified-package convention). After the generator runs, check `git diff --stat package.json package-lock.json`. If either changed — i.e. the generator added a per-primitive `@radix-ui/react-accordion` package — revert both files, reinstall nothing, and rewrite the primitive's import to pull `Accordion as AccordionPrimitive` from `"radix-ui"` exactly as `sheet.tsx` does for Dialog.

    3. **RTL correction (mandatory before commit).** The stock shadcn `AccordionTrigger` ships a physical-direction text-alignment utility in its class string. Replace it with the logical equivalent `text-start`. Then run `npm run lint:rtl` and fix any other physical-direction utility the generator emitted (`ml-*`, `mr-*`, `pl-*`, `pr-*`, `left-*`, `right-*`, `border-l*`, `border-r*`) with its logical counterpart. Change nothing else in the generated file — the chevron's `rotate-180` on `data-state=open` is a transform and is already direction-safe; leave the generator's focus-ring and animation classes as shipped (they are outside Contract §4's scope, which is scoped to `button.tsx`).

    4. Create `src/components/blocks/FaqBlock.tsx`:
       - `export async function FaqBlock({ block, index }: { block: FaqData; index: number })` where `FaqData = Extract<NonNullable<Page["layout"]>[number], { blockType: "faq" }>`.
       - `const t = await getTranslations("blocks")`; `const items = block.items ?? []`.
       - Section wrapper exactly: `` `${sectionBg(index)} px-md py-2xl md:px-lg md:py-3xl xl:px-xl xl:py-4xl` `` with an inner `<div className="mx-auto max-w-[1280px]">`, matching every other block.
       - Optional `sectionTitle` heading using the same `mb-lg text-center text-heading font-semibold` treatment the sibling blocks use.
       - Zero items → render the shared `t("emptyState")` fallback paragraph, styled like the sibling blocks. Never render an empty accordion shell.
       - Otherwise render `<Accordion type="single" collapsible>` with one `AccordionItem` per entry, `value` derived from the index (e.g. `item-{i}`), the question in `AccordionTrigger` and the answer in `AccordionContent`. **No item is pre-opened** — do not pass `defaultValue`.
       - **No card wrapper.** Do not wrap the accordion in `rounded-card`/`shadow-card`/`border` — Contract §6 specifies the accordion's own row dividers, following the StatsBandBlock lightweight-band precedent.
       - Constrain the accordion's readable width with a logical, direction-safe centering utility (`mx-auto` plus a max-width) — no physical-direction utilities anywhere in this file.
       - Head comment in the project's citation style referencing `08-UI-SPEC Contract §6` and `08-CONTEXT.md` (FAQ scope resolved to build).

    5. Register in `src/components/blocks/RenderBlocks.tsx`: import `FaqBlock` alongside the other renderer imports and add `faq: FaqBlock` to `BLOCK_MAP`. The key must be the literal `faq`, matching the config slug.

    6. Extend `tests/int/blocks-placeholder.spec.ts` following the existing Phase 7 pattern: add `const { FaqBlock } = await import("@/components/blocks/FaqBlock");` to the dynamic-import group, and a new `describe` with at least two cases — (a) a zero-items block renders the shared empty-state text and does not throw, (b) a populated block renders both a long question string and a long answer string. Reuse the existing `EMPTY_STATE_TEXT` constant and the existing mocks; add no new mock for `radix-ui`.
  </action>
  <verify>
    <automated>test -f src/components/ui/accordion.tsx && test "$(grep -Fc 'text-left' src/components/ui/accordion.tsx)" = "0" && grep -Fc 'radix-ui' src/components/ui/accordion.tsx && test -z "$(git diff --stat package.json package-lock.json)" && grep -Fc 'type="single"' src/components/blocks/FaqBlock.tsx && test "$(grep -Fc 'defaultValue' src/components/blocks/FaqBlock.tsx)" = "0" && test "$(grep -Fc 'rounded-card' src/components/blocks/FaqBlock.tsx)" = "0" && grep -Fc 'faq: FaqBlock' src/components/blocks/RenderBlocks.tsx && npm run lint:rtl && npm run lint && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/accordion.tsx` exists and `grep -Fc 'radix-ui' src/components/ui/accordion.tsx` returns `>= 1`; `grep -Fc '@radix-ui/react-accordion' src/components/ui/accordion.tsx` returns `0`.
    - `git diff --stat package.json package-lock.json` is empty — zero new npm dependencies.
    - `grep -Fc 'text-left' src/components/ui/accordion.tsx` returns `0` and `npm run lint:rtl` exits 0 (the generator's physical-direction utility was converted).
    - `grep -Fc 'type="single"' src/components/blocks/FaqBlock.tsx` returns `1` and `grep -Fc 'collapsible' src/components/blocks/FaqBlock.tsx` returns `>= 1`.
    - `grep -Fc 'defaultValue' src/components/blocks/FaqBlock.tsx` returns `0` (no item pre-opened, per Contract §6).
    - `grep -Fc 'rounded-card' src/components/blocks/FaqBlock.tsx` returns `0` and `grep -Fc 'shadow-card' src/components/blocks/FaqBlock.tsx` returns `0` (no card wrapper).
    - `grep -Fc 'emptyState' src/components/blocks/FaqBlock.tsx` returns `>= 1`.
    - `grep -Fc 'px-md py-2xl md:px-lg md:py-3xl xl:px-xl xl:py-4xl' src/components/blocks/FaqBlock.tsx` returns `1` (identical rhythm string to the sibling blocks).
    - `grep -Fc 'faq: FaqBlock' src/components/blocks/RenderBlocks.tsx` returns `1`.
    - `grep -Fc 'FaqBlock' tests/int/blocks-placeholder.spec.ts` returns `>= 2` and `npm test` exits 0 with the new FAQ cases passing.
    - `npm run lint`, `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>The Accordion primitive exists with zero new dependencies and zero physical-direction utilities, `FaqBlock` renders a single-collapsible, nothing-pre-opened, card-less accordion with the shared empty-state fallback and the standard section rhythm, it is registered under `BLOCK_MAP.faq`, and int tests cover both its empty and populated states.</done>
</task>

<task type="auto">
  <name>Task 3: Seed a real FAQ section onto the Contact page</name>
  <files>src/lib/seed-content.ts</files>
  <read_first>
    - `src/lib/seed-content.ts` lines 40-80 — the block-helper style (`featureGrid`, `statsBand`, `trustBar`, `exportProcess`, `testimonials`): a small factory returning `{ blockType: "<slug>" as const, ...fields }` with a head comment citing its UI-SPEC section and any placeholder-honesty caveat.
    - `src/lib/seed-content.ts` lines 443-455 — the seeded `contact` page (`compactHero(...)` + `contactBlock()`, no CTABand because the page is the destination). The FAQ section is appended here.
    - `scripts/seed-pages.ts` lines 60-80 and 165-180 — confirms only `mediaGallery` and photo-variant `featureGrid` blocks get post-processing; a plain `faq` block needs no seed-script change.
    - `08-UI-SPEC.md` **Copywriting Contract** (FAQ row: `sectionTitle` is CMS-editable, example placeholder "Frequently Asked Questions"; the empty state reuses the existing shared key and no new empty-state string is invented).
  </read_first>
  <action>
    1. Add a `faq(sectionTitle: string, items: { question: string; answer: string }[])` helper to `src/lib/seed-content.ts` in the same one-line-factory style as `testimonials(...)`, returning `{ blockType: "faq" as const, sectionTitle, items }`. Head comment cites `08-UI-SPEC Contract §6` and notes that the seeded answers restate only already-published process facts.

    2. Append one `faq(...)` call to the seeded `contact` page's `layout` array, after `contactBlock()`, with `sectionTitle` "Frequently Asked Questions" and exactly these four question/answer pairs (placeholder-honesty discipline: these state process only — do not invent certifications, volumes, prices, lead times, or client names):
       - Q "What should I include in my first inquiry?" — A "Tell us the product, your destination country, the volume you need, and your preferred incoterm. That is enough for our export team to come back with an indicative quotation."
       - Q "How quickly will I hear back?" — A "Our export team responds to every inquiry within one business day."
       - Q "Can you provide export documentation for customs clearance?" — A "Yes. Every shipment travels with the documentation set agreed at order confirmation, prepared for your destination market's requirements."
       - Q "Do you send samples before a first order?" — A "Yes, samples can be arranged for qualified buyers. Mention it in your inquiry and include the delivery address."

    3. Change nothing else in `seed-content.ts` — no edits to other pages, no edits to `HOME_EN_SEED`, and no change to `scripts/seed-pages.ts` (a plain block needs no post-processing).

    4. The seed is English-only, consistent with every other seeded page (`PAGES_EN_SEED`); other locales fall back to English per the Phase 1 fallback rule until a human translation is authored in /admin.
  </action>
  <verify>
    <automated>grep -Fc 'blockType: "faq"' src/lib/seed-content.ts && grep -Fc 'Frequently Asked Questions' src/lib/seed-content.ts && grep -Fc 'within one business day' src/lib/seed-content.ts && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'blockType: "faq"' src/lib/seed-content.ts` returns `1` (the single `faq()` helper).
    - `grep -Fc 'faq(' src/lib/seed-content.ts` returns `>= 2` (helper declaration + one call on the contact page).
    - `grep -Fc 'Frequently Asked Questions' src/lib/seed-content.ts` returns `1`.
    - The seeded FAQ sits inside the `contact` page's `layout` array (the entry after `contactBlock()`), verified by reading lines around the `slug: "contact"` block — no other page's layout changed.
    - `git diff --stat scripts/seed-pages.ts` is empty (no seed-script change needed).
    - `git diff --stat src/lib/seed-content.ts` is the only seed-related change; `HOME_EN_SEED` is untouched.
    - `npm run typecheck` exits 0 (the `faq()` return type satisfies the regenerated `Page["layout"]` union from Task 1).
    - `npm run lint` and `npm test` exit 0.
  </acceptance_criteria>
  <done>`seed-content.ts` exposes a `faq()` block helper and the seeded Contact page carries a four-item FAQ section whose answers restate only already-published process facts; no other page or seed script changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code generator → repo (`npx shadcn@latest add accordion`) | A remote generator writes a source file and may mutate `package.json`/lockfile — an unvetted dependency could enter the build here. |
| CMS editor → FaqBlock render | Editor-authored `question`/`answer` strings are rendered inside the accordion. |
| Migration → prod Postgres (Neon) | The committed migration runs automatically in the Vercel build via `vercel.json`'s `payload migrate` step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-SC | Tampering | `npx shadcn@latest add accordion` — supply chain | high | mitigate | Official shadcn registry only (08-UI-SPEC Registry Safety: no vetting gate for official registry; zero third-party registries used). Hard gate: `git diff --stat package.json package-lock.json` must be EMPTY — `radix-ui@1.6.2` already exports `Accordion`, so no package install is authorized. If the generator adds one, revert both files and rewrite the import to the existing unified `radix-ui` package. No `[ASSUMED]`/`[SUS]` package enters the tree, so no legitimacy checkpoint is required. |
| T-08-03-01 | Tampering | Generated Postgres migration `<ts>_phase8_faq_block` | high | mitigate | Migration must be strictly additive (`CREATE TABLE` for the new faq block/array tables only). The generated diff is reviewed before commit and any unrelated pre-existing schema drift is stripped from both `up()` and `down()` — the exact failure the Phase 7 executor hit. Acceptance criteria assert zero `DROP TABLE` and no modification to any already-committed migration file. |
| T-08-03-02 | Tampering | Generated `accordion.tsx` source | medium | mitigate | The generated file is read before commit; only the RTL correction is applied. `npm run lint`, `npm run lint:rtl` and `npm run typecheck` gate it, and its import source is asserted to be the already-vendored `radix-ui` package. |
| T-08-03-03 | Information Disclosure | Editor-authored FAQ question/answer content | low | accept | Rendered as React text nodes (auto-escaped); `FaqBlock` uses no `dangerouslySetInnerHTML` and no rich-text serializer. FAQ is a block on `Pages`, which already gates create/update/delete behind `Boolean(user)` — no new access surface. |
| T-08-03-04 | Spoofing | Seeded FAQ answers making unverifiable business claims | medium | mitigate | Seed copy is constrained by acceptance criteria to process statements already published elsewhere on the site (response time, documentation set, samples); inventing certifications, volumes, prices or client names is explicitly prohibited, following the Phase 7 Pitfall-5 placeholder-honesty discipline. |
</threat_model>

<verification>
- `npx payload generate:types` succeeds; `payload-types.ts` contains the `blockType: "faq"` union member.
- One new `src/migrations/*phase8_faq_block.{ts,json}` pair exists, registered in `src/migrations/index.ts`, additive-only, with no pre-existing migration modified.
- `git diff --stat package.json package-lock.json` is empty — zero new dependencies.
- `npm run lint`, `npm run lint:rtl`, `npm run typecheck` all exit 0; `npm test` green including the new FaqBlock empty + populated cases.
- Slug consistency confirmed across all four registration points (`Faq.ts` slug, `Pages.layout.blocks`, `BLOCK_MAP` key, `payload-types.ts` literal).
- **Human-needed:** live render of the FAQ on `/en/contact` and `/ar/contact` (RTL chevron/direction) is a blocking checkpoint in plan `08-04` (Wave 2), per 08-UI-SPEC's "verify at build time, don't assume" instruction for the accordion chevron under RTL.
</verification>

<success_criteria>
- An editor can add an FAQ section to any page from /admin, with question/answer pairs, localized per Pages.layout.
- The FAQ renders as a single-collapsible, nothing-pre-opened, card-less accordion with the standard section rhythm and the shared empty-state fallback.
- Zero new npm dependencies; zero physical-direction utilities; lint, lint:rtl, typecheck and vitest all green.
- A strictly-additive Postgres migration is committed so prod picks the tables up on next deploy.
- The seeded Contact page carries a live, honest four-item FAQ.
</success_criteria>

<output>
Create `.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-03-SUMMARY.md` when done.
</output>
