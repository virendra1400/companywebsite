# Deferred Items — Phase 08

Out-of-scope discoveries logged during execution, not fixed per the executor scope boundary rule (only auto-fix issues directly caused by the current task's changes).

| Plan | Discovered during | File | Issue | Status |
|------|-------------------|------|-------|--------|
| 08-01 | Task 2 verify (`npm run lint`) | `src/app/(site)/[locale]/insights/not-found.tsx` | 4x `@next/next/no-html-link-for-pages` errors (`<a>` instead of `next/link`) — pre-existing, confirmed present on baseline commit before this plan's edits (`git stash` diff check) | Not fixed — out of scope for 08-01, which only touches FeatureGridBlock/SpecTable/StatsBandBlock/ExportMapBlock |
| 08-01 | Task 2 verify (`npm run lint`) | `src/components/blocks/RenderBlocks.tsx` | 1x `@typescript-eslint/no-explicit-any` error — pre-existing, confirmed present on baseline commit before this plan's edits | Not fixed — out of scope for 08-01 |
| 08-04 | Task 1 gate suite (`npm run lint`) | `src/app/(site)/[locale]/insights/not-found.tsx`, `src/components/blocks/RenderBlocks.tsx` | Same 5 pre-existing lint errors logged above under 08-01 — reconfirmed present on the merged Wave 1 tree; `npm run lint` on the full phase diff still reports exactly these 5 errors, none in Phase 8 files | Not fixed — out of scope for 08-04 (verification-only plan, no source files change here) |
| 08-04 | Task 1 gate suite (`npm run test:e2e`, isolated rerun) | `src/components/chrome/GlobalFooter.tsx` | `responsive-rtl.spec.ts` "sm (ltr) — header/footer/hero render without overflow" fails deterministically (reproduced in single-worker isolation): two footer legal-links (`text-neutral-100 underline-offset-4 hover:text-accent-600 hover:underline`) overflow the 640px viewport by ~10-83px. Confirmed pre-existing via `git log ca33ce5..HEAD -- src/components/chrome/GlobalFooter.tsx` (zero commits since the Phase 8 baseline) | Not fixed — out of scope for 08-04, unrelated file not in any Wave 1 plan's `files_modified` |
| 08-04 | Task 1 gate suite (`npm run test:e2e`) | `src/components/blocks/ContactForm.tsx` | `contact-error-state.spec.ts` (3 cases, both en/ar projects) time out on `getByLabel("Company", { exact: true })`. Traced via direct DOM inspection: the required-field asterisk markup (`<span aria-hidden="true">*</span>`, added by quick task 260725-69i on 2026-07-25, predating Phase 8) makes the label's computed accessible name not equal the exact string "Company" (`getByLabel` non-exact finds 2 matches — "Company" + "Company Website" — exact finds 0). `ContactForm.tsx` is not in any Wave 1 plan's `files_modified` | Not fixed — out of scope for 08-04, pre-existing test/markup mismatch from a prior quick task |
| 08-04 | Task 1 gate suite (`npm run test:e2e`) | `tests/e2e/nav-links.spec.ts` | Footer-nav-links case times out intermittently (hit `/manufacturing` in one run, `/company` in another) — the test performs 7+ sequential `page.goto` calls inside one 30s test budget, a pre-existing test-design fragility unrelated to any Phase 8 file. `language-switcher.spec.ts` and `locale-routing.spec.ts` failed in the full parallel run but passed clean on an isolated low-concurrency rerun — confirmed dev-server-contention flakiness, not a regression | Not fixed — environmental/pre-existing, not caused by Phase 8 changes |
