---
phase: 08-component-polish-pass-apply-amended-design-system-across-car
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified: []
requirements: [POLISH-VR]
autonomous: false
must_haves:
  truths:
    - "The three Wave 1 plans' merged output passes the full automated gate suite together, not just individually: lint, lint:rtl, typecheck, the vitest suite, a production Next build, and the existing Playwright e2e suite."
    - "A human has confirmed on a live render that both dark-surface secondary CTAs (HeroBlock and CTABandBlock) still show a white 1px outline over a genuinely transparent interior — the hero photo/gradient visible through the button, not an opaque panel — closing the one rendering-path change in Contract §4 that source greps cannot prove."
    - "A human has confirmed a visible gold focus-visible ring on keyboard Tab at all 8 Contract §4 Button call sites, since the ring moved from per-call-site overrides into the base cva and this repo has no automated a11y or visual-regression harness."
    - "A human has confirmed the FAQ accordion renders correctly in both LTR (/en/contact) and RTL (/ar/contact) — including the chevron behavior 08-UI-SPEC's RTL contract explicitly says to verify at build time rather than assume."
    - "A human has confirmed the card and digit fixes landed visually: FeatureGrid and SpecTable cards carry the hairline border + soft resting shadow matching product cards, and StatsBand/ExportMap/SpecTable digits are equal-width so numeric columns align."
    - statement: "Visual regression across all 8 Button call sites, the two restyled card families, the three tabular-figure sites, and the new FAQ block — no automated visual-regression test exists in this repo (the same gap Phase 6 recorded for its own card-recipe change)."
      verification: backstop
  artifacts:
    - path: ".planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-04-SUMMARY.md"
      provides: "Recorded gate results plus the human sign-off (or the defect list that blocks it)"
      contains: "08-04"
  key_links:
    - "This plan owns the '🧪 backstop, human-needed' row from 08-UI-SPEC's UI Considerations table. Plans 08-01/08-02/08-03 are source-complete but NOT sign-off-complete until this checkpoint passes — treating their greens as phase completion would silently skip the one check the design contract flagged as needing a human."
    - "The dev server must serve seeded content for the FAQ route to exist: run `npm run db:seed` first if /en/contact has no FAQ section, because plan 08-03 seeds it into src/lib/seed-content.ts rather than authoring it in /admin."
---

<objective>
Verify the merged Phase 8 output as one system: run the full automated gate suite across all three Wave 1 plans together, then run the blocking human live-render check that 08-UI-SPEC explicitly flagged as needed for the Button consolidation and the new FAQ accordion.

Purpose: 08-UI-SPEC's UI Considerations table marks exactly one item `🧪 backstop` — the Button consolidation's visual regression — because `outlineOnDark` is a genuine rendering-path change (an explicit transparent background replacing an implicit tailwind-merge suppression) and this repo has no visual-regression harness. The FAQ's RTL chevron carries the same "verify at build time, don't assume" instruction. Neither can be closed by grep. This plan is where the phase actually gets signed off.

Output: `08-04-SUMMARY.md` recording gate results and the human verdict. No source files change here — if the checkpoint finds defects, they are fixed in the owning plan's files and re-verified, not patched from this plan.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-UI-SPEC.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-01-SUMMARY.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-02-SUMMARY.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-03-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase-wide symbol list (this plan creates no source symbols — it verifies all of them):

| Symbol / file | Kind | Owning plan |
|---------------|------|-------------|
| `FeatureGridBlock` / `SpecTable` card hairline recipe | modified class strings | P01 |
| `SpecTable` / `StatsBandBlock` / `ExportMapBlock` `tabular-nums` | modified class strings | P01 |
| `buttonVariants` base cva — accent focus ring as the default | modified primitive | P02 |
| `buttonVariants` `default` variant — brand hover baked in | modified primitive | P02 |
| `buttonVariants.variant.outlineOnDark` | **new Button variant** | P02 |
| `src/components/ui/accordion.tsx` — `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent` | **new shadcn primitive** | P03 |
| `src/blocks/Faq.ts` — `Faq` (Payload `Block`, slug `faq`) | **new file** | P03 |
| `src/components/blocks/FaqBlock.tsx` — `FaqBlock` | **new file** | P03 |
| `RenderBlocks.tsx` — `BLOCK_MAP.faq` entry | modified registry | P03 |
| `payload-types.ts` — `blockType: "faq"` union member | regenerated types | P03 |
| `src/migrations/<ts>_phase8_faq_block.ts` / `.json` | **new migration** | P03 |
| `src/lib/seed-content.ts` — `faq()` helper + Contact-page FAQ section | new seed content | P03 |
| `08-04-SUMMARY.md` — gate results + human sign-off | **new artifact** | **P04** |

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate suite on the merged Wave 1 output</name>
  <files>(none — verification only)</files>
  <read_first>
    - `08-01-SUMMARY.md`, `08-02-SUMMARY.md`, `08-03-SUMMARY.md` — what each plan actually changed, including any deviations recorded during execution.
    - `package.json` scripts block — the exact gate commands (`lint`, `lint:rtl`, `typecheck`, `test`, `test:e2e`, `build`, `db:seed`).
    - `08-UI-SPEC.md` **RTL & Locale Contract** — the `lint:rtl` must-stay-green requirement covering all of §1-§4 plus the generated accordion.
  </read_first>
  <action>
    Run each gate on the merged working tree and record actual output in the summary. Do not fix anything from this plan — if a gate fails, identify the owning plan (01/02/03), fix it in that plan's declared files, and re-run the full suite.

    1. `npm run lint` — expect 0 errors.
    2. `npm run lint:rtl` — expect green. This is the gate most at risk: plan 08-03 generates `src/components/ui/accordion.tsx` from a remote generator that ships a physical-direction text-alignment utility by default.
    3. `npm run typecheck` — expect exit 0. This is what proves `outlineOnDark` is a real member of the Button variant union at both consumer call sites, and that `FaqBlock`'s `Extract<..., { blockType: "faq" }>` resolves against the regenerated `payload-types.ts`.
    4. `npm test` — full vitest suite, including plan 08-03's new FaqBlock empty + populated cases.
    5. `npm run build` — a production Next build. This exercises the Payload config with the new `faq` block registered. If the environment lacks the `.env` / local database the build needs, report that as a blocker in the summary — do not record the gate as passed and do not skip it silently.
    6. `npm run db:seed` then `npm run test:e2e` — the existing Playwright suite covers the surfaces this phase touched (`homepage.spec.ts`, `chrome-consistency.spec.ts`, `product-detail.spec.ts`, `rtl-arabic.spec.ts`, `responsive-rtl.spec.ts`, `whatsapp-header-cta.spec.ts`), so it is the closest thing to a regression net for the Button consolidation. Seeding first is what makes the FAQ section exist on `/en/contact` for the Task 2 checkpoint. If browsers cannot be installed/run in this environment, report it as a blocker in the summary rather than silently omitting the gate.
    7. Confirm the phase diff stayed inside the declared file set: `git diff --name-only` against the phase start must contain only the files listed in the three Wave 1 plans' `files_modified` (plus the two new migration files and `08-*-SUMMARY.md` artifacts). Flag anything else.
  </action>
  <verify>
    <automated>npm run lint && npm run lint:rtl && npm run typecheck && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npm run lint:rtl` exits 0.
    - `npm run typecheck` exits 0.
    - `npm test` exits 0 and its output includes the new FaqBlock cases.
    - `npm run build` exits 0, or the summary records a concrete environment blocker (missing `.env`/database) with the failing output quoted — never an unexplained skip.
    - `npm run test:e2e` exits 0, or the summary records a concrete environment blocker with the failing output quoted — never an unexplained skip.
    - `git diff --name-only` (phase start → now) lists only files declared in 08-01/08-02/08-03 `files_modified`, the generated `src/migrations/*phase8_faq_block.{ts,json}` pair, and `.planning/` artifacts.
    - `git diff --stat package.json package-lock.json` is empty across the whole phase — zero new dependencies were introduced.
  </acceptance_criteria>
  <done>Every automated gate has been run against the merged Wave 1 output with its real result recorded, and the phase diff is confined to the declared file set with no new dependencies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-render sign-off on the Button consolidation, card recipe, tabular figures, and FAQ</name>
  <files>(none — human verification)</files>
  <read_first>
    - `08-UI-SPEC.md` **Contract §4 "Verification note (backstop, human-needed)"** and the **UI Considerations** row `Visual regression (button consolidation)` — the exact reason this checkpoint exists.
    - `08-UI-SPEC.md` **RTL & Locale Contract**, FAQ row — the accordion chevron under RTL must be verified, not assumed.
  </read_first>
  <action>
    Stop and hand control to the human. Present the `<what-built>` summary and the numbered `<how-to-verify>` steps below verbatim, then wait for the `<resume-signal>`. Do not self-approve, do not infer a pass from green automated gates, and do not edit source from this task — this checkpoint exists precisely because the `outlineOnDark` rendering-path change, focus-ring visibility and RTL accordion behavior are unprovable by grep. If the human reports a defect, record it in `08-04-SUMMARY.md`, name the owning Wave 1 plan and file, fix it there, re-run Task 1's full gate suite, and re-present this checkpoint.
  </action>
  <what-built>
    Wave 1 delivered: (a) the hairline card recipe on FeatureGrid and SpecTable, (b) `tabular-nums` on StatsBand, ExportMap and SpecTable figures, (c) the Button consolidation — brand hover and full-opacity accent focus ring moved into `src/components/ui/button.tsx`, plus a new `outlineOnDark` variant with an explicit transparent background replacing the previously implicit override at the Hero and CTA-band secondary CTAs, (d) the new FAQ block (Payload config, shadcn Accordion primitive, renderer, registration, migration) seeded onto the Contact page.

    Everything above is source-verified by greps and by lint/typecheck/test/build/e2e. The one thing automation cannot prove is what it looks like: `outlineOnDark` is a real rendering-path change, the focus ring moved between layers, and this repo has no visual-regression or a11y harness.
  </what-built>
  <how-to-verify>
    Start the app and, if the Contact page has no FAQ yet, seed first:

    1. `npm run db:seed` (only if `/en/contact` shows no FAQ section), then `npm run dev`.

    **A. Dark-surface secondary CTAs — the actual backstop item**

    2. Open http://localhost:3000/en. In the hero, look at the secondary (WhatsApp) button next to the primary CTA. Expected: a thin WHITE outline with a fully TRANSPARENT interior — the hero photograph/gradient must be visible through the button. A solid or tinted panel behind the label is a FAIL (it would mean the base `outline` variant's opaque background is still painting).
    3. Hover that button. Expected: a subtle white 10% wash appears; it must not become opaque.
    4. Scroll to the dark closing CTA band near the bottom of the same page and repeat checks 2-3 on its secondary button.

    **B. Focus rings — all 8 Contract §4 call sites**

    5. From the top of http://localhost:3000/en press Tab repeatedly. Expected: a clearly visible ~3px GOLD ring on the header "Get a Quote" button, the header WhatsApp icon button, the hero primary CTA, the hero secondary CTA, and both CTA-band buttons. No button may focus with an invisible or washed-out ring.
    6. Narrow the window to mobile width, open the mobile nav sheet, and Tab to its quote CTA — same gold ring expected.
    7. Open any product page (http://localhost:3000/en/products/ — pick a product from the list) and Tab to the RFQ button — same gold ring expected. Also confirm the primary buttons still hover to a lighter green than their resting color.

    **C. Card recipe and tabular figures**

    8. On http://localhost:3000/en, look at the "why buyers choose us" feature-card grid. Expected: each card now has rounded corners, a hairline grey border and a soft resting shadow — visually the same treatment as the product cards. Expected: NO lift/hover animation on these cards (that is reserved for whole-card links).
    9. On the same page, look at the stats band and the export-map stat tiles. Expected: the digits are equal-width, so figures stack in a straight column instead of jittering (compare a value containing a "1" against one containing an "8").
    10. On the product page from step 7, look at the specifications panel. Expected: rounded corners + hairline border + soft shadow, on its light-grey panel background (grey is intentional, not a bug), and the spec values' digits are equal-width.

    **D. FAQ block, LTR then RTL**

    11. Open http://localhost:3000/en/contact and scroll to the FAQ. Expected: four questions, ALL collapsed on load. Click one — it expands and its chevron rotates. Click a second — the first collapses (single-open mode). Click the open one again — it collapses (nothing stays forced open). No card border/shadow box around the accordion; row dividers only.
    12. Open http://localhost:3000/ar/contact. Expected: the FAQ question text starts at the RIGHT edge, the chevron sits on the LEFT edge, expand/collapse and chevron rotation still work, and there is no horizontal scrollbar or clipped text at any width.
    13. Resize to mobile width on both `/en/contact` and `/ar/contact` and confirm no overflow and no clipped question text.
  </how-to-verify>
  <verify>
    <human-check>All 13 steps above observed as described, with A2/A4 (transparent interior) and D12 (RTL FAQ) explicitly confirmed.</human-check>
    <automated>npm run lint && npm run lint:rtl && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Steps A2 and A4: both dark-surface secondary CTAs render with a visible white outline and a transparent interior showing the background through them — explicitly confirmed, not assumed.
    - Steps B5-B7: a visible gold focus ring appears on all 8 Contract §4 call sites under keyboard Tab.
    - Steps C8-C10: FeatureGrid cards and the SpecTable panel show rounded corners + hairline border + resting shadow; FeatureGrid cards have no hover lift; stat and spec digits are equal-width.
    - Steps D11-D13: the FAQ loads fully collapsed, behaves as single-open collapsible, has no card wrapper, and renders correctly right-aligned in Arabic with no overflow at mobile width.
    - The human response is recorded verbatim in `08-04-SUMMARY.md`. Any FAIL is written up as a defect naming the owning plan (08-01/08-02/08-03) and the specific file, and the phase is NOT signed off until it is fixed and re-verified.
  </acceptance_criteria>
  <resume-signal>Type "approved" to sign off Phase 8, or describe exactly what looked wrong and on which step.</resume-signal>
  <done>A human has confirmed all 13 checks on a live render, or has reported defects that have been fixed in their owning plan's files and re-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated gates → phase sign-off | Green greps and green unit tests do not prove rendered output; treating them as sufficient is exactly the failure this plan exists to prevent. |
| Keyboard/RTL user → shipped UI | Focus-ring visibility and RTL correctness have no automated coverage in this repo and are only observable by a human. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering | Merged Wave 1 output across all six touched subsystems | medium | mitigate | Full gate suite (lint, lint:rtl, typecheck, vitest, production build, Playwright e2e) is run on the merged tree, plus a `git diff --name-only` confinement check against the three plans' declared `files_modified`. |
| T-08-04-02 | Repudiation | Keyboard focus indicator after the base-cva ring relocation | medium | mitigate | Blocking human checkpoint step B explicitly tabs to all 8 Contract §4 call sites; a missing or washed-out ring is a FAIL that blocks sign-off, since no automated a11y harness exists here. |
| T-08-04-03 | Denial of Service | RTL layout of the newly generated accordion | medium | mitigate | Checkpoint step D12/D13 requires the Arabic FAQ to render right-aligned with no horizontal overflow or clipped text at mobile width — the "verify at build time, don't assume" instruction from 08-UI-SPEC's RTL contract. |
| T-08-04-04 | Tampering | Silent gate skipping (build/e2e unavailable in the environment) | medium | mitigate | Acceptance criteria forbid an unexplained skip: any unrunnable gate must be recorded in the summary as a named environment blocker with the failing output quoted. |
</threat_model>

<verification>
- Every gate in Task 1 has a recorded, real result (pass, or a quoted environment blocker).
- The Task 2 checkpoint returned an explicit human verdict, recorded verbatim in the summary.
- No source file was modified from this plan; any defect was fixed in its owning Wave 1 plan and the full suite re-run.
- The 08-UI-SPEC `🧪 backstop` row and the FAQ RTL-chevron instruction are both explicitly closed.
</verification>

<success_criteria>
- lint, lint:rtl, typecheck, vitest, production build and Playwright e2e all green on the merged tree (or blockers explicitly documented).
- Human sign-off recorded covering: transparent-interior dark CTAs, gold focus rings at all 8 call sites, hairline cards, equal-width digits, and the FAQ in both LTR and RTL.
- Phase diff confined to the declared file set; zero new npm dependencies across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-04-SUMMARY.md` when done.
</output>
