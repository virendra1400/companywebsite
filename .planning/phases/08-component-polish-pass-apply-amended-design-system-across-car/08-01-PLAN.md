---
phase: 08-component-polish-pass-apply-amended-design-system-across-car
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/blocks/FeatureGridBlock.tsx
  - src/components/products/SpecTable.tsx
  - src/components/blocks/StatsBandBlock.tsx
  - src/components/blocks/ExportMapBlock.tsx
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05]
autonomous: true
must_haves:
  truths:
    - "FeatureGridBlock's feature cards render with the same hairline card recipe already shipped on ProductCard/InsightCard/CertCard/TestimonialsBlock — rounded-card radius, 1px neutral-300 border, white surface, shadow-card resting elevation — instead of the shadcn primitive defaults rounded-xl/shadow-sm (08-UI-SPEC Component Audit #1 / Contract §1)."
    - "SpecTable's data panel renders with the same rounded-card/neutral-300 border/shadow-card recipe while keeping its deliberate bg-neutral-100 data-panel surface (08-UI-SPEC Component Audit #2 / Contract §2)."
    - "Every stat figure and spec value on the site renders with fixed-width (tabular) digits so columns of numbers align: StatsBandBlock stat values, ExportMapBlock StatTiles values, and SpecTable <dd> value cells (08-UI-SPEC Component Audit #3/#4/#5 — the tabular-nums wiring both 06-CONTEXT.md and 07-CONTEXT.md explicitly deferred to Phase 8)."
    - "Populated state verified: the hairline recipe applied here is the identical class string already visually verified on ProductCard/InsightCard/CertCard/TestimonialsBlock in Phase 6/7 — a mechanical convergence, not a new visual design (08-UI-SPEC UI Considerations: Populated, covered)."
    - "Long-text state verified: tabular-nums only changes digit glyph width and never wraps or truncates non-numeric text, so it is safe on mixed numeric/text cells such as SpecTable's packaging value (08-UI-SPEC UI Considerations: Long-text, covered)."
    - "No new design token, no color change, no spacing/radius/shadow value change, no body-tier typography change — every class used here already exists in globals.css @theme or is native Tailwind v4. npm run lint:rtl stays green (zero physical-direction utilities introduced)."
  artifacts:
    - path: "src/components/blocks/FeatureGridBlock.tsx"
      provides: "Feature card converged on the Phase 6 hairline card recipe"
      contains: "gap-sm rounded-card border border-neutral-300 bg-white p-lg shadow-card"
    - path: "src/components/products/SpecTable.tsx"
      provides: "Data-panel card on the hairline recipe (bg-neutral-100 retained) + tabular-nums spec values"
      contains: "gap-0 rounded-card border border-neutral-300 bg-neutral-100 p-lg shadow-card"
    - path: "src/components/blocks/StatsBandBlock.tsx"
      provides: "Stat figures with fixed-width digits"
      contains: "text-display font-semibold tabular-nums text-primary-700"
    - path: "src/components/blocks/ExportMapBlock.tsx"
      provides: "StatTiles figures with fixed-width digits"
      contains: "text-display font-semibold tabular-nums text-primary-700"
  key_links:
    - "rounded-card / shadow-card / shadow-card-hover are @theme tokens declared in src/app/globals.css by Phase 6 — if the token names drift, the classes silently no-op and the cards fall back to unstyled corners with no build error. Verify visually in Wave 2 (08-04), not just by grep."
    - "tabular-nums is a native Tailwind v4 utility (font-variant-numeric), NOT a project token — it requires no globals.css entry and is direction-agnostic, so it does not interact with the Arabic numberingSystem:'latn' rule (both stay independently in force per 06-UI-SPEC RTL contract)."
    - "TestimonialsBlock.tsx already contains the identical hairline class string — it is the reference implementation; the file-scoped greps below must not be widened to repo scope or they will match it too."
---

<objective>
Converge the two card-family stragglers (`FeatureGridBlock`, `SpecTable`) onto the Phase 6 hairline card recipe, and wire the `tabular-nums` utility onto every numeric figure on the site (`StatsBandBlock`, `ExportMapBlock` StatTiles, `SpecTable` value cells) — the exact work that `06-CONTEXT.md` and `07-CONTEXT.md` deferred to this phase.

Purpose: These are five of the nine residual v1 inconsistencies the 08-UI-SPEC codebase audit found. Every fix is a verbatim class-string change against a component that already exists — no new visuals, no new tokens, no behavior change. Getting them done removes the last card-recipe divergence and the last proportional-digit stat figures from the site.

Output: Four modified `.tsx` files, all class-string-only diffs.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-CONTEXT.md
@.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-UI-SPEC.md
@src/components/blocks/TestimonialsBlock.tsx
</context>

## Artifacts this phase produces

Full phase-wide symbol list (this plan owns rows marked **P01**):

| Symbol / file | Kind | Owning plan |
|---------------|------|-------------|
| `FeatureGridBlock` card class string (hairline recipe) | modified class string | **P01** |
| `SpecTable` card class string (hairline recipe) | modified class string | **P01** |
| `SpecTable` `<dd>` `tabular-nums` | modified class string | **P01** |
| `StatsBandBlock` stat value `tabular-nums` | modified class string | **P01** |
| `ExportMapBlock` `StatTiles` value `tabular-nums` | modified class string | **P01** |
| `buttonVariants` base cva — `focus-visible:ring-accent-600` default | modified primitive | P02 |
| `buttonVariants` `default` variant — baked-in brand hover | modified primitive | P02 |
| `buttonVariants.variant.outlineOnDark` | **new Button variant** | P02 |
| `src/components/ui/accordion.tsx` (`Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent`) | **new shadcn primitive** | P03 |
| `src/blocks/Faq.ts` (`Faq` Payload Block, slug `faq`) | **new file** | P03 |
| `src/components/blocks/FaqBlock.tsx` (`FaqBlock`) | **new file** | P03 |
| `RenderBlocks.tsx` `BLOCK_MAP.faq` entry | modified registry | P03 |
| `src/migrations/<ts>_phase8_faq_block.ts/.json` | **new migration** | P03 |
| `seed-content.ts` `faq()` helper + Contact-page FAQ section | new seed content | P03 |

<tasks>

<task type="auto">
  <name>Task 1: Converge FeatureGridBlock and SpecTable cards on the Phase 6 hairline recipe</name>
  <files>src/components/blocks/FeatureGridBlock.tsx, src/components/products/SpecTable.tsx</files>
  <read_first>
    - `src/components/blocks/FeatureGridBlock.tsx` — the `<Card key={i} className="gap-sm p-lg">` at line 51 is the only line that changes.
    - `src/components/products/SpecTable.tsx` — the `<Card className="gap-0 bg-neutral-100 p-lg">` at line 25 is the only line that changes in this task.
    - `src/components/blocks/TestimonialsBlock.tsx` — the reference implementation of the recipe (its `<Card>` already carries the exact target class string for the white-surface case, including the resting-elevation-only treatment with no hover state).
    - `08-UI-SPEC.md` sections **Component Audit rows #1 and #2** and **Component Contracts §1 and §2** — the exact target class strings, to be applied verbatim.
  </read_first>
  <action>
    Apply the two Contract §1/§2 diffs verbatim. Do not restyle anything else in either file.

    1. `src/components/blocks/FeatureGridBlock.tsx` line 51 — change the `<Card>`'s className from `gap-sm p-lg` to `gap-sm rounded-card border border-neutral-300 bg-white p-lg shadow-card`. Keep `key={i}`. This is resting elevation ONLY — do not add any hover/`shadow-card-hover`/`group-hover` treatment; feature cards are not whole-card links, matching the TestimonialsBlock precedent (hover elevation is reserved for ProductCard/InsightCard).

    2. `src/components/products/SpecTable.tsx` line 25 — change the `<Card>`'s className from `gap-0 bg-neutral-100 p-lg` to `gap-0 rounded-card border border-neutral-300 bg-neutral-100 p-lg shadow-card`. `bg-neutral-100` MUST stay: SpecTable is deliberately a distinct data-panel surface, not a white content card — only radius/border/shadow converge on the shared recipe.

    3. Add a short source comment above each changed `<Card>` in the project's existing citation style, referencing the originating decision by identifier (`08-UI-SPEC Contract §1` / `§2`) rather than restating the class list. Do not paste the pre-change class string into any comment.

    4. Nothing else in either file changes — no layout, no grid, no typography, no spacing tokens, no new imports.
  </action>
  <verify>
    <automated>grep -Fc 'gap-sm rounded-card border border-neutral-300 bg-white p-lg shadow-card' src/components/blocks/FeatureGridBlock.tsx && grep -Fc 'gap-0 rounded-card border border-neutral-300 bg-neutral-100 p-lg shadow-card' src/components/products/SpecTable.tsx && npm run lint:rtl && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'gap-sm rounded-card border border-neutral-300 bg-white p-lg shadow-card' src/components/blocks/FeatureGridBlock.tsx` returns `1`.
    - `grep -Fc 'gap-0 rounded-card border border-neutral-300 bg-neutral-100 p-lg shadow-card' src/components/products/SpecTable.tsx` returns `1`.
    - `grep -Fc 'bg-neutral-100' src/components/products/SpecTable.tsx` is `>= 1` (the data-panel surface was not dropped).
    - `grep -Fc 'shadow-card-hover' src/components/blocks/FeatureGridBlock.tsx` returns `0` (resting elevation only, no hover treatment added).
    - `git diff --stat src/components/blocks/FeatureGridBlock.tsx src/components/products/SpecTable.tsx` shows edits confined to these two files.
    - `npm run lint:rtl` exits 0.
    - `npm run typecheck` exits 0.
    - `npm test` exits 0 (the existing `tests/int/blocks-placeholder.spec.ts` FeatureGrid cases still pass).
  </acceptance_criteria>
  <done>FeatureGridBlock's feature cards and SpecTable's data panel both render with rounded-card radius, a 1px neutral-300 hairline border and shadow-card resting elevation; SpecTable keeps bg-neutral-100; no hover elevation was introduced on either.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tabular-nums onto every stat figure and spec value</name>
  <files>src/components/blocks/StatsBandBlock.tsx, src/components/blocks/ExportMapBlock.tsx, src/components/products/SpecTable.tsx</files>
  <read_first>
    - `src/components/blocks/StatsBandBlock.tsx` — the stat-value `<p>` at line 27.
    - `src/components/blocks/ExportMapBlock.tsx` — the identical stat-value `<p>` inside the local `StatTiles` component at line 31.
    - `src/components/products/SpecTable.tsx` — the `<dd>` template-literal className at lines 35-37.
    - `08-UI-SPEC.md` **Component Audit rows #3, #4, #5**, **Component Contracts §2 (second diff) and §3**, and the **RTL & Locale Contract** row for `tabular-nums`.
  </read_first>
  <action>
    Apply the Contract §2 (`<dd>`) and §3 diffs verbatim — insert `tabular-nums` immediately after `font-semibold` in each of the three class strings, changing nothing else.

    1. `src/components/blocks/StatsBandBlock.tsx:27` — stat value `<p>` className becomes `text-display font-semibold tabular-nums text-primary-700`.

    2. `src/components/blocks/ExportMapBlock.tsx:31` — the `StatTiles` stat value `<p>` className becomes the same `text-display font-semibold tabular-nums text-primary-700`. (This is the same stat-figure primitive duplicated independently; it is fixed for the same reason, per Audit row #4.)

    3. `src/components/products/SpecTable.tsx:35-37` — inside the `<dd>` template literal, the segment `text-body font-semibold text-neutral-900` becomes `text-body font-semibold tabular-nums text-neutral-900`. Keep `py-sm`, `text-start`, `break-words` and the interpolated `${borderClass}` exactly as they are — `text-start` is the RTL-safe logical utility and must not be touched.

    4. `tabular-nums` is a native Tailwind v4 utility (`font-variant-numeric: tabular-nums`) — do NOT add any token to `globals.css`, and do not add a `@theme` entry. It applies unconditionally to these cells including mixed numeric/text values such as SpecTable's packaging string, because it only affects digit advance width.

    5. Do not touch the `<dt>` label cells, the `text-label` sublabels, or `ExportMapBlock`'s SVG/`PRIMARY_500` fill logic.
  </action>
  <verify>
    <automated>grep -Fc 'text-display font-semibold tabular-nums text-primary-700' src/components/blocks/StatsBandBlock.tsx && grep -Fc 'text-display font-semibold tabular-nums text-primary-700' src/components/blocks/ExportMapBlock.tsx && grep -Fc 'text-body font-semibold tabular-nums text-neutral-900' src/components/products/SpecTable.tsx && npm run lint:rtl && npm run lint && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'text-display font-semibold tabular-nums text-primary-700' src/components/blocks/StatsBandBlock.tsx` returns `1`.
    - `grep -Fc 'text-display font-semibold tabular-nums text-primary-700' src/components/blocks/ExportMapBlock.tsx` returns `1`.
    - `grep -Fc 'text-body font-semibold tabular-nums text-neutral-900' src/components/products/SpecTable.tsx` returns `1`.
    - `grep -Fc 'text-start' src/components/products/SpecTable.tsx` is unchanged from before the edit (still `2` — `<dt>` and `<dd>`).
    - `git diff src/app/globals.css` is empty (no token was added — `tabular-nums` is native Tailwind).
    - `npm run lint:rtl` exits 0 and `npm run lint` exits 0.
    - `npm test` exits 0 (existing StatsBand/ExportMap int + e2e-independent tests unaffected).
  </acceptance_criteria>
  <done>StatsBandBlock stat values, ExportMapBlock StatTiles values, and SpecTable `<dd>` spec values all carry `tabular-nums`; no globals.css/@theme change was made; lint:rtl is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS editor → block renderer | Editor-authored `stat.value` / spec `row.value` strings flow into the elements being restyled here — already-escaped React text nodes, unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | FeatureGridBlock / SpecTable / StatsBandBlock / ExportMapBlock render output | low | mitigate | Diff is class-string-only with zero logic, data-flow or prop changes; enforced by file-scoped greps plus `npm run typecheck`, `npm run lint`, `npm test` in every task's verify, and by `git diff --stat` confirming edits stay inside the four declared files. |
| T-08-01-02 | Tampering | Design-token surface (`src/app/globals.css` `@theme`) | low | mitigate | This phase adds no tokens; acceptance criteria assert `git diff src/app/globals.css` is empty so an accidental token edit cannot ride along. |
| T-08-01-03 | Information Disclosure | Editor-authored stat/spec strings | low | accept | No change to how values are rendered (still React text nodes, auto-escaped); `tabular-nums` is a CSS font-variant only and introduces no `dangerouslySetInnerHTML` path. |
</threat_model>

<verification>
- `npm run lint` — 0 errors.
- `npm run lint:rtl` — green; no physical-direction utility introduced (`rounded-card`, `shadow-card`, `tabular-nums`, `bg-white`, `border-neutral-300` are all direction-agnostic).
- `npm run typecheck` — exits 0.
- `npm test` — full vitest suite green.
- `git diff --stat` touches exactly the four files in `files_modified`; `src/app/globals.css` is untouched.
- Live-render confirmation of the visual result is deferred to plan `08-04` (Wave 2 checkpoint), which loads `/en`, `/en/products/<slug>` and checks card elevation + digit alignment.
</verification>

<success_criteria>
- Both card stragglers carry the exact Contract §1/§2 class strings (grep-verified, file-scoped).
- All three stat/spec figure sites carry `tabular-nums` (grep-verified).
- Zero token, color, spacing or typography-tier changes.
- lint, lint:rtl, typecheck and the vitest suite are all green.
</success_criteria>

<output>
Create `.planning/phases/08-component-polish-pass-apply-amended-design-system-across-car/08-01-SUMMARY.md` when done.
</output>
