---
phase: 03-product-catalog
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/products/ProductCard.tsx
  - src/app/(site)/[locale]/products/page.tsx
  - src/components/chrome/GlobalHeader.tsx
  - src/components/chrome/GlobalFooter.tsx
  - src/components/chrome/MobileNavPanel.tsx
  - tests/e2e/catalog-index.spec.ts
autonomous: true
requirements: [CAT-01, CAT-04]
must_haves:
  truths:
    - "A visitor at /products sees products grouped into a section per category, ordered by displayOrder, each product shown as a ProductCard (image, name, teaser, category tag) linking to /products/<slug>"
    - "The Oilseeds category (zero published products) renders its header followed by the generic empty-state copy, not an empty grid"
    - "A ProductCard with no gallery image shows the ImageOff placeholder box, never a broken image"
    - "The `products` nav link now resolves to /products in GlobalHeader, GlobalFooter, and MobileNavPanel (no longer excluded), and the existing nav-links.spec still passes for both en and ar"
    - "The /products page renders inside the existing Phase 1 chrome and is RTL-correct on /ar (gallery/card order not mirrored; physical-direction gate passes)"
  artifacts:
    - path: "src/components/products/ProductCard.tsx"
      provides: "Catalog grid card — image + name + teaser + category tag, whole card is the link"
      min_lines: 30
    - path: "src/app/(site)/[locale]/products/page.tsx"
      provides: "CatalogIndex route grouped by category with anchor-nav + empty states"
      exports: ["default"]
    - path: "tests/e2e/catalog-index.spec.ts"
      provides: "e2e coverage for grouped sections, empty-category state, RTL"
  key_links:
    - from: "src/app/(site)/[locale]/products/page.tsx"
      to: "getProductsByCategory"
      via: "server-component fetch"
      pattern: "getProductsByCategory"
    - from: "src/components/products/ProductCard.tsx"
      to: "/products/<slug>"
      via: "Link wrapping the whole card"
      pattern: "products/"
    - from: "src/components/chrome/GlobalHeader.tsx"
      to: "/products"
      via: "NAV_KEYS + NAV_HREFS entry"
      pattern: "products"
---

<objective>
Ship the catalog index: `/products` renders products grouped by category (CAT-01) using a new `ProductCard`, an optional category anchor-nav, per-category and whole-catalog empty states (CAT-04), reusing the Phase 1 chrome and Phase 2 Hero-compact/CTABand surfaces. Wire the long-parked `products` nav entry to the real route in all three chrome components (D-06).

Purpose: this is the first user-visible catalog capability — a buyer can browse the whole product range grouped by category and click into any product.
Output: `/products` route + `ProductCard` component + nav wired to `/products` + passing `catalog-index.spec.ts` e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-product-catalog/03-CONTEXT.md
@.planning/phases/03-product-catalog/03-UI-SPEC.md
@.planning/phases/03-product-catalog/03-RESEARCH.md
@.planning/references/taste-techniques.md

<interfaces>
<!-- Contracts from Plan 01 + existing chrome — no exploration needed. -->

From Plan 01 — src/lib/payload-fetch.ts:
- getProductsByCategory(locale): Promise<{ category: Category; products: Product[] }[]>  (categories in displayOrder, products pre-filtered to published + grouped)
- Product/Category types in payload-types.ts. Product.imageGallery: array of { image: Media | number | null }. Product.category: Category | number. Product.name/shortDescription localized text. Product.slug string.

Media populate guard (use verbatim) — from MediaGalleryBlock.tsx:
- const image = item.image && typeof item.image === "object" ? item.image as Media : null; if (!image?.url) return placeholder.

Existing route conventions — src/app/(site)/[locale]/[slug]/page.tsx:
- default async page reads params: Promise<{locale,slug}>, awaits, calls setRequestLocale(locale) from next-intl/server, returns <main>...</main>.

Reusable surfaces:
- HeroBlock (src/components/blocks/HeroBlock.tsx): takes { block, index } where block is a hero-shaped object { blockType:"hero", variant:"compact", eyebrow?, headline, subhead?, primaryCta?, secondaryCta? }. data-testid="hero" is on the section (nav-links.spec asserts it exists on every nav target).
- CTABandBlock (src/components/blocks/CTABandBlock.tsx): { block:{ blockType:"ctaBand", heading, body?, primaryCta?, secondaryCta? }, index }.
- CertCard (src/components/blocks/CertCard.tsx) — not used on the index, only Plan 03.
- next/image sizes for cards per UI-SPEC: "(min-width:1024px) 25vw, (min-width:768px) 33vw, 50vw".
- shadcn primitives available: Card, Badge, AspectRatio, Button (no new installs).

i18n (added in Plan 01): namespace "products" → heading, catalogEmptyHeading, catalogEmptyBody, breadcrumbRoot; existing "blocks.emptyState" for per-category empty. "nav.products" already translated in all locales.

Chrome nav files (identical NAV_KEYS/NAV_HREFS blocks in each) — GlobalHeader.tsx, GlobalFooter.tsx, MobileNavPanel.tsx currently list keys home/about/certifications/manufacturing/export/company/contact and a comment "products is excluded (Phase 3)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing e2e spec for the catalog index (RED)</name>
  <files>tests/e2e/catalog-index.spec.ts</files>
  <read_first>
    - tests/e2e/certifications.spec.ts (page-object style, en+ar coverage, section/heading assertions)
    - tests/e2e/nav-links.spec.ts (how nav targets are asserted — data-testid="hero" must exist on the /products page)
    - .planning/phases/03-product-catalog/03-UI-SPEC.md §CatalogIndex + Copywriting Contract
    - .planning/phases/03-product-catalog/03-VALIDATION.md Per-Requirement map (CAT-01/CAT-04 rows)
  </read_first>
  <action>
    Create `tests/e2e/catalog-index.spec.ts` describing the target behavior (it will fail until Task 2 lands the route). Cover, for en (and at least the RTL-critical checks on /ar): visiting /products returns 200 and renders a heading region (data-testid="hero"); a section exists per seeded category heading (Grains, Spices, Pulses, Oilseeds); product cards link to /products/<slug> (assert an anchor href matching /products/); the Oilseeds section shows the generic empty-state copy (reuse the string from blocks.emptyState) and no product cards; on /ar the page has dir="rtl" on the html element. Use the seeded fixture products from Plan 01 (do not create data in the e2e test).
  </action>
  <verify>
    <automated>cd "D:/PW" && npx playwright test tests/e2e/catalog-index.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - Spec file compiles and is discovered by Playwright (`--list` shows the cases).
    - Assertions cover: grouped sections, product-card hrefs, empty-category copy, /ar dir="rtl".
    - The spec references seeded category/product names, not test-created data.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ProductCard + CatalogIndex route (GREEN)</name>
  <files>src/components/products/ProductCard.tsx, src/app/(site)/[locale]/products/page.tsx</files>
  <read_first>
    - src/components/blocks/CertCard.tsx (Card composition + Media/Image usage + Badge patterns to match visual language)
    - src/components/blocks/MediaGalleryBlock.tsx (AspectRatio ratio={4/3} + Media populate guard + next/image usage)
    - src/components/blocks/HeroBlock.tsx and CTABandBlock.tsx (props shape to synthesize compact-hero + CTABand on this non-CMS route)
    - src/app/(site)/[locale]/[slug]/page.tsx (setRequestLocale + params-await route shape)
    - .planning/phases/03-product-catalog/03-UI-SPEC.md §ProductCard + §CatalogIndex (grid classes, tokens, empty states)
  </read_first>
  <action>
    Create `src/components/products/ProductCard.tsx` (server component): wraps the whole card in a next-intl `Link` to `/products/${product.slug}`. Inside a shadcn `Card` (p-md md:p-lg): AspectRatio ratio={4/3} with next/image (object-cover, the UI-SPEC card `sizes` string) from `imageGallery[0].image` via the Media populate guard — if absent, a bg-neutral-100 box with a centered lucide `ImageOff` (text-neutral-600). Below: category tag as `Badge variant="secondary"` (bg-neutral-100 text-neutral-900) using product.category.name (guard typeof==="object"); product name Body-600 with line-clamp-2; shortDescription Body with line-clamp-2. Hover/focus: `-translate-y-[1px]` + shadow bump, focus-visible ring accent-600. Widths use bracket values only (no max-w-sm/md/etc — Phase 2 token collision).

    Create `src/app/(site)/[locale]/products/page.tsx` as a NEW sibling folder (RESEARCH Pitfall 2 — do NOT special-case "products" in [slug]/page.tsx; Next resolves static-over-dynamic automatically). Default async component: await params {locale}, setRequestLocale(locale), getTranslations("products"), call getProductsByCategory(locale). Compose per UI-SPEC: a compact Hero (synthesize a hero-shaped object { blockType:"hero", variant:"compact", headline: t("heading") } passed to HeroBlock so data-testid="hero" is present for nav-links.spec) → optional category anchor-nav (Badge/outline-Button chips, href="#<category-slug>", gap-sm flex-wrap; each section header gets id="<category-slug>") → one alternating-background section per category (`py-2xl md:py-3xl`, `<h2>` Heading token, optional category shortDescription in max-w-[720px]) with a `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-md md:gap-lg` of ProductCards; an empty category renders its header + `blocks.emptyState` copy instead of an empty grid → CTABand (synthesize a ctaBand-shaped object, general "Request a Quote"/WhatsApp-style CTA). If getProductsByCategory returns zero categories, render the whole-catalog empty state (t("catalogEmptyHeading")/t("catalogEmptyBody") with a /contact link) inside a data-testid="hero" region so the route still satisfies nav-links.spec.
  </action>
  <verify>
    <automated>cd "D:/PW" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/products/page.tsx` lives in its own folder; no edit to `[slug]/page.tsx`.
    - ProductCard renders the ImageOff placeholder when imageGallery is empty (Red Lentils fixture proves this).
    - Category tag uses Badge variant="secondary" (neutral, NOT accent gold).
    - No named `max-w-*` utility used anywhere in the two files (bracket values only).
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire `products` nav link + run catalog e2e green</name>
  <files>src/components/chrome/GlobalHeader.tsx, src/components/chrome/GlobalFooter.tsx, src/components/chrome/MobileNavPanel.tsx, tests/e2e/catalog-index.spec.ts</files>
  <read_first>
    - src/components/chrome/GlobalHeader.tsx, GlobalFooter.tsx, MobileNavPanel.tsx (identical NAV_KEYS/NAV_HREFS blocks + the "products excluded" comments to remove)
    - tests/e2e/nav-links.spec.ts (asserts every nav href is a real 200 with a data-testid="hero")
  </read_first>
  <action>
    In each of the three chrome files, add "products" to NAV_KEYS (positioned after "about", per the translated nav order) and add `products: "/products"` to NAV_HREFS. Remove the now-stale "products excluded (Phase 3)" / T-02-18 comments. Do not otherwise change chrome layout. The `nav.products` message key already exists in all locales, so no i18n change is needed here.

    Then run the catalog e2e (now that the route + nav exist) and the RTL gate; fix any assertion mismatches in catalog-index.spec.ts against the real rendered markup (adjust selectors, not behavior).
  </action>
  <verify>
    <automated>cd "D:/PW" && npx playwright test tests/e2e/catalog-index.spec.ts tests/e2e/nav-links.spec.ts && node scripts/check-physical-direction.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `products` appears in NAV_KEYS/NAV_HREFS in all three chrome components; no dead comments remain.
    - `nav-links.spec.ts` passes for both en and ar (the new /products link resolves to 200 with a hero region).
    - `catalog-index.spec.ts` passes.
    - `node scripts/check-physical-direction.mjs` reports no physical-direction (ml-/mr-/left/right) violations in the new files.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor → catalog index | Anonymous server-rendered read of grouped published products |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | products/page.tsx | mitigate | Page consumes getProductsByCategory, which filters published:true (mitigated in Plan 01, T-03-01) — the index never queries products directly, so no draft can leak through this surface |
| T-03-06 | Denial of Service | getProductsByCategory | accept | Explicit `limit` (500/100) on the underlying queries; no unbounded scan and no public write path in this phase |
| T-03-07 | Tampering | next/image src from Media.url | accept | Image hosts already constrained by next.config.ts remotePatterns (established Phase 1/2 boundary); no new host introduced |
</threat_model>

<verification>
- `npm run build` exits 0 with `/products` prerendered per locale.
- `catalog-index.spec.ts` + `nav-links.spec.ts` green; physical-direction gate clean.
- Manual (deferred to phase gate): editor adds a Category/Product in /admin → appears on /products without redeploy (ISR).
</verification>

<success_criteria>
- `/products` shows products grouped by category with per-category + whole-catalog empty states (CAT-01, CAT-04).
- ProductCard reuses the site card language, whole-card link, ImageOff fallback, neutral category tag.
- `products` nav link live in header/footer/mobile, RTL-correct on /ar.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-catalog/03-02-SUMMARY.md`
</output>
