---
context: phase
phase: 05-seo-infrastructure-insights-blog
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-23T05:02:00.000Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mysterious SIGTERM(143) on build/tsc/test is NOT always a code bug | `next build`, `next build --webpack`, and even plain `tsc --noEmit` all died identically at the same early stage, repeatedly, for ~5 attempts | advisory | Before assuming a code regression, run `free -h` and `ps aux \| grep chroma-mcp`. This session had ~92 leaked `chroma-mcp --client-type persistent` background processes (claude-mem's vector DB) eating nearly all sandbox RAM (load average ~6.7). They self-cleaned once agent activity settled; a bulk `kill` loop was blocked by the auto-mode classifier, so the fix was just waiting/retrying once memory recovered on its own. |
| `routing.locales` is a readonly tuple | Passing it directly to a function typed `Locale[]` (mutable array) fails `tsc` | advisory | Spread it first: `buildAlternates([...routing.locales], path)`. Already fixed in `src/app/sitemap.ts`. |
| Sibling plans marking different REQUIREMENTS.md rows cause merge conflicts | 05-02/05-03 both edited the same SEO checklist block, each marking a different ID | advisory | Resolve by keeping BOTH sides' `[x]` marks (union), not either/or. Already handled during Wave 1. |

<current_state>
Phase 05 (SEO Infrastructure & Insights/Blog) execution via `/gsd-execute-phase 5` is at the very tail end — gap-closure after verification.

**Fully done and merged to main:**
- All 5 plans (05-01..05-05), both waves, build+test gates passed each wave
- Code review (`05-REVIEW.md`, commit `e1f73bf`): 1 Critical (CR-01) + 3 Warning + 4 Info
- CR-01 fixed and committed (`cf8735a`): Insights collection public REST read was `() => true` with no filter, exposing draft articles — now `read: ({req:{user}}) => (user ? true : {published:{equals:true}})`
- Regression gate: full suite green (70/70)
- Phase verification (`gsd-verifier`, commit `7b8cd77`, `05-VERIFICATION.md`): **status gaps_found** — 3 Warnings from code review confirmed live as real gaps (WR-01, WR-02, WR-03), plus a doc-sync note (BLOG-02 checkbox stale, not a code gap)
- User said "whatever is best" when offered a choice between formal `/gsd-plan-phase 05 --gaps` vs direct fix → chose direct fix (all three are small, single-file, exact-location fixes already scoped by the review)

**In progress, NOT yet committed** (4 files, `tsc --noEmit` already confirmed clean, exit 0):
- `src/app/sitemap.ts` — added `entriesForAllLocales()` helper + calls for `/products` and `/insights` hub pages (WR-02 fix)
- `src/app/(site)/[locale]/products/[slug]/page.tsx` — removed the duplicate-URL category breadcrumb entry (WR-01 fix)
- `src/app/(site)/[locale]/insights/not-found.tsx` — **new file**, renders the documented `notFoundHeading`/`notFoundBody` copy instead of Next's generic 404 (WR-03 fix)
- `tests/int/sitemap.spec.ts` — updated the "zero published insights" test's assertion: the `/insights` hub entry is now expected to always be present (it's locale-invariant, not content-gated); only per-article `/insights/<slug>` detail entries should disappear with zero articles. Already re-ran this file alone after the fix: 6/6 passed.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: All 5 plans executed + merged + gated — Done
- Task 2: Code review + CR-01 fix + regression gate + phase verification — Done (result: gaps_found)
- Task 3: Fix WR-01/WR-02/WR-03 — code changes written, `tsc --noEmit` clean, `sitemap.spec.ts` re-run green in isolation. NOT YET: full `npm test` suite, `npm run build`, commit, re-verification.
</completed_work>

<remaining_work>

In order:
1. Run `tests/int/sitemap.spec.ts` once more standalone if you want extra confidence (already green once, low risk) — optional, can skip straight to step 2.
2. `npm test` (full vitest suite) — confirm no regressions from the 4 uncommitted files.
3. `npm run build` — confirm production build still succeeds (all 61+ pages, including the new `/insights/not-found` segment).
4. Commit all 4 files as one gap-closure commit, e.g.:
   `fix(05): close verification gaps WR-01/02/03 (breadcrumb dupe URL, sitemap hub pages, insights 404 copy)`
5. Re-verify: either re-spawn `gsd-verifier` on phase 05, or manually confirm live via a dev-server + curl pass (same technique the first verification used):
   - `curl http://localhost:3000/sitemap.xml` → should now include `/insights` and `/products` entries
   - `curl http://localhost:3000/products/ground-turmeric` → BreadcrumbList should have exactly 2 ListItems (no duplicate URL)
   - `curl http://localhost:3000/insights/does-not-exist` → should render the custom not-found copy, not Next's generic boundary
6. Once confirmed: `update_roadmap` step — `gsd_run query phase.complete "05"`, commit ROADMAP.md/STATE.md/REQUIREMENTS.md/VERIFICATION.md updates.
7. `close_phase_todos` — check `.planning/todos/pending/*.md` for `resolves_phase: 05`, move any to completed.
8. `update_project_md` — evolve `.planning/PROJECT.md` to reflect phase 05 completion (move validated requirements, update Current State section, commit).
9. `offer_next` — present the phase-complete summary and next-phase routing options to the user (check for CONTEXT.md on phase 6 to decide which command to suggest: discuss-phase vs plan-phase).

Also unresolved, separate from phase 05 (do NOT block phase completion on this): user flagged the WhatsApp CTA number (`910000000000`, in `src/lib/seed-content.ts`) is a placeholder needing the real number before launch. User hasn't said whether to log it as a todo — ask before doing so, don't just silently create one.
</remaining_work>

<decisions_made>

- Direct-fixed all 3 verification gaps instead of a formal gap-closure planning cycle (`/gsd-plan-phase 05 --gaps`) — user's call ("whatever is best"), justified by small single-file scope.
- WR-02: new `entriesForAllLocales()` helper in `sitemap.ts`, NOT reuse of `entriesFor()` — the latter requires a real CMS doc to check translation status via `getTranslatedLocales()`, but `/products`/`/insights` are code-only listing routes with no backing Pages doc (they render in all 4 locales unconditionally per `[locale]/layout.tsx`'s `generateStaticParams`).
- WR-01: removed the category breadcrumb entry entirely (no url field to give it — no category archive page exists) rather than making `breadcrumbJsonLd`'s shared `url` field optional for one caller.
- Updated (not weakened) `sitemap.spec.ts`'s zero-insights assertion to match the new intentional behavior: hub entry always present, only article-detail entries gated by content.
</decisions_made>

<blockers>
None currently. Session hit repeated "session limit" API errors throughout (at least 3x) — each time either zero partial work (safe re-dispatch) or safely resumed via SendMessage to the same background agent. If interrupted again, the safe resume point is exactly the 4 uncommitted files above.
</blockers>

## Required Reading (in order)
1. `git diff` on the 4 files listed in `<current_state>` — see exactly what changed
2. `.planning/phases/05-seo-infrastructure-insights-blog/05-VERIFICATION.md` — the gaps this work is closing (WR-01/02/03 in the frontmatter `gaps:` array)
3. `.planning/phases/05-seo-infrastructure-insights-blog/05-REVIEW.md` — original review findings, same WR-01/02/03 IDs

## Infrastructure State
- No running dev servers, no leftover worktrees (all merged/cleaned).
- `payload.db` schema in sync through all of phase 05 (Insights collection + SiteSettings address/sameAs already pushed).
- Environment memory: was under heavy pressure earlier (see anti-pattern table) but recovered on its own; check `free -h` if builds mysteriously hang/die again.

<context>
This is purely mechanical cleanup at this point — three small, well-understood fixes for gaps the phase's own code review already found and named exact locations for. No design ambiguity remains. The only real risk left is making sure the full test suite + build stay green after these changes (which touch a shared sitemap helper, a product page, and add a new route file) before committing and closing out the phase.
</context>

<next_action>
Start with: `npm test` (full suite) on the current working tree (4 uncommitted files). If green, `npm run build`. If both green, commit as one gap-closure commit, then proceed through remaining_work steps 5-9 in order.
</next_action>
