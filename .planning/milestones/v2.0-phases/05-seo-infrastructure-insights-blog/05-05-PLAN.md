---
phase: 05-seo-infrastructure-insights-blog
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - tests/int/sitemap.spec.ts
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/(site)/[locale]/layout.tsx
  - src/app/(site)/[locale]/products/[slug]/page.tsx
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-05]
user_setup:
  - service: hosting (Vercel)
    why: "sitemap.ts, robots.ts, and metadataBase all emit absolute URLs from NEXT_PUBLIC_SITE_URL; a localhost value shipped to prod produces localhost canonicals/sitemap entries (severe SEO-05 bug, Pitfall 4)."
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "Vercel Project → Settings → Environment Variables (real production origin). Same var introduced in 05-02's .env.example."
must_haves:
  truths:
    - "sitemap.ts default export returns MetadataRoute.Sitemap entries covering home + interior marketing slugs + every published product + every published insights article, one <url> per translated locale of each item (SEO-03)."
    - "Each sitemap item's per-locale entries carry an identical reciprocal alternates.languages map (all translated locales + exactly one x-default), the same map buildAlternates produces for generateMetadata — no page produces duplicate <url> entries with conflicting canonicals (SEO-03 adjacency)."
    - "Draft/unpublished products and insights are excluded from the sitemap (every enumeration filters published:true) — no sitemap URL 404s or exposes a draft (Pitfall 2, T-05-02)."
    - "Zero published insights articles still yields a valid sitemap (home + interior + products) — never a 500 (SEO-03 empty case)."
    - "Sitemap entry order is deterministic across builds (items enumerated in a fixed query order, locales in routing.locales order) — repeated builds produce no flaky diffs (SEO-03 ordering case)."
    - "Interior marketing slugs (about/certifications/manufacturing/export/company/contact) emit hreflang gated through getTranslatedLocales('pages', slug), NOT an unconditional 4-locale array — today that is en + x-default only until real translations land (RESEARCH assumption A2, D-07)."
    - "robots.ts serves allow '/', disallow '/admin' and '/api', and points sitemap at NEXT_PUBLIC_SITE_URL/sitemap.xml (SEO-03, admin/API not crawlable)."
    - "The (site)/[locale]/layout.tsx sets metadataBase from NEXT_PUBLIC_SITE_URL (falling back to localhost in dev only) so every descendant generateMetadata resolves absolute canonical/OG URLs (SEO-01/SEO-05)."
    - "Every page inherits a valid per-locale title/description from the layout metadata default (title template + default + description); content pages override via generateMetadata — no page emits blank/undefined meta tags (SEO-01 empty case)."
    - "The product detail page emits generateMetadata (title/description/OG + reciprocal hreflang via getTranslatedLocales('products', slug)), a Product JSON-LD (D-10: no price/offers/review), and a BreadcrumbList JSON-LD (Products → category → product, D-11) via the shared <JsonLd> component."
    - "Organization JSON-LD is emitted once via the locale layout, sourced from SiteSettings (name/logo/url/address/sameAs, D-09), on every public page (SEO-04)."
    - "The English root canonical carries no locale prefix and never conflicts with its hreflang set on any wired page (SEO-05)."
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "single-file localized sitemap — home + interior + products + insights, reciprocal hreflang, published-filtered"
      contains: "MetadataRoute.Sitemap"
      min_lines: 30
    - path: "src/app/robots.ts"
      provides: "robots.txt — allow /, disallow /admin + /api, sitemap pointer"
      contains: "disallow"
    - path: "src/app/(site)/[locale]/layout.tsx"
      provides: "metadataBase + Organization JSON-LD (edit-in-place)"
      contains: "metadataBase"
    - path: "src/app/(site)/[locale]/products/[slug]/page.tsx"
      provides: "generateMetadata + Product/BreadcrumbList JSON-LD (edit-in-place)"
      contains: "generateMetadata"
    - path: "tests/int/sitemap.spec.ts"
      provides: "sitemap entry/locale/published-exclusion/no-dupe coverage (SEO-03)"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/seo/translated-locales.ts"
      via: "per-item getTranslatedLocales gates locale entries"
      pattern: "getTranslatedLocales"
    - from: "src/app/sitemap.ts"
      to: "src/lib/seo/alternates.ts"
      via: "localeUrl builds per-locale absolute URLs"
      pattern: "localeUrl"
    - from: "src/app/(site)/[locale]/products/[slug]/page.tsx"
      to: "src/lib/seo/metadata.ts"
      via: "generateMetadata calls buildMetadata"
      pattern: "buildMetadata"
    - from: "src/app/(site)/[locale]/products/[slug]/page.tsx"
      to: "src/lib/seo/json-ld.tsx"
      via: "renders productJsonLd + breadcrumbJsonLd via <JsonLd>"
      pattern: "productJsonLd"
    - from: "src/app/(site)/[locale]/layout.tsx"
      to: "src/lib/seo/json-ld.tsx"
      via: "renders organizationJsonLd via <JsonLd>"
      pattern: "organizationJsonLd"
  prohibitions:
    - statement: "MUST NOT emit a sitemap locale entry or hreflang alternate for a locale lacking real published translation — availability must never be misrepresented to crawlers; every interior/product/insights entry MUST gate its locale set through getTranslatedLocales (D-07/A2), never routing.locales unconditionally."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT allow /admin or /api (Payload's admin UI and auto-generated REST/GraphQL endpoints) to be crawlable — robots.ts MUST disallow both; without it the admin surface is indexable by default."
      status: unresolved
      verification: judgment
---

<objective>
Wire the SEO infrastructure into the live site: the single-file localized `sitemap.ts`, `robots.ts`, `metadataBase` + Organization JSON-LD on the locale layout, and `generateMetadata` + Product/BreadcrumbList JSON-LD on the product detail page. This closes SEO-01/02/03/04/05 for the crawler-facing surface by consuming the 05-02/05-03 libraries and the 05-01 collection.

Purpose: SEO-03 (sitemap + robots), and the page-level wiring of SEO-01 (metadata), SEO-02 (hreflang), SEO-04 (Organization/Product/Breadcrumb JSON-LD), SEO-05 (canonical). Depends on 05-01 (insights collection for enumeration), 05-02 (getTranslatedLocales/buildMetadata/localeUrl), 05-03 (json-ld builders + SiteSettings address/sameAs).
Output: sitemap.ts, robots.ts, edited layout.tsx, edited products/[slug]/page.tsx, and a passing sitemap integration test.
</objective>

<execution_context>
@/workspaces/companywebsite/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/companywebsite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-infrastructure-insights-blog/05-CONTEXT.md
@.planning/phases/05-seo-infrastructure-insights-blog/05-RESEARCH.md
@.planning/phases/05-seo-infrastructure-insights-blog/05-PATTERNS.md
@src/app/(site)/[locale]/layout.tsx
@src/app/(site)/[locale]/products/[slug]/page.tsx
@src/lib/payload-fetch.ts
@src/i18n/routing.ts
@tests/int/products-revalidate-hook.spec.ts
@tests/int/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — failing integration test for sitemap.ts</name>
  <files>tests/int/sitemap.spec.ts</files>
  <read_first>
    - tests/int/products-revalidate-hook.spec.ts (getTestPayload fixture + create-with-disableRevalidate pattern to mirror)
    - tests/int/config.ts (getTestPayload)
    - .planning/phases/05-seo-infrastructure-insights-blog/05-RESEARCH.md (Pattern 3 sitemap shape; Pitfall 2 draft exclusion)
    - src/i18n/routing.ts (locales/defaultLocale for expected URL shapes)
  </read_first>
  <action>
    Create `tests/int/sitemap.spec.ts` (vitest `int` project) that FAILS until Task 2. Set `process.env.NEXT_PUBLIC_SITE_URL` to a fixed base (e.g. `https://example.test`) before importing sitemap. Using getTestPayload, seed one PUBLISHED product and one PUBLISHED insights article (with required coverImage) and one DRAFT (published:false) of each, all via `context: { disableRevalidate: true }`. Import the default export from `@/app/sitemap` and call it. Assert:
    - The returned array is non-empty and every entry has a `url` starting with `https://example.test`.
    - The published product's en URL (`https://example.test/products/<slug>`) appears; the DRAFT product/insight slug does NOT appear anywhere in the entries (published exclusion, Pitfall 2).
    - No two entries share the same `url` (no duplicate <url>).
    - The home entry exists and its en URL is the bare base (no `/en` prefix, SEO-05).
    - Each entry carries an `alternates.languages` map containing exactly one `x-default` key.
    - Calling the sitemap with ZERO published insights (delete/skip) still returns a valid non-throwing array containing home + product entries (empty-insights resilience).
    Clean up all created docs in afterAll.
  </action>
  <verify>
    <automated>npx vitest run --project=int tests/int/sitemap.spec.ts 2>&1 | grep -Eq 'FAIL|No test files|Cannot find|Failed to resolve' && echo "RED as expected" || echo "expected RED before Task 2"</automated>
  </verify>
  <acceptance_criteria>
    - tests/int/sitemap.spec.ts exists, valid TS, RED before Task 2.
    - It asserts published-only inclusion, draft exclusion, no duplicate URLs, en-root-no-prefix, exactly one x-default per entry, and empty-insights resilience.
  </acceptance_criteria>
  <done>The SEO-03 sitemap behavior (locales, published filter, no dupes, reciprocity, empty resilience) is encoded in a RED integration spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sitemap.ts + robots.ts</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <read_first>
    - .planning/phases/05-seo-infrastructure-insights-blog/05-RESEARCH.md (Pattern 3 sitemap.ts full code + robots.ts Code Example — copy these, with the A2 fix)
    - src/lib/seo/translated-locales.ts + src/lib/seo/alternates.ts (getTranslatedLocales + localeUrl from 05-02)
    - src/lib/payload-fetch.ts (payload.find published:true query shape to enumerate products/insights)
    - src/i18n/routing.ts (routing.locales/defaultLocale)
  </read_first>
  <behavior>
    - sitemap enumerates home + INTERIOR_SLUGS + published products + published insights; per item, one <url> per translated locale carrying the identical reciprocal alternates.languages map (all translated locales + one x-default).
    - Interior slugs gate their locale set via getTranslatedLocales("pages", slug) (A2 fix — NOT routing.locales unconditionally).
    - robots allows '/', disallows '/admin' and '/api', points sitemap at NEXT_PUBLIC_SITE_URL/sitemap.xml.
  </behavior>
  <action>
    Create `src/app/sitemap.ts` (root of src/app/, OUTSIDE both route groups) per RESEARCH Pattern 3: `export default async function sitemap(): Promise<MetadataRoute.Sitemap>`. Reuse `localeUrl` and `getTranslatedLocales` from `@/lib/seo/*` — do NOT re-implement the prefix logic. Build an `entriesFor(path, collection?, slug?)` helper that computes `translatedLocales` (for home/interior: `getTranslatedLocales("pages", slug)` — APPLY THE A2 FIX, do not fall back to routing.locales; for products/insights: `getTranslatedLocales(collection, slug)`), builds ONE reciprocal `languages` map (x-default at defaultLocale + every translated locale), and returns one entry per translated locale with the identical map. Enumerate: `entriesFor("/")`; each of INTERIOR_SLUGS = ["about","certifications","manufacturing","export","company","contact"] via `entriesFor("/"+slug, "pages", slug)`; published products via `payload.find({ collection:"products", where:{ published:{ equals:true } }, locale:"en", overrideAccess:true, limit:500 })` → `entriesFor("/products/"+p.slug, "products", p.slug)`; published insights via the same shape on "insights" → `entriesFor("/insights/"+a.slug, "insights", a.slug)`. Determinism: iterate in fixed query/locale order.

    Create `src/app/robots.ts` (root of src/app/) per RESEARCH Code Example: `export default function robots(): MetadataRoute.Robots` returning `{ rules: { userAgent: "*", allow: "/", disallow: ["/admin", "/api"] }, sitemap: `${BASE}/sitemap.xml` }` with `BASE = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"`.
  </action>
  <verify>
    <automated>npx vitest run --project=int tests/int/sitemap.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/sitemap.ts exists at the root of src/app/, returns MetadataRoute.Sitemap, filters published:true for products and insights, and gates interior-page locales via getTranslatedLocales("pages", slug) (grep confirms no unconditional routing.locales fallback in entriesFor).
    - src/app/robots.ts disallows /admin and /api and points at /sitemap.xml.
    - The Task 1 integration spec passes GREEN.
  </acceptance_criteria>
  <done>The localized sitemap and robots exist, exclude drafts, emit reciprocal hreflang gated on real translations, and keep /admin + /api out of the index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: metadataBase + Organization JSON-LD on layout; generateMetadata + Product/Breadcrumb JSON-LD on product detail</name>
  <files>src/app/(site)/[locale]/layout.tsx, src/app/(site)/[locale]/products/[slug]/page.tsx</files>
  <read_first>
    - src/app/(site)/[locale]/layout.tsx (existing structure — add metadata export + one <JsonLd> in the body; DO NOT touch (payload)/layout.tsx)
    - src/app/(site)/[locale]/products/[slug]/page.tsx (existing ProductDetailPage — add generateMetadata + two <JsonLd> renders; category + certs + images are already destructured)
    - src/lib/seo/metadata.ts + translated-locales.ts + alternates.ts (05-02 helpers)
    - src/lib/seo/json-ld.tsx (organizationJsonLd/productJsonLd/breadcrumbJsonLd/JsonLd from 05-03)
    - src/lib/payload-fetch.ts (getSiteBrand — Organization source; extend or read SiteSettings for address/sameAs)
    - .planning/phases/05-seo-infrastructure-insights-blog/05-RESEARCH.md (Pattern 1 generateMetadata + metadataBase; Pattern 4 JSON-LD field mapping D-10/D-11)
  </read_first>
  <behavior>
    - layout.tsx exports a `metadata` (or generateMetadata) with metadataBase = new URL(NEXT_PUBLIC_SITE_URL ?? localhost) plus a title template + default + description so all descendants inherit valid metadata; renders <JsonLd data={organizationJsonLd(...)} /> once in the body using SiteSettings name/logo/url/address/sameAs.
    - products/[slug]/page.tsx exports generateMetadata returning buildMetadata({ title:name, description:shortDescription, imageUrl:imageGallery[0], translatedLocales:getTranslatedLocales("products",slug), path:`/products/${slug}` }); renders <JsonLd data={productJsonLd(...)} /> (D-10, no price/offers/review) and <JsonLd data={breadcrumbJsonLd([Products, category?, product])} /> (D-11).
    - The English canonical stays un-prefixed; Organization JSON-LD emits once per page, not per product.
  </behavior>
  <action>
    Edit `src/app/(site)/[locale]/layout.tsx`: add `export const metadata: Metadata = { metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"), title: { template: "%s · Star Agrevolution", default: "Star Agrevolution" }, description: <a concise brand description sourced from siteName/positioning> }`. In the body (inside the existing tree, e.g. just inside <body> alongside the existing Script), render `<JsonLd data={organizationJsonLd({ siteName, url: process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000", logoUrl, address, sameAs })} />`. Source `address`/`sameAs` from SiteSettings — extend `getSiteBrand()` in payload-fetch.ts to also return `address`/`sameAs`, OR read the global directly in the layout; prefer extending getSiteBrand so the single cached query still serves everything. DO NOT modify `(payload)/layout.tsx` (auto-generated). metadataBase set here cascades to every generateMetadata below (RESEARCH Pattern 1).

    Edit `src/app/(site)/[locale]/products/[slug]/page.tsx`: add `export async function generateMetadata({ params })` that awaits params, fetches the product (reuse getProduct), returns `{}` when missing, else `buildMetadata({ title: product.name, description: product.shortDescription, imageUrl: images[0]?.url, translatedLocales: await getTranslatedLocales("products", slug), path: `/products/${slug}` })`. In the page body render `<JsonLd data={productJsonLd({ name, images: images.map(i=>i.url), description: shortDescription, categoryName: category?.name, certNames: certs.map(c=>c.name) })} />` (D-10 — pass NO price/offers/review) and `<JsonLd data={breadcrumbJsonLd([{ name: t("breadcrumbRoot"), url: localeUrl(locale,"/products") }, ...(category ? [{ name: category.name, url: localeUrl(locale,"/products") }] : []), { name: product.name, url: localeUrl(locale, `/products/${slug}`) }])} />` (D-11). Reuse the already-computed `category`, `certs`, and `images` variables in the page — do not re-fetch.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -Ei 'layout\.tsx|products/\[slug\]' || echo "no product/layout typecheck errors"; npx vitest run --project=int tests/int/sitemap.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx exports metadata with metadataBase from NEXT_PUBLIC_SITE_URL and a title template/default + description; renders organizationJsonLd once via <JsonLd>; (payload)/layout.tsx untouched.
    - products/[slug]/page.tsx exports generateMetadata delegating to buildMetadata with getTranslatedLocales("products", slug), and renders productJsonLd (no price/offers/review) + breadcrumbJsonLd via <JsonLd>.
    - The English product canonical has no /en prefix.
    - `npx tsc --noEmit` clean; the sitemap int spec stays green.
  </acceptance_criteria>
  <done>Every public page emits metadataBase-resolved per-locale metadata + Organization JSON-LD, and product detail pages emit correct hreflang + Product/BreadcrumbList structured data (D-10/D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler → /sitemap.xml, /robots.txt | Public; must expose only published content and keep /admin + /api out. |
| CMS translation status → sitemap hreflang | Locale set advertised per URL is derived from real translation existence; a wrong derivation misrepresents availability. |
| env NEXT_PUBLIC_SITE_URL → absolute sitemap/canonical/OG URLs | A wrong base URL poisons canonicals site-wide. |
| CMS product name → Product/Breadcrumb JSON-LD | Routes through the shared <JsonLd> escaper (05-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering (search-ranking signals) | sitemap hreflang / generateMetadata alternates | medium | mitigate | Every locale set gated through getTranslatedLocales (interior pages included — A2 fix); unit + int tests assert no phantom alternates. |
| T-05-02 | Information Disclosure | sitemap product/insights enumeration | medium | mitigate | Every enumeration filters published:true; int test asserts draft slugs never appear. |
| T-05-05 | Information Disclosure | robots.ts /admin + /api exposure | high | mitigate | robots.ts disallows /admin and /api explicitly; without it Payload's admin/REST surface is crawlable by default. Blocking threshold high — mitigated in-plan. |
| T-05-05a | Information Disclosure | metadataBase from env | medium | mitigate | localhost fallback dev-only; user_setup requires the prod env var; phase-gate view-source confirms no localhost canonical ships. |
| T-05-03 | Tampering / Information Disclosure | productJsonLd/breadcrumbJsonLd via <JsonLd> | high | mitigate | Product/category/name strings route through the single 05-03 <JsonLd> escaper; no second inline JSON-LD render. |
| T-05-SC | Tampering | npm installs | low | accept | No new package installed. |
</threat_model>

<verification>
- `npx vitest run --project=int tests/int/sitemap.spec.ts` passes.
- `npx tsc --noEmit` clean for sitemap.ts/robots.ts/layout.tsx/products/[slug]/page.tsx.
- Existing product-detail e2e (`tests/e2e/product-detail.spec.ts`) still passes (no regression from the generateMetadata/JSON-LD additions).
- Manual (phase gate, requires deploy): (1) view-source on home/product/article per locale confirms title/description/OG + reciprocal hreflang + one x-default; (2) Screaming Frog crawl reports zero hreflang conflicts against canonical; (3) Google Rich Results Test validates Organization + Product + BreadcrumbList. These require NEXT_PUBLIC_SITE_URL set on the deploy (user_setup).
</verification>

<success_criteria>
- SEO-01/02/03/04/05 satisfied on the crawler-facing surface: a reachable localized sitemap free of drafts and duplicate canonicals, robots excluding admin/API, per-locale metadata with reciprocal real-translation-gated hreflang, un-prefixed English canonicals, and validating Organization/Product/BreadcrumbList structured data.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (excluded from drift verification as newly-created):
- `src/app/sitemap.ts` — default `sitemap()` route + `entriesFor` helper + `INTERIOR_SLUGS`
- `src/app/robots.ts` — default `robots()` route
- `src/app/(site)/[locale]/layout.tsx` — edited to add `metadata` (metadataBase + title template) + Organization `<JsonLd>` (possibly an extended `getSiteBrand` returning address/sameAs)
- `src/app/(site)/[locale]/products/[slug]/page.tsx` — edited to add `generateMetadata` + Product/BreadcrumbList `<JsonLd>`
- `tests/int/sitemap.spec.ts` — new integration spec

## Probe assumptions (surfaced, not dropped)

- Edge-probe rows SEO-03 (adjacency/empty/ordering) and the SEO-01/SEO-02 wiring rows reclassified by the planner to `covered` and authored as concrete `must_haves.truths` per specless-fallback §C — surfaced, not dropped.
- RESEARCH assumption A2 (interior pages must use getTranslatedLocales("pages", slug), not routing.locales) is implemented as the explicit A2 fix in Task 2 — the flagged assumption is resolved in-plan.
- Prohibitions "no misrepresented hreflang availability" and "no crawlable /admin + /api" (transparency/judgment tier) are authored descriptor-less → flagged-unverified (never auto-dismissed).
- Canon-referral (not minted): JSON-LD injection is canon output-encoding covered by 05-03's <JsonLd> + T-05-03 + /gsd-secure-phase — breadcrumbed, not duplicated.

<output>
Create `.planning/phases/05-seo-infrastructure-insights-blog/05-05-SUMMARY.md` when done.
</output>
