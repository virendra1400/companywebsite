---
phase: 02-core-marketing-pages-trust-surfaces
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/blocks/FeatureGrid.ts
  - src/blocks/StatsBand.ts
  - src/blocks/DocumentCard.ts
  - src/blocks/index.ts
  - src/components/blocks/FeatureGridBlock.tsx
  - src/components/blocks/StatsBandBlock.tsx
  - src/components/blocks/DocumentCardBlock.tsx
  - src/components/blocks/RenderBlocks.tsx
  - src/collections/Pages.ts
  - src/lib/seed-content.ts
  - scripts/seed-pages.ts
  - src/i18n/messages/en.json
  - src/i18n/messages/ar.json
  - src/i18n/messages/fr.json
  - src/i18n/messages/ru.json
  - payload-types.ts
  - tests/int/blocks-placeholder.spec.ts
  - tests/e2e/company.spec.ts
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [TRUST-05, PAGE-01, PAGE-02]
must_haves:
  truths:
    - "The homepage renders a FeatureGrid of value props (icon variant) and a StatsBand of capability figures, in the UI-SPEC order (Hero → FeatureGrid → CertStrip → StatsBand → … → CTABand)"
    - "The Company/Compliance page (/[locale]/company) renders leadership bios (FeatureGrid photo variant), compliance narrative (RichText), and a Company Profile document card that reuses CertCard's PDF-present/PDF-absent states"
    - "The Company Profile card, with no real profile PDF yet, shows 'available on request' and NO fabricated IEC/registration number appears anywhere on the page"
    - "Every FeatureGrid/StatsBand renders without layout break under long placeholder strings and with an empty items[] (shows the generic empty-state line, never a crash)"
  artifacts:
    - path: "src/blocks/FeatureGrid.ts"
      provides: "featureGrid block config with variant icon|photo + items[]"
      contains: "variant"
    - path: "src/blocks/StatsBand.ts"
      provides: "statsBand block config with stats[] (value/label)"
      contains: "stats"
    - path: "src/components/blocks/DocumentCardBlock.tsx"
      provides: "Company-profile doc card rendering via the shared CertCard primitive (no new card component)"
  key_links:
    - from: "src/components/blocks/DocumentCardBlock.tsx"
      to: "src/components/blocks/CertCard.tsx"
      via: "reuse CertCard with the generic prop shape"
      pattern: "CertCard"
    - from: "src/components/blocks/RenderBlocks.tsx"
      to: "FeatureGridBlock / StatsBandBlock / DocumentCardBlock"
      via: "BLOCK_MAP featureGrid/statsBand/documentCard entries"
      pattern: "featureGrid"
---

## Phase Goal

**As a** first-time B2B buyer, **I want to** read the exporter's value propositions, capability figures and the compliance/leadership behind its claims, **so that** I trust the company enough to inquire.

<objective>
Second trust slice: the value-prop / capability / leadership blocks (FeatureGrid + StatsBand) plus a complete Company/Compliance page. FeatureGrid and StatsBand also fill the remaining homepage sequence gaps (PAGE-01) and are available to About (PAGE-02). The company-profile document card reuses the CertCard primitive built in 02-03 — no new card component.

Purpose: TRUST-05 (Company/Compliance page + company-profile PDF link, no fabricated cert numbers) and completing the homepage/About block sequences.
Output: featureGrid + statsBand + documentCard block configs and render components, composed Company page, enriched homepage, seeded placeholders (leadership photos, no fabricated numbers), int + e2e tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-RESEARCH.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-CONTEXT.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-VALIDATION.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-02-PLAN.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-03-PLAN.md
@./CLAUDE.md

<interfaces>
Consume from 02-02 + 02-03 (already built — extend, don't redefine):
- Block registration points: src/blocks/index.ts barrel, src/collections/Pages.ts `blocks: []` array, src/components/blocks/RenderBlocks.tsx BLOCK_MAP + sectionBg(index).
- src/components/blocks/CertCard.tsx (02-03) — generic prop shape `{ name, subtitle?, logo, pdf, halal?, t }`. DocumentCardBlock passes a doc-shaped object (title as name, no logo or a doc-icon, pdf = the company-profile Media, halal false) into it. Reuse, do not fork.
- PAGES_EN_SEED (src/lib/seed-content.ts) + scripts/seed-pages.ts (idempotent en-only upsert, disableRevalidate).
- Media upload via payload.create({collection:'media', filePath, data:{alt}}) — leadership photos are Media uploads (typeof-object guard, Pitfall 3).

Binding specs: UI-SPEC §3 FeatureGrid (icon|photo, item-count→column-count capped at 4, shadcn Card tiles, Body-600 title), §4 StatsBand (Display number text-primary-700, Label caption, no card border, grid-cols-2 md:grid-cols-4), Page Composition rows "Homepage" / "About" / "Company/Compliance", and the note that leadership bios live on Company (NOT duplicated on About). CTABand ignores bg alternation (02-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FeatureGrid + StatsBand + DocumentCard blocks, render components, registration, i18n</name>
  <read_first>
    - 02-UI-SPEC.md §3 FeatureGrid, §4 StatsBand (fields, layout, tokens), Copywriting Contract empty-state line
    - src/components/blocks/CertCard.tsx (02-03 — the generic prop shape DocumentCardBlock feeds)
    - src/components/blocks/RenderBlocks.tsx + src/blocks/index.ts + src/collections/Pages.ts (registration points)
    - src/components/ui/card.tsx + aspect-ratio.tsx (02-01 shadcn primitives)
    - src/i18n/messages/en.json (certs namespace added in 02-03 — add a blocks namespace here)
  </read_first>
  <action>
    Create block configs (NO localized on nested fields — cascade from Pages.layout, per 02-02 Pattern 2):
    - src/blocks/FeatureGrid.ts (slug 'featureGrid'): sectionTitle (text, localized, optional), variant (select icon|photo, NOT localized), items array — icon (text, lucide icon name, e.g. for icon variant), photo (upload→media, optional, for photo variant), title (text, localized, required), body (textarea, localized, required).
    - src/blocks/StatsBand.ts (slug 'statsBand'): sectionTitle (text, localized, optional), stats array — value (text, e.g. "15+"), label (text, localized).
    - src/blocks/DocumentCard.ts (slug 'documentCard'): title (text, localized, required), description (textarea, localized, optional), file (upload→media, optional — drives PDF-present/absent via CertCard).
    Append all three to src/blocks/index.ts and src/collections/Pages.ts blocks array.
    Create render components (server components, {block, index}, own section via sectionBg(index)):
    - src/components/blocks/FeatureGridBlock.tsx: grid-cols-1 md:grid-cols-2, lg columns from item count (3 items→lg:grid-cols-3, 4→lg:grid-cols-4, cap 4), gap-lg; each tile shadcn Card p-lg; icon variant → lucide icon by name (map a small allow-list of lucide icons; unknown name → fallback icon, never crash) + Body-600 title + Body body; photo variant → 96px circular photo (Media guard) + title + body. Empty items[] → generic empty-state line (Copywriting Contract), no crash.
    - src/components/blocks/StatsBandBlock.tsx: grid grid-cols-2 md:grid-cols-4 gap-lg, centered; each tile Display value text-primary-700 + Label caption text-neutral-600; NO card border. Empty stats[] → empty-state line.
    - src/components/blocks/DocumentCardBlock.tsx: build a doc-shaped object from the block (name=title, subtitle=description, logo=null or a doc-icon slot, pdf=Media guard on file, halal=false) and render <CertCard>. This is the company-profile card — reuses CertCard's PDF-present/absent states verbatim.
    Add featureGrid/statsBand/documentCard entries to BLOCK_MAP.
    Add a `blocks` i18n namespace (all 4 catalogs, en real + others copy en) with the generic empty-state string(s) from the Copywriting Contract ("This section is being updated — check back shortly.").
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl</automated>
  </verify>
  <acceptance_criteria>
    - src/blocks/{FeatureGrid,StatsBand,DocumentCard}.ts exist with slugs featureGrid/statsBand/documentCard; all exported from index.ts AND present in Pages.ts blocks array
    - FeatureGrid has variant icon|photo; StatsBand has stats[] (value/label); no nested field sets localized:true
    - DocumentCardBlock renders via CertCard (grep shows CertCard import/use) — no forked card markup
    - RenderBlocks BLOCK_MAP has featureGrid/statsBand/documentCard; empty items[]/stats[] render the empty-state line, not a crash
    - npx tsc --noEmit exits 0; npm run lint:rtl passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Compose Company page + enrich homepage/About; seed leadership + company-profile placeholders</name>
  <read_first>
    - 02-UI-SPEC.md Page Composition rows Homepage / About / Company/Compliance + the "About vs Company/Compliance split" note (leadership → Company only) + placeholder Hero copy table
    - 02-CONTEXT.md D-03 (realistic placeholders), specifics (no fabricated IEC/registration numbers)
    - src/lib/seed-content.ts + scripts/seed-pages.ts (from 02-02/02-03 — structures to extend)
  </read_first>
  <action>
    Update PAGES_EN_SEED (src/lib/seed-content.ts):
    - home layout: insert featureGrid (variant icon, 4 value props — Quality / Reliability / Compliance / Global Reach with realistic long-string bodies) after the CertStrip, and statsBand (capability figures, realistic "15+"/"40+"/"500+" shaped values, NO fabricated precise counts presented as fact) after it — matching UI-SPEC homepage order (ExportMap slot filled in 02-06).
    - company layout: replace the thin 02-02 stub with: compact hero ("Company & Compliance") → featureGrid (variant photo, leadership bios placeholders) → richText (IEC/registration narrative — generic compliance prose, NO fabricated IEC/FSSAI number) → richText (logistics & documentation support) → documentCard (Company Profile, file absent → "available on request") → ctaBand.
    - about layout: keep it as hero(compact) → richText → ctaBand (leadership stays on Company per UI-SPEC; do NOT duplicate FeatureGrid photo on About).
    Extend scripts/seed-pages.ts to seed leadership photo Media placeholders (self-authored generic placeholder images from scripts/seed-assets/, reuse the 02-03 asset pattern — no real people photos). Do NOT seed a company-profile PDF (absent state is intended). Idempotent, en only.
    Regenerate types; run seed + build.
  </action>
  <verify>
    <automated>npx payload generate:types && npm run db:seed && npx tsc --noEmit && npm run lint:rtl && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - home layout order: hero → certStrip → featureGrid(icon) → statsBand → … → ctaBand (matches UI-SPEC, ExportMap gap left for 02-06)
    - company layout has featureGrid(photo leadership) + 2 richText + documentCard(file absent) + ctaBand; about is NOT given a leadership FeatureGrid
    - No fabricated IEC/FSSAI/registration/certificate number in any seeded string
    - npm run db:seed idempotent; npm run build exits 0; /company and / prerender
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: e2e (company, homepage sequence) + int placeholder-resilience tests</name>
  <read_first>
    - tests/int/payload-fallback.spec.ts + tests/int/config.ts (int conventions)
    - tests/e2e/locale-routing.spec.ts + tests/e2e/rtl-arabic.spec.ts (en+ar conventions)
    - 02-VALIDATION.md rows TRUST-05, PAGE-01, TRUST-06
  </read_first>
  <action>
    Create tests/int/blocks-placeholder.spec.ts (Vitest): render/validate FeatureGrid + StatsBand block data with (a) long placeholder strings and (b) an empty items[]/stats[] — assert the render path produces the empty-state line and no thrown error (drives TRUST-06 resilience for these blocks).
    Create tests/e2e/company.spec.ts (Playwright, en+ar): /[locale]/company renders leadership FeatureGrid (photo tiles), the compliance RichText, and the Company Profile document card showing the "available on request" line (no download anchor, no fabricated number visible). Assert no layout break under dir=rtl.
    Create tests/e2e/homepage.spec.ts (Playwright, en+ar): assert the homepage renders the block sequence so far in order — Hero, then CertStrip, then FeatureGrid value props, then StatsBand (ExportMap assertion added in 02-06). Keep assertions order-tolerant to the ExportMap gap (assert relative order of the present blocks).
    Run the RTL gate.
  </action>
  <verify>
    <automated>npx vitest run tests/int/blocks-placeholder.spec.ts && node scripts/check-physical-direction.mjs && npx playwright test tests/e2e/company.spec.ts tests/e2e/homepage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/int/blocks-placeholder.spec.ts passes (long strings + empty arrays, no crash, empty-state line present)
    - tests/e2e/company.spec.ts passes en+ar (leadership grid, compliance text, doc card "available on request", no fabricated number)
    - tests/e2e/homepage.spec.ts passes en+ar (relative order Hero → CertStrip → FeatureGrid → StatsBand)
    - node scripts/check-physical-direction.mjs passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public browser → Next RSC → Payload Local API | Pages/leadership/doc content read server-side, overrideAccess:true; no public REST/GraphQL exposure (unchanged) |
| public browser → Media URL | Leadership photos + (future) company-profile PDF served as static Media URLs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Repudiation/Legal | Fabricated IEC/registration numbers or claims in compliance copy | mitigate | Compliance RichText uses generic prose only; company-profile PDF left in absent "available on request" state; assertion in company e2e + seed acceptance criteria forbids fabricated numbers (Pitfall 5/9) |
| T-02-09 | Information Disclosure | Leadership photo Media exposure | accept | Self-authored generic placeholder images only, no real personal photos; same Media access model as 02-02 |
| T-02-10 | Tampering | lucide icon name from CMS field | mitigate | FeatureGrid icon variant maps a fixed allow-list of lucide icons; unknown name falls back safely (no dynamic import of arbitrary module names) |
</threat_model>

<verification>
- `npx vitest run tests/int/blocks-placeholder.spec.ts` green
- `npx playwright test tests/e2e/company.spec.ts tests/e2e/homepage.spec.ts` green (en + ar)
- `npm run build` exit 0; RTL gate passes
- No fabricated compliance numbers in seed or rendered page
</verification>

<success_criteria>
The Company/Compliance page is complete (leadership, compliance narrative, company-profile document card) with the profile PDF honestly in its absent state; the homepage carries value props + capability stats in the correct sequence; About stays a clean story page. TRUST-05 satisfied; PAGE-01/PAGE-02 sequences advanced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-04-SUMMARY.md`
</output>
