---
phase: 02-core-marketing-pages-trust-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/label.tsx
  - src/components/ui/form.tsx
  - src/components/ui/aspect-ratio.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [PAGE-04]
must_haves:
  truths:
    - "Every shadcn semantic CSS variable referenced by Card/Badge/Input/Textarea/Label/Form/AspectRatio and Button's outline/destructive/secondary variants resolves to a real brand-mapped value (no transparent/invisible rendering)"
    - "shadcn generic --accent maps to neutral-100 (hover slot), NOT brand gold — gold never floods hover states"
    - "npx tsc --noEmit and npm run build stay clean after adding the 7 primitives"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Full shadcn :root semantic variable set mapped to Phase 1 brand tokens"
      contains: "--destructive"
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card primitive"
    - path: "src/components/ui/form.tsx"
      provides: "shadcn Form/FormField/FormMessage react-hook-form wrapper primitives"
  key_links:
    - from: "src/components/ui/*"
      to: "src/app/globals.css :root"
      via: "CSS custom properties (var(--border), var(--ring), var(--accent), ...)"
      pattern: "var\\(--(border|ring|accent|muted|card|secondary|destructive|input)"
---

<objective>
Close the Phase 1 shadcn CSS-variable gap and install the shadcn primitives every Phase 2 block depends on, BEFORE any block component is written.

Purpose: RESEARCH Pitfall 1 (HIGH confidence, directly verified) — `globals.css :root` defines only 4 shadcn vars (`--background`, `--foreground`, `--primary`, `--primary-foreground`). `Card`/`Badge`/`Input`/`Textarea`/`Label`/`Form`/`AspectRatio` and `Button`'s `outline`/`destructive`/`secondary` variants reference `--destructive`, `--border`, `--input`, `--ring`, `--accent(-foreground)`, `--muted(-foreground)`, `--card(-foreground)`, `--secondary(-foreground)`, `--popover(-foreground)` which do not exist yet — they render transparent/broken silently. This is a hard prerequisite for every block plan in this phase.
Output: Complete brand-mapped `:root` variable set + 7 new shadcn primitives, tsc/build/rtl-gate clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-RESEARCH.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/01-foundation-cms-decision/01-UI-SPEC.md
@src/app/globals.css
@src/components/ui/button.tsx
@./CLAUDE.md

<interfaces>
Existing @theme brand tokens already live in globals.css (consume these, do NOT redefine):
  --color-primary-900/700/500/100, --color-accent-800/600/100, --color-neutral-900/600/300/100

Existing :root (only these 4 shadcn vars today — lines 57-62):
  --background: #ffffff; --foreground: var(--color-neutral-900);
  --primary: var(--color-primary-700); --primary-foreground: #ffffff;

Phase 1 pattern precedent: PrimaryButton overrides focus ring to accent-600
  (`focus-visible:ring-accent-600`) — the new --ring var must match this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile shadcn :root semantic variables (brand-mapped, accent -> neutral-100)</name>
  <read_first>
    - src/app/globals.css (the :root block, lines 56-62, and the @theme brand ramp)
    - src/components/ui/button.tsx (which variants reference which vars)
    - 02-RESEARCH.md Pitfall 1 (exact var->token mapping table, MANDATORY)
    - 01-UI-SPEC.md Color table (brand token hex values)
  </read_first>
  <action>
    Extend the `:root` block in src/app/globals.css with the missing shadcn semantic variables, mapping each to a Phase 1 brand token per the RESEARCH Pitfall 1 table (not shadcn generic defaults). Add exactly: `--destructive: #B91C1C` (UI-SPEC "activated this phase" value); `--destructive-foreground: #ffffff`; `--border: var(--color-neutral-300)`; `--input: var(--color-neutral-300)`; `--ring: var(--color-accent-600)` (matches Phase 1 PrimaryButton focus-ring); `--muted: var(--color-neutral-100)`; `--muted-foreground: var(--color-neutral-600)`; `--card: #ffffff`; `--card-foreground: var(--color-neutral-900)`; `--popover: #ffffff`; `--popover-foreground: var(--color-neutral-900)`; `--secondary: var(--color-neutral-100)`; `--secondary-foreground: var(--color-neutral-900)`; `--accent: var(--color-neutral-100)`; `--accent-foreground: var(--color-neutral-900)`. CRITICAL per Pitfall 1: shadcn's generic `--accent` is the hover-background slot on outline/ghost buttons and menu items — it MUST map to neutral-100, NEVER to brand gold (`accent-600`/`accent-800`), so gold stays reserved for the explicitly-designed Halal badge / PDF-icon / divider uses and never floods hover states. Add a code comment on the `--accent` line stating this rule. Do not add a `--radius` token unless a newly added primitive fails to compile without it (add only if required, mapped to a plain px value).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - globals.css :root now contains --destructive, --border, --input, --ring, --muted, --card, --secondary, --accent (and their -foreground pairs where listed)
    - `--accent` resolves to var(--color-neutral-100) with an inline comment forbidding gold in the hover slot
    - `--ring` resolves to var(--color-accent-600)
    - `--destructive` is #B91C1C
    - No brand-gold token appears in the --accent/--accent-foreground values
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the 7 shadcn primitives and verify clean build</name>
  <read_first>
    - src/components/ui/button.tsx (confirms shadcn new-york preset + import conventions already in use)
    - 02-UI-SPEC.md Registry Safety table (the exact 7 components + their consumers)
    - 02-RESEARCH.md "shadcn components to add" table + install command
    - scripts/check-physical-direction.mjs (confirms src/components/ui/** is gate-excluded)
  </read_first>
  <action>
    Run `npx shadcn add card badge input textarea label form aspect-ratio` to add the primitives to src/components/ui/. Per 01-03-SUMMARY.md Deviation #1, the shadcn CLI historically did NOT install peer deps into this project — after the add, confirm `react-hook-form` is NOT pulled in as an unexpected dep here (it is installed deliberately in Plan 06, not this plan) and that any missing peer (e.g. `@radix-ui/*` already covered by the `radix-ui` package, `class-variance-authority` already present) resolves; if `npx tsc --noEmit` reports a missing module, `npm install` that exact module. Do NOT hand-edit the generated primitives (Registry Safety). The generated files legitimately use physical-direction classes internally; they are excluded from the RTL gate by the existing `src/components/ui/**` skip — do not touch that exclusion. Confirm the primitives render against the Task 1 variables by running the full build.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/{card,badge,input,textarea,label,form,aspect-ratio}.tsx all exist
    - npx tsc --noEmit exits 0
    - npm run lint:rtl passes (no physical-direction classes in hand-authored code; ui/** excluded)
    - npm run build exits 0
    - No unexpected new runtime dependency beyond what the primitives require (react-hook-form deferred to Plan 06)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build-time CSS/registry | No runtime trust boundary crossed this plan — pure design-token + component-scaffold work |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | shadcn registry fetch (`npx shadcn add`) | accept | Official shadcn registry only (Registry Safety PASS in UI-SPEC); no third-party registry; generated files reviewed via `npm run build` before commit |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run build` exit 0
- `npm run lint:rtl` passes
- Grep confirms `--accent` value contains `neutral-100` and NOT `accent-600`/`accent-800`
</verification>

<success_criteria>
All shadcn semantic variables resolve to brand tokens; the 7 primitives are installed and the project builds clean. No block component can now silently break on a missing CSS variable. Downstream block plans are unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-01-SUMMARY.md`
</output>
