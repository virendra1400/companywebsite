---
phase: 02-core-marketing-pages-trust-surfaces
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - src/blocks/ContactBlock.ts
  - src/blocks/index.ts
  - src/components/blocks/ContactBlockView.tsx
  - src/components/blocks/ContactForm.tsx
  - src/components/blocks/RenderBlocks.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/label.tsx
  - src/components/ui/form.tsx
  - src/collections/Pages.ts
  - src/lib/contact-schema.ts
  - src/lib/seed-content.ts
  - src/i18n/messages/en.json
  - src/i18n/messages/ar.json
  - src/i18n/messages/fr.json
  - src/i18n/messages/ru.json
  - payload-types.ts
  - package.json
  - tests/e2e/contact.spec.ts
autonomous: true
requirements: [PAGE-03]
must_haves:
  truths:
    - "A visitor on /[locale]/contact sees the registered address, a WhatsApp entry with a visible text label (not icon-only), email, and phone — each with its value shown"
    - "The inquiry form renders (name, company, country, message) with client-side zod validation: submitting empty required fields shows per-field error text and marks the field aria-invalid, and never issues any network request"
    - "The contact page renders inside the same global header/footer/language-switcher as every other page, and mirrors correctly under dir=rtl on /ar"
  artifacts:
    - path: "src/components/blocks/ContactForm.tsx"
      provides: "Client-side, NON-submitting inquiry form stub (react-hook-form + zod) with a11y wiring; Phase 4 attaches the server action"
      contains: "aria-invalid"
    - path: "src/lib/contact-schema.ts"
      provides: "Shared zod schema for the inquiry fields — reused verbatim by the Phase 4 server action"
      exports: ["contactSchema"]
    - path: "src/blocks/ContactBlock.ts"
      provides: "contactBlock Payload block config (localized address/whatsapp/email/phone + optional intro)"
      contains: "whatsapp"
  key_links:
    - from: "src/components/blocks/RenderBlocks.tsx"
      to: "ContactBlockView"
      via: "BLOCK_MAP contactBlock entry"
      pattern: "contactBlock"
    - from: "src/components/blocks/ContactForm.tsx"
      to: "contactSchema"
      via: "zodResolver(contactSchema)"
      pattern: "zodResolver"
---

## Phase Goal

**As a** buyer ready to reach out, **I want to** find the exporter's real contact channels (address, WhatsApp, email, phone) and an inquiry form, **so that** I can start a conversation the way I prefer — even before the form is wired to send (Phase 4).

<objective>
Final Phase 2 page vertical slice on the 02-02 block system: a contactBlock (localized contact details) + a client-side inquiry form STUB (D-07 — validates but does NOT submit). Phase 4 (LEAD) later adds the server action, spam defense, and email/CRM using the exact same `contactSchema`.

Purpose: PAGE-03. Output: contactBlock config, ContactBlockView (two-column: info column + form), ContactForm (rhf+zod stub with aria-invalid/aria-describedby via shadcn Form), shared contactSchema, the shadcn form primitives (Input/Textarea/Label/Form) if not already present, rhf/zod/resolvers install, seeded contact page, e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-RESEARCH.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-CONTEXT.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-VALIDATION.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-02-PLAN.md
@./CLAUDE.md

<interfaces>
Consume these 02-02 outputs (already built — do NOT redefine):
- `src/collections/Pages.ts` — `layout` blocks field, `blocks: [...]` imported from `@/blocks`. Append contactBlock to this array.
- `src/blocks/index.ts` — barrel re-exporting block configs. Append ContactBlock.
- `src/components/blocks/RenderBlocks.tsx` — `BLOCK_MAP` keyed by blockType + `sectionBg(index)`. Add `contactBlock` entry.
- `src/lib/seed-content.ts` — `PAGES_EN_SEED` keyed by slug. Add a `contact` page whose layout is a compact hero + contactBlock.
- `scripts/seed-pages.ts` — idempotent per-slug upsert; extend for the contact page.

Locale-aware links: `import { Link } from "@/i18n/navigation"`. Locale type: `import type { Locale } from "@/i18n/routing"`.
UI-SPEC §ContactBlock is the binding visual contract: two-column (info + form), WhatsApp NOT icon-only (visible label + aria-label), form labels above inputs, error text below, border-destructive on invalid, destructive color = #B91C1C for validation only.
RESEARCH resolver/zod pitfall: after installing `@hookform/resolvers`, run `npx tsc --noEmit` IMMEDIATELY (TS-overload breakage between resolver 5.2.x and zod v4.3.x) — pin compatible versions if tsc errors.
CRITICAL (D-07): the form MUST NOT POST anywhere. `onSubmit` only sets a local "success" UI state (or is a no-op console log). No fetch, no server action, no route handler.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install form deps + shadcn form primitives; shared contactSchema; contactBlock config + registration + i18n keys</name>
  <read_first>
    - 02-RESEARCH.md contact-form section (rhf/zod/@hookform/resolvers versions + resolver↔zod tsc pitfall)
    - 02-UI-SPEC.md §ContactBlock + Contact form spec (field list, validation states, WhatsApp visible-label rule, aria wiring, destructive usage)
    - CLAUDE.md (approved stack: react-hook-form 7.81.x, zod 4.4.x, @hookform/resolvers)
    - src/components/blocks/RenderBlocks.tsx + src/blocks/index.ts + src/collections/Pages.ts (registration points from 02-02)
    - src/i18n/messages/en.json (chrome-string structure to extend)
    - src/components/ui/button.tsx + 02-01 primitives (existing shadcn setup, new-york style)
  </read_first>
  <action>
    Install `react-hook-form`, `zod`, `@hookform/resolvers` (npm). Run `npx tsc --noEmit` immediately after; if resolver/zod TS-overload errors appear, pin to a compatible pair per RESEARCH and re-check.
    Add the shadcn primitives the form needs if not already present from 02-01: `input`, `textarea`, `label`, `form` (via `npx shadcn add input textarea label form`, new-york style, into src/components/ui/). If any already exist, leave them.
    Create src/lib/contact-schema.ts exporting `contactSchema` (zod object): name (string, min 1), company (string, min 1), country (string, min 1), message (string, min 1, max ~2000). This schema is SHARED — Phase 4's server action imports it verbatim. Export the inferred type too.
    Create src/blocks/ContactBlock.ts (slug 'contactBlock'): localized fields — intro (textarea, optional), address (textarea, required), whatsapp (text — E.164 number, required), email (text, required), phone (text, required). Append to src/blocks/index.ts barrel and to Pages.ts `blocks` array (do not touch existing entries). Add `contactBlock` entry to RenderBlocks BLOCK_MAP.
    Add i18n keys under a `contact` namespace to all 4 catalogs (en real; ar/fr/ru copy en as placeholder): field labels (name/company/country/message), field error messages, submit button label ("Send Inquiry"), whatsappLabel ("Message us on WhatsApp"), whatsappAria ("Message Star Agrevolution on WhatsApp"), and a success-state line. These are next-intl chrome strings, NOT CMS content.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists react-hook-form, zod, @hookform/resolvers; `npx tsc --noEmit` exits 0
    - src/components/ui/{input,textarea,label,form}.tsx exist
    - src/lib/contact-schema.ts exports contactSchema with name/company/country/message required
    - src/blocks/ContactBlock.ts slug is 'contactBlock', exported from index.ts, present in Pages.ts blocks array, and in RenderBlocks BLOCK_MAP
    - 4 message catalogs contain the contact namespace keys incl. whatsappLabel + whatsappAria
    - npm run lint:rtl passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ContactForm stub (rhf+zod, a11y, no network) + ContactBlockView two-column + compose & seed contact page</name>
  <read_first>
    - 02-UI-SPEC.md §ContactBlock (two-column layout, info-column entries, WhatsApp visible label, form field order/validation visuals) + Copywriting Contract (submit/success/error copy)
    - src/lib/contact-schema.ts (from Task 1)
    - src/components/ui/form.tsx (shadcn Form/FormField/FormMessage — provides aria-invalid + aria-describedby automatically)
    - src/lib/seed-content.ts + scripts/seed-pages.ts (seed structures from 02-02/02-03 to extend)
    - 02-CONTEXT.md D-07 (NON-submitting stub) + D-03 (realistic placeholders)
  </read_first>
  <action>
    Create src/components/blocks/ContactForm.tsx ("use client"): react-hook-form + `zodResolver(contactSchema)`. Fields name/company/country/message using shadcn Form/FormField/FormItem/FormLabel/FormControl/FormMessage so each input gets aria-invalid + aria-describedby wired to its error. Labels ABOVE inputs; error text (FormMessage) below in text-destructive; border-destructive ring on invalid field. `onSubmit` sets a local success UI state ONLY — NO fetch/server action/route handler (D-07). Submit button = shadcn Button (primary-700) with label from i18n. Logical properties only.
    Create src/components/blocks/ContactBlockView.tsx (server component, {block, index}): two-column responsive layout (`grid grid-cols-1 md:grid-cols-2 gap-xl`, stacks on mobile). Info column: intro (if set), then address, WhatsApp (`<a href="wa.me/{whatsapp}?text=...">` opening target=_blank with a VISIBLE text label from i18n `whatsappLabel` beside the icon + `aria-label` whatsappAria — NOT icon-only), email (`mailto:` with visible address), phone (`tel:` with visible number) — each with a lucide icon (no-mirror) + its visible value. Form column: render <ContactForm/>. Wrap with sectionBg(index).
    Add the `contactBlock` component to RenderBlocks BLOCK_MAP (if Task 1 registered only the config, wire the view here).
    Extend PAGES_EN_SEED (seed-content.ts) with a `contact` page: compact hero + contactBlock with realistic placeholder details (plausible address/number/email — but a clearly placeholder company address, no fabricated registration/IEC numbers). Extend scripts/seed-pages.ts to seed it idempotently. Regenerate types if the Page block union changed; run seed + build to confirm no empty-content prerender error.
  </action>
  <verify>
    <automated>npx payload generate:types && npm run db:seed && npx tsc --noEmit && npm run lint:rtl && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ContactForm.tsx uses zodResolver(contactSchema); onSubmit issues NO network call (no fetch/action/route) — grep shows no fetch/axios/server-action import
    - Each field renders via shadcn Form so invalid fields carry aria-invalid + an aria-describedby-linked FormMessage
    - ContactBlockView shows a VISIBLE WhatsApp label (not icon-only) plus aria-label, and visible email/phone/address values
    - PAGES_EN_SEED contains a contact page with a contactBlock; `npm run db:seed` idempotent; no fabricated registration/IEC numbers
    - npm run build exits 0 locally (SQLite); /contact prerenders
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: e2e test — contact info visible, form validation + no-submit, chrome + RTL</name>
  <read_first>
    - tests/e2e/rtl-arabic.spec.ts + tests/e2e/locale-routing.spec.ts (en+ar project + dir assertions)
    - 02-VALIDATION.md Per-Task Verification Map row PAGE-03
    - 02-UI-SPEC.md §ContactBlock (exact labels, error behavior, WhatsApp label text)
  </read_first>
  <action>
    Create tests/e2e/contact.spec.ts (Playwright, en+ar): visit /[locale]/contact; assert address, a WhatsApp link with a VISIBLE text label (not just an icon) whose href starts with a wa.me URL, an email mailto link, and a phone tel link are all present with visible values; submit the empty form and assert per-field error text appears AND at least one input has aria-invalid="true"; assert NO navigation/network occurred on submit (URL unchanged, still on /contact); on /ar assert the two-column block is under dir=rtl without overflow. Run the RTL gate in verification.
  </action>
  <verify>
    <automated>node scripts/check-physical-direction.mjs && npx playwright test tests/e2e/contact.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/contact.spec.ts passes on en and ar projects
    - Asserts WhatsApp visible label + wa.me href, email mailto, phone tel, address all visible
    - Asserts empty-submit shows field errors + aria-invalid and does NOT navigate/POST
    - node scripts/check-physical-direction.mjs passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public browser → Next RSC → Payload Local API | Contact page content read server-side via Local API (overrideAccess), same as 02-02/02-03 |
| public browser → ContactForm (client) | Form runs entirely client-side in Phase 2; NO server endpoint exists yet |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Spoofing/Abuse | Inquiry form submission | defer | No submission path exists in Phase 2 (D-07 stub). Spam defense (honeypot/rate-limit/Turnstile) + server validation land in Phase 4 (LEAD-03) when the action is wired — the shared contactSchema is the reuse seam |
| T-02-15 | Information Disclosure | Contact details are public by design | accept | Address/WhatsApp/email/phone are intentionally public marketing content; placeholder values only until real details supplied via CMS |
| T-02-16 | Tampering | WhatsApp/mailto/tel external anchors | mitigate | wa.me link opens target=_blank with rel="noopener noreferrer"; no reverse-tabnabbing |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (incl. resolver/zod compatibility)
- `npx playwright test tests/e2e/contact.spec.ts` green (en + ar)
- `npm run build` exit 0; `npm run lint:rtl` / `node scripts/check-physical-direction.mjs` pass
- Form issues NO network request on submit (D-07); WhatsApp is not icon-only; no fabricated registration numbers in seed
</verification>

<success_criteria>
The Contact page shows real contact channels (address, WhatsApp with visible label, email, phone) and a validating, non-submitting inquiry form inside the global chrome, correct in LTR and RTL. PAGE-03 is satisfied; the shared contactSchema + form are ready for Phase 4 to wire submission with zero rework.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-07-SUMMARY.md`
</output>
