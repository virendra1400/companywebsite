---
phase: 02-core-marketing-pages-trust-surfaces
plan: 08
type: execute
wave: 8
depends_on: [04, 05, 06, 07]
files_modified:
  - src/components/chrome/GlobalHeader.tsx
  - src/components/chrome/GlobalFooter.tsx
  - src/components/chrome/MobileNavPanel.tsx
  - src/i18n/messages/en.json
  - src/i18n/messages/ar.json
  - src/i18n/messages/fr.json
  - src/i18n/messages/ru.json
  - tests/e2e/nav-links.spec.ts
  - tests/e2e/chrome-consistency.spec.ts
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [PAGE-04]
must_haves:
  truths:
    - "Every header, footer, and mobile-nav link resolves to a real /[locale]/<slug> route (no href=\"#\") for the full D-08 page set"
    - "Header/footer/language-switcher render identically on all 7 pages (home, about, certifications, manufacturing, export, company, contact)"
    - "The homepage renders its full block sequence Hero -> FeatureGrid -> CertStrip -> StatsBand -> ExportMap -> CTABand in order, once all blocks exist"
  artifacts:
    - path: "src/components/chrome/GlobalHeader.tsx"
      provides: "Header nav wired to the real D-08 route set (no # stubs)"
      contains: "certifications"
  key_links:
    - from: "src/components/chrome/GlobalHeader.tsx"
      to: "the [locale]/[slug] routes"
      via: "next-intl Link href per nav key"
      pattern: "href="
---

## Phase Goal

**As a** visitor on any page, **I want to** use the header/footer nav to reach every section (About, Certifications, Manufacturing, Export, Company, Contact), **so that** the site feels like one coherent, navigable whole — not a set of orphan pages.

<objective>
Final integration slice: retire the Phase-1 `href="#"` nav placeholders and wire the global chrome (header, footer, mobile panel) to the real Phase-2 routes per D-08. Runs LAST (depends on all page-creating plans) so every nav target actually resolves. Adds the nav-links + chrome-consistency e2e coverage RESEARCH's test map mandates, and closes the homepage full-block-order assertion (deferred while ExportMap was still being built in 02-06).

Purpose: PAGE-04 (the substantive nav coverage — 02-01 only reconciled chrome CSS tokens). Success criterion 4 ("all pages through the same chrome consistently") depends on this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-CONTEXT.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-UI-SPEC.md
@.planning/phases/02-core-marketing-pages-trust-surfaces/02-02-PLAN.md
@./CLAUDE.md

<interfaces>
- Routes exist from prior plans: home + about (02-02/02-04), certifications (02-03), company (02-04), manufacturing (02-05), export (02-06), contact (02-07). The Pages collection uses a plain-text `slug`; routing is `[locale]/[slug]` (or the home-at-root + `[slug]` split 02-02 established — follow whatever 02-02 shipped).
- Chrome files currently ship Phase-1 placeholders: `GlobalHeader.tsx` / `GlobalFooter.tsx` / `MobileNavPanel.tsx` with `href="#"` and `NAV_KEYS = ["products","certifications","manufacturing","contact"]` (incomplete). `GlobalFooter.tsx` has a comment "placeholder link stubs (anchor #, real pages ship Phase 2)".
- D-08 (locked): nav set = home, about, certifications, manufacturing, export, company/compliance, contact. `products` stays a placeholder/disabled entry (catalog is Phase 3) — keep it visibly present but clearly not-yet-live (or omit from primary nav; do NOT wire it to a dead route).
- Locale-aware links: `import { Link } from "@/i18n/navigation"` (auto locale-prefix, preserves the Phase-1 switcher behavior). Nav labels are next-intl chrome strings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire header/footer/mobile-nav to real D-08 routes + nav label i18n keys</name>
  <read_first>
    - src/components/chrome/GlobalHeader.tsx, GlobalFooter.tsx, MobileNavPanel.tsx (current NAV_KEYS + href="#" to replace)
    - 02-CONTEXT.md D-08 (exact nav set + products carve-out)
    - 02-UI-SPEC.md chrome specs (nav one-line desktop, mobile panel from inline-end, RTL rules) — from Phase 1, unchanged structure
    - src/i18n/messages/en.json (nav namespace to extend)
    - the slug values actually seeded by 02-02..02-07 (so hrefs match real routes)
  </read_first>
  <action>
    Update NAV_KEYS (or the nav-item source) in GlobalHeader.tsx, GlobalFooter.tsx, and MobileNavPanel.tsx to the full D-08 set: home, about, certifications, manufacturing, export, company, contact. Replace every `href="#"` with a next-intl `<Link href="/<slug>">` matching the real seeded slugs (home → "/"). Keep `products` as a visibly-present but clearly not-live entry OR omit from primary nav per D-08 — do NOT point it at a dead route. Preserve the existing Phase-1 structure/RTL behavior (logical properties, mobile Sheet from inline-end, language switcher) — only the link targets + key list change.
    Add/extend `nav` namespace i18n keys in all 4 catalogs (en real; ar/fr/ru copy en placeholder) for any new labels (about, export, company). These are chrome strings, not CMS content.
    Do not restyle — 02-01 already reconciled the tokens; this is wiring only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint:rtl</automated>
  </verify>
  <acceptance_criteria>
    - No `href="#"` remains in GlobalHeader.tsx, GlobalFooter.tsx, MobileNavPanel.tsx (grep returns nothing)
    - Nav item source lists home, about, certifications, manufacturing, export, company, contact
    - `products`, if present, is not wired to a live/dead route (disabled/placeholder or omitted)
    - 4 catalogs contain the nav labels for about/export/company
    - npx tsc --noEmit exits 0; npm run lint:rtl passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: e2e — nav links resolve, chrome consistent across pages, homepage full block order</name>
  <read_first>
    - tests/e2e/locale-routing.spec.ts + rtl-arabic.spec.ts (en+ar conventions)
    - tests/e2e/homepage.spec.ts (from 02-04 — the ExportMap-tolerant order assertion to tighten now that ExportMap exists)
    - 02-VALIDATION.md rows PAGE-04 + homepage sequence
    - 02-UI-SPEC.md Homepage block sequence (Hero → FeatureGrid → CertStrip → StatsBand → ExportMap → CTABand)
  </read_first>
  <action>
    Create tests/e2e/nav-links.spec.ts (Playwright, en+ar): from the homepage, assert each header nav link (about, certifications, manufacturing, export, company, contact) has a real href (starts with `/` or `/ar/`, not `#`) and navigating it lands on a 200 page whose main content renders (not a 404). Repeat the check for footer links.
    Create tests/e2e/chrome-consistency.spec.ts: visit all 7 routes and assert the GlobalHeader (brand wordmark), GlobalFooter, and LanguageSwitcher are present on each; assert dir attribute correct on /ar for a sampled page.
    Extend tests/e2e/homepage.spec.ts: now that ExportMap (02-06) exists, assert the COMPLETE homepage block order Hero → FeatureGrid → CertStrip → StatsBand → ExportMap → CTABand (replace the earlier ExportMap-tolerant/partial assertion). Keep en + ar.
    Run the RTL gate as part of verification.
  </action>
  <verify>
    <automated>node scripts/check-physical-direction.mjs && npx playwright test tests/e2e/nav-links.spec.ts tests/e2e/chrome-consistency.spec.ts tests/e2e/homepage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/nav-links.spec.ts passes: every header + footer nav link resolves to a real 200 route (no # / no 404), en + ar
    - tests/e2e/chrome-consistency.spec.ts passes: header/footer/switcher present on all 7 pages
    - tests/e2e/homepage.spec.ts asserts the full 6-block order incl. ExportMap and passes
    - node scripts/check-physical-direction.mjs passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public browser → static nav links | Nav is static internal links to first-party routes; no user input, no new server surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | Internal nav links | accept | First-party relative routes only; no external targets, no user-controlled hrefs |
| T-02-18 | Information Disclosure | Nav exposes a not-yet-built products route | mitigate | `products` kept placeholder/disabled per D-08 — not wired to a dead/authenticated route |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0; `npm run lint:rtl` / `node scripts/check-physical-direction.mjs` pass
- nav-links + chrome-consistency + homepage-order e2e green (en + ar)
- No `href="#"` anywhere in chrome components
</verification>

<success_criteria>
Header/footer/mobile nav link to every real Phase-2 route; chrome renders consistently across all 7 pages in LTR and RTL; homepage full block order verified. PAGE-04 satisfied end-to-end — the site is one navigable whole.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-pages-trust-surfaces/02-08-SUMMARY.md`
</output>
