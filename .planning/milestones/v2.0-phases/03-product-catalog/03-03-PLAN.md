---
phase: 03-product-catalog
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/products/SpecTable.tsx
  - src/components/products/ProductGallery.tsx
  - src/app/(site)/[locale]/products/[slug]/page.tsx
  - tests/e2e/product-detail.spec.ts
autonomous: true
requirements: [CAT-02, CAT-03, CAT-04]
must_haves:
  truths:
    - "A visitor at /products/<slug> sees a PageHeader (breadcrumb + name + category tag), a gallery, description, a SpecTable (specifications + packaging row), applicable-certification badges, and an RFQ CTA"
    - "The RFQ CTA links to /contact?product=<slug>&productName=<url-encoded name> — a link only, no form submission this phase (Phase 4 seam)"
    - "The SpecTable renders as a <dl>/<dt>/<dd>; a product with zero specifications omits the region entirely; a product with an empty packaging value omits only that row"
    - "Gallery thumbnails are real <button> elements with aria-label 'View image {n} of {total}' and aria-pressed on the selected thumbnail; clicking swaps the primary image; no lightbox/carousel library"
    - "generateStaticParams queries Payload for published product slugs (not a hardcoded list) and dynamicParams stays at the Next default (true), so a product added after build renders on first request without a rebuild (CAT-03)"
    - "An untranslated locale shows the English content plus the LocaleFallbackNotice (product detail only); an invalid/unpublished slug 404s; the page is RTL-correct on /ar with a non-mirrored gallery"
  artifacts:
    - path: "src/components/products/SpecTable.tsx"
      provides: "Semantic <dl> spec table incl. packaging row, per-row + region omission logic"
      contains: "<dl"
    - path: "src/components/products/ProductGallery.tsx"
      provides: "Client thumbnail-swap gallery with aria-pressed a11y, no lightbox"
      contains: "use client"
    - path: "src/app/(site)/[locale]/products/[slug]/page.tsx"
      provides: "ProductDetail route with generateStaticParams querying slugs + dynamicParams default"
      exports: ["default", "generateStaticParams"]
    - path: "tests/e2e/product-detail.spec.ts"
      provides: "e2e for detail regions, SpecTable <dl>, gallery aria-pressed, RFQ CTA href, spec-count variants"
  key_links:
    - from: "src/app/(site)/[locale]/products/[slug]/page.tsx"
      to: "getProduct"
      via: "server-component dual-query fetch"
      pattern: "getProduct"
    - from: "src/app/(site)/[locale]/products/[slug]/page.tsx"
      to: "/contact?product="
      via: "RFQ CTA href with encodeURIComponent(name)"
      pattern: "contact\\?product="
    - from: "src/components/products/[slug]/page.tsx generateStaticParams"
      to: "products collection"
      via: "payload.find published slugs at build"
      pattern: "generateStaticParams"
---

<objective>
Ship the product detail page: `/products/[slug]` (CAT-02) renders a lightweight PageHeader, a thumbnail-swap gallery, description, the new `SpecTable` primitive (specs + packaging row), applicable-certification badges (reusing Phase 2 `CertCard`), and an RFQ CTA that links to `/contact` carrying product context (Phase 4 seam). `generateStaticParams` queries Payload for slugs and `dynamicParams` stays default so new products render without a rebuild (CAT-03). The catalog is structured to accept 0/1/N spec rows and real specs later (CAT-04).

Purpose: the second user-visible catalog capability — a buyer opens a product and evaluates specs, packaging, imagery, and certifications before inquiring.
Output: `/products/[slug]` route + `SpecTable` + `ProductGallery` components + passing `product-detail.spec.ts` e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-product-catalog/03-CONTEXT.md
@.planning/phases/03-product-catalog/03-UI-SPEC.md
@.planning/phases/03-product-catalog/03-RESEARCH.md
@.planning/references/taste-techniques.md

<interfaces>
<!-- Contracts from Plan 01 + existing code — no exploration needed. -->

From Plan 01 — src/lib/payload-fetch.ts:
- getProduct(slug, locale): Promise<{ product: Product | null; isTranslated: boolean }>  (dual query, published-filtered, English fallback + isTranslated flag).
- Product type: name/shortDescription/packaging localized text; description richText (lexical JSON); specifications flat array {label,value}; imageGallery array of {image: Media|number|null}; category Category|number; certifications (Certification|number)[]|null.

Existing patterns to reuse verbatim:
- CertCard (src/components/blocks/CertCard.tsx): props { name, subtitle?, logo: Media|null, pdf: Media|null, halal?, t:(k)=>string }. Reuse for cert badges — filter certifications to typeof==="object", map to CertCard with logo/pdf guards (typeof cert.logo==="object"?...:null). t comes from getTranslations("certs") (keys halalBadge/downloadPdf/pdfUnavailable exist).
- LocaleFallbackNotice (src/components/chrome/LocaleFallbackNotice.tsx): <LocaleFallbackNotice locale={locale} /> — render when !isTranslated (mirrors [slug]/page.tsx; RESEARCH A1: product detail only, NOT the index).
- Media populate guard: const img = item.image && typeof item.image==="object" ? item.image as Media : null; skip if !img?.url.
- RichText rendering: reuse the existing Phase 2 RichText container convention (src/components/blocks/RichTextBlock.tsx) for description, max-w-[720px].
- Route shape: [slug]/page.tsx awaits params, setRequestLocale, generateStaticParams present. INTERIOR_SLUGS there is a fixed list — do NOT copy that pattern (RESEARCH Pitfall 1); query Payload instead.
- notFound() from next/navigation for missing product; 404 copy keys products.notFoundHeading/notFoundBody (added Plan 01).

i18n (added Plan 01): namespace "products" → rfqCta, breadcrumbRoot, galleryThumbAria ("View image {n} of {total}"), notFoundHeading, notFoundBody. Existing "certs" namespace for CertCard.

shadcn primitives available: Card, Badge, AspectRatio, Button (no new installs). Breadcrumb chevron DOES mirror in RTL (Phase 1 iconography). Thumbnail active ring: ring-2 ring-primary-700 (primary, NOT accent). Widths: bracket values only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing e2e spec for product detail (RED)</name>
  <files>tests/e2e/product-detail.spec.ts</files>
  <read_first>
    - tests/e2e/certifications.spec.ts + tests/e2e/export-map-a11y.spec.ts (a11y assertion style — aria-label/aria-pressed, roles)
    - .planning/phases/03-product-catalog/03-UI-SPEC.md §ProductDetail + §SpecTable + Gallery a11y + Copywriting Contract
    - .planning/phases/03-product-catalog/03-VALIDATION.md (CAT-02/CAT-04/a11y rows)
  </read_first>
  <action>
    Create `tests/e2e/product-detail.spec.ts` (fails until Task 3 lands the route), using seeded fixtures from Plan 01. Cover for en (+ RTL checks on /ar):
    - Basmati Rice (5 specs, gallery ≥2, certs Halal+FSSC): 200; h1 has the product name; a breadcrumb region; a `<dl>` spec table with ≥5 rows plus a "Packaging" row; applicable-certification cards present; RFQ CTA anchor whose href matches `/contact?product=premium-long-grain-basmati-rice&productName=`; gallery thumbnails are `<button>` with aria-label matching "View image", clicking the 2nd thumbnail sets aria-pressed="true" on it.
    - Whole Cumin Seeds (0 specs, no packaging): NO `<dl>`/spec-table region rendered (region omitted).
    - Red Lentils (empty gallery): the ImageOff placeholder shown, no broken image.
    - An unknown slug (e.g. /products/does-not-exist): 404 / not-found copy.
    - On /ar/products/<slug>: html dir="rtl"; gallery thumbnail order is source order (not reversed).
  </action>
  <verify>
    <automated>cd "D:/PW" && npx playwright test tests/e2e/product-detail.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - Spec compiles and Playwright lists the cases.
    - Assertions cover: SpecTable <dl> + packaging row, 0-spec region omission, gallery aria-pressed swap, RFQ href shape, empty-gallery ImageOff, 404, /ar dir + non-mirrored gallery.
    - Uses seeded fixture slugs, not test-created data.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SpecTable + ProductGallery components</name>
  <files>src/components/products/SpecTable.tsx, src/components/products/ProductGallery.tsx</files>
  <read_first>
    - src/components/blocks/MediaGalleryBlock.tsx (AspectRatio + Media guard + next/image + "no lightbox" ruling)
    - src/components/chrome/MobileNavPanel.tsx ("use client" + useState + useTranslations client pattern for the gallery island)
    - .planning/phases/03-product-catalog/03-UI-SPEC.md §SpecTable (dl/dt/dd, grid-cols-[minmax...], hairline dividers, packaging row, omission rules) + Gallery (thumbnail buttons, aria-pressed, ring-2 ring-primary-700)
    - .planning/references/taste-techniques.md (2-col grid over row-heavy borders; grid over calc)
  </read_first>
  <action>
    Create `src/components/products/SpecTable.tsx` (server component). Props: `rows: { label: string; value: string }[]` and an optional `packaging?: string`. If rows is empty AND packaging is falsy → render nothing (region omission). Otherwise render inside a shadcn `Card` (bg-neutral-100, p-lg) a semantic `<dl>` styled as CSS Grid `grid grid-cols-[minmax(120px,auto)_1fr] md:grid-cols-[minmax(160px,auto)_1fr]`. Each spec row: `<dt>` (Label token, text-neutral-600, text-start) + `<dd>` (Body-600, text-neutral-900, text-start, break-words), with `py-sm` and a `border-b border-neutral-300` (last row no border). Append packaging as one additional row (label from a provided "Packaging" string) ONLY if packaging is truthy. Never text-left/text-right — logical text-start only (RTL). No bordered table cells, no zebra, no shadcn Table.

    Create `src/components/products/ProductGallery.tsx` ("use client"). Props: `images: { url: string; alt: string }[]`. useState activeIndex (default 0). If images is empty → render the ImageOff placeholder box (bg-neutral-100, centered lucide ImageOff, text-neutral-600) at AspectRatio 4/3. Otherwise: primary image AspectRatio ratio={4/3} object-cover next/image (priority — LCP candidate) for images[activeIndex]; below, a thumbnail row `grid grid-cols-4 gap-sm` (mobile overflow: overflow-x-auto + scroll-snap, NO carousel lib). Each thumbnail is a real `<button type="button">` with aria-label built from a passed-in label function/string ("View image {n} of {total}"), aria-pressed = (i===activeIndex), onClick sets activeIndex, ring-2 ring-primary-700 on the selected one, visible focus ring. Gallery does NOT flex-reverse in RTL (source/CMS order in both directions). Pass the localized aria-label text in from the server page (products.galleryThumbAria) since this is a client component.
  </action>
  <verify>
    <automated>cd "D:/PW" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SpecTable emits `<dl>`/`<dt>`/`<dd>` (not divs, not a Table); omits the whole region when rows empty and packaging falsy; omits only the packaging row when packaging falsy but specs present.
    - ProductGallery is "use client", thumbnails are `<button>` with aria-label + aria-pressed; no carousel/lightbox dependency imported.
    - Both files use logical properties (text-start, no ml-/mr-/left/right) and bracket max-w only.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ProductDetail route (generateStaticParams + fallback + RFQ CTA) → e2e green</name>
  <files>src/app/(site)/[locale]/products/[slug]/page.tsx, tests/e2e/product-detail.spec.ts</files>
  <read_first>
    - src/app/(site)/[locale]/[slug]/page.tsx (params-await, setRequestLocale, LocaleFallbackNotice, notFound — but NOT its hardcoded-slug generateStaticParams)
    - src/components/blocks/CertCard.tsx + src/components/blocks/RichTextBlock.tsx + src/components/blocks/CTABandBlock.tsx (reuse for cert badges / description / closing band)
    - .planning/phases/03-product-catalog/03-UI-SPEC.md §ProductDetail (PageHeader not full Hero; two-column region; region sequence) + Copywriting Contract (RFQ label, not-found copy)
    - .planning/phases/03-product-catalog/03-RESEARCH.md Pitfall 1 (generateStaticParams queries Payload, dynamicParams default true) + Pattern 4 + Code Examples (CertCard reuse + gallery guard)
  </read_first>
  <action>
    Create `src/app/(site)/[locale]/products/[slug]/page.tsx` as a NEW sibling under products/ (static products/page.tsx already wins the exact /products match — this handles the two-segment /products/<slug>). Do NOT add any "products" special-casing to [slug]/page.tsx.

    `export async function generateStaticParams()`: getPayload({config}), payload.find collection "products" where published equals true, locale "en", overrideAccess:true, limit 500; map to `{ slug }`. Leave dynamicParams at the Next default (do NOT export dynamicParams=false) so post-build products render on first request (CAT-03).

    Default async page: await params {locale, slug}, setRequestLocale(locale), getTranslations("products") + getTranslations("certs"), call getProduct(slug, locale). If !product → notFound() (or render the products.notFound* copy region per UI-SPEC — pick one, matching the existing 404 pattern). Compose per UI-SPEC region sequence:
    - If !isTranslated → <LocaleFallbackNotice locale={locale} /> (detail only).
    - PageHeader region (bg-white, py-lg md:py-xl): breadcrumb `Products / [Category] / [name]` (Label token, chevron that mirrors in RTL; Products link → /products, category name from product.category guard), product name as `<h1>` (Display token), category tag Badge (same neutral treatment as ProductCard).
    - Two-column region `grid grid-cols-1 lg:grid-cols-2 gap-2xl`: ProductGallery (map imageGallery via the Media guard to {url,alt}; pass the localized galleryThumbAria text) inline-start; info column inline-end — shortDescription as a Body-600 lead paragraph, description via the RichText container convention (max-w-[720px]), then the RFQ CTA: a PrimaryButton-styled link `href={/contact?product=${product.slug}&productName=${encodeURIComponent(name)}}` labeled t("rfqCta"). encodeURIComponent is mandatory (threat T-03-08).
    - SpecTable region (full width): render <SpecTable rows={product.specifications ?? []} packaging={product.packaging ?? undefined} /> (component self-omits when empty).
    - Applicable Certifications region: only if certifications has ≥1 populated entry — a `grid grid-cols-2 md:grid-cols-3 gap-md` of CertCard (filter typeof==="object", logo/pdf guards, halal from cert.halal, t from certs). Omit region if none.
    - CTABand (synthesize a ctaBand-shaped object for the general Request-a-Quote/WhatsApp closing band — distinct from the per-product RFQ CTA above).

    Then run the e2e + gate and reconcile selectors in product-detail.spec.ts against real markup (adjust selectors, not behavior).
  </action>
  <verify>
    <automated>cd "D:/PW" && npm run build && npx playwright test tests/e2e/product-detail.spec.ts && node scripts/check-physical-direction.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `generateStaticParams` queries the products collection (no hardcoded slug array); no `dynamicParams` export.
    - RFQ CTA href is `/contact?product=<slug>&productName=<encodeURIComponent(name)>` — verified by the e2e href assertion.
    - Untranslated locale renders LocaleFallbackNotice; invalid slug 404s; SpecTable region omitted for the 0-spec product.
    - `npm run build` exits 0 and prerenders each seeded product slug × 4 locales.
    - `product-detail.spec.ts` passes; physical-direction gate clean on the new files.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug → getProduct | Detail route passes the path segment to a published-filtered Payload query |
| Product name/slug → /contact query string | RFQ CTA builds an outbound URL carrying product context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering / (low) Information Disclosure | RFQ CTA href builder | mitigate | `productName` is wrapped in `encodeURIComponent`; React/Next JSX auto-escapes on render. Flag for Phase 4 (LEAD-02): if /contact ever renders this param via dangerouslySetInnerHTML, re-verify then |
| T-03-09 | Information Disclosure | getProduct published filter | mitigate | Detail route only reaches published products (Plan 01 T-03-01); an unpublished slug resolves to notFound(), not a leaked draft — asserted by the invalid-slug 404 e2e case |
| T-03-10 | Denial of Service | generateStaticParams / getProduct | accept | Explicit `limit: 500` on the slug query; per-request getProduct is a single indexed slug lookup (unique+index) — no unbounded scan; dynamicParams renders-on-demand then caches, no rebuild amplification |
</threat_model>

<verification>
- `npm run build` exits 0, prerenders every seeded product slug × 4 locales; generateStaticParams reads slugs from Payload.
- `product-detail.spec.ts` green (SpecTable <dl>, gallery aria-pressed, RFQ href, 0-spec omission, 404, /ar RTL); physical-direction gate clean.
- Manual (phase gate): a product added in /admin after deploy resolves 200 on first request (dynamicParams) — production-build behavior, per VALIDATION manual-only note.
</verification>

<success_criteria>
- `/products/[slug]` renders name, gallery, description, SpecTable (+ packaging row), applicable-cert badges, and RFQ CTA (CAT-02).
- generateStaticParams queries slugs + dynamicParams default → new products render without rebuild (CAT-03).
- SpecTable resilient to 0/1/N spec rows and empty packaging; structure accepts real specs later (CAT-04).
- a11y: gallery thumbnails are buttons with aria-label + aria-pressed; SpecTable is <dl>/<dt>/<dd>; RTL-correct on /ar.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-catalog/03-03-SUMMARY.md`
</output>
