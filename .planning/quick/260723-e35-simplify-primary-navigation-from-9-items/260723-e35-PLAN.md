---
quick_id: 260723-e35
slug: simplify-primary-navigation-from-9-items
date: 2026-07-23
status: complete
---

# Quick Task 260723-e35: Simplify primary navigation (9 → 5)

## Goal

Premium-redesign brief: cut top-nav clutter. Header showed 9 items; brief wants
5 (Products, About, Global Markets, Certifications, Contact) + a "Request a
Quote" CTA. Prioritize clarity over quantity.

## Changes

- `src/components/chrome/GlobalHeader.tsx` — primary `NAV_KEYS` 9 → 5:
  products, about, export, certifications, contact. Drops home (logo covers it),
  manufacturing, company, insights.
- `src/components/chrome/GlobalFooter.tsx` — add `insights` (footer is the
  comprehensive discovery surface; already carries manufacturing/company/home,
  so no page is orphaned on desktop after the header trim).
- `src/i18n/messages/en.json` — `nav.export` label "Export" → "Global Markets"
  (brief wording; applies consistently to header + footer + mobile, which share
  the `nav.export` key). English only — other locales keep their translation
  (English-first, translation deferred).

## Untouched (deliberate)

- `MobileNavPanel` keeps the full set — the drawer has room and mobile has no
  hover-footer, so the full list aids reachability there.
- CTA already reads "Request a Quote" (`hero.cta`) — brief requirement already met.
- `NAV_KEYS`/`NAV_HREFS` remain duplicated across 3 chrome files. Extracting a
  shared nav module is a real DRY win but out of scope for this slice; each
  surface intentionally shows a different subset now, so the duplication is not
  yet actively harmful. (ponytail: shared nav module if a 4th surface appears.)

## Verify

- `tsc --noEmit` clean.
- No e2e asserts specific header nav items (chrome-consistency + page-routing
  only visit page slugs, which all still exist).
